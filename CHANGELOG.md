# moomoo_trade_v1.py 変更履歴

`moomoo_trade_v1.py` 本体には最新数バージョンのみ 1 行要約を残し、詳細はこのファイルに記録します。コード内の `★ v2.xx:` `★ v3.xx:` 形式のインラインコメントは、各箇所の実装意図を残す目的で保持しています（履歴ではなく現行仕様の根拠）。

---

## v3.9.159 (2026-08-21) — OVN保有中の再起動で建玉が日中ロジックに吸収される事故への多重防御

認定サポーターの実機報告（デモ・OVN保有中に v3.9.157 へ更新のため再起動 →
起動6分後にトレイリングストップで早期決済・本来の翌寄り売却の約5時間半前）への対応。
吸収の機序は「起動時の所有権復元（永続状態→ovn_held フラグ）が何らかの理由で
False を返し、台帳の初回作成が通常建玉として取り込んだ」ことまで特定済み。
所有権復元が False になった根本原因は当該環境の状態ファイルの実物を待って継続調査中
（こちらの再現試験では全保有フェーズで復元は成功する）。原因を待たず多重防御を入れる:

- **所有権の二重ソース判定＋自己修復**（_ovn_owns_now）: メモリのフラグが落ちていても
  永続状態ファイルが保有フェーズならフラグを立て直す（30秒キャッシュ）。
  日中ロジックの決済ガード（_is_other_owner）・台帳の初回作成／吸収・起動時復元の
  全読者をこの二重ソース判定に変更。フラグ1本への依存を排除
- **起動時の診断ログ**: 永続状態の phase / entry_date / env / 所有権の判定結果を
  判定に関わらず必ず1行出す（今回の事故は「復元しましたログの不在」という否定証拠
  しか残らず、原因特定が難航した）。OVNループ起動時も非保有の phase を明示
- 実装中に**「monotonic 初回抑制バグ」の再発を2箇所発見・修正**: 自己修復キャッシュと
  v3.9.157 の OpenD 不達通知の間引きが、環境により起動直後（それぞれ約30秒／約120秒
  ——まさに事故が起きる窓）は機能しなかった。本体 :4211 の HEARTBEAT と同じ
  None センチネル方式に統一
- 表示: 起動ログの時間帯別しきい値「OVN=」を「Overnight時間帯=」へ（夜間持ち越し
  機能と紛らわしい・認定サポーターの指摘）

### 修正自体への「壊す枠」再検査（同日・v3.9.159b として同版に含む）
Codex・新規コンテキストClaude・クラウド多エージェント（8体）で再検査し、
修正が新しく作った穴・残穴を**14件検出→全て同日中に追修正**（Gemini は本日の
無料枠切れのため欠——課金の紐づけは要対応）。主なもの:
- **自己修復の再ラッチ**（3レーン一致）: 売却完了でフラグを解除→保存の間の await 中に、
  自己修復が旧phaseのファイル／30秒キャッシュを読んでフラグを誤って立て直し、
  以後1時間〜終日QQQが日中ロジックから閉め出される → **全解除サイトを「保存→
  フラグ解除」の順に統一**＋**_ovn_save 成功でキャッシュを無効化**＋キャッシュ
  読取をロックで原子化（破れ読み対策）＋読取失敗は30秒固定せず次回再試行（ログ付き）
- **読者の取り残し**（2レーン一致）: 予算計上（sync・起動時復元）と起動時整合性検査が
  フラグ単独読みのまま＝フラグ喪失時に予算二重計上・誤警報が残存 → 二重ソース判定へ
  統一（redundantなフラグ前置も撤去し _ovn_owns_now を単一権威に）
- **保留バッファの鮮度**（3レーン一致）: 窓の終端で残った記事が翌日の窓で「新着」として
  委譲され、前日の決算見出しで発注し得る／WS側と別ID名前空間のため二重監視にもなる
  → 鮮度上限600秒で破棄（ログ付き）
- **一括委譲の上限とAIの視野**（Codex）: 上限10で委譲すると6件目以降は「既読化される
  のにAIに見えない」まま消費される → 上限5・新しい順に変更
- 委譲のスケジュール失敗（ループ停止間際）で記事が消える穴 → pop をスケジュール
  成功後に移動（_threadsafe_future が成否を返すよう変更）
- wait_if_busy の待機上限120秒は実運用の連鎖で容易に超過（消費済み記事の損失）
  → 900秒へ（in_progress は finally で必ず解放されるため上限は純粋な保険）
- 窓の終端間際の委譲は30秒待機がセッションゲートで無に帰す → 残り時間が待機+15秒
  未満なら即時判定（旧挙動）に切替
- WS側の呼び出しも wait_if_busy=True に（ポーリングが監視枠を持つ時間が延びた分、
  WS記事の「監視中→スキップ＝永久消失」が増えるのを防止）
- _earnings_momentum_check 内の get_quote 直呼びを to_thread へ（半死OpenDでの
  イベントループ閉塞を防ぐ・この経路は委譲で高頻度化した）
- 起動診断の状態ファイル二重読みを解消（読み込み済み dict を判定に渡し、判定結果を
  キャッシュへプライム）／headline=None での TypeError／articleId 無し記事の無音破棄

既知の限界（設計上の割り切り・文書化）: 直列化により、連発する決算見出しの2本目は
方向確認の起点価格が初動後になり「見送り」になりやすい（初動は1本目が判定済みのため
実害は限定的）。フェッチ層の「取得時点で既読化」という根本構造は次の大型改修の候補
（30秒ループと共有のため今回は触らない）。

回帰テスト 144件（今回追加分の要所8件は変異検証済み）。

## v3.9.158 (2026-08-21) — 決算2秒ポーリングを「30秒待機→方向確認」へ委譲（設計判断・確定）

決算監視の2つの経路のうち、2秒ポーリング側だけが待機・方向確認なしで
AI判定→発注へ直行し、さらに scope 付き既読を先に刻むため、後から来る
WebSocket側の方向確認まで抑止していた（認定サポーターC-3・設計当初からの構造）。
両経路とも _earnings_momentum_check（30秒待機→±N%の方向確認→決算セッション
ゲート）に統一する。判断の要点:

- 決算戦略の設計意図は「発表直後の初動に飛びつかず、方向が確定してから入る」。
  片方の経路だけがそれを素通りするのは設計と実装の不一致だった
- 初版の委譲は壊す枠レビュー（新規コンテキストClaude）が2件のMAJORを検出:
  フェッチ層が取得時点で記事を既読化するため（_STOCK_NEWS_CACHE）、「見送って
  次の周回で拾う」が成立せず、監視中の見送り分・同一バッチの2件目以降が
  **無音で永久消失**していた。**保留バッファ方式に組み直し**:
  取得した記事は銘柄ごとのバッファへ貯め（上限10件）、監視が空いたら未消費分を
  一括で委譲。委譲先（_earnings_momentum_check）は複数記事対応＋wait_if_busy
  （投入直後にWS側が監視を始めても記事を捨てず、監視終了を待って判定）
- EARNINGS_MOMENTUM_WAIT_SEC=0 の設定では従来どおり即時判定（挙動不変）

## v3.9.157 (2026-08-21) — OpenD停止時の全凍結を根治＋一晩通し検証3報告への対応13件

認定サポーター2名の一晩通し検証（Windows/macOS・デモ・計26時間分）と機序特定の
追加報告に基づく。実機で4時間23分の全凍結（QQQショート8株が290分無監視）が
観測され、機序が特定されたため最優先で対処した。

### 最重要: OpenD停止でBotの監視が全部止まる構造（P0）
- 機序（サポーターの特定・コードで裏取り）: SDK のコンテキストコンストラクタは
  OpenD と繋がるまで**例外もタイムアウトも無しに6秒間隔で無限再試行**する。
  これが await を伴わない同期呼び出し経由でイベントループ上で起きると、
  心拍・損切り・時間切れ・トレール・パニックが同時に停止する。
- 対処: v3.9.147 で起動バナー専用だった二段構え（①素のTCPで当たりを取る
  ②別スレッドで期限を切る）を、**毎回使う生成経路（_quote_ctx/_trade_ctx）へ展開**
  （_make_ctx_bounded・期限は MOOMOO_CTX_BUILD_TIMEOUT_SEC=15秒・期限後に遅れて
  生成されたコンテキストは即closeして資源を漏らさない）。無限ブロックの代わりに
  OpenDUnavailableError を投げ、各ループの既存の「失敗しても続行」に拾わせる。
- 「OpenD に接続できません／応答がありません」の**名指し通知**を追加（120秒間引き・
  イベントループ非依存の使い捨てスレッドで送信・従来は「Bot応答なし」という
  間接情報のみだった）。特定された凍結経路（process_headlines 内の同期
  sync_positions 直呼び）も await 化。復帰通知に「時間切れ超過の建玉は直ちに
  決済されます」を明記。

### 決済裏取りの穴（A-1・v3.9.156 の残穴）
- 建玉照会の**空応答1回**で「建玉ゼロ確認」と確定していた（クローズ注文が失効して
  いても停止に入る——v3.9.156 が塞いだ「発注できた≠約定した」と同じ形が検証器に
  残っていた）。スイープ中に出した決済注文を記録し、**全注文の FILLED_ALL／約定数
  到達まで照合してから確定**する（UNKNOWN・生存中は再確認・未約定の終端は再決済へ）。

### 表示の自己矛盾（A-2・v3.9.156 の残穴）
- 実口座 REAL_SHORT_ENABLED=false で「SHORT も実発注対象です」と偽の注記が出て、
  同じ起動ログ内の正しい案内と矛盾していた。**口座別ゲート（実口座買い専用／
  デモSHORT不可）まで適用した実効サイド一覧を1関数に集約**し、バナー・起動
  サマリの全表示をそこから作る。実発注ゼロになる組み合わせは口座種別を問わず明記。

### 気配の鮮度検査（A-3・v3.9.156 の残穴）
- オフセット付き時刻表記（-04:00等）で逆判定になっていた（小数秒の切り詰めが
  オフセットごと落とす＋無条件のUTC付与。現行 Alpaca は Z 表記のため実害未発生・
  実測で再現確認）。**超過小数桁だけを削りオフセットを保持する形**に修正
  （サポーター提案のコードを採用）。

### そのほかの対応
- 除外キーワードの語尾に -er(s)/-ing を追加（golfers/footballers/filming/touring。
  -ed は "concerted effort" の巻き添え測定により見送り——サポーターの
  辞書23万語の測定結論を採用）
- 個人資産運用Q&A見出し（"Can I Retire...?" 等）の上流フィルタを追加。META疑問形
  ガードが無関係の実シグナルをバッチ巻き込みで中立化する事故の入口を塞ぐ
  （METAガード本体は現状維持——サポーターの裁定に同意）
- 同一見出しの既読TTLを 60分→180分（同一文字列がちょうど60分で再判定→二重発注
  していた。HEADLINE_DEDUP_TTL_MIN で変更可）
- 発注数量の1株切り上げが残余力を超える穴を place_buy/place_short の計算経路にも
  ガード（C-1・v3.9.108 の判定が qty 明示側にしか無かった）
- 週末/デモ日次の全決済ループに銘柄ごとの例外受け（C-2・1銘柄の例外で後続が
  未処理のままループ再起動していた）
- 起動時バックフィルを「取引分」基準の合成時刻で保存（C-4・実時間cutoffで前夜分が
  取得直後に全滅し、セッション開始から約60分トレンドガードが不在だった）
- OVN発注ログに orderId と約定確認行を追加（受付1行だけでは約定可否を追えなかった）
- データ収集の送信エラーに応答の型・キー・短縮表現を出力（「送信エラー: None」で
  切り分け不能だった）
- 当日サマリの保存を口座別（REAL/DEMO）に分離し、Discord見出しに 🔴実口座/🟡デモ口座
  を明記（案B・実口座⇄デモ切替時に両口座の損益が同じ集計に混ざっていた）

### 修正自体への「壊す枠」4レーン再検査（同日・v3.9.157b として同版に含む）
Codex・新規コンテキストClaude・Gemini・クラウド多エージェント（計8エージェント）で
再検査し、**修正が新しく作った穴を12件検出→全て同日中に追修正**した。主なもの:
- 半死OpenD（TCPは通るが生成が返らない）で生成スレッドが無制限に堆積
  → 同時実行上限3＋タイムアウト後20秒ブレーカー＋公開/closeのロック原子化
- 起動順の暗黙契約を壊していた（OpenDより先にBotが起動すると即 exit(1)。
  旧版は無限ブロックが「偶然の待機」だった）→ TCPで明示的に待つ起動待機
  ループを追加（既定10分・OPEND_STARTUP_WAIT_MIN）
- OpenD不達時に get_quote が Alpaca/Finnhub フォールバックへ到達せず全ゼロを
  返していた（「監視を続ける」通知と実態が矛盾する盲目監視）→ 到達するよう修正
- 決済裏取りの注文記録が「対象なし」の早期returnで掃除されず、フラットな日に
  前日の記録（当日照会でUNKNOWN）を審査し続けて停止に入れず偽の🔴警報
  → 記録に時刻を付け鮮度5分で失効・早期returnでも掃除・try/finallyで必ず畳む・
  再試行時は未解決の証拠だけマージ
- 既読TTL一律180分は「25bp→50bp」のような数値だけ違う続報まで3時間落とす
  （数値正規化との複合・Codex指摘）→ **二層化**: 完全一致=180分スライディング
  （二重発注の機序を根絶）／正規化一致=従来60分（続報の再判定を保つ）
- バックフィルの合成時刻案は4レーン一致で反証（夜間ギャップを「直近の値動き」に
  圧縮し、変化率・モメンタム検知・乖離基準・AI文脈を汚す）→ **実時刻に差し戻し**、
  セッション開始直後の約60分はガードが効かない fail-open を INFO で明示
- 個人Q&Aフィルタの絞り込み（"we" と素の "roth ira" を除外——市場エディトリアルと
  商品ニュースの巻き添え）／数量ガードに個別株上限（STOCK_MAX_USD）併用／
  注記の誤帰属（select_v1無効時）／送信エラーの非dict応答／env値の防御パース ほか

最終ラウンドにも Codex の確認を挟み、さらに3件を追修正（v3.9.157c）:
生成スレッドの上限を「生存中の本数」で数える（呼出元の15秒減算ではハング中の
スレッドが数に入らず堆積し続けた）・裏取りのマージが同一銘柄の旧証拠を追い出す件は
銘柄ごとの集約審査（どれか1本が全量約定なら充足／UNKNOWNが残れば未確定／全て
未約定終端なら再決済）に変更・起動待機時間の nan/負値ガード。

さらに Codex の再々確認で1件（旧注文の約定だけで数量が増えた新注文の取消を
見逃す）→ 充足判定を「最新注文基準」（最新が約定 or 約定数合計が最新の数量に到達）
へ変更して収束（同ラウンドの他の確認点は全て問題なし）。

回帰テスト 127件（追加分の要所13件は変異検証済み）。

残課題: 決算2秒ポーリング経路の方向確認ゲート委譲（設計判断・PAN確認待ち）／
OpenD が停止した原因自体の切り分け（サポーター側で継続中）。

## v3.9.156 (2026-08-20) — 過去5日分（v3.9.141〜152）の徹底レビューで確定した16件を修正

PAN指示で過去5日分の変更に4レーン（Codex・新規コンテキストClaude・Gemini・
クラウド多エージェント）＋検証エージェント2本の総当たりレビューを実施し、
全所見を現物コードで裏取りしたうえで一括修正した。

### 実損に直結（最優先）
- **夜間持ち越しの旧状態移行が保有状態を握り潰す**: v3.9.145 の移行処理が
  「1回だけ読む」マーカーを置くだけで新形式ファイルへ保存しておらず、
  main の所有権チェック（1人目の読者）がマーカーを焼くと、巡回ループ
  （2人目の読者）が空を受け取り phase=HELD が消えていた。ovn_held=True は
  残るため、週末決済・パニックセル・リスク監視の全てが見送り、建玉が
  無監視で取り残される。**先に新形式へ保存し、成功したときだけマーカーを置く**。
- **夜間持ち越しの約定判定が「終端」であって「約定」ではなかった**: 予約売りが
  取消・拒否・失効でも、建玉ゼロの再照会と重なると「寄り付きで約定しました」
  と告知し実在しない往復の損益を集計へ送っていた。**約定状態（FILLED_ALL）か
  約定数の照合を挟み、未約定の終端は3巡の再確認後に「手動決済の可能性」として
  確定（偽の約定通知・損益記録は送らない。一部約定は実約定数で記録）**。
- **週末全決済の「完了」が発注受付どまり**: place_close_all の "full" は
  「全量を発注できた」であって約定確認ではなく、そのまま監視を止めて
  「全決済しました」と通知していた。**停止前に実口座を再同期し、Bot 所有の
  建玉ゼロを裏取りしてから停止する（_sweep_close_verified・デモ日次決済も同様）**。
- **実口座で「実発注は発生しません」の偽バナー**（v3.9.155 の退行）:
  シャドーループ側の注記に SIMULATE ガードが無く、実口座＋DEMO_SHORT_ENABLED=false
  で偽の安全表示が出ていた（SHORT スキップはデモ限定 :13145）。**注記と
  select_v1 実効サイド表示を環境で分岐する共通ヘルパーに集約**（3版連続で
  片側だけ直る「表示の版ずれ」の根治）。
- **決算の「30秒待機→方向確認」ゲートを直行ルートが素通り**（v3.9.154 で実動化
  した退行）: 直行ルートが決算銘柄を即時にAI判定→発注し、scope 付き既読を
  先取りするため決算モメンタム経路が二度と走らなかった。**直行ルートの対象から
  決算銘柄（EARNINGS_PRE/AFTER）を除外**し、決算は専用機構（待機→方向確認→
  セッションゲート）に一本化。しきい値の防衛線は process_stock_news に集約:
  決算銘柄が override 無しの経路（通常個別株ループ等）から来たときは
  **max(決算0.80, セッション別しきい値)** で判定（CONFIDENCE_OVERNIGHT=0.90 等の
  厳格設定が 0.80 に下書きされる件を解消）。決算機構自身が明示の
  thresh_override（既定0.80）を渡す決算窓内の判定は、従来どおり専用しきい値
  （決算戦略の設計値・変更なし）。

### 安全・可用性
- 建玉ゼロの実口座で発注ブロックが再起動まで解除されなかった件: 建玉明細ゼロ＋
  accinfo 3列すべて $0 の「確定ゼロ」を確認できたときだけ解除する経路を追加
  （accinfo の裏取りは10分に1回・照会失敗は解除しない側に倒す）。状況ログの
  「建玉あり」も「建玉の有無を確認できず」に訂正。
- Alpaca フォールバック気配に鮮度検査を追加（ALPACA_QUOTE_MAX_AGE_SEC・既定300秒）。
  時間外の古い気配が指値基準と価格履歴に入り、履歴が古値で自己参照化して乖離検査が
  恒久的に素通りする穴を塞ぐ（OpenD の相場権限を別端末に奪われた場面で顕在化）。
- _ovn_position_all: position_side が読めない負数量の行をショートとして数える
  （sync_positions と同じフォールバック）。ネッティング型ショートの混在防止ガード
  素通りを塞ぐ。
- OVN_SKIP_WEEKEND=true × OVN_SKIP_LONG_HOLIDAY=false のとき「週末2日は見送るのに
  3連休は持ち越す」逆転を解消（== 2 → >= 2）。
- 起動時整合性検査が OVN 保有中の建玉に「監視されていない→手動決済して」と誤警報
  していた件: OVN 所有中は監視対象とみなす（案内に従うと翌朝の OVN 売却と衝突）。
- GAS v9.24: 数式注入エスケープ（s_）をトレード行・観察行の name / tickers /
  beneficiaries / victims / headline_first / block_reason にも適用（v9.23 の取り残し）。
  **Apps Script 側への再デプロイが必要**。

### 品質・掃除
- get_quote: 合成クォート（two_sided=False）では last を中値で上書きしない
  （実在しない価格の捏造・履歴汚染の防止）。クロス気配（bid > ask）は負の
  スプレッドで必ず合格していたため不合格に。到達不能だった旧フォールバック
  ブロックを削除し、枯渇経路の return を無条件 False/False に単純化。docstring を
  現契約（from_book / two_sided）に更新。
- 回帰テストを 51→79 件に拡充（OVN 移行・約定照合・裏取り・注記・鮮度・GAS 等の
  再現テストを追加）。脆かったソース検査（1200字窓・"\ndef " 境界・重複読込）を
  共通ヘルパー（_main_src / _function_body）に置き換え。追加テスト4件は
  変異検証済み（わざと壊して落ちることを確認）。

修正後に「壊す枠」3レーン（Codex・新規コンテキストClaude・Gemini）で再検査し、
Codex が修正自身の穴2件を検出→同日中に追修正（v3.9.156b として同版に含む）:
①決済後の裏取りが累積フラグ（_account_scan_valid）を信じており、照会失敗＋
事前ゼロクリアの組で「建玉ゼロ」の偽成功が成立 → 完全スキャンの世代番号
（_account_scan_seq）で「いまの同期が完走した」ことを要求。
②accinfo の NaN が float() を通り abs(NaN)>0=False で「$0」に化けて発注ブロックを
誤解除 → math.isfinite の有限値検査を追加。
さらに新規コンテキストClaudeの再検査（MAJOR 0・MINOR 4）にも同日対応:
③発注ブロック解除の判定も累積フラグでなく「この回の走査が完走した」世代番号を要求
（data=None の0件走査で過去のフラグを信じる穴）。④クロス気配検査をスプレッド上限
（既定0=無効）のオプトインから独立させ、既定構成でも効くように移動。⑤Alpaca を
鮮度で弾いた分だけ通過率が上がる Finnhub 経路にも同じ鮮度上限（t・unix秒）を適用。
⑥裏取りが1回目の照会の反映遅延で False になると再決済（二重決済の窓・デモの
ネッティングでは反対建玉化）へ直行していた件——間隔を置いて最大3回（10秒＋20秒×2）再確認し、
それでも残るときだけ再決済へ回す（Codexの最終確認指摘を受け、注文拒否などで
本当に決済できていない場合の再決済が閉場までに間に合う間隔に調整）。ほか移行マーカーの補完・建玉走査のスナップショット化。
回帰テストは 84 件（今回追加分は要所を変異検証済み）。
検出経緯と裁定の記録は 2026-08-20 の作業ログ参照。残課題: 決算2秒ポーリング経路は
設計当初から方向確認ゲートを持たない（今回の退行とは別・要設計判断）。
moomoo_trade_ovn_test.py（未追跡のテスト用フォーク）は v3.9.154 時点の複製のまま
なので、次に使う際は本体から再生成すること。

## v3.9.155 (2026-08-20) — 新レビュー体制の初回成果（新規コンテキストClaudeの指摘4件）

3社レビュー体制（Codex＋新規コンテキストClaude＋Gemini）の導入初回で、
**Codexが7往復で見逃した問題**を新規コンテキストのClaudeレビュアーが検出した。

- **【高】決算銘柄の直行ルートがしきい値0.80を素通り**: v3.9.154で開通した
  直行ルートは決算監視銘柄も扱うのに、決算用しきい値（EARNINGS_CONFIDENCE・
  既定0.80）を渡しておらず通常の0.75で判定していた。さらにこのルートが
  scope付き既読を先に付けるため、決算監視ループの0.80判定が二度と走らず、
  **決算しきい値が実質0.75に差し替わっていた**。直行ルートに thresh_override を付与
- **【中】板薄ガードの適用軸を修正**: v3.9.154 は「板由来か」を軸にしたため、
  Alpaca フォールバックの実在する両側気配までスプレッド検査から外れていた
  （OpenD 不調時＝板が薄い場面と相関しやすい時にこそ検査が要る）。
  軸を「両側が独立に観測された気配か」（two_sided）に変更。last で埋めた
  合成値と Finnhub だけを判定不能扱いにする
- **【低】固有名詞の -ys 複数形**（Grammys/Emmys）が素通りしていた退行を修正
- **【低】select_v1×デモSHORT無効の組み合わせで実発注ゼロになる旨を起動表示に明記

いずれも回帰テストを追加（計51件）。
検出経緯: 修正者の意図・経緯を一切知らせない新規コンテキストのレビュアーに
差分とコードだけを渡し「壊す枠」で依頼する方式の初回適用。

追補（同日）: Codex が「全ソース枯渇・片側だけの板のとき two_sided=True の偽契約を
返す」を検出→末尾経路で実態からフラグを再計算。select_v1×デモSHORT無効の
表示矛盾がもう1箇所（モメンタムループ起動時の warning）にも残っていた件も修正。
最終確認は5レーン（テスト51件・Codex・新規Claude・Gemini・クラウド多エージェント
レビュー=指摘0件）で実施。残課題として起票: 決算銘柄の直行ルートが決算モメンタム
待機（WAIT_SEC→方向確定）の段取りを経由しない件（v3.9.153由来の構造・要設計判断）。

---

## v3.9.154 (2026-08-20) — v3.9.153 の確認結果5件（認定サポーターKの指摘）

### §1 社名エイリアスが個別株AIに届いていなかった（最重要）

`is_new_article` は**判定と同時に既読へ記録する**副作用を持つため、通常News側で
1回通した記事を直行ルートで `process_stock_news` に渡すと、そこで必ず False に
なり `if not new: return` で戻っていた。**直行ルート自体が以前から一度も
個別株AIに到達していなかった**（v3.9.153 で INFO ログを足したことで、
「ルーティングは動いている」と読めるようになっていた）。

- 直行ルート専用の引数 `already_deduped` を追加し、そこだけ articleId 判定を飛ばす
- `bypass_filter` の意味は元どおり（prefix のみ）に戻した。決算監視ループは
  2秒周期でフィードを取り直すため、一括で飛ばすと同じ記事を毎回再判定して
  重複発注の恐れがある（レビューで検出）

### §2 単語境界化で複数形が通るようになっていた（v3.9.153 の退行）

キーワード表は `olympic` `esport` `movie` のように単数形で登録されており、
旧実装（部分一致）が複数形を自動的に拾っていた。`\b` を付けた時点でその前提が
外れ、`Olympics` `esports` `movies` 等23語形が通るようになっていた。

- 語尾に `(?:e?s)?` を許可。子音+y の語は `y→ies`（celebrity→celebrities）
- 市場語（inflation / factory / concerted / tourism / Oscar Health / e.l.f. Beauty /
  Netflix(NFLX)）は通し続けることを17ケースで確認

### §3 通常News側にも「バッチ全体を捨てる」が残っていた

個別株側（v3.9.153）で直した形が通常News側に残っており、先頭が再配信の見出しだと
同じバッチの新規見出しも巻き添えで捨てられていた。記録も先頭1件だけで、
2件目以降は dedup 辞書に一度も入っていなかった。1件ずつ選別・全件記録に変更。
判定と同時に印を付け、AI 呼出が例外で落ちたら戻す（個別株側と同型）。

### §4 起動時 WARNING の未修正

v3.9.153 で直したのは `log.info` 側だけで、`log.warning` 側（起動直後の
「live-eligible サイド」）は env の生値のままだった。直前の「SPY除外」の行と
矛盾していたため、こちらも絞り込み後の実効サイドを表示する。

### §5 気配値フォールバックの表示と板薄判定

- 板由来かどうかを戻り値（`from_book`）とログで区別する。従来はフォールバック時も
  「OpenD ORDER_BOOK」と同じ文面で、ログから板の取得可否を判定できなかった
- スプレッド過大の判定は**板由来のときだけ**行う。板が空で ask=bid になると
  spread が必ず 0 になり、検査が素通りしていた（「広くないから安全」ではなく
  「測れていない」ため、そう記録する）

Codex の反復レビュー3回（決算ループの重複発注リスク・from_book の付け漏れ4経路・
通常News側の判定/記録レース・子音+y の複数形などを検出・修正）。

---

## v3.9.153 (2026-08-20) — ニュース入力側の取りこぼし2件＋表示の正確化（認定サポーター3名の報告）

### A. 除外キーワードの部分一致を修正（最重要）

除外キーワードが素の部分一致（`kw in lower`）だったため、**"nfl" が inflation を、
"actor" が factory を巻き添えにして、市場ニュースを AI 判定の手前で捨てていた**
（認定サポーターNの指摘・受講生報告が発端）。Win365 の実測（約40時間）では
'nfl' 165件のうち75%が FOMC・CPI・中東情勢の見出しだった。SPY はマクロキーワード
経由でしか発注対象にならないため、**SPY のマクロ発注経路が実質塞がれていた**。

- 全語を**単語境界つきの正規表現**に変更（"disney+" 等の記号語は自動で境界を調整）
- スポーツ略号（NFL/NBA/MLB/NHL/UFC/FIFA）は**大文字完全一致のみ**
  （"NFL" は落ち "inflation" は通る。小文字の "nfl playoffs" は AI へ流れるが
  ノイズ判定で止まるためコスト増のみ）
- director / beauty / oscar / diet はフレーズ化（Oscar Health・e.l.f. Beauty 等の
  実在ティッカー材料と、取締役関連の開示を落とさない）
- **除外語別の日次カウンタを追加**（修正前後の比較用）。修正前の数字は
  Win365 ログで確保済み

### B. 個別株ニュースの既読共有を分離

通常News（市場全体への影響を判定）と個別株（銘柄への直接関係を判定）が
**同じ既読辞書を共有**していたため、先着する通常News側の印で個別株側が
弾かれていた（受講生実測: NVDA 新着8件 → AI判定0件）。

- 既読を**ルート別（scope）に分離**。既存の呼び出しは既定値でそのまま動く
- バッチ先頭1件ではなく**1件ずつ**重複判定（先頭が既出でも2件目以降を失わない）
- 判定と同時に印を付け（原子的）、AI 呼出が例外で落ちたら印を戻す
- ティッカー直行ルートを**社名でも**拾えるように（"Nvidia" → NVDA 等11銘柄。
  Oracle of Omaha・Big Apple 等と衝突する一般名詞は入れない）
- 直行ルートに INFO ログを追加（従来この経路は動いたか分からなかった）

### 表示・集計の正確化（認定サポーターT・Wの報告）

- モメンタムの「発注実行」WARNING → 「発注試行」（ガードで見送られる場合がある）
- シグナル切替の決済後に entry_time が残り「時間切れ（建玉なし）」の事後通知が
  出ていた件を修正（全量決済できた回は時計を返す）
- get_order_book フォールバックの文言を実挙動（セッション中は維持）に一致
- 終了サマリのラベルを実態に（「注文数(新規+決済)=」「強制決済(損切+時間切れ)=」）
- select_v1 有効時の起動バナーは**絞り込み後**の実効サイドを表示（SPY が
  出続けて説明と食い違っていた）

Codex の反復レビュー4回（うち1回は2つのレビューが食い違ったため事実の裁定を実施。
エイリアスの一般名詞衝突・ソース帰属のずれ・既読印の巻き戻し漏れ等を検出・修正）。

---

## v3.9.152 (2026-08-19) / Wizard v1.43 — [5b] の表記を実挙動に一致＋仮想損切り計測の復活

認定サポーターNの6週分析で、**損切り幅プロファイル（SMH×3.0 等）がモメンタムの
実発注に一度も適用されていない**ことが確定した（コード・記録・実発火分布の3面で
確認済み。実発火は SPY/QQQ/SMH とも 0.52〜0.54% に張り付いており銘柄差が無い）。
v3.9.123 の方針文書・Wizard [5b] の表示・CHANGELOG の過去記述はこの点で
実装と食い違っていた。**この版では実発注の挙動は一切変えず**、次の2つを行う。

### Wizard v1.43: [5b] の表記修正（応急処置）

- 「実発注」と表示していた表を実挙動に合わせ、**モメンタム実発注には適用されない**
  旨を明記（実発注は [5] の基準値・全銘柄共通で動作）
- 上級者向け `MOMENTUM_STOP_MULT_<SYM>` にも同じ注記
- 設定値・書き出しキーは不変更。**再設定は不要**

### Bot: 仮想損切り 狭/広 の計測を復活（v3.9.110 で撤去した列）

実発注に倍率を適用すべきかを実データで判断するため、観察ログの 46-47 列
（仮想損切り狭%/広%）の計測を復活させる。

- 狭 = 現行の実発注と同じ幅（MOMENTUM_STOP_LOSS_PCT・全銘柄共通）
- 広 = 同じ基準値 × **標準プロファイルの倍率**（利用者の選択に依存させず
  フリート全体で同じ定義で測る）
- **モメンタム観察（momentum_shadow）に限定**・サンプリング既定30%
  （env OBS_VEXIT_SAMPLE_PCT・GAS 負荷対策）
- 旧式の更新経路（再起動前の残り建玉）には載せない（GAS の更新が 31-39 列を
  まとめて書き直し、既存の値を消す危険があるため）

v3.9.110 が撤去した際の根拠「v3.9.32 で実装済＝検証完了」は、モメンタムに
ついては成立していなかった（撤去→未計測のまま Wizard へ出荷、という順序の誤り）。

なお 7/15 の検証では「損切り幅を広げても改善しない」という逆向きの所見も出ている。
どちらが正しいかをこの計測（8/17 以降のクリーンな期間・2週目安）で判定し、
実発注への適用（+volstop タグでの段階導入）を判断する。

あわせて `_parse_pct_env` が NaN を素通りさせる穴も修正
（OBS_NEWS_SAMPLE_PCT / OBS_VEXIT_SAMPLE_PCT 共通・非有限値は既定値に倒す）。

Codex の反復レビュー4回（サンプリングが一度も発火しない block_stage の参照誤り・
GAS 更新経路の破壊的上書き・広の定義が利用者選択に依存する問題などを検出・修正）。

---

## v3.9.151 (2026-08-19) — パニックセルの対象統一＋Anthropic通知の正確化（認定サポーターの報告）

### パニックセルの対象統一

`panic_sell_all` だけが個別株（STOCK_TICKERS）と決算銘柄（EARNINGS_*）を対象に
含めておらず、急落時にそれらのロングだけが逃げ遅れる形だった（意図的でないことを
確認のうえ修正）。「実発注・監視の対象集合」を組み立てる6箇所がすべて同じ銘柄群を
含むようになった。あわせて、Bot 以外の建玉（夜間持ち越し・手動）は通知を組む前に
対象から除き、「決済します」と告知した銘柄に手を出さない食い違いを解消。

### Anthropic API エラー通知の正確化（認定サポーターの報告3点＋提案1点）

- **401 の誤表示**: API キーの誤り・失効（401）のとき、正しい通知に加えて
  「クレジット切れ検知」という誤ったラベルの通知が重ねて届いていた。
  401 経路は音だけにし、通知は正しい文面の1通に集約
- **表示時刻**: 重大アラートの「JST」表示が実際はマシンのローカル時刻だった。
  実体を JST に統一（日本以外の環境でも正しい時刻になる）
- **402 の未検知**: 支払い情報の問題（カード失効・決済失敗など・402 billing_error）が
  どの分岐にも掛からず通知ゼロだった。専用の通知を追加（30分クールダウン）。
  文字列判定の残高切れ（400）より先に status で判定し、誤routeを防ぐ
- **原因を問わない警報（提案の採用）**: 既知の形の列挙は Anthropic 側の変更で
  穴が開くため、「分類できない失敗が3回続いたら鳴る」を health_warning_loop に追加。
  応答の解析エラーも数える。一時的なもの（429/5xx/接続断）は数えない。
  AI 判定の成功でリセット
- 健全性ループ自体の例外が debug（端末に出ない）に落ちていた件を warning に変更
  （「気づかせるための仕組みが同じ形で気づけない」構造の解消）

Codex の反復レビュー4回で、パッチ未適用のまま「実装済み」と誤認していた2件を含む
後退（402 判定の順序・解析エラーの計上漏れ・通知の氾濫経路）をすべて塞いで ship 判定。

---

## v3.9.150 (2026-08-18) — v3.9.149 の検査結果への対応（新しく開いた経路4つ＋届いていない範囲）

v3.9.149 を認定サポーターに検査していただいた結果への対応。**v3.9.149 の修正が
新しく開けてしまった経路が4つ**見つかり、いずれも塞いだ。指摘の中心にあった
「毎回、値の分離はできているが、同じクラスの問題が1階層下に現れている」という
観察を受け、個別の修正に加えて構造側も直した。

### 1-1. 週末をまたぐ一時停止で翌営業日が丸一日動かない
`market_open_event` の set/clear を「状態遷移の立ち上がり」でしか行っていなかった
ため、停止中にプロセスが一時停止（スリープ等）して遷移を跨いで再開すると、
イベントが clear のまま復帰しなかった。**毎周回のレベル駆動**（停止条件なら clear・
でなければ set）に変更。通知は従来どおり立ち上がりだけ。この型は構造的に作れなくなる。

### 1-2. 建玉が残っているのに「完了」として週末停止に入る
決済結果に第6の値 **"unknown"** を追加。内部数量が 0 のとき（証券会社の応答から
銘柄が欠けた場合と区別できない）は "full" と名乗らず、一斉決済は再試行に回し、
時計も消さない。

### 1-3. 損切り・トレールの「全失敗」警報が鳴らない
v3.9.149 で結果を5値化した際、損切り・トレールの2箇所だけ真値判定のまま残り、
"failed" が見送り分岐に吸われていた。membership 判定に統一。

### 1-4. 受付時に消した時計が、注文が通らなくても戻らない
同期のたびに「建玉があるのに時計が無い」状態を自己修復する。決済注文が処理待ちの
間（プロセス内の管理表・証券会社側の生きている注文の両方を確認）は戻さない。
起動時復元にも同じガードを適用（再起動をまたいだ注文への対応）。

### 2-1. 戦略種別の復元が既存保有者に効かない
台帳の初回作成時にも、メモリ上の種別が分かる場合は記録するようにした。
銘柄からの推定は「分からない」を「確信をもって誤り」に変えるため行わない。

### 2-2. 「必ず成功する検査」の撤去と作り直し
v3.9.61 の整合性検査は、監視集合に検査対象を含めてから差を取っていたため
恒等的に空だった。撤去し、**口座に実在した銘柄**（設定で絞り込む前に記録）と
監視対象を比べる検査を起動時復元後に新設。照会が完走していないときは
「検査できていない」と明示する（黙って成功したふりをしない）。

### その他
- チェイサー: 取消失敗＝注文消滅と誤認して管理から落とす経路を修正（終端を照会で
  確認できたときだけ外す）。取消確認が不明の回は再発注しない（二重決済防止）
- watchdog: 終端判定の表記漏れ（CANCELED・DISABLED）を修正。全約定は取消と
  区別し、再発注ブロックに入れない。部分約定は残数を管理表に書き戻す
  （従来は元の株数のまま残り、再発注で売りすぎの恐れがあった・既存の問題）
- 注文の終端判定を SDK の語彙と突き合わせた完全一致に変更（未知の値は非終端＝安全側）
- C-3 の確定損益行が TRIGGER_TICKERS に QQQ が無い設定で集計されない問題を修正
- 誤った前提のコメント2箇所（「標準 SPY,QQQ,SMH」）・docstring・紛らわしいログを訂正

Codex の反復レビュー8回で、修正の過程で作り込んだ後退（FILLED_PART の取り扱い・
未初期化変数・検査の実行順序ほか）をすべて塞いだうえで ship 判定。

---

## v3.9.149 (2026-08-17) — 認定サポーターの検査結果4件＋entry_time（案A）

配布中の v3.9.148 を精査していただいた結果への対応。**4件とも v3.9.148 で
入ったものではなく、以前からある構造**（新しく壊れた箇所は無し、と確認済み）。

### ① 再起動で建玉の戦略属性が失われる（SMH は時間切れが復帰しない）

起動時に建玉を復元するループが `EXECUTION_MAP`（= TRIGGER_TICKERS）だけを回って
いたため、モメンタムで実発注される SMH のように TRIGGER_TICKERS に無い銘柄は
`entry_time` が復元されず、**時間切れ決済が永久に発火しなかった**。
Wizard の「標準的な設定」は `SPY,QQQ` である一方、選抜プロファイル v1 は
`SMH:SELL_SHORT` を書くため、素直に設定するとこの状態になる。

- 復元対象を `EXECUTION_MAP ∪ STOCK_TICKERS ∪ 決算 ∪ モメンタム実発注` に統一
  （リスク監視と同じ式）。v3.9.61 の原則「実発注銘柄=必ず監視対象」をこの経路にも通す
- 建玉台帳に戦略種別（`entry_ai_category`）を保存し、起動時に復元。従来は
  再起動後にモメンタム建玉が通常建玉として扱われ、時間切れが 60分→10分に、
  損切りが銘柄倍率つきの緩い値（SMH で設計の約1.8倍）に変わっていた
- 旧レコードに種別が無い場合も安全に動く（`None` 許容・省略時は既存値を保つ）

### ② 金曜15:45の全決済のあと約60秒で発注が再開する（実口座）

週末決済が `prev_session = SESSION_WEEKEND` を立てていたため、次の60秒周回で
「週末から復帰した」と誤判定され、監視と発注が再開していた。

- 「本当に週末だった」と「週末決済を実行した」を別の値に分離
  （`SESSION_WEEKEND_CLOSED` を新設）
- 停止の維持は専用フラグ `_weekend_halt`（**決済に成功した回だけ**立てる）で行う。
  決済しきれなかった回は従来どおり監視を続ける
- 連休前決済（水曜など）のあと金曜まで解除されない不具合も同時に修正
- 「月曜プリマーケットまで待機」の説明9箇所を、実際の挙動に合わせて更新
  （プリマーケットが「発注しない」設定なら月曜09:30 まで、を明記）

### ③ 決済の戻り値が「全量決済できた」を意味していない

`place_close_all` は1本でも発注できれば `True` を返すため、**一部しか決済でき
なかった回に一斉決済の再試行が回らず、重大通知も出なかった**。

- 戻り値の型は変えず（呼び出し13箇所のうち8箇所は戻り値を見ない）、結果を
  「全量受付／一部受付／対象外／処理待ち／失敗」の5種類で呼び出し元へ渡す
- 一斉決済は「一部受付」を完了扱いにせず、再試行に回す
- 数量の食い違い（多い側・少ない側とも）も未完了として扱う

### ④ 夜間持ち越しの記録（C-3 / C-9）

- **C-3**: 日次サマリ（GAS）が拾える形の確定損益行を出す。従来は「記録のみ」の日
  だけが載り、**実際に売買した日が欠ける**という逆転が起きていた
- **C-9**: エントリー時のモメンタム5分/15分%が、次の建玉の記録に混ざらないよう
  発注時に確定させる（決済時にクリアすると、いま決済した玉の記録からも消えるため）

### entry_time（案A）

損切り・トレールの成功時に `entry_time` を返す。③が入ったことで「全量さばけた回」
だけに限定でき、残玉の時間切れ監視を落とさずに実施できるようになった。

Codex の反復レビュー（5回）で、修正の過程で作り込んだ後退
（連休前の停止解除漏れ・記録のクリア位置・部分決済でのカウンタ二重計上・
決済結果の記録漏れ・非同期の記録レース・失敗した一斉決済で監視が止まる・
停止通知の繰り返し）をすべて塞いだうえで ship 判定。

---

## v3.9.148 (2026-08-17) — 番兵の衝突による「決済したつもり」の修正（最優先）

認定サポーターの指摘で、v3.9.145 で追加した番兵 `RET_OWNED_SKIP = -2` が、
v3.9.63 からある `_RET_ALREADY_CLOSED = -2` と同じ値だったことが判明した。
どちらも同じ関数（`_close_one_position_id`）が返すため数値では区別できず、
**1本も発注していないのに全決済が成功（True）を返す**経路があった。
呼び出し側は成功と受け取って建玉の追跡をやめるため、証券会社側に建玉が
残ったまま Bot が見失う。**この版への更新を強く推奨する。**

- **番兵の衝突を解消**: `RET_OWNED_SKIP = -3`（`RET_OK`=0 / `RET_ERROR`=-1 /
  `_RET_ALREADY_CLOSED`=-2 のいずれとも重ならない）。全決済に見送り専用の分岐を
  設け、対象外を「既に決済済み」として消化しないようにした。
- **見送りと失敗の区別**: 見送りには「Bot 以外の建玉になった（決済対象外）」と
  「既存の決済注文の処理待ち（あとで再試行が要る）」の2種類がある。理由を持たせ、
  前者は完了を妨げず、後者は境界の一斉決済を完了扱いにしない。損切り・トレール・
  時間切れの各経路も見送りを失敗に数えず、誤ったアラートを出さない。
- **見送りの記録をスレッドごとに分離**: 週末の一斉決済とリスク監視が同じ銘柄を
  同時に扱ったときに互いの記録を消し合い、本当の失敗が握り潰される窓を閉じた。
- **建玉照会の空応答対策 (A-3)**: 証券会社の応答は「本当に建玉ゼロ」と「応答が
  空」を同じ 0 で返す。夜間持ち越しの3経路（買い注文の確認・翌朝の売却・予約の
  約定確認）で、再照会と注文照会の裏取りを経てから確定するようにした。
  これまでは、約定済みの建玉が管理外に出たり、売り注文を1本も出していないのに
  実在しない往復の確定損益が集計へ送られたりする可能性があった。
- **状態が読めないときの可視化**: 上記の裏取りが通らない間は安全側に倒して判断を
  保留する（誤って建玉を手放さないため）。保留が1時間続いたら通知し、以後も続く
  かぎり6時間ごとに繰り返す。保留中は新しい夜間持ち越しも行わない。
- **通知の間引き**: 30秒ごとに再入する経路から直接通知を出さないようにした
  （不調が続いた日に大量の通知が届くのを防ぐ）。状態ファイルの保存失敗通知も
  初回は即時・以後30分ごとに変更。
- ログ・通知の文言を実態に合わせた（「全 position_id 発注失敗」の内訳表示、
  「未約定とみなしリセット」→ 決済済みの場合も含む表現、
  「entry_time クリア」の誤記など）。

Codex の反復レビュー（11回）で、修正の過程で作り込んだ後退
（部分決済時の時間切れ監視の消失・見送り記録の競合・状態が読めないときの
無限保留・通知の氾濫）をすべて塞いだうえで ship 判定。

なお決済後も `entry_time` が残る件（損切り・トレール経路）は、認定サポーターの
報告にある案 A / 案 B の判断が必要なため、この版には含めていない。

---

## v3.9.147 (2026-08-16) — 起動バナーに OpenD の版数とログイン状態を表示

不具合の相談で「OpenD が古い」「相場サーバーに繋がっていない」が原因のケースが
繰り返し起きていたため、ログだけで切り分けられるようにする。表示のみの追加で、
売買のロジックには一切触れない。

    OpenD: ver=1009  取引ログイン=OK  相場ログイン=OK

- `get_global_state()` の `server_ver` / `trd_logined` / `qot_logined` を1行で表示。
  `server_ver` は数値のビルド番号で、OpenD アプリの画面表示（9.x.xxxx 形式）とは
  表記が異なるため加工せず生の値を出す。
- OpenD 停止時の対策を二段構えにした。SDK の `OpenQuoteContext` は接続先が居ないと
  コンストラクタが無限に再試行してブロックし（`set_sync_query_connect_timeout` は
  クエリ側にしか効かない）、しかも張られる再接続スレッドが daemon ではないため
  プロセスの終了まで妨げる（いずれも実測で確認）。そこで
  ①先に素の TCP 接続で当たりを取り、繋がらなければ SDK を触らない
  ②繋がっても応答しない場合に備え、別スレッドで10秒の期限を切る、とした。
  停止時は `OpenD: 接続できません（host:port）／OpenD が起動していない可能性があります`
  と表示して起動を続行する。

---

## v3.9.146 (2026-08-16) — 夜間持ち越しの建玉を日中予算から分離（A-11）

夜間持ち越し（OVN）が建玉を保有している間、その評価額が日中売買の予算
（BUDGET_USD）に計上され、保有分だけ日中の新規発注枠が狭まっていた。
OVN は OVN_BUDGET_USD の別枠で管理する設計のため、二重計上を解消する。

- **sync_positions の予算計上**: OVN 保有中の銘柄は「予算計上額の補正」の
  対象から除外。手動建玉（Bot 外）の計上は従来どおり残す（口座全体の露出を
  保守的に数える既存仕様）。
- **起動時復元**: OVN 保有銘柄は日中予算に計上しない。position_id の確認・
  台帳判定・外部建玉フラグの解除は従来どおり通す。
- 日中側の新規発注は建玉台帳のガード（Bot 以外の建玉がある銘柄は見送り）で
  引き続き保護されるため、同一銘柄の建玉が混ざることはない。

Codex の追レビューで、発注応答〜フラグ設定の間に sync が走ると計上が残留する
レースが見つかり、フラグを発注前に立てる形で同版内で修正した。残る2点
（応答なしで受理された注文の孤児化・日中と OVN の入口同士の相互レース）は
この版以前から存在する狭い窓で、既存の外部建玉分類が安全側に受け止める。
恒久対策は A-9 の識別子方式と合わせて次のまとまった改修で扱う。

なお A-9（BUY_INTENT からの復旧の identity 結合)は、注文に識別子を載せて
照会で突き合わせる設計と実機での往復確認が必要なため、この版には含めない。

---

## v3.9.145 (2026-08-16) — 修正方針の訂正版（認定サポーター）への対応5件

v3.9.144 の直後に届いた「修正方針の訂正版」への対応。訂正の指摘どおり、
v3.9.144 の A-6 は番兵 `-1` が SDK の `RET_ERROR` と衝突していた。

- **A-6 訂正**: 見送りの番兵を `RET_OWNED_SKIP = -2` に変更（`RET_OK`/`RET_ERROR` と
  衝突しない）。watchdog・チェイサーは見送りを情報ログのみで扱い、エラー扱いの
  リトライを続けない。他の呼び出しは失敗側に落ちるだけで安全（Codex確認済み）。
- **A-5 訂正**: 一斉決済の「対象外」銘柄を通知に反映。移行前全決済の完了通知は
  「Bot 管理の保有ポジションを全決済しました」＋対象外の注記つきになり、
  夜間持ち越しの保有が残っていても文面が矛盾しない。
- **A-4 訂正**: OVN 状態ファイルを環境ごとに分離（`ovn_state.REAL.json` /
  `ovn_state.DEMO.json`）。競合の余地自体を消す。旧形式は環境タグ一致のとき
  1回だけ引き継ぎ（マーカーで再読を防止・古い状態の復活経路を閉鎖）。
- **A-10 訂正**: 状態に口座IDを記録し、「記録なしは通す・有って食い違えば拒否・
  不正値も拒否・デモは照合しない」。ループ初期化の破棄条件にも適用。
  既存の保有状態は引き継がれ、この版に上げても管理を失わない。
- **入口 refresh**: 1日1回の入口ガードだけ `refresh_cache=True` で照会し、
  直前に建った建玉の見逃しを防ぐ（レート制限には収まることを確認済み）。

Codex の追レビューで見つかった3件（口座ID照合のループ初期化への未適用・
不正値の素通り・旧ファイルの復活経路）も同版内で修正済み。
自動検証 120 項目すべて合格。

---

## v3.9.144 (2026-08-16) — 実機6ケース検証の指摘対応（認定サポーター）＋週末見送り設定

認定サポーターによる実機6ケース検証と12点のコードレビュー（A-1〜A-11）のうち、
月曜のセッション前に入れる価値が明確な8点＋設定1点。全項目を Codex で追検証済み。

### 一斉決済の偽アラート（A-5・月曜の晩に確実に発火する組み合わせだった）

一斉決済（週末前・デモ日次・移行前）の対象選択が所有権を見ておらず、
夜間持ち越し（OVN）の建玉も対象に含めていた。決済側の所有権ガードが守って
False を返すため「決済失敗」として集約され、v3.9.142 の再試行と組み合わさると
**正常なのに🔴通知が最大5回出て、本物の決済失敗が埋もれる**。
CLOSE_BEFORE_INACTIVE=true の環境では、OVN が建玉を持つ日の移行前決済
（19:55 ET）で毎回発生する。対象選択の時点で除外し「対象外」ログに変更。

### 入口ガードがショートの共存を見逃す（A-1）

実口座はロングとショートが別建玉として共存できるが、OVN の入口は
ロングだけを数えていた。ショートが残ったまま OVN が買うと、銘柄単位の
所有権ガードでショートまで日中の監視から外れてしまう。
`_ovn_position_all()`（ロング・ショート別集計）を新設し、入口は
「両方ゼロ」のときだけ買う。既存の呼び出しは3要素互換のまま。

### 一過性の照会失敗が1日1回の試行枠を消費（実機検証②）

入口の照会失敗4経路（建玉・注文・日足・発注直前価格）でも entry_date が
消費され、15:56〜15:59 の残り巡回で再挑戦しなかった。OpenD が一瞬詰まる
だけでその日を落とす。失敗時のみ entry_date を返上して再挑戦する。
条件不成立・建玉混在の見送りは従来どおり1日1回で確定。

### そのほかの修正

- 終端判定に SDK 定義の `DISABLED`（已失効）を追加（A-7）。無いと失効注文を
  生存扱いし、取消・再照会を反復し続ける。
- 引け後の売り予約分岐に建玉照会の失敗・ゼロの検査を追加（A-2）。失敗を
  数量ゼロと誤解して予約を出し損ね、その晩の「Bot を止めても売れる」保険を
  失っていた。ゼロは再照会で裏を取ってから確定する。
- `_close_one_position_id` の所有権ガードが裸の False を返していた（A-6）。
  呼び出し側の2要素展開で TypeError になるため `(-1, 理由)` に統一。
- `place_short` にも決済失敗ロックの検査を追加（A-8）。place_buy には
  v2.97 からあった検査が SHORT 側に無かった。
- 日次集計の発注件数が `[ORDER] SHORT` を拾えていなかった（正規表現に追加）。

### OVN_SKIP_WEEKEND 設定（新規・既定 false）

週末（閉場ちょうど2日）の持ち越しを見送る任意設定。既定は従来どおり
持ち越す（バックテスト・実口座検証済みの挙動を変えない）。連休判定
（閉場3日以上）とは独立で、起動バナーに方針を1行表示する。

自動検証 111 項目すべて合格（既存101＋今回の targeted 10）。

---

## v3.9.143 (2026-08-16) — 起動時の「推定復元」を廃止し、建玉不明時は実口座の新規発注を止める

認定サポーターと外部レビューが独立に指摘した残件（P1-3）。

### 何が問題だったか

起動時に建玉明細の照会（position_list_query）が空でも、口座の評価額
（accinfo）が非ゼロなら「建玉はあるが中身が不明」と分かる。従来はここで
評価額÷現在値から株数を**推定**して内部状態に書き込み、「発注は継続します」
と通知していた。

- 推定した建玉には position_id が無く、**決済すらできない**（指摘済み）。
  推定を続ける利点がそもそも無い。
- 何を保有しているか分からないまま、実口座で新規発注できる穴だけが残る。

### 変更点

- 推定復元を削除。建玉不明フラグを立て、実口座の新規発注を止める。
- 明細の照会が成功し建玉を確認できた時点で、フラグは自動解除される（従来からの経路）。
- 既存建玉の損切り・監視・決済・watchdog は一切ゲートしない（Codex で確認済み）。
- デモ口座の発注は止まらない（発注ゲートは実口座限定）。

### Codexレビューで見つかった通知の不備も修正

- デモ口座にも「新規発注を止めています」という事実と異なる Discord が
  届く経路があった。専用通知を撤去し、既存の実口座限定の汎用通知に集約
  （実口座で2通重複する問題も同時に解消）。
- 定期表示の「発注ブロック中」も、デモでは「デモのため発注は継続」と
  表示するよう修正。

自動検証 101 項目すべて合格。

---

## v3.9.142 (2026-08-15) — v3.9.141 の追いレビューで見つかった3件を塞ぐ（Fable 5 + Codex 二重レビュー）

v3.9.141 の修正自体を、モデルと Codex の両方で再レビューした。双方が同じ
最重要欠陥（週末決済の戻り値無視）に到達し、モデル側レビューはさらに
v3.9.141 のコミット後の姿にしか無い後退を1件検出した。

### 🔴 建玉ゼロが「失敗」扱いになる後退（v3.9.141 で混入・未配布のうちに検出）

`close_all_for_weekend()` の「決済対象なし」の早期 return が裸の `return`
（= None = 偽）のままだった。v3.9.141 で呼び出し側に成否判定を入れたため、
**建玉ゼロの正常な状態を失敗と誤判定し、移行前全決済が毎分エラーを出し続ける**。
`CLOSE_BEFORE_INACTIVE=true` の環境（Win365 含む）では毎日発火していた。
対象なし＝成功（True）に修正。

### 週末決済・デモ日次決済が失敗しても再試行せず監視まで止めていた（既存）

呼び出し側が戻り値を無視し、完了フラグを決済の**前**に立てていたため、
決済に失敗してもその日は再試行されず、`market_open_event.clear()` で
損切り・時間切れの監視まで止めて建玉を持ち越していた。
- 失敗時は 60 秒間隔で最大 4 回再試行する。
- それでも決済しきれない場合は**監視を止めずに**通常運転を続け、
  moomoo アプリでの確認を促す（損切り・時間切れは動き続ける）。

### Windows にプロセス間ロックが無かった（v3.9.141 の残余）

台帳のプロセス間ロックは `fcntl` 前提で、Windows（Win365 本番）では
実質無効だった。`msvcrt.locking` による同等の排他を追加。取得に失敗した
場合は従来どおり無ロックで続行する（悪化しない構造）。

### 検証

- 自動検証 101 項目すべて合格（OVN 24 / 台帳 27 / v3.9.141 検証 34 /
  モック実機 16）。
- モック実機試験: 偽の証券会社を差した実関数で、部分決済の重複発注ガード・
  取消未確定時の見送りと deferred 記録・建玉ゼロの成功判定を実走で確認。
  実プロセス2本 × 各50回の同時保存で台帳の両環境が残ることを確認（fcntl 実動作）。
  実 OpenD への読み取り照会で accinfo 3列が読めることを確認。
- 発注を伴う実機テスト（トレール発動・60分経過後の決済・LONG/SHORT）は
  市場が閉まっているため未実施。週明けに実施する。

### 見送った指摘（次版で判断）

- パニックセル・損切り・トレール経路は「見送り」を失敗として警告する
  （動作は安全側: 次サイクルで再試行され、二重発注は起きない。文言のみ誤解を招く）。
- 再同期後の時間切れ決済経路も deferred を読まない（同上・安全側）。
- 上限超過時の「部分決済に失敗」の文言（実際は自注文の約定待ち）。

---

## v3.9.141 (2026-08-15) — 決済まわりの安全性を固める（認定サポーター2名からの指摘 + Codexレビュー）

v3.9.139 で入れた監視継続の修正に、後退が含まれていた。あわせて以前から
残っていた決済・記録まわりの欠陥をまとめて塞ぐ。

### 🔴 v3.9.139 で作り込んだ後退（最優先）

`place_close_partial()` は `-> None` で、成功時も `None` を返していた。
v3.9.139 で追加した成否判定は必ず「失敗」に倒れ、`continue` せず後続へ進む。
その結果、**部分決済を受け付けた直後の同じサイクルで損切り・トレールが成立すると、
未約定の部分決済を残したまま全決済を重ねて発注する**経路ができていた。
売り過ぎ・ショート反転になりうる。

- 戻り値を `bool` に変更。早期returnはすべて `False`、1件でも注文を出せたら `True`。
- 成功時は必ず次のサイクルへ送り、同期済みの数量で再評価する。
- 未約定の決済注文が残っている間は、部分決済を新たに出さない。

### 決済の二重発注（以前から）

- `_cancel_pending_closes_for_symbol()` は「試みた件数」を返していたため、
  取消に失敗して生きた決済注文が残っていても、呼び出し側は成功と解釈して
  差し替えを発注していた。**確定した件数**を返すように変更。
- `place_close_all()` は、取消できていない決済注文が残っていたら決済を見送る。
- 「安全のため見送った」と「発注に失敗した」を区別する。見送りは連続失敗
  カウントを進めない（誤った重大通知・強制同期を防ぐ）。

### 起動時の建玉判定

- `short_mv` はショート建玉に対して**負**で返る（デモ口座で実測。1株の空売りで −24.86）。
  `_v > 0` ではショートを検知できず、ショートだけの口座で「建玉なし」と誤判定していた。
  `abs(_v) > 0` に変更。
- `_startup_position_unknown = True` の代入がコード上どこにも存在せず、実口座の
  発注ブロック2か所が**到達不能**だった。照会の失敗・空応答・列の欠落・例外の
  4経路でフラグを立てる。3列すべてを読めたときだけ「確定ゼロ」と見なす。
- デモ口座の挙動は変えていない（ブロックは実口座のみ）。

### 部分約定の扱い

- 約定確認の**リトライ経路**だけ `"filled" in status` の部分文字列一致が残っており、
  `FILLED_PART` を全約定として処理していた（主経路は v3.9.120 で厳密化済み）。
  部分約定は追跡を維持し、残量は watchdog の取消・再発注に委ねる。

### 建玉台帳

- REAL と DEMO を同時に動かすと、後から保存した側が先行の更新を消していた。
  保存直前にファイルを読み直し、自分の環境のキーだけ差し替える方式に変更。
- プロセス間ロック（`fcntl.flock`）を追加。読み込み〜差し替えの全体を守る。
- 台帳が壊れて読めないときは**保存しない**。自分の環境だけで上書きすると、
  もう一方の環境の記録を巻き込んで消すため。
- `fsync` を追加。停電・強制終了での消失を防ぐ。

### 日次集計

- ログ解析の正規表現が LONG 形式（`[確定損益]` + `sell_avg` + `buy_avg`）だけに
  対応しており、SHORT が1件も拾えず「損益N/A・決済0回」になっていた。
  実在する4種類のタグ（`[確定損益]` `(SC)` `(リトライ)` `(SC・リトライ)`）すべてを
  拾い、決済方向を問わず `realized_pnl` と `qty` だけを見る形に統一。
- 一斉決済 `close_all_for_weekend()` は各決済の戻り値を捨てて `None` を返しており、
  全決済が失敗しても「全決済しました」と通知していた。成否を集約し、全成功で
  ないかぎり完了マーカーを立てず、次の巡回で再試行する。

### 秘匿情報

- マスクが `record.msg` にしか掛かっておらず、トレースバックと Discord 本文が
  素通りしていた。`mask_text()` を切り出し、トレースバックと Discord 送信直前にも適用。

---

## v3.9.140 / GAS v9.22 (2026-08-14) — 夜間持ち越しの売買を集計シートに載せる／OVN建玉の表示を直す

### 集計シートへの記録（新規）

夜間持ち越し（OVN）は日中のニュース売買と経路を分けているため、決済しても
`_send_trade_result` を通らず、**集計シートにも日次サマリにも残らなかった**。
Discord とログにしか記録がなく、複数環境の成績を横断で集計できない状態だった。

- `_ovn_report_trade()` を追加。決済が確定した2経路（予約が寄り付きで約定 /
  翌朝 Bot が売却）から呼び、既存の `_send_trade_result` に相乗りして送る。
- 建値は発注直前価格を状態に保存し、約定後に実際の平均取得単価で上書きする。
- `ai_category="OVN"` / `session="ovn"` で送るため、モメンタムの日次・週次
  損失上限（サーキットブレーカー）には積まれない。
- GAS の payload に `strategy`（`NEWS` / `MOMENTUM` / `OVN`）を追加。
  GAS 側はトレード詳細の53列目「戦略」で受ける（v9.22）。
  Bot v3.9.140 未満から届いた行は空欄になるので、集計時は空欄＝NEWS として扱う。

記録の送信に失敗しても売買には影響しない（例外を握って警告のみ）。

### OVN建玉の表示（修正）

実口座の検証中に、OVN が買った建玉へ
「⚠️ Bot 以外の建玉のため自動売買は停止中」と表示されていた。
日中ロジックが触らないのは設計どおりだが、理由の表示が誤りだった。
サポーター向け資料で「この表示が QQQ に出たら不具合」と案内していたため、
そのままでは正常動作を不具合として報告させてしまう。

- OVN の建玉には「🌙 夜間持ち越しが管理中です（日中の自動売買は触りません。
  翌朝の寄り付きで売ります）」を INFO で表示する。
- 本当に Bot 以外の建玉のときだけ、従来の警告を WARNING で出す。

---

## v3.9.139 (2026-08-14) — 時間切れ判定で損切り・トレールの監視が止まっていた不具合を直す

認定サポーターからの指摘。`risk_monitor_loop` の銘柄ごとの処理は
「① 上限超過 → ② 時間切れ → ③ 株価取得 → ④ 損切り → ⑤ トレール」の順に走るが、
①②の分岐が `continue` で次サイクルへ飛ばしており、③〜⑤に到達しない経路があった。

### 何が起きていたか

- **モメンタム建玉でトレールが有効になったあと、時間切れの時刻を過ぎると、
  以降どのサイクルでも損切りもトレールも評価されない。**
  「トレールで利確を待つ」ためにタイムアウトを見送る設計だったが、
  その待つ相手であるトレール自体が動かなくなっていた。無防備な建玉が残る。
- 決済発注のクールダウン（60秒）中も同様に監視が飛んでいた。
- ポジション上限超過の分岐は、部分決済が**失敗しても**無条件に飛ばしていた。
  上限超過が続くあいだ、ずっと監視されない。

### 変更点

- 時間切れの節を「決済するかどうか」だけの判定に変え、見送り・クールダウンでは
  `continue` せず ③〜⑤ へ進むようにした。実際に決済を試みた場合のみ従来どおり次サイクルへ。
- 上限超過の部分決済は戻り値を見て、成功時のみ `continue`。失敗時は警告を出して監視を継続する。

### 影響

決済の条件は変えていない。これまで決済されていた場面はそのまま決済される。
変わるのは「決済しなかったときに監視を続けるかどうか」だけ。

---

## v3.9.138 (2026-08-13) — 夜間持ち越しの金額設定を明確化し、紛らわしいログ文言を直す

- `OVN_BUDGET_USD` の「0 なら1株」を廃止。未設定・0以下なら買わない。
  0 は予算ゼロなのに買う挙動で直感に反し、実際に混乱を招いたため。
  1株だけ試す場合は 1株ぶんより少し多い金額を書く（例 800）。
- 1株に足りない場合の通知を、前日終値ではなく発注直前価格で比較する。
- 起動時に、夜間持ち越しの金額が Bot 本体の予算とは別枠であることを表示する。
- `sync_positions` の「外部保有ポジション検出」を「建玉あり・予算計上が未設定」に改称。
  v3.9.134 の建玉台帳の「Bot 以外の建玉」と紛らわしく、不具合と誤読されたため。
  予算計上額の補正のみで自動売買の停止とは無関係。動作は変えず表示のみ。

---

## v3.9.137 (2026-08-13) — 夜間持ち越しの「記録だけ取る」モードを日次成績に反映

`OVN_MODE=shadow`（記録だけ取る）は、これまで条件が成立した時点で終わっており、成績がどこにも残らなかった。実際に発注する前に「使っていたらどうなったか」を利用者自身が確かめられるようにする。

### 変更点

- `SHADOW_HELD` フェーズを追加。条件が成立した日は、発注はせずに発注直前価格と株数を保存して翌朝まで持ち越す。
- 翌営業日 9:31 ET に気配値を取得し、買っていた場合の損益を算出して Discord 通知＋ログに残す。
- 日次サマリ（Discord・端末）の「★ シャドー成績」ブロックに `▼ 夜間持ち越し(記録のみ)` を追加。件数・勝敗・仮想損益・1件ごとの `買値 → 売値` を表示する。
- 集計シート送信の payload に `ovnshadow_trades / ovnshadow_pnl / ovnshadow_pct / ovnshadow_wins / ovnshadow_losses` を追加。実発注の確定損益とは別枠で集計し、混ざらない。

### 安全性

- 記録のみのフェーズ名は実建玉を示すフェーズ一覧に含めないため、起動時の建玉復元や日中売買のガードには影響しない。
- 気配値が取れない場合はその回を記録せず `DONE` にする（誤った数字を残さない）。

---

## v3.9.136 (2026-08-13) — 夜間持ち越し（OVN）を隠し機能として追加（既定は無効）

### 何をするか

引け際（15:55 ET）に QQQ を買い、翌営業日の寄り付き（9:31 ET）で売る。昼のニュース売買が動いていない時間だけを使う。

買う条件は「QQQ の前日終値 > 200日移動平均」かつ「VIXY の前日比が閾値以下」。連休の前と休場日は持ち越さない。

### 設定（`.env` のみ。Wizard には出さない）

| 設定 | 既定 | 内容 |
|---|---|---|
| `OVN_ENABLED` | **false** | 書かなければ何も起きない |
| `OVN_MODE` | live | live=実際に売買 / shadow=記録のみ |
| `OVN_VIX_LEVEL` | normal | loose 0% / normal −2% / strict −3% |
| `OVN_BUDGET_USD` | 0 | 0 は「1株だけ買う」。金額を書くとその範囲で株数を決める |
| `OVN_SKIP_LONG_HOLIDAY` | true | 連休の前は持ち越さない |

**既定が無効なので、設定を書かない環境の挙動は一切変わらない。**

### 日中のニュース売買との分離

同じ QQQ を両方が扱うため、これが無いと日中の時間切れ決済が OVN の建玉を売ってしまう。`TickerState.ovn_held` を追加し、決済3経路・発注ガード・監視ループの計6箇所を `_is_other_owner()` 経由に統一した。建玉台帳（v3.9.134）には OVN の建玉を載せないため、`sync_positions` 側にも専用の分岐を入れてある。

### 実口座とデモの違い

実口座では引け後（16:05 ET）に `session=RTH` の成行売りを置くと翌寄りで約定する（2026-08-07 引け後に発注 → 2026-08-10 09:30 に約定を実機で確認）。デモは当日20時 ET で失効するため予約を使わず、翌朝 9:31 に売る。

### 開発中に見つけて直した主なもの

- **日足を数か月前の値で判定していた。** `request_history_kline(max_count=N)` は「範囲の先頭から N 本」を返す。1回の呼び出しでは古い側しか取れず、最終足が 2026-04-08（実際は 08-12）だった。ページを最後まで手繰り、昇順に整えて新しい側の N 本を使う。途中失敗・ページキーの反復・列欠落は空にして発注を見送る
- 発注の「受付」を約定とみなしていたのを、証券会社への照会で終端確認するまで進めないよう変更（`BUY_INTENT` / `BUY_PENDING`）
- 発注**前**に状態を永続化し、落ちても口座の実建玉から復元できるようにした
- 買う前に証券会社側の未約定注文を確認する（アプリでの手動発注を検出）
- 金曜に月曜向けの予約を出したまま稼働すると、土曜に取り消して休場中に売り直す経路があった
- 実口座（JP）の決済に必要な `position_id` と特定口座の指定が抜けていた
- 状態ファイルに口座種別を記録し、実口座とデモの取り違えを防ぐ
- `Session` を任意 import にした（古い SDK でも起動が壊れない）

### 検証

Codex による多層レビューを重ね、自動テスト24項目に合格（既定で無効・状態の保存と口座別復元・日中ロジックからの隔離4経路・証券会社への照会・日足取得）。市場が開いている時間でしか確認できない項目（引け際の買い、引け後の予約、翌寄りの売り）はデモ口座で実施する。


## v3.9.135 (2026-08-12) — 取引ロックの警告を鳴らし続けないようにする（売買挙動は不変）

### 症状

OpenD でトレードロックを解除しても、赤い警告が60秒おきに鳴り続けていた。

`_trade_locked` フラグを下ろす条件が「新規ロングの発注が成功したとき」だけだったため。発注が1日出ない日もあり、その間ずっと鳴り続ける。解除済みの利用者からは「解除できていない」ようにしか見えない。

あわせて、警告文に「moomoo アプリの場合」という案内があったが、**この Bot は OpenD とだけ通信する**ため、アプリ側で操作しても解除されない。誤解を招く記載だった。

### 調べたこと

moomoo にロック状態を照会する API は無い。

- SDK の `Trd_UnlockTrade` 応答（S2C）は空で、状態を返さない
- `get_global_state` にロックの項目は無い（市場状態とログイン状態のみ）
- 公式ドキュメントに **"No query API exists."** と明記

ただし、ロック解除が必要なのは **発注** と **注文の変更・取消** だけで、建玉照会などはロック中でも通る。ここから、**存在しない注文IDの取消**を投げれば、注文を1件も出さずに判定できる。

- ロック中 → unlock を促すエラー（取消の手前で弾かれる）
- 解除済み → 「このorderIDは存在しません」（ロック検査は通過）

実口座（解除済み）で後者を確認した。

### 変更

- **定期的な鳴らし直しを廃止**。警告は「実際に発注がブロックされた瞬間」だけ。連続でブロックされた場合も30分に1回まで
- ロック中と記録している間だけ、**10分おきに上記の方法で状態を確認**し、解除されていれば自動で表示を消す（`_probe_trade_unlocked`）。注文は出さない。moomoo の上限10回/30秒に対して十分な余裕がある。デモは解除不要なので確認しない
- 確認できるまでは `[状況]` 行に静かに1行だけ出す（失敗した時刻つき。それより後に解除したかを利用者が判断できる）
- **フラグを下ろす経路を3つに拡大**。従来は新規ロングのみ → 新規ショート・決済でも下ろす
- 警告文から moomoo アプリの案内を削除し、「アプリ側で操作しても解除されません」と明記

### 影響

表示と通知のみ。売買の判断・発注・決済のロジックは変えていない。

---

## v3.9.134 (2026-08-11) — 🔴【重要】Bot が建てていない建玉を決済しないようにする（建玉台帳）

### 症状（実口座で発生を確認）

Bot は「口座にある建玉はすべて自分のもの」として起動時に取り込み、時間切れ・損切り・トレールの管理下に置いていた。このため、**利用者が同じ口座で SPY / QQQ / SMH を手で保有していると、Bot がそれを勝手に決済する**。

2026-08-11、実口座で実際に発生した。別用途で買っていた QQQ 1株を Bot が起動時に自分の建玉として取り込み、232分後に時間切れ決済で処分した。

```
17:00:58 ▼ SELL │【QQQ】│ 1株 @ $718.20 │ premarket │ 時間切れ決済: 232.4分経過 │ 実口座
```

232.4分は「建玉を持ってからの時間」ではなく「Bot を起動してからの時間」。取り込み時に `entry_time` を現在時刻へ設定していたため、再起動のたびに時間切れの時計が巻き戻る問題も抱えていた。

### 変更

**建玉台帳 `bot_position_ledger.json` を導入し、Bot が建玉を持っている銘柄を記録する。**

台帳が答えるのは **「この銘柄で Bot は建玉を持っているか」だけ**。株数は突き合わせない。株数は部分約定・発注受付・取消・再起動・API 遅延で常にズレるため、判定材料にすると一時的なズレと本物の食い違いを取り違え、**Bot 自身の損切りを止めてしまう**（開発中に実際に起きたため、この設計に改めた）。

| 口座の状態 | 動き |
|---|---|
| 台帳にこの銘柄が無い ＋ 建玉がある | 自動売買を停止（決済も新規発注もしない） |
| 台帳にこの銘柄がある | 従来どおり管理（**挙動は不変**） |

- 決済は `place_close_all` / `place_close_partial` / `_close_one_position_id` の3箇所すべてでガード。実際に発注する低レベル関数を含むため、時間切れ・損切り・トレール・パニック・watchdog・チェイサーのどの経路からも決済されない
- 新規発注も止める（決済が銘柄単位の全決済のため、混ぜると Bot の分だけ決済できなくなる）
- 建玉が口座から無くなれば自動的に再開する

**自動売買が止まっていることに気づけるよう、4箇所で知らせる。**

1. 検出した瞬間に Discord
2. 画面の `[状況]` 行に常時表示（`⚠️ 自動売買停止中: QQQ`）
3. 一定時間ごとに Discord で再通知（既定6時間・`EXT_HOLD_REMIND_HOURS` で変更可）
4. 発注機会を見送ったときにも通知

**副次的な改善**: Bot の建玉は `entry_time` を台帳から復元するため、**再起動しても時間切れの時計が巻き戻らなくなった**。

### 影響

- Bot だけで運用している場合、**挙動は一切変わらない**（台帳に全建玉が載るため）
- この版に上げた時点で建玉を持っていた場合、初回起動に限りその建玉を Bot の建玉として台帳に取り込む（＝旧版と同じ扱い）。取り込んだことは Discord で通知する
- 台帳ファイルが読めない場合は既存建玉をすべて管理対象外にしたうえで、log.error と Discord で強く警告する

### 残る限界

Bot がすでにその銘柄の建玉を持っている最中に手で買い増した分は巻き込まれる。これは v3.9.133 以前と同じ挙動で、悪化はしない。

### その他

- コード内コメントに残っていた個人名を「利用者X」表記に統一（売買挙動は不変）

---

## v3.9.133 / GAS v9.21 (2026-08-08) — 損切り%の記録を実挙動に合わせる（売買挙動は不変）

### 背景（今週の全トレード集計で判明）
シートの「実効損切り%」列は、モメンタム建玉でも**常に高ボラ倍率を掛けた値**を記録していた（`_send_trade_result`）。しかし `risk_monitor_loop` はモメンタム建玉に対して倍率を**意図的にスキップ**する実装（v3.9.93 の方針）のため、**記録=1.50% / 実際=0.50%（SMH・標準プロファイル）** と系統的に乖離していた。強制損切りだけでなく建玉トレールも同じ経路。**この列を根拠にした損切り幅・銘柄プロファイルのチューニング結論は無効**になるため是正した。

なお v3.9.114/v3.9.123 のコメントは「モメンタムは損切り幅のみボラ別に拡大する」と記載しており、**実行コードが方針変更に追従していない**状態だった（実挙動は一律 0.50%）。本版は記録の是正のみで、**損切りの実挙動は一切変更していない**（方針の確定は別途・シャドー計測で検証予定）。

### 変更
- `TickerState.enforced_stop_pct` を追加。`risk_monitor_loop` が損切り%を確定した直後にその値を保存する。
- 決済記録は**決済時点のスナップショットを引数で受け取り**、それをそのまま出力する（本処理は別スレッドで走るため、後から state を読むと同一銘柄の次の建玉や sync のクリアで値が入れ替わりうる）。
- 監視ループが一度も評価していない建玉のみ設計値で補完。優先順位を監視ループと一致させた（決算 > モメンタム > 通常。モメンタムは倍率を掛けない）。
- 建玉がフラットになった時点で `enforced_stop_pct` をクリア（前の建玉の値が次の記録に紛れるのを防ぐ）。
- 送信項目に `base_stop_pct`（設定の素の値・倍率なし）と `stop_pct_applied`（1=この決済で損切り閾値が発動 / 0=非発動）を追加。利確トレール決済は損切り閾値を使わないため 0 になる。
- **GAS v9.21**：トレード詳細に 51列目「設定損切り%」・52列目「損切り発動」を追加（末尾追加のため既存の列位置と `TRADE_ID_COL=38` は不変）。旧Botからの送信では両列が空欄になる（後方互換）。

### 注意
- **GAS v9.21 を先にデプロイしてから本版を配布する**こと。逆順だと新しい2列がシートに保存されない。

---

## v3.9.132 (2026-08-08) — エラーログの分類是正（1,548件→630件・売買挙動は不変）

今週のエラーログ1,548件を精査したところ、**本物のエラーでないものが多数混入**しており、真のエラーが埋もれていた。

- **情報バナーの除外漏れを追加**：「戦略プロファイル:」（165件）・「実口座モードが有効」（61件）・「3連敗到達／サイズを50%に縮小」（設計どおりの自動調整）を除外リストへ追加。
- **一時的エラーの途中経過を除外**（新規 `_ERROR_LOG_TRANSIENT_TAGS`）：Alpaca再接続待機・Anthropic再試行(N/M)・RSS初回失敗。**再試行後も回復しない最終的な失敗（「接続不全」等）は従来どおり記録される**。
- **🔴 影響区分の判定バグを修正**：「実発注は SHORTのみ」等の**説明文に含まれる「発注」に反応**して、情報バナー178件が `execution_risk`（売買に影響する高リスク）へ誤分類されていた。情報バナーをキーワード判定より先に `warning_only` へ確定させるよう修正。→ `execution_risk` が 353件→175件となり、本当に対処が必要なエラーだけが残る。

今週の実データで検証：1,548件 → **630件（41%）**。残ったものは sync_positions 失敗87件・Anthropic接続60件・約定確認リトライ37件・Alpaca認証失敗など、いずれも実在のエラー。

---

## v3.9.131 (2026-08-07) — ハートビート監視を追加（イベントループ凍結の検知・通知）

複数の利用者環境で「Botが数時間、ログもDiscordも一切出さず完全沈黙する」事象（イベントループ凍結）が v3.9.130 でも再現していた（最長243分・[Cache統計]の定期出力も欠落）。凍結中は asyncio 上の通知ロジック自体が動けないため、既存の警告（consecutive_fail ベース）では検知できない。

### 実装：ハートビート監視（既定で有効・通知のみ・自動再起動なし）
- **beatループ**（asyncio）：20秒ごとに「鼓動」を刻む。イベントループが凍結すると鼓動が止まる。
- **監視スレッド**（独立OSスレッド）：30秒ごとに「最後の鼓動からの経過」を確認し、`HEARTBEAT_TIMEOUT_MIN`（既定10分・2〜120分）以上無音なら**端末（赤字）＋Discord＋ログへ警告**（30分おきに再通知）。asyncio が止まっても OS スレッドは動くため、完全沈黙を破って通知できる。
- **復帰通知**：鼓動が再開したら「約N分の無応答がありました」と凍結時間つきで1回通知（凍結中の警告がネットワーク断で届かなかった場合でも、復帰通知単体で全容が伝わる）。
- `.env`: `HEARTBEAT_WATCHDOG_ENABLED`（既定 true）／`HEARTBEAT_TIMEOUT_MIN`（既定 10）。

### 品質確認（二重レビュー＋シミュレーションテスト）
- 詳細レビューで2件修正：①初回アラート抑制バグ（monotonic はOS起動からの経過秒のため、「PC再起動→Bot起動→30分以内に凍結」で初回警告が抑制される）→ None センチネル方式に変更。②通知順序（Discord/print を logging より先に）。
- 外部レビュー（Codex）指摘2件を追加修正：③警告送信を使い捨てスレッドに分離（logging ロックが凍結に巻き込まれた病的ケースでも監視本体が塞がれない）。④復帰通知に凍結時間を記載。
- 状態機械のシミュレーション3ケース PASS（起動直後の凍結で初回発火／90分凍結で30分毎3回＋復帰通知／正常時は無通知）。
- 既知の挙動：Windows で長いスリープから復帰した直後に「応答なし→復帰」のペア通知が1回出ることがある（実質「Bot が止まっていた」事実の通知であり許容）。

### 補足
- 通知のみで自動再起動はしない（実口座では OpenD 再起動後に取引ロックが再設定され、一見正常でも発注不能になる危険があるため）。
- 凍結の根本源（async ループ内の未ラップなブロッキングSDK呼び出し）の棚卸しでは、`close_order_chaser` に未ラップ呼び出しが現存することを確認済み。非同期化とセットで次版以降に対応予定。

---

## v3.9.130 (2026-07-31) — OpenD障害時の通知漏れ修正 ＋ 決済/観察PnLの堅牢化（実発注ロジックは不変）

OpenD無応答/切断まわりの通知と、決済処理・観察PnL算出まわりの堅牢化。実発注の判定・サイズ・利確/損切りの設定値は変更なし（安全側の追加のみ）。

### 1. OpenD障害時の通知漏れを修正（sync_positions）
`sync_positions` の外側 `except Exception` が失敗を `log.warning` のみで握りつぶし、`_sync_health["consecutive_fail"]` を加算していなかった。OpenDプロセスがクラッシュ/消滅して `_trade_ctx()`/照会が「ret=-1」ではなく「例外」を投げると、失敗が握りつぶされ `health_warning_loop` の発火条件（consecutive_fail>=3）に到達せず、数時間ポジション取得できなくても Discord 通知ゼロになる不具合（利用者報告：約5時間の停止・通知ゼロの実測と一致）。→ 例外失敗も consecutive_fail 加算＋last_error 記録し、検知→通知が出るように修正。前版 v3.9.129 の to_thread 化（イベントループ凍結対策）とは別箇所のため本修正が必要。

### 2. 決済取消の追跡を堅牢化（_cancel_pending_closes_for_symbol）
`finally: pop` が取消の成否に関わらず追跡から外していたため、取消失敗（生きた決済注文が残った状態）でも `_pending_orders` から消え、watchdog のタイムアウト取消・重複整理が届かず、古い決済＋新しい決済が両方約定 → 売り過ぎ（5/27 型ショート反転）の起点になり得た。→ 取消成功時のみ pop。未確定は追跡に残置し watchdog のタイムアウト取消に委譲。

### 3. 損切りチェイスに軽量ガード（close_order_chaser）
`modify_order(CANCEL)` の戻りは「受付」までで実際の取消確定を含まないのに、受付時点で再発注していた（二重決済の余地）。→ 取消受付後に `order_list_query` で1回確認し、「取消/失敗」を確認できた時だけ再発注。「生存/約定済み」を確認した時は今サイクルの再発注を見送り（5秒後に再評価）。照会失敗時は従来どおり再発注（現行動作と同等・悪化なし）。※本格の非同期化（ポーリング確認）は別途。

### 4. 観察PnLの最寄りバー抽出に±3分の許容
`pnl_Nmin` の「+N分に最も近いバー」抽出が argmin で上限なし、かつ候補に -120分側のウォームアップバーも含むため、+N分にデータが届かない場合に判定時刻より前のバーが無警告で採用され、計測値が静かに歪む可能性があった。→ ±3分（180秒）を超える最寄りバーは欠測（None）で記録。

---

## v3.9.129 (2026-07-25) — 🔴【重要】OpenD無応答でBotが完全沈黙する不具合を修正（利用者A報告・利用者D検証）

### 症状（利用者A・実口座）
Botログが**完全に沈黙**（エラーも停止メッセージも無し・前後は正常）。7/23=正確に120分／7/14=約4時間54分。PCスリープ・OpenDクラッシュ・pythonクラッシュはいずれも本人が消去法で否定。

### 根本原因（実コード＋実機SDKでCONFIRMED）
1. **ブロッキングSDK呼び出しがイベントループを凍結**：`risk_monitor_loop`（async）が `sync_positions()`（内部で `ctx.position_list_query` ＝ブロッキング）を **`asyncio.to_thread` 無しで直呼び**（L16069/16262/16347）。他に process_stock_news / process_dynamic_stock / main にも同型の直呼びあり。`main()` は14本のループを単一 `asyncio.gather` で回すため、1本が固まると**全ループが同時に沈黙**。例外でも戻りでもないので `_loop_guard`（例外時再起動）も health watchdog（`consecutive_fail` 前提）もすり抜ける。
2. **接続待ちが無限**：実機SDK（moomoo 10.05.6508）を精査。応答は内部で12秒(query)/33秒(conn_alive)で頭打ちされるが、**「接続READY待ち」は `_sync_query_connect_timeout=None`（＝無限）**（`open_context_base.py` L294-299）。OpenDがTCPは生きたままアプリだけ無応答になるとここで永久待ち。Botはこの設定を一度も呼んでいなかった（grep 0件）。「正確に120分」はWindows既定TCP keepalive(2h)が半死接続をようやく破棄→復帰した線と整合。

### 修正（多層防御 (a)+(b)）
- **(a) to_thread化**：async関数内の `sync_positions` 直呼び7箇所を `await asyncio.to_thread(sync_positions, trd_env)` に変更（L14709/15124/16069/16262/16347/17599/17608）。OpenDが固まってもワーカースレッドが待つだけで**イベントループは凍らない＝全停止しない**。※sync関数(place_buy等)内の直呼びは元からto_thread経由で呼ばれるため対象外。既存16485は元からto_thread済み＝この変更は既存パターンと整合。
- **(b) 接続タイムアウト明示**：`_apply_ctx_timeout()` を新設し、`_make_trade_ctx`/`_make_quote_ctx` の生成直後に `ctx.set_sync_query_connect_timeout(MOOMOO_CONNECT_TIMEOUT_SEC=既定20秒)` を設定。接続待ちの無限を有限化。旧SDK互換のため getattr で防御（メソッドが無ければ黙ってスキップ）。`.env` の `MOOMOO_CONNECT_TIMEOUT_SEC` で調整可（0で無効）。

### 検証
- 実SDKに `set_sync_query_connect_timeout` が OpenQuoteContext / OpenSecTradeContext 双方に存在することを確認。
- 全 `sync_positions` 呼び出しの囲み関数を機械的に分類し、async直呼び7箇所のみを変換（sync関数内は非対象）ことを確認。構文チェックOK。

### 残（任意・別途相談）
- **(c) heartbeatウォッチドッグ**：「最後の正常ログからN分無音」を別スレッドで検知して警告/再起動。(a)(b)を貫通する未知のハングの保険。規模が大きいため今回は見送り。
- パターンA（`moomoo_OpenD.exe` の c0000374 ヒープ破損・自己回復あり）はOpenD側/moomoo証券の問題でコードとは別レイヤー。今回の修正対象外。

---

## v3.9.128 / Wizard v1.41 (2026-07-25) — 標準プロファイルの建玉トレール→固定0.5%損切り を段階導入・計測可能に

Codex/週次レビューの最優先課題（建玉トレールが最大の出血源：今週-$3,902/勝率4.1%、全期間-$25,724/勝率6.5%）への対応。**設定挙動の変更はゼロ**で、既存の `MOMENTUM_TRAIL_FROM_ENTRY=false`（＝建玉トレールOFF＝固定損切り。損切り幅の既定は 0.50%）を**測定できるテスト群として整備**した。

- **`+flatstop` タグ**：標準プロファイル × 建玉トレールOFF のとき `BOT_VERSION_TAGGED` に `+flatstop` を付与（select_v1 は従来どおり `+select_v1` 優先）。シートの「botバージョン」列で「標準(建玉トレールあり・無印)」対「標準+flatstop(固定損切り)」対「select_v1」を分離集計できる。GAS/シート側の変更は不要。
- **起動ログ**：標準+flatstop 有効時に「検証中・+flatstopタグが付く・戻し方」を warning で明示。
- **review_gen.py**：戦略プロファイル別集計を2群→**3群**（select_v1 / 標準+flatstop / 標準・建玉トレールあり）に拡張。AIレビュー指示文に「実データ直接比較だが利用者の任意選択でランダム化なし。各群数十件×複数週で傾向一致を確認してから標準既定化を判断」を追記。
- **Wizard v1.41**：STEP13 [6] の [2]固定損切りに「★現在の検証対象／標準で選ぶと+flatstopが付き比較検証に使われる」旨を追記（選択の動作自体は不変）。

**段階導入の狙い**：一斉に標準の既定を変えるのではなく、協力者が Wizard で [1]標準プロファイル → [6][2]固定損切り を選ぶ（または `.env` に `MOMENTUM_TRAIL_FROM_ENTRY=false`）ことでテスト群になり、実データで2週以上比較。現実版反実仮想（全期間Δ+約$5,660）が実運用でも再現するかを確認してから、標準既定化を判断する。※ 群割り当てはランダム化されていないため因果効果とは断定しない。

---

## v3.9.127 (2026-07-25) — Discord日次サマリの送信時刻を「取引終了後」に動的化

- 従来は固定で **15:50 ET**（RTHクローズ前）に送っていた日次サマリを、**一日の取引が終わってから**送るように変更（PAN要望）。
- 送信時刻 `_daily_summary_send_time(trd_env, date)`：
  - **実口座 × アフター非対象**（CONFIDENCE_AFTERHOURS≧2.0）: **RTH終了後（16:00 ET）**。早期クローズ日は `get_early_close_time()` の時刻。
  - **実口座 × アフター対象**: **アフターアワーズ終了後（＝オーバーナイト開始 20:00 ET）**。
  - **デモ口座**: RTH後は価格更新されず 15:45 等で日次決済・停止するため、アフター設定に関わらず **RTH終了後**。
- `_send_daily_summary_if_needed(trd_env)` に口座種別を渡すよう変更。`market_schedule_loop` は毎分ポーリングで最下部到達するため、16:00/20:00 いずれの時刻でも1日1回発火する（demo日次決済ブロックは `_demo_daily_close_done` ガードで1回のみ early-continue するため、以降の周回で送信判定に到達することを確認済み）。
- 動作検証：実/デモ × アフター有無 × 通常/早期クローズ日 の6パターンで送信時刻が期待通りになることを確認。
- ※ Discordサマリは Bot メモリ内集計から生成し GAS/シートに依存しないため、この変更は集計シートやレポートに影響しない。オーバーナイトも発注対象にしている場合は別途相談（本変更はRTH/アフターの2ケースのみ対応）。

---

## v3.9.126 / GAS v9.20 (2026-07-25) — 🔴【重要】トレードがシートに記録されず消える不具合を修正（利用者G報告）

### 症状
- 受講生報告：「7/23 の日次レポートに自分の成績が反映されていない」（QQQ SHORT・qty8・確定損益 +$84.16・DEMO）
- Bot ログには「✅ トレード送信完了 QQQ pnl=+84.16」（ET 15:46:44）が出ているのに、**シートに行が存在しない**。
- 調査で判明：当該受講生の 7/23 のトレードは **1件も記録されていなかった**（全期間検索で +84.16 は0件）。

### 根本原因（GAS `_withWriteLock_`）
旧実装は「`tryLock` できない場合は**データ欠損を避けるためロック無しで続行**する」設計だった。しかしこれは逆効果で、

1. `appendRow` は「現在の最終行の次」に書く
2. ロック無しの並行実行が**同じ行番号を取得**し、互いに上書きする
3. 例外が出ないため `ok:true` が返り、Bot は「送信完了」を表示する（**無音の欠落**）

ET 15:45 のデモ日次一斉決済で全台が同時に送信するため、この競合が発生した。

**通信障害でないことの確認**：同じ受講生の同日データで、観察ログ13件は正常記録、さらに**該当トレードの2分後（ET 15:48:11）のエラーログは記録されている**。GAS への到達自体は成功しており、トレード詳細シートへの書き込みだけが失われていた。

### 修正
- **GAS v9.20：fail-closed 化。** ロックを取れない場合は書き込まず `LOCK_BUSY` で失敗を返す。Bot は 0/5/15 秒で再送し、全失敗ならローカルキューへ退避 → `gas_retry_loop` が後で再送するため**データは失われない**（＝欠損を本当に防げるのはこちら）。
- **GAS v9.20：** ロック待ちを 5秒 → 10秒（`LOCK_WAIT_MS`）。Bot 側の最短 timeout(15秒) に収まる範囲で粘る。`doPost` は `LOCK_BUSY` を専用コードで返し、実行ログでも `lock_busy` として通常障害と区別する。
- **Bot v3.9.126：** 一斉決済（デモ日次／週末前強制／移行前全決済）由来のトレード送信のみ、送信前ジッターを 5秒 → **30秒**（`_GAS_SEND_JITTER_SEC_BULK`）に拡大し、競合そのものを減らす。通常の決済は従来どおり5秒。日次レポートは翌朝生成のため遅延の実害はない。

### 検証
- 一斉決済の判定を実データの決済理由8種で確認（3種を bulk 判定・5種を通常判定、全件一致）。
- GAS のロック挙動をモックで再現：ロック取得可→`ok:true`・1行書込／取得不可→`LOCK_BUSY`・**書込0件**。
- Bot の `_gas_send_with_retry` に `LOCK_BUSY` 応答を与えて実行：戻り値 False・3回試行・**キューへ退避**を確認。

### 影響範囲
- 発注ロジックは不変（記録経路のみ）。実際の売買・損益には影響していない。
- **GAS v9.20 の反映が必須**（Bot だけ更新しても欠落は止まらない）。デプロイは既存デプロイのバージョン更新で行うこと（URL不変）。

---

## v3.9.125 (2026-07-23) — 【重要】別口座の建玉を誤認する不具合を修正（利用者B報告・実口座）

### 症状（利用者B報告 2026-07-23 / v3.9.120+select_v1・SMH）
- 設定中の実口座に SMH の建玉が無いのに、Bot が「SMH 1株・取得単価 $551.53 を保有」と認識。Discord に含み益・トレール決済の通知が出る。
- 決済時に `Invalid position ID` / `ロング決済 全position_id発注失敗` が **約60秒ごとに反復**。実際の注文・約定は無し。
- 影響: 存在しないポジション通知／決済失敗の反復／SMH 新規ロングが15分ブロック／内部の建玉金額・売買判断への影響。

### 根本原因（コードとSDKで独立検証）
`sync_positions` の ret=-1 フォールバックに `acc_id=0` のパターンが含まれていた（旧パターン2・4）。ここに2つの誤解があった。

1. **`acc_id=0` は「全口座」ではない。** SDK の `position_list_query` は `acc_id=0` のとき `_get_acc_id_by_acc_index(trd_env, acc_index=0)` を呼び、**口座リストの先頭口座**を選ぶ。コード内コメントの「全口座」表記が誤りだった。
2. **戻り DataFrame に `acc_id` 列が無い**（SDK の `col_list` に非含有）。そのため取得後に「どの口座の建玉か」を判別・除外できない。

結果、設定口座(`REAL_ACC_ID`)が `RET_OK`・0件でも後続パターンへ進み、**先頭口座（別口座）の建玉を取り込み**、その `position_id` を設定口座で決済しようとして `Invalid position ID` が永久に解消しないループになっていた。
※ 0件でも試行を続ける挙動自体は v3.9.118 の意図（moomoo JP は建玉があっても空を返すことがある）で、これ自体は正しい。問題は「別口座を引きに行っていた」こと。

### 修正
- **実口座の試行パターンから `acc_id=0` を撤廃**し `REAL_ACC_ID` 固定に。ret=-1 のフォールバックは `asset_category` の有無のみで行う（他口座の混入を構造的に不可能にする）。デモは従来どおり `acc_id=0`（単一口座前提）。
- **安全網**: `place_order` が `Invalid position ID` を返した場合、既存の「Not enough positions」と同じく `_RET_ALREADY_CLOSED` 扱いにして内部状態から除去し、`sync_positions` を再実行。60秒ループと15分ブロックを止める。
- ログ表記の誤り「acc_id=全口座」を「acc_id=先頭口座(acc_index=0)」へ修正。

### 影響範囲
- 実発注ロジック・注文条件は不変。取得口座の限定と、存在しない建玉の掃除のみ。
- `MOOMOO_ACC_ID` は起動時に自動検出・検証される仕組みが既にあるため、設定済み環境ではそのまま動作する。

---

## v3.9.124 (2026-07-21) — websockets 警告の案内コマンド修正＋requirements.txt に追記

- Alpaca News の「websockets 未インストール」警告が案内していた `pip install websockets` は、`pip` 単体コマンドが存在しない環境（macOS標準など）で `command not found` になる（PAN環境で実発生）。案内を **Mac: `python3 -m pip install websockets` / Windows: `python -m pip install websockets`** に修正。
- 根本原因: requirements.txt に websockets が未記載だった（Alpaca News は任意機能のため漏れていた）。`websockets>=12,<16` を追記 — 以後のセットアップ/差し替え時に `-r requirements.txt` で自動導入される。

---

## v3.9.123 (2026-07-21) — 方針決定：モメンタムは「幅のみ拡大・サイズは縮小しない」（v3.9.122の÷を撤回）

### 経緯
v3.9.122 では 利用者B報告（表示と実発注の不整合）を「サイズ÷倍率を実発注にも効かせる」方向で解消した。しかし PAN の設計思想は「**ボラティリティが高い銘柄ほど手数料に勝てる＝ボラで稼ぐ**」であり、サイズ縮小はこの思想と逆行するため、同日中に方針を確定して撤回した（v3.9.122 は未配布・フリート影響なし）。

### 確定した設計（本版で明文化）
- **モメンタム実発注：損切り幅のみボラ別に拡大（SMH×3.0等）。発注サイズは縮小しない。**
  - 根拠1（手数料）: サイズを1/3にすると手数料ハードルが 0.073%→0.218% に跳ね、利確0.22%の設計と両立しない。
  - 根拠2（エッジ配分）: SMH SHORT は全期間 勝率51.2%・2,656件の最主力。最良のエッジから資金を削るのは矛盾。
  - 損失の総量管理はサイズではなく**日次/週次サーキットブレーカー**（MOMENTUM_DAILY/WEEKLY_LOSS_BUDGET_PCT）で行う。
- **ニュース系（qty自動計算）**: place_buy/short 内の ÷_symbol_loss_mult は従来どおり維持（変更なし）。

### 変更内容
- v3.9.122 で追加したモメンタム側 `effective_size_pct ÷ _symbol_loss_mult` を撤回し、方針コメントを明文化。
- **維持**: v3.9.122 の place_short 再構成（$ベース発注額計算を `qty is None` の内側へ移動）。qty 明示発注時に「高ボラ銘柄サイズ調整 ÷3.0 = $3,000」という**実発注に使われない紛らわしいログ**が出ていた問題（利用者Bの混乱の元）はこれで解消済み。
- _MOMENTUM_STOP_MULT のヘッダコメント・Wizard v1.40 [5b] の説明文を新方針に一致（旧「幅拡大とセットでサイズ÷倍率」文言を削除）。
- シャドー観察の想定発注額はサイズ等倍のまま＝**過去集計との連続性は維持される**。

---

## v3.9.122 (2026-07-21・未配布・同日撤回) — 高ボラサイズ縮小を qty 明示発注に適用（→v3.9.123で方針転換）

### 症状（利用者B報告 7/20・SMH実取引で確認）
モメンタム実発注は株数を明示して渡すため、place_short 内の高ボラサイズ調整（÷倍率）が**表示されるだけで実発注に反映されず**、SMH が設計値（÷3.0 ≒ $2,000）の約3倍（10株 ≒ $5,614）で約定していた。損切り幅は×3.0（実効1.5%）に広げられているため、「幅×N ↔ サイズ÷N＝1トレード想定損失一定」のリスクパリティが崩れ、**SMH の1トレード想定損失が設計の約3倍**（$90 vs $30）になっていた。v3.9.108（その利用者の前回報告）で残余力キャップは入れたが、高ボラ縮小の qty 反映が取り残されていた。

### 修正
- **①（本命）モメンタム側で ÷_symbol_loss_mult(symbol) を適用**: `effective_size_pct` にリスクパリティを組み込み。発注ログの想定額・シャドー観察の理論値・実発注 qty がすべて一致する位置。SMH×3.0→サイズ1/3、QQQ×2.0→1/2、SPY×1.3→1/1.3。flat プロファイル・.env個別上書きもそのまま反映される。
- **② place_short の $ベース発注額計算を `if qty is None:` の内側へ移動**（place_buy と対称化）: qty 明示発注時に「高ボラ銘柄サイズ調整 ÷3.0 = $3,000」という**実態と異なるログ**が出る混乱の元を解消。残余力キャップ（v3.9.108）は共通処理として維持。

### 影響と注記
- 標準・select_v1 とも、SMH/QQQ/SPY のモメンタム実発注サイズが設計どおりに縮小される（select_v1 は SHORT のみ実発注のため SMH/QQQ SHORT に効く）。
- シャドー観察の「想定発注額・想定株数」も÷後の設計値に変わるため、**週次集計の想定PnLは本版から不連続**（過去は過大なサイズ想定だった）。レビュー時は v3.9.122 前後で区切って解釈すること。

---

## v3.9.121 (2026-07-20) — 認定サポーター(利用者H)レビュー対応：NameError修正・発注二重化の封鎖

### ① 🔴 `_long_min_ok` 未定義による NameError（実害発生中だった）
- v3.9.114 でロングレンジ判定を `_long_range_ok` に改名した際、シャドー観察の見送り理由ログ分岐（旧L1819）だけ旧名が残存。実発注モードでBUYがレンジ外見送りになるたびに NameError → ループ全体 except（DEBUG）に捕捉され、**その周回の後続銘柄の走査・シャドー記録・実発注が丸ごとスキップ**されていた。実ログで20回の発生を確認。
- 修正: `_long_range_ok` に統一し、見送り文言もレンジ（下限〜上限）表記に更新。

### ② 発注二重化の封鎖（3点）
- **②-1 銘柄単位ロック**: モメンタム実発注（Phase 1）を `async with _get_sym_lock(symbol):` で包み、ニュース系（process_headlines / process_stock_news）と発注を直列化。place_buy/place_short/place_close_all は sync のためデッドロックは増えない。
- **②-2 place_buy の既存LONGチェックを qty 指定経路にも適用**: 旧実装は `if qty is None:` の内側のみ（ショートカバー等を想定した抜け道）で、qty 明示のモメンタムBUY・動的個別株BUYがバイパスし、ニュースLONG保有中の同銘柄へ追い玉が入り得た（entry_time 上書きで帰属もズレる）。無条件チェックに変更。ショートカバー（position_qty<0）は従来どおり通過。place_short 側の無条件ガードと対称になった。
- **②-3 `MOMENTUM_MAX_OPEN_POSITIONS`（既定1）を実装**: 定義のみで未参照の死に設定だったものを有効化。モメンタム由来（entry_ai_category=="MOMENTUM"）の建玉数が上限以上なら新規実発注を見送り（シャドー記録は全件継続・見送り理由も明示）。

### ③ GASトークン（調査結果・コード変更なし）
- GAS側（v9.18）を精査: doGet はバージョン/シート名のみ、doPost は token 検証後すべて append/update 系で **read系アクションなし＝データ露出経路なし**。残るリスクは偽データ書き込みに限られるため、write-only 設計の維持と集計シートの共有リンク運用の点検で対応（コード変更なし）。

---

## v3.9.120 (2026-07-19) — 外部AIレビュー(Codex)指摘6件の修正（部分約定・ショートカバー再発注ほか）

外部AI（ChatGPT Codex）による静的レビューで確認された指摘のうち、実コードで裏取りできた6件を修正。

### High ① 部分約定を全量約定として処理していた（`_check_order_filled`）
- 旧実装は `"filled"` の部分文字列一致が `FILLED_PART` にも一致し、決済系では残株があってもポジションを全クリアしていた（v3.9.105 の C1 計測で「次版で厳密化予定」としていた対応の実施）。
- 修正: `dealt_qty < qty` または status に `part` を含む場合を部分約定と判定。
  - 注文は `_pending_orders` に残し、`qty` を残量に更新（タイムアウトで既存機構が残量キャンセル→決済系は残量を再発注）。
  - ロング決済の部分約定: 残量を position/avg_cost/tracked に復元して監視継続。
  - ショートカバーの部分約定: 残量を `position_qty=-残` `is_short=True` で復元（place_close_all が発注時に is_short を下ろすため復元必須）。
  - 約定分の PnL は従来どおり dealt 数量で記録。pending_avg_cost 等は残量の後続決済のため部分約定時はクリアしない。

### High ② ショートカバー再発注の誤スキップ（watchdog）
- ショートは `position_qty` が負で保持され、`place_close_all` がカバー発注時点で tracked と `is_short` をクリアするため、旧判定 `qty<=0 and tracked<=0` は未約定のまま残った生きたショートまで「約定済み」と誤判断し再発注をスキップしていた。
- 修正: カバー注文は「`qty==0`（完全フラット）」の場合のみスキップ。ロング決済は従来判定を維持。

### Med ③ GAS再送キューの上書き競合でレコード消失
- 旧実装は「ロック内で読む→ロック外で送信→古いスナップショットで全上書き」のため、送信中に enqueue された新規レコードが消え得た。
- 修正: 読み取り時にファイルを空化（以後の enqueue は空ファイルへ安全に追記）し、送信後は残件を「追記」で戻す方式に変更。

### Med ④ 移行前全決済が一度の失敗で永久スキップ
- 完了マーク（`_preclose_done_for`）を決済試行「前」に付けていたため、OpenD 瞬断などの例外で失敗すると残りの猶予時間内に再試行されなかった。
- 修正: マークとDiscord通知を成功後に移動。失敗時は次の1分ポーリングで境界まで再試行。

### Med ⑤ 重大アラートDiscord通知の同期ブロック＋クールダウン競合（v3.9.119の強化）
- 修正: 送信を常に daemon スレッドで実行（メインループ不在時の同期送信フォールバックを廃止）。クールダウンの check-then-set を `_ALERT_DISCORD_LOCK` で排他。

### Med ⑥ GASキュー重複排除が生テキストの部分一致
- 修正: JSONL 各行をパースして `key` を厳密比較（エスケープ・空白差による一致漏れを解消）。

### レビューで誤検出と確認した指摘（変更なし）
- 「MAX_LOSS_PCT が負値・過大値を受け付ける」→ v3.9.105 で範囲外は起動拒否済み。
- 「休場日に market_open_event がクリアされない」→ v2.87 の休日専用分岐でクリア済み。
- 「_pct_frac の旧表記変換が無警告」→ v3.9.73 の起動時警告で対応済み。

---

## v3.9.119 (2026-07-19) — 重大アラート6条件を Discord にも通知

### 内容
アラート音の対象である重大6条件（Anthropic クレジット切れ / moomoo SDK 旧すぎ / OpenD 接続不可 / 取引ロック / 強制損切り全失敗 / ショートカバー全失敗）で、`DISCORD_WEBHOOK_URL` 設定時に Discord 通知も送るようにした。

### 実装
- `_notify_alert_discord(event_key)` を新設し、`_play_alert_sound()` の冒頭で呼ぶ（既存6ヶ所の呼び出し側は無変更）。
- 通知はアラート音の有効/無効（`ENABLE_ALERT_SOUND`）・夜間ミュートと**独立**（音を切っていても通知は届く。Discord はサイレント通知のため夜間も送る）。
- 連発防止: 同一イベントは 10 分（`_ALERT_DISCORD_COOLDOWN_SEC=600`）に 1 回まで。音の 60 秒クールダウンとは別管理。
- 通知失敗は握りつぶし、本体処理を止めない。

---

## v3.9.118 (2026-07-16) — 建玉ゼロ口座で sync_positions が連続失敗するバグ修正（受講生報告）

### 背景
実口座（--live）を**建玉0件**の状態で起動すると、`sync_positions` の建玉取得が起動直後から失敗を繰り返す（報告環境で一晩約1,250回）。受講生がコードまで特定して報告。

### 原因
`sync_positions` の取得パターンループで、`position_list_query` が **`ret=RET_OK` かつ DataFrame空（＝建玉0件の正常応答）** を返しても `_df_has_rows()` が False のため break せず次パターンへ進み、最後の `asset_category` なし照会（旧SDK向けフォールバック）が新SDKで返す **`ret=-1` が、先に取れていた正常な0件応答を上書き**。ループ後 `if ret != RET_OK: return（失敗）` で「建玉ゼロ（正常）」を「取得失敗」に誤分類していた（`consecutive_fail` 加算・週次エラーログの `sync_positions ret=-1` の主因）。

### 修正
`RET_OK`かつ空の応答を `_ok_empty_data` に保持し、行付き結果を探して試行は続ける（moomoo JP の `position_list_query` は建玉があっても空を返す事があり、別パターンで返る＝多段フォールバックの存在意義を維持）。ループ後、行付き成功が無くても **いずれかのパターンが `RET_OK(空)` を返していれば `ret=RET_OK` に復元** → 「ポジションなし（正常）」として扱う。全パターンが `ret!=OK` の時のみ真の取得失敗。
- 受講生の提案（最初の`RET_OK`空で即break）は、JP不安定時に別パターンで返る建玉を取りこぼす回帰になるため不採用。0件正常判定は保持しつつ多段試行を維持する形にした。
- 検証: 5シナリオのロジック確認（0件→正常 / 全OK空→正常 / 1st空→2ndで建玉→取得 / 1stで建玉→取得 / 全失敗→失敗）で意図どおり。

## v3.9.117 (2026-07-11) — Bot Station.app 互換（起動モード引数 demo/real を受付）

### 背景
署名・公証済みの **Bot Station.app**（GUIランチャー：セットアップ／デモ口座で起動／リアル口座で起動）は、起動時に `moomoo_trade_v1.py demo` / `real` と**位置引数**を渡す。しかし本体は `--live` フラグのみ定義していたため、argparse が `unrecognized arguments: demo/real` でエラー → **アプリのデモ/リアルボタンからは起動できない**状態だった（セットアップボタンのみ動作）。Week3 でこのアプリを標準の起動方法として案内するため、本体側を後方互換で対応。

### 変更
- `__main__` の argparse に**位置引数 `mode`（`demo` / `real`・省略可）**を追加。`real` または `--live` で実口座、`demo`／省略でデモ。`_is_live = args.live or args.mode == "real"` に集約し `main(live=_is_live)` へ。
- 従来の `python moomoo_trade_v1.py` / `--live`（.command ランチャー・手動起動）は**完全に後方互換**。不正値は argparse が明示エラー。
- 検証：`[] / demo / real / --live / 不正値` の5パターンで期待どおり（demo=デモ・real/--live=実・不正=エラー）を確認。

---

## v3.9.116 (2026-07-11) — 戦略プロファイル「選抜プロファイル v1」（Wizard選択・既定は従来どおり）＋ Wizard v1.35

### 背景
全期間データの再集計（実発注 MOMENTUM 4,266件・5/6〜7/10 ＋ シャドー観察ユニークシグナル4,952件・6/9〜7/10）で、負けの構造が3点に分解できた：(1)建玉トレールが全期間 −$25,724/勝率6.5%（利益トレール +$28,243 を相殺する最大の出血源）、(2)方向がSHORT 92%に偏り、SPYショートは全出口方式で期待値マイナス、(3)ET 11時台・14時以降が慢性赤字帯（14時以降SHORTだけで −$4,715）。同一シグナル集合の出口比較では「MFE（含み益ピーク）トレール」だけが安定プラス（勝率61%）。この3条件＋出口を組み合わせた「SHORTのみ・SPY除外・ET 9/10/12/13時台・建玉トレールOFF」は、ユニークシグナル1,284件で **+0.170%/勝率73%・5週すべてプラス・前半/後半スプリットでも劣化なし**（過去観察であり将来を保証しない）。

### 変更（オプトイン・既定は完全に従来どおり）
- **新env `MOMENTUM_STRATEGY_PROFILE`**（`standard`（既定）/ `select_v1`）。不正値は silent fallback で standard。
- **select_v1 が有効な場合のみ**、実発注を次の条件に絞る（トレンド/プレマーケットフィルタと同型の実発注ゲート方式）：
  - ① 方向：SHORT のみ（LONG はブロック）
  - ② 銘柄：SPY を除外（`_SELECT_V1_EXCLUDED_SYMBOLS`）
  - ③ 時間帯：ET 9・10・12・13 時台のみ新規実発注（`_SELECT_V1_ENTRY_HOURS_ET`）
  - ④ 出口：`MOMENTUM_TRAIL_FROM_ENTRY` を強制 false（固定損切りに戻す。利益トレールは従来どおり＝自然に「含み益ピーク追従」の2段構えになる）
  - ⑤ 安全網：`MOMENTUM_DAILY_LOSS_BUDGET_PCT` の既定を 3.0→1.5%（env 明示があればそちら優先）
- **シャドー観察は全銘柄・全サイド・全時間帯で継続**（ブロックは実発注のみ）。プロファイル自体の有効性を毎週の集計で検証し続けられる。ブロック時は `_order_note` に「選抜プロファイル: <理由> → シャドーのみ」を記録。
- 起動時ログにプロファイルの適用状態を明示（select_v1 有効時は warning で条件一覧＋戻し方を表示）。
- **決済記録・終結ログでのプロファイル判別**: ① settings スナップショットと観察ログ送信の `bot_version` を select_v1 有効時 `v3.9.116+select_v1` にタグ付け（GAS はこの値をシート「botバージョン」列に書くため、**GAS/シート側の変更なしに**どの決済・観察行がプロファイル有効時のものか集計・監査できる。standard は無印＝従来どおり）。加えて `settings.strategy_profile` も送信（将来のGAS列追加用）。② 日次サマリ（端末/Discord・実トレードなしの日も含む）の先頭に「🎛 戦略プロファイル: 選抜プロファイル v1／標準」を常時表示。③ 決済ごとの「✅ トレード送信完了」ログに select_v1 有効時 `[選抜プロファイルv1]` タグを付加。
- ※ v3.9.41 の「固定時間帯フィルタ廃案」は3日間の検証に基づく判断。今回は5週間・出口方式別の再検証で時間帯×方向の構造が安定確認されたため、**既定OFFのオプトイン限定**で復活（全体既定は不変）。
- Wizard v1.35：STEP 13 冒頭に **[0] 戦略プロファイル** を追加（1=標準〔今まで通り・既定〕/ 2=選抜プロファイル v1）。選択内容と根拠データを画面に表示し、確認画面にも反映。
- **シャドー観察の当番制（GAS負荷削減）**: momentum_shadow は全受講生（直近週99名）が同じ市場シグナル×同じ結果を重複送信していた（5週間で13,536行 vs ユニークシグナル4,952件）。市場結果は誰が送っても同一のため、`hash(生徒名＋ET日付)` による**日替わり当番（既定20%≒毎日20名前後）だけが送信**する方式に変更。env `MOMENTUM_SHADOW_REPORTER_PCT`（100=従来どおり全員送信。**講師・認定サポーターは100を推奨**）。非当番は pending 登録・+60分PnL計測ごとスキップ（CPU/POSTゼロ）。実発注トレード記録・端末/Discordの日次サマリ（自分のシャドー成績）・trend/premarket A/B計測は全員従来どおり。当番の決定論により再起動でも同日は同一判定・毎日メンバー交代で恒久欠落なし。起動ログに当番状態を表示。
- **仮想MFEトレールの復活**（`mfe_trail_pnl_pct` / `mfe_trail_min`）: v3.9.110 で「下流未使用」として撤去したが、**select_v1 の選定根拠かつ継続検証の基幹指標**になったため `_compute_virtual_exits` に再実装（段階幅: ピーク≥1.0%→0.08 / ≥0.5%→0.12 / ≥0.3%→0.20・未起動は60分窓末決済）。GAS列は既存のまま再populate。tests の `_vexit_trail_drop` テストも復帰（RSI反転のみスキップ継続）。
- **シャドー計測の棚卸し（結論の出た計測を終了・週約1,600行削減）**: 全block_stage×全列を「日次/週次レポート・講師分析での使用有無」で監査。下流未使用かつ検証完了の3計測を撤去——①`peak_retrace_sim`（v3.9.17微利確sim・週470行）＝「含み益をどう守るか」は復活した仮想MFEトレールが上位互換で計測、②`timeout_extension`（v3.9.16延長sim・週372行）＝momentum_shadowのpnl_60min＋v3.9.111出口列で代替可能、③`仮想15分撤退`（exit15_*・momentum_shadow行の2列）＝5週集計で勝率43%と全出口方式中最下位＝不採用と結論。**保持**＝momentum_shadow本体（当番制で送信）、trend_filter_ab/premarket_filter_ab/forced_stop_ab（有効化判断待ちの現役A/B）、smh_strict（配備済みルールの監視・v3.9.10で実際に閾値調整に使用）、shadow_short（ショート非実発注ユーザー向け・週20行）、ordered文脈行（実発注の監査証跡）、ニュース文脈3種（30%サンプリング済み・AI較正用）。GAS列は残置（今後は空欄＝復活時も互換）。
- **用語統一（PAN指示・2026-07-13）**: 軍事的表現の旧用語を全面禁止し、本体・Wizard・tests・本CHANGELOG・GASソース・講座スライド5本の計115箇所を「実発注」（文脈により「実口座」）へ置換。文言が焼き込まれていた図版3枚（w1_v397_changes / w1_wizard_sides / w5_sides）も再生成し、Week1/4/5デッキを再ビルド。GASのシート列ヘッダ（旧「実発注可否」相当）はソース更新済み・デプロイ済みシートの見出しは次回GAS更新時に反映。日付付きバックアップpptxのみ旧表記が残存（アーカイブとして許容）。
- **日次・週次レポートにプロファイル集計を追加**（daily_report/make_daily_report.py・make_weekly_report.py）: ①「戦略プロファイル別（MOMENTUM実発注）」表＝botバージョン末尾 `+select_v1` で標準/選抜を分けて成績比較（選抜利用者ゼロの週は注記のみ）。②週次シャドー章に「**選抜プロファイル条件の検証**」表＝全シグナル vs 選抜条件（売り・SPY除外・9/10/12/13時台）の MFEトレール/60分保有（判定時価格で%正規化）を毎週並記——プロファイルの優位が崩れていないかを継続監視し、崩れたら見直し・停止を検討する運用。

---

## v3.9.115 (2026-07-11) — 日次サマリのディスク永続化（再起動でも当日集計を保持）

### 背景
認定サポーター 利用者Bの 7/10 実口座ログ指摘：確定損益は 2 件合計 −$56.27（−31.49＋−24.78）だったが、Bot 再起動後の日次サマリには 1 件 −$24.78 のみ表示された。原因は、端末/Discord 用の当日集計バッファ（`_today_trades`・シャドー2種）が**メモリのみ**で、再起動でクリアされ、再起動前に決済したトレードが表示から抜けていたこと。全トレードは Google シートに記録済みで、**表示側だけの過小表示**（データ欠落ではない）。同じ現象は受講生環境でも誤解を生みうるため恒久対策。

### 変更（表示のみ・発注挙動やフリートへの影響なし）
- 当日サマリ3バッファ（`_today_trades` / `_today_shadow_short` / `_today_shadow_momentum`）を `today_summary_state.json`（スクリプト直下）へ**原子的に保存**（追記のたびに全体を書き換え・`os.replace` で置換）。
- 起動時に `_load_today_state()` を `main()` 冒頭で1回実行。**保存日付が当日 ET のときのみ**読み戻してマージ（別日の残骸は無視）。DATA_COLLECT 非依存で常に動作。
- ET 日付跨ぎ（`_maybe_reset_today_trades`）で永続ファイルも当日（空）へ更新。
- 保存/読込の失敗はすべて黙殺（表示専用のため運用を止めない）。GAS 非依存・送信負荷ゼロ。

### 補足（利用者B共有の他2点は現行仕様として想定どおり）
- SMH ショートの 158.4 分保有：時間切れ判定はウォールクロックで稼働中のみ評価。約149分のログ空白＝プロセス停止/スリープ中はルール発火不可。再開時に即決済（初回「Trade is not unlocked」は空白中に OpenD の Unlock 切れ→再試行で決済成功＝設計どおりのリカバリ）。運用側でスリープ無効・OpenD 常時接続を維持。
- SMH ロング見送り：SMH 専用の confidence≥0.78 厳格化＋直近30分高値0.05%以内の高値づかみガードが意図どおり作動（見送り後の上下は不可避・複数日平均で評価）。

---

## v3.9.114 (2026-07-08) — モメンタム：ロング発注レンジ＋損切り幅プロファイル（20日データ・PAN承認）＋ Wizard v1.34

### 背景
20日のシャドー計測（momentum_shadow・60分保有・約1.3万件）で、(1)ロングは弱シグナルが低調で0.70%以上でプラス寄り・1.50%超は反転、(2)損切り幅は銘柄ボラで最適が異なる（SMHは60分変動が最大、DRAMは5分バーストが最大＝0.5%固定では即切られる）と判明。PAN承認のうえ標準化。※将来の成績を保証するものではない。

### 変更（モメンタム・実発注挙動／フリート影響あり）
- **ロング発注のシグナル強度レンジ**（5分モメンタム pct_5 の下限＋上限）：`MOMENTUM_LONG_MIN_SIGNAL_PCT`（既定 0.15→**0.70**）＋新設 `MOMENTUM_LONG_MAX_SIGNAL_PCT`（既定 0.80）。LONGは `[下限, 上限)` の内側のみ実発注。選択レンジ内なら山型(15分基準)の「強=発注なし(0)」を解除して強帯も発注（弱/中の縮小配分は維持）。ショートは対象外＝従来どおり。
- **損切り幅プロファイル（ボラ別・自動）**：`MOMENTUM_STOP_PROFILE`（narrow/standard/wide/flat、既定 standard）。`_symbol_loss_mult` を銘柄別倍率表へ（standard: SMH×3.0 / QQQ×2.0 / SPY×1.3 / DRAM×2.0 / IWM×1.0、narrow≈×0.7 / wide≈×1.3）。幅拡大とセットで発注サイズは÷倍率（1回想定損失は一定＝リスクパリティ）。上級者は `.env` の `MOMENTUM_STOP_MULT_<SYM>` で個別上書き（最優先）。flat=全銘柄1.0（従来）。既存 `STOCK_HIGHVOL_LOSS_MULT` は表外の高ボラ株のフォールバックとして維持。
- ※ 実発注は SPY/QQQ/SMH、IWM/DRAM はシャドーのみ（現行方針維持）。DRAM×2.0 は短期バースト対応（シャドー計測に適用）。
- 検証: 全プロファイル×銘柄の損切り幅が承認表と完全一致、LONGレンジ判定も想定どおりを単体テストで確認。

### Wizard v1.34（連動）
- STEP13 に [2-d] ロング発注レンジ（標準0.70〜0.80／中程度以上0.70〜1.00／強帯も許可0.80〜1.50／すべて0.15〜0.80／ロング発注しない=BUYサイド除外）と [5b] 損切り幅プロファイル（狭め/標準/広め/一律/env個別）を追加。表現は数値基準＋過去観察（非保証）で統一し「勝てる」等の断定表現は不使用。

### 注意
- フリート全体の売買挙動が変わるため、配布前に実発注1台で1〜2日の動作確認を推奨（LONGの発注件数減・SMH/QQQの損切り幅拡大が意図どおりか）。最適幅は v3.9.111 の「ピーク戻り%/実効損切り%」で数日計測して微調整予定。

## v3.9.113 (2026-07-08) — OVERNIGHT/週末スイープの建玉取りこぼし修正＋Anthropic 401 通知

### 背景（月火トレードのレビューで判明）
- 7/6に QQQ-SHORT/SPY-SHORT が**22時間放置**され翌日「時間切れ1327分」で大損（QQQ −140＝当日最悪）。原因は `close_all_for_weekend`（OVERNIGHT/週末/日次スイープ）が**内部stateの `position_qty!=0` だけ**で対象を判定しており、同期漏れで内部stateが0の実建玉を素通りしていた。
- 7/6ログに Anthropic **401（認証エラー）が4件**。従来 401 は `log.error` のみで **Discord/端末バナーが出ず**、AI判定が静かに全停止していた（クレジット切れ=400のみ通知対象だった）。

### 変更
- **スイープの実口座同期＋対象拡大**（`close_all_for_weekend`）：決済前に `sync_positions()` で実口座と同期し orphan 建玉を再捕捉。対象を「設定universe ∪ 内部stateの全銘柄」で `qty!=0` を全決済（universe外の建玉も拾う）。→ 放置建玉の再発防止。
- **Anthropic 401 の専用通知**（`_is_auth_error` / `_handle_anthropic_auth_error`）：初回に端末バナー＋Discord＋アラート音、継続中は30分おきに再通知、成功で回復通知（`_handle_anthropic_auth_recovery`）。クレジット切れ(400)とは分離。文言に「APIキー確認/再発行→.env更新→再起動」を明記。損切り/トレール/時間切れは通常動作を継続。

### 備考（別途対応・データ提案）
- モメンタムのLONG/SHORT標準化、高ボラETFの損切り幅の自動最適化（Wizard自動＋env個別override）は、フリート全体の売買挙動に影響するため、20日データの提案を提示してPAN承認後に実装予定。

## v3.9.112 (2026-07-07) — 終了時のトレード集計が画面に2回表示される不具合を修正

### 背景
Ctrl+C / 終了時、`_print_daily_summary_to_terminal()` が集計を stderr の枠付きブロックで表示した直後に `log.info("[終了サマリ]\n…")` でも同内容を出していた。`log` には stdout の StreamHandler(INFO) が付いているため、log.info が画面にも出力され、**同じ集計が2回表示**されていた（PAN報告）。

### 変更
- 終了サマリの記録を `log.info` から**ファイルハンドラ（`_file_handler`）への直接記録**に変更。端末表示は従来どおり stderr の整形ブロック1回のみ、ログファイルへの記録も維持し、画面の重複だけを解消。売買・集計の内容は不変。

## v3.9.111 (2026-07-06) — 出口チューニング解析用のログ拡張（利用者B多変量解析要望B案）＋ GAS v9.17

### 背景
利用者Bの多変量解析要望（20260706）を精査。要望フィールドの約6〜7割は既収集で、目玉の統計的発見の多くは同義反復（`pnl_15min`↔`pnl_60min`の経路重複、`TRAIL`vs`FORCED_STOP`は結果変数の循環）だった。唯一実用的で有望な「出口（トレール発動位置・幅・早期撤退）」の解析に絞り、**すべて決済時に計算できる派生列だけを追加**（B案）。列追加はGAS負荷にほぼ影響しない（負荷はPOST数=行数で決まり、v3.9.110で対処済み）ことを確認済み。VIX/出来高/購買力の発注時取得は約定遅延リスクのため却下。

### 変更（トレード詳細＝決済ごと1POSTに相乗り・POST数不変）
- トレード送信ペイロードに9列を追加：`realized_pnl_pct`（確定損益%・建玉額比＝予算非依存）／`peak_pnl_pct`（最大含み益%）／`drawdown_from_peak_pct`（ピークからの戻り幅%）／`trail_triggered`（TRAIL/ENTRY_TRAILで1）／`trail_width_pct`（設定値・参考）／`stop_loss_pct_effective`（高ボラ倍率込み実効損切り%）／`fee_estimate`（往復手数料概算$・env `FEE_PER_SHARE_RT` 既定0.011で調整）／`momentum_5m_pct`・`momentum_15m_pct`（エントリー時のモメンタム値）。
- `momentum_5m_pct`/`momentum_15m_pct` は `TickerState.entry_pct_5m/15m` に**モメンタム実発注時に記録**し、決済時に `_send_trade_result` が読み出してトレード行へ相乗り（発注パスにAPI追加なし）。
- エラーログに `error_impact`（`execution_risk` / `data_send_only` / `warning_only`）を付与（`_classify_error_impact`）。売買影響ありのエラーと記録・送信のみのエラーを解析で切り分け可能に（利用者B前回Point5とも整合）。

### GAS v9.17（要再デプロイ・既存デプロイのバージョン更新で）
- トレード詳細に42〜50列目を追加（`ensureTradeSchema` が既存シートへ自動マイグレーション）。
- エラーログ詳細に8列目「影響区分」を追加（`ensureErrorSchema` を新設・自動マイグレーション）。
- ※ Bot v3.9.111 とセットで反映（GAS未更新だと新フィールドはシートに記録されない）。

### 却下・保留（利用者B要望のうち）
- `vix_at_entry`/`volume_ratio`/`actual_buying_power`（発注ごと取得）：約定遅延リスクのため却下。VIX等は日次レポート側で外部取得し突合する方針。
- `pnl_5min`/`pnl_15min` 再追加：`pnl_60min`＋`exit15`（15分撤退計測）で代替済みのため見送り。
- `headline_id`/`news_age_minutes`/`duplicate_news_flag`：ニュースモデルが過学習（n=267・1週間）のためROI低・保留。
- 導出可能な各種flag/`symbol_side_key`/`effective_order_amount` 等：解析側で既存列から算出（ボットに持たせない）。

## v3.9.110 (2026-07-05) — シャドー観察の軽量化（GAS負荷削減・160台スケール先回り）

### 背景
GAS負荷の約92%はシャドー（観察ログ）POSTで、160台規模では単一GASのApps Script総実行時間がWorkspace上限（360分/日）に接近する試算（PANと確認）。GAS分割の前に、まず「計測が完了/下流未使用のシャドー項目」を棚卸しして送信量そのものを削減する方針（PAN決定）。日次・週次レポートもアシスタント集計スクリプト（aggregate_daily.py / aggregate_momentum_obs.py）も、反実仮想（「もし〜で決済していたら」）系の列を一切参照していないことをコードで確認。

### 変更（すべて観測ログ専用・売買ロジックは不変）
- **未使用の仮想exit列を撤去**（`_compute_virtual_exits`）：①MFE可変トレール（`仮想MFEトレールPnL%`/保有分）、②RSI(5分)反転（`仮想RSI反転PnL%`/保有分）、高ボラ損切り 狭/広（`仮想損切り狭%`/`広%`・拡大はv3.9.32で実装済＝検証完了）、基準PnL%（`基準PnL%(60分)`・`pnl_60min`と重複）、金曜15:30フラグ、計測メモ（`vexit_note`）。**継続計測する「③15分撤退」のみ残置**。関連ヘルパー（`_wilder_rsi`/`_vexit_trail_drop`/`_VEXIT_TRAIL_*`/`_VEXIT_RSI_PERIOD`）も削除。
- **5分/15分PnL列の送信停止**：レポート/集計は60分のみ使用のため `pnl_5min`/`pnl_15min` を送らない（`pnl_60min` は維持）。→ 観察ログは実質 47列→約34列。
- **フラッシュ間隔を 150〜210秒 → 300〜450秒に拡大**（`observation_batch_flush_loop`）：1POSTあたりの束ね行数を増やしPOST数を約半減。シャドーはバッファ全件を一括送信するためデータ欠落なし。適応スロットル（不調時1.8倍/混雑時3倍・最大10分）は継続。
- **ニュース系“文脈”観察のサンプリング導入**：`OBS_NEWS_SAMPLE_PCT`（env・0〜100・既定30）。本体シャドー（`momentum_shadow`/`trend_filter_ab`/`premarket_filter_ab`/`forced_stop_ab`/`shadow_short_*`/`peak_retrace_sim`）と実発注（outcome="ordered"）は常に100%記録。母数の大きい見送り文脈（`conf_threshold`/`observation_headline`/`meta_question` 等）のみ確率的に間引く。

### 効果（試算・160台）
- 観察POST：約 8,808 → 4,917 /日（**-44%**）。GAS総実行 約176〜264分 → **98〜148分/日**（Workspace 360分に余裕）。この後のGAS分割（トレード＝軽量GAS隔離／シャドー＝専用GAS）と合わせて恒久的にスケール可能に。
- ※ 数値は1POSTあたりGAS実行時間の推定を含む。実GAS実行ログで検証予定。

### GAS側
- 送信を止めた列は新規データで空欄になるだけで、削減自体にGAS webhookの変更は不要。シートから列自体を消すかは任意（後日クリーンアップ可）。
- **GAS v9.16（計測ログ・要再デプロイ）**：Plan A（削減を本番投入→実測で分割要否を判定）の検証用に、`doPost` に負荷ゼロの計測ログを追加。`tag:REQ`（payload種別・件数・処理ms・成否）と `tag:LOCK`（ロック取得待ちms・取得可否／逼迫時のみ）を `console.log`（Cloud Logging専用・シート書き込みなし）。**「バースト起因のタイムアウト＝失敗/高レイテンシが時刻でクラスタ化しトレード急増と一致」を、実行数ダッシュボード＋Cloud Loggingで判定する**。合否基準：観測1〜2週間でトレードPOSTの失敗率>1% or バースト時p95>10秒 or 同時実行ピーク≈25以上 → 分割B実施。既存URLを保つため「既存デプロイのバージョン更新」で反映する（新規デプロイでURLが変わると全Botの送信先が変わるため不可）。

## v3.9.109 (2026-07-04) — 設定別レポート対応（モメンタムLVの記録）＋ GAS v9.15 ＋ Wizard v1.32

### 背景
日次レポートに「設定別パフォーマンス（ニュース駆動／MOMENTUM別）＋別設定シャドー（理論値）」セクションを追加する方針（PAN決定）。L1〜L5別の集計にはトレード行単位のレベル記録が必要だった。また、トレール発動/幅（STEP3）はWizardが選択式のみで、.envを編集できない受講生は任意の値を設定できなかった。

### 変更
- Bot: トレード送信ペイロードに `momentum_level`（L1-5設定値）を追加。
- GAS v9.15: トレード詳細 41列目「モメンタムLV」を追加（要再デプロイ・ensureTradeSchemaで自動マイグレーション）。
- Wizard v1.32: STEP3（トレール発動/トレール幅）を自由入力対応（選択肢番号のほか数値の直接入力可・0.05〜5.0%）。プリセット外の既存カスタム値はEnterで維持と明示。
- レポート仕様（アシスタント向けガイド）を更新：指標を「平均リターン%＝損益÷建玉額」（予算非依存）に変更、各設定表にWizardの設定場所（STEP番号）を明記、シャドー行名を平易な日本語に（例:「含み益のピークから戻したら決済（MFEトレール）」）、用語説明を追加。見本=「設定別パフォーマンス_レポートサンプル.pdf」。

## v3.9.108 (2026-07-03) — qty明示発注が予算チェックを素通りするバグを修正（レビューH1・利用者B報告で実害確認）

### 背景
利用者B経由の実口座ログで確認。BUDGET_USD=$10,000・QQQ $7,860 保有中に、モメンタムが SMH SHORT 13株（約$7,766）をそのまま発注し「Insufficient buying power」で3回拒否。place_buy/place_short の $ベース調整（confidence配分・高ボラ縮小・残余力キャップ）は `qty is None` のときだけ株数に反映され、qty明示（モメンタム実発注・動的個別株）では**全て無視**されていた（外部レビューH1の実害）。ログ上「高ボラ調整 $6,000÷2.5=$3,000」と出た直後に13株$7,766が送られていたのはこのため。実余力が大きい口座では拒否されず BUDGET_USD を静かに超過し得た。

### 変更
- place_short: qty明示でも残余力（BUDGET_USD−既存建玉合計）で株数をキャップ（不足時はスキップ・「余力調整(qty)」ログを出力）。
- place_buy: qty明示の新規買いに同キャップを適用。ショートカバー（position_qty<0 の買い戻し＝露出減）は決済を止めないため対象外。
- 検証: 利用者B報告シナリオ（QQQ$7,860保有・SMH13株@597.39）で 13株→3株（$1,792）に制限され建玉合計 $9,652 ≤ BUDGET を確認。
- ※ confidence配分・高ボラ縮小はqty明示（モメンタム）には従来どおり適用しない（モメンタムは山型配分など自前サイズを持つため。適用するとフリートのモメンタムロットが意図せず変わる）。

## v3.9.107 (2026-07-03) — コンソールなし実行（pythonw/タスクスケジューラ）で起動即クラッシュを修正

### 背景
受講生報告。Windowsタスクスケジューラ＋pythonw.exe（コンソールなし）で自動起動すると、`_IS_TTY = sys.stdout.isatty()` が import 時に `AttributeError: 'NoneType' object has no attribute 'isatty'` で必ず落ちていた（pythonw では sys.stdout/stderr が None）。コンソールからの手動起動では発生しない。

### 変更
- `_IS_TTY` 判定を None-safe 化（`sys.stdout is not None and sys.stdout.isatty()` を try で保護）。コンソールなしでは非TTY扱い（色なし表示）で正常継続。
- 他の `sys.stdout/stderr` 直接参照（reconfigure・対処法表示・Ctrl+C/終了サマリの stderr 出力）は既に try 保護済みであることを全箇所確認（追加修正不要）。

## v3.9.106 (2026-07-02) — 信用余力不足の表示分岐＋実購買力チェック（利用者B報告対応）

### 背景
実口座で「Insufficient buying power（信用余力不足）」の発注拒否が「貸株在庫なし」と誤表示されていた（SHORT拒否判定の貸株キーワードに裸の "insufficient" が含まれていたため）。根本要因は BUDGET_USD が自己申告値で、実口座の信用余力と乖離し得ること（Bot内の残余力チェックは BUDGET_USD 基準のため防げない）。

### 変更
- SHORT拒否時、"buying power"/"purchasing power"/"余力" を貸株判定より先に評価し「信用余力不足」として表示・Discord通知（BUDGET_USD を実余力の範囲内にする案内付き）。
- --live 起動時に accinfo_query で実口座の購買力(power)を取得し、BUDGET_USD が上回る場合は警告（ログ＋Discord）。取得失敗時は起動を止めず続行（best-effort）。
- Wizard v1.31: STEP1（予算）と実口座ID設定後に「BUDGET_USD は実際の信用余力の範囲内に（複数銘柄の同時保有を考慮）」の注意文を追加。
- ※ MOMENTUM_MAX_PCT 標準値(80)の変更は保留（PAN判断: 小予算では余力温存に実益がなく手数料負けリスクが上がるため。予算連動の可変MAXを今後の検討課題とする）。

## v3.9.105 (2026-07-02) — 外部コードレビュー対応（即修正5件＋C1計測ログ）

### 背景
別スレッド（Fable 5）による moomoo_trade_v1.py / setup_wizard.py 全行レビューで、資金リスクに直結する確定バグが複数報告された。指摘を実コードで検証し、事実と確認できた「数行で直る確定バグ」を先行修正（構造対策のC3等は別途段階対応）。

### 変更（Bot）
- **C2**: watchdogの重複注文キャンセルから決済注文(`is_close`)を除外。place_close_all は建玉ごとに複数の決済注文を同時発注するため、従来は「重複」と誤認キャンセルされ「決済済み扱いなのに実際は未決済（損切り監視なし）」の建玉が生まれ得た。除外は従来 `is_cover` のみだった。
- **H2**: 個別株の1銘柄上限チェック（STOCK_MAX_USD）が常に無効だった確定バグを修正。`getattr(TradeState,'_tracked_position_cost')` は存在しない属性で常に0 → モジュール辞書 `_tracked_position_cost.get(symbol)` に修正。
- **H5**: BUDGET_USD / MAX_LOSS_PCT が「記載されているのに不正」な場合は起動拒否に変更（従来はBUDGET=$50,000へ黙ってフォールバック＝カンマ入り等の記載ミスで意図の数倍の予算で発注され得た）。MAX_LOSS_PCT は旧小数表記（0<値<0.05、例 0.003）も検出して停止（0.003%と解釈され即損切り連発になるため。推測変換はしない）。未設定（空欄）は従来どおり既定値。
- **猶予の限定**: v3.9.101 の約定直後猶予（MOMENTUM_ENTRY_GRACE_SEC）に `_is_momentum` 条件が漏れており、ニュース/決算建玉の損切りまで15秒遅延していたのを修正（モメンタム建玉のみに限定）。
- **C1計測（挙動不変）**: `_check_order_filled` に order_status 生値の収集ログを追加（新規値の初回記録＋部分約定疑い dealt<qty のWARNING）。部分約定(FILLED_PART)の substring 誤判定（C1）を enum 厳密比較へ移行する前の実地確認用。売買処理は不変。

### 変更（Wizard v1.30）
- STEP5: カスタムしきい値／ベース変更後の旧値を Enter「維持」すると黙って「発注しない(2.00)」に置換されるバグを修正（custom検出で既存値を維持・非数値既存値でもクラッシュしない・カスタム値≥2.0は発注しない扱いで移行前全決済の対象に）。
- STEP11: MOOMOO_PORT を検証（数値1〜65535・不正は再入力）。無検証保存だと本体がモジュール読込時の int() で即トレースバック死するため。

### 見送り・保留（レビュー指摘のうち今回対象外）
- C1本修正（enum厳密比較）: 実環境のステータス文字列を上記計測で確認してから（誤ると「約定済みを未約定扱い」の逆バグ）。
- C3（イベントループ上の同期I/O）/ H3・H9（ロック設計）: 構造対策として段階対応（リグレッションリスクが大きいため一括変更しない）。
- H7（実発注フラグ既定ON）: v3.9.68 でPANが意図した仕様（データ収集方針）のため変更せず。
- GASトークン個別化: 労力対効果で保留（シャーディング/Webull判断と同時期に再検討）。

### 配布時の注意
BUDGET_USD / MAX_LOSS_PCT が不正な .env は**起動しなくなる**（明確なエラーメッセージ表示）。配布案内に明記のこと。

## v3.9.104 (2026-07-01) — エラーログ肥大の削減（起動時の良性の空応答を除外）

### 背景
エラーログ詳細シート（GAS）に、起動時のJP口座API不安定による良性メッセージ「[起動時復元] ⚠ position_list_queryは空だが accinfo=$… → JP口座APIの既知の不安定さ（ブロックなし）」が多数（累計約400件）混入し、"エラー"件数を水増し・GAS送信量を増やしていた（売買影響なし）。

### 変更
- `_ERROR_LOG_EXCLUDE_TAGS` に「position_list_queryは空だが」を追加。この良性メッセージのみエラーシート送信対象から除外（端末表示は維持）。他の[起動時復元]警告（accinfoエラー／position_id取得不能）は引き続き送信。
- プレマーケット新規エントリー抑制フィルターを追加（既定は計測のみ）。premarket が恒常的な出血源のため、premarket 中の新規モメンタム実発注を対象化。`MOMENTUM_PREMARKET_FILTER_ENABLED=false`（既定）＝売買不変で、would-be-live の premarket 建てを観察ログ `block_stage="premarket_filter_ab"` に記録し、既存の +5/+15/+60分 仮想exitで「抑制したら成績がどう変わるか」を後追い計測。=true で premarket 実発注を実ブロックに昇格。起動ログに状態表示。

## v3.9.103 (2026-06-29) — 当日トレンド・フィルター（逆張り抑制／既定は計測のみ）

### 背景
6/29 など「上昇日にショートが集中」して大きく負ける日が損失の主因。向きの制御を、まず売買を変えずに効果検証できる形で導入。

### 変更
- `_market_trend_pct()` を追加：SPY（無ければQQQ）の `MOMENTUM_TREND_LOOKBACK_MIN`(既定60)分騰落率を当日トレンドの代理指標とする。
- モメンタム発注判定に against-trend 判定を追加：トレンドが +`MOMENTUM_TREND_FILTER_PCT`(既定0.5)% 超の上昇日のショート、−閾値超の下落日のロングを逆張りと判定。
- 既定は計測のみ（`MOMENTUM_TREND_FILTER_ENABLED=false`）＝売買は一切不変。against-trend の判定を観察ログ `block_stage="trend_filter_ab"` に記録し、既存の +5/+15/+60分 仮想exitパイプラインで「抑制したら成績がどう変わるか」を後追い計測する。
- `MOMENTUM_TREND_FILTER_ENABLED=true` にすると against-trend の実発注を実ブロック（昇格は計測で効果確認後）。
- 起動ログに状態（計測のみ／実ブロック）を表示。閾値・参照時間・ON/OFFは env で調整可。

## v3.9.102 (2026-06-29) — 決済表記の改善（利益トレール戻りを「強制損切り」と表記しない）

### 背景
利益トレール作動後（含み益が +発動閾値 に達してトレール管理下に入った建玉）が戻して切れた決済が「強制損切り」と表記され、見栄え・実態と乖離していた。

### 変更
- 損失保護決済のラベルを3分岐に：建玉トレール（_pan_trail_mode）→「建玉トレールストップ」(ENTRY_TRAIL)／利益トレール作動後の戻り（ts.trail_active）→「トレイリングストップ（利益確保後の戻り）」(TRAIL)／一度も利益が乗らずライン到達で切れた純粋な損切りのみ「強制損切り」(FORCED_STOP)。
- forced_stop A/B 計測（観察ログ）も純粋な強制損切りのみを対象にし、トレール戻り分は除外（対照群を汚さない）。
- ※ ニュース駆動の建玉トレール化は (50) データで検証 → 強制損切り177件中、建玉トレールで改善し得たのは約4%（MFE中央値$0・ほぼ一直線の逆行）のため効果なしと判断し、変更せず。

## v3.9.101 (2026-06-29) — 建玉トレール/強制損切りの約定直後・誤発火を修正

### 背景
認定サポーターより、実口座のSMHモメンタムSHORTで建玉トレールがエントリー直後に即時発火する報告（指値626.05・実約定629.25・現値629.23・決済「建玉トレールストップ」・実損益+$1.13）。発注時 avg_cost は指値で仮設定され、`entry_pending_fill` で約定確定まで判定をロックする設計だが、sync_positions がポジション認識時に「実約定単価が未確定（指値の仮設定）のまま」フラグを解除する穴があり、指値基準で約0.5%逆行と誤判定して即決済していた（LIMIT_BUFFER_PCT=0.50 が指値↔実約定の差≒0.50%を作り、トレール幅0.50%に一致して発火）。5/13 SPYの既知事例と同根。

### 変更（2層）
- 根本：TradeStateに `avg_cost_confirmed` を追加。`_check_order_filled` が実約定単価を反映した時のみ True。sync_positions は (1) 確定済みなら従来どおりボット値維持で解除、(2) 未確定でもAPIの実建玉単価があれば指値の仮設定を破棄してそれを建値に採用し解除、(3) 単価が取れなければ解除せずロック維持、に変更。指値を建値としたまま判定解放されることがなくなった。
- 保険：`MOMENTUM_ENTRY_GRACE_SEC`（既定15秒・0で無効）を追加。約定直後の猶予中は建玉トレール/強制損切り（損失保護）を抑止（利益確定トレールは対象外）。実約定反映までの過渡期の誤発火を二重に防止。

## v3.9.100 (2026-06-28) — 起動エラーガイドの修正（OpenDのみで可・moomoo PCアプリ不要）

### 背景
`--live` の接続失敗／口座取得失敗の枠で「moomoo PC アプリが起動済み」を必須条件として表示していたが、実際は OpenD が起動・実口座にログイン・「Connected」表示であれば moomoo の PC 取引アプリを開いておく必要はない（OpenD 単体でサーバーに接続）。誤誘導になっていた。

### 変更
- 起動時のガイド枠2か所（OpenD接続失敗／口座一覧取得失敗）の確認事項を、moomoo PCアプリ要件を外して OpenD 基準に統一：「OpenD が起動」「OpenD で(実口座/デモ)にログイン済み」「OpenD が『Connected』表示」。
- 全角を含む可変行（ログイン種別）の枠右端を表示幅で揃える簡易パッドに変更（`★ v3.9.100`）。
- ※「moomooアプリでポジションを確認」等の運用案内（決済確認用）は実態に即しているため変更なし。

## v3.9.99 (2026-06-27) — 実取引/デモの判別列（trade_env）＋ GAS v9.14

### 背景
レポートが「デモ口座の実発注を含む全トレード」を集計しており、実取引とデモを分離できなかった。実取引のみの成績を見たいニーズに対応。

### 変更
- **Bot：トレード送信に `trade_env`（REAL/DEMO）を付与**。1プロセス=1環境（`--live`の有無で `trd_env` 確定）なので、`main()` でグローバル `_RUN_TRADE_ENV` を確定し全トレードにタグ付け（既定は安全側 DEMO）。
- **GAS v9.14：トレード詳細に 40列目「口座区分」(trade_env) を追加**。`ensureTradeSchema` で自動マイグレーション。`trade_id` 列は38で固定。
- これでレポート/集計は「口座区分=REAL」だけを抽出して実取引成績を分離表示できる。

### 反映・確認
- Bot を v3.9.99 に差し替え、GAS を v9.14 に再デプロイ。構文OK・test_safety 20件green。
- 既定 DEMO のため、`--live` 起動の実口座のみ REAL とタグされる。

### 背景
単一GASのまま、シャドー（観察）のGAS負荷をさらに下げる。従来は1観察につき「エントリ追記」＋「+60分後のPnL更新」の**2回**POSTし、更新側はGASが対象行を全件探索する重い処理だった。

### 変更（トレード記録は最優先・不変／シャドーは最低優先）
- **観察エントリと+60分PnLを1回の追記に統合**。pending（価格>0＝+60分PnLが取れる観察）はエントリを即送らず保持し、+60分PnL確定時に「エントリ＋PnL＋仮想exit」を**1回の `observation_batch` 追記**でまとめて送る。→ **観察関連POSTが約半減**＋GASの「行探索→更新」処理が不要に。
  - pending情報＆ディスク永続(`_PENDING_OBS_PATH`)に観察エントリ(`entry`)を保持。停止/再起動でも +60分到達分を復元して統合送信（データ欠落なし）。
  - 価格不明（+60分PnLが出ない）観察のみ従来どおり即時にエントリ送信。
  - 旧Bot/旧pending（entryなし）は従来の `observation_pnl` 更新にフォールバック（互換）。
- **GAS v9.13**：`appendObservationRow` が pnl(19-22)/仮想exit(31-39)/仮想損切り(46-47)列も、payloadに含まれていれば追記時に同梱書き込み。
- 補足：過負荷で pending がFIFO上限(5000)に達した場合、最古の観察（最低優先）から落ちる＝シャドーを優先的に間引く設計（トレード・成績には影響なし）。

### 反映・確認
- Bot を v3.9.98 に差し替え、GAS を v9.13 に再デプロイ。構文OK・test_safety 20件green。
- トレード記録（最優先・クールダウン迂回）は一切変更なし。

### 背景
単一GAS運用を維持したまま、Bot側で送信負荷をさらに下げる。共有GASの過負荷は「混雑した“その時”に皆が送り続ける」ことが主因。

### 変更（Bot側のみ・GAS変更なし）
- **適応スロットル**：観察/PnLのフラッシュ間隔を、GASの健全性に応じて自動調整。
  - 平常：150〜210秒（従来）
  - 不調の兆し（連続失敗2回以上）：約1.8倍（最大7分）
  - 混雑中（クールダウン or 連続失敗5回以上）：約3倍（最大10分）
  → 混雑している“まさにその時”だけ送信を絞り、回復したら自動で通常ペースへ戻る。バッファは満杯(200)で即時flushするためデータ欠落なし。
- **送信前ジッター 2.0→5.0秒**：市場イベント時に全受講生が同時刻に殺到する相関バーストを更に分散。

### 位置づけ
- v3.9.95（段階的バックオフ＋送信本数削減）／v3.9.96（トレード最優先・エラー送信削減）と合わせ、単一GASでの緩和策を厚くする。観察/PnL（高頻度・低優先）を絞り、トレード記録（最優先）は従来どおり最優先で送る。
- さらなる構造的削減が必要なら「観察エントリと+60分PnLを1回の書き込みに統合（POST半減・GAS更新スキャン廃止）」が次の一手（要・観察パイプライン改修）。

### 反映・確認
- Bot を v3.9.97 に差し替え（GAS変更なし）。構文OK・test_safety 20件green。

### 送信の優先度（トレードが最重要）
- **trade / summary はクールダウンを迂回して直送**（`_GAS_COOLDOWN_EXEMPT_KINDS`）。過負荷で退避させるのは高頻度・低優先の **observation / observation_pnl のみ**に限定。トレード記録は最優先で送信機会を得る（失敗時はキュー退避するため消失なし）。
- 結果：観察送信が過負荷でクールダウン中でも、トレード/成績サマリは止めずに送る＝「トレード先・エラー(観察)後」を担保。

### エラー送信の削減
- 日次サマリ同梱の `error_logs` を **50→25件** に削減（GAS送信量・エラーログ詳細シートの肥大を抑制。`error_count` は全件カウントのまま）。エラーは元々ストリーミングでなくサマリ同梱（1日1回）。

### 版ヘッダの整理
- 本体先頭のバージョン履歴（旧 v3.9.63〜90 の1行要約群）を**削除**し、`BOT_VERSION` を「★現在の版はここ★」の明示ブロックに集約。**履歴はすべて CHANGELOG.md** に一本化。

### 反映・確認
- Bot を v3.9.96 に差し替え（GAS変更なし）。構文OK・test_safety 20件green。

### 背景
6/24-26、フリート全体で GAS が非JSON(HTMLログイン/エラーページ・len≈638)応答とタイムアウトを多発（先生ログでも本日 非JSON93/タイムアウト75/COOLDOWN73）。`/exec?t=` は v9.12 のJSONを正常返却＝デプロイ/権限は正常で、原因は**57名が単一Apps Scriptへ集中する過負荷**（同時実行30枠・クォータ）。売買・データ損失なし（記録送信のみ失敗→再送キューで自動回復）。

### 変更（Bot側のみ・GAS変更なし）
- **段階的バックオフ**：連続失敗が続くほどクールダウンを指数延長（180s→360→720→1440→上限1800s）。`_GAS_FAIL_COOLDOWN_MAX_SEC=1800` 追加。長時間過負荷での無駄な連投・HTML応答の量産を抑制（しきい値は従来どおり連続5回）。
- **送信本数の削減**：観察/PnLバッチ上限 100→**200**、フラッシュ間隔 90-120s→**150-210s**、最低優先の `observation_pnl` は**隔回フラッシュ**（POST数を半減）。バッファ満杯(200)時は即時flushするため欠落なし。

### 注意
- これは緩和策。160名スケールの恒久対策は**GASエンドポイントのシャーディング**（複数デプロイ/シートへ分散）。
- 先生の稼働中botが旧版の場合は本ファイル(v3.9.95)へ差し替えを（旧版はクールダウンが弱い）。
- v3.9.91〜95（決済コード/エラー除外/観察ログ列/LONG下限/本件）はまだフリート未浸透。次回差し替え時に一括反映を推奨。

---

## v3.9.94 (2026-06-24) — LONG の最小シグナル閾値 0.15%（弱LONG実発注の抑制）

### 背景（実データ・LONG 1,699件）
LONGはエントリー前変動(5分騰落)で損益が明確に分かれる。`<0.10%` の弱シグナルLONGが損失のほぼ全て（その帯だけで約 −$5,275）。閾値別の累計成績：全件 −$4,599 → `≥0.10%` で +$676 → `≥0.20%` で +$695。勝率も 0.52→0.58→0.64 と単調改善。SHORTは利益源のため対象外。

### 変更
- **`MOMENTUM_LONG_MIN_SIGNAL_PCT`（デフォルト 0.15）を新設**。LONG(BUY)シグナルの5分騰落率がこの下限未満なら**実発注を見送る**（`_will_live_order` に `_long_min_ok` を追加）。安全マージンを取り 0.15%（371件・WR53%・+$627 相当）。
- **シャドー観察(momentum_shadow)は全件継続記録**＝見送ったLONGもデータには残り、後から閾値の妥当性を再評価できる。SHORT・他カテゴリは不変。
- 起動時のモメンタム設定バナーに「LONG下限 5m≥0.15%」を表示。`0` で無効（従来どおり）。

### 注意・確認
- 本集計はニュース由来LONGも含むため、デプロイ後に決済コード×カテゴリで「モメンタムLONGのみ」に絞って効果を再確認すること。閾値は `.env` の `MOMENTUM_LONG_MIN_SIGNAL_PCT` で調整可（0.10〜0.20が候補）。
- Bot を v3.9.94 に差し替え（GAS変更なし）。構文OK・test_safety 20件green。

---

## v3.9.93 (2026-06-23) — 案a: 高ボラ損切り拡大を「全員シャドー」で検証（GAS v9.12）

### 背景（コードで判明した設計不整合）
モメンタムの損切りは全銘柄一律 `MOMENTUM_STOP_LOSS_PCT=0.50%`。一方、高ボラ銘柄(SMH等)は発注時にサイズを `÷STOCK_HIGHVOL_LOSS_MULT(2.5)` で縮小（リスクパリティ前提）していた。ところが損切り側は「二重拡大回避」コメント(15743)で倍率を**意図的に外して**おり、結果 SMH-SHORT は「小さいサイズ＋狭い損切り(0.50%)」に。SMHの15分変動中央値は0.52%で、**自分のノイズより狭い損切り**＝whipsawで弾かれ、実発注SMH-SHORTの主損失源になっていた（シャドーでは理論プラスなのに実発注は負け）。

### 方針
実取引のロジックは一切変えず（＝全受講生の挙動は不変）、**全員のシャドー計測**で「現行0.50%幅 vs 高ボラ倍率の広幅」を比較し、損切り幅拡大の是非を検証する。建玉トレール導入時と同様、検証→（有意なら）後日に挙動変更、の順を守る。

### 変更
- **Bot：`_compute_virtual_exits` に固定幅ストップの仮想決済を追加**。`narrow_stop_frac`（現行0.50%）/`wide_stop_frac`（0.50%×`_symbol_loss_mult`）で、同じ前進バー上で決済した仮想PnL%を算出（`stop_narrow_pnl_pct`/`stop_wide_pnl_pct`）。Step0観察パイプラインで**全銘柄・全員**に計測（非高ボラは倍率1で狭=広）。実発注は不変。
- **GAS v9.12：観察ログに46列「仮想損切り狭%」/47列「仮想損切り広%」を追加**。`_applyObsPnlToRow_` が書込み、`ensureObservationSchema` が自動拡張。
- 先の live opt-in 案（`MOMENTUM_HIGHVOL_STOP` env・起動バナー・settings の `mom_hv_stop`）は撤回（実取引を変えないため不要）。

### 反映・確認
- Bot を v3.9.93 に差し替え、GAS を v9.12 に再デプロイ。実取引は変わらない。
- 数日〜1〜2週間後、観察ログの46/47列を SMH-SHORT 等で比較（広%が狭%を有意に上回るか・最大損失・件数）。明確に優位なら、その時点で初めて実取引への反映（v3.9.93より後のバージョン）を検討。
- 構文OK・test_safety 20件green。

---

## v3.9.92 (2026-06-23) — A: データ品質＋エラー削減（観察ログ掃除 / エラーシート除外拡張・GAS v9.11）

### 背景
シャドー%が報告書と再集計で食い違う原因は、観察ログのヘッダが正規45列 → 330列まで「仮想exitブロック」が重複増殖していたこと（データ行は空・ヘッダのみ汚染）。加えてエラーログ13,237件の大半は「GAS送信の再送退避」と「正常トレードイベント」で、真のエラーが埋もれていた。

### 変更
- **GAS v9.11：`cleanupObservationSchema()` を追加**（手動実行）。観察ログの余剰列(46列目以降・空)を削除し、ヘッダを正規 OBSERVATION_HEADERS(45列) へ再設定。`ensureObservationSchema` は超過列を検知してログ警告（自動削除はしない＝高頻度書込みでの誤削除を回避）。
- **Bot v3.9.92：`_ERROR_LOG_EXCLUDE_TAGS` を拡張**。GAS送信失敗・再送退避（「記録送信のみ失敗」「[データ収集] 通信エラー」）と、正常トレードイベント（強制損切り/トレイリングストップ/建玉トレールストップ/パニックセル/時間切れ/[モメンタム実発注]/サーキットブレーカー/1日損失上限/デモ日次決済/週末前強制決済）を「エラーログ詳細」への送信から除外。端末表示は維持・売買とデータ送信に影響なし。

### 反映・確認
- Bot を v3.9.92 に差し替え、GAS を v9.11 に再デプロイ。
- GASエディタから **一度だけ `cleanupObservationSchema()` を手動実行**（US市場クローズの静かな時間帯に）。以後、観察ログは45列でシャドー分析が信頼できる状態になる。
- 構文OK・test_safety 20件green。

---

## v3.9.91 (2026-06-22) — 決済コード(exit_code)で「その他」撲滅 ＋ GAS v9.10

### 背景
6/22 日次レポートの「最大の出血源『その他』-$3,062」の実体は、自由文字列の決済理由が表記揺れで取りこぼされた **建玉トレール**（61件 -$3,057・WR13%）だった。集計ルールに「建玉トレールを独立分類」と書いていても、自由文字列依存では誤分類が再発する。

### 変更
- **Bot：`_exit_code_from_reason()` を追加**し、トレード送信ペイロードに構造化 `exit_code` を付与（`ENTRY_TRAIL / TRAIL / FORCED_STOP / TIMEOUT / PANIC / LS_REVERSE / DEMO_DAILY / SESSION_CLOSE / TAKE_PROFIT / OTHER / UNKNOWN`）。人間用の文章は `exit_reason` に残す。13ケースのマッピング検証済み。
- **GAS v9.10：トレード詳細に 39列目「決済コード」を追加**。`ensureTradeSchema` で既存シートへ自動マイグレーション。`trade_id` 列参照を `TRADE_ID_COL=38` で固定し、列追加でのズレを防止。
- **集計ルール指示文（2026/06/22版）：列38 exit_code を一次分類キーに昇格**。空欄の旧行のみ従来のキーワード分類にフォールバック。

### 反映・確認
- Bot を v3.9.91 に差し替え、GAS を v9.10 に再デプロイ（既存シートは自動で39列目が増える）。
- 以後のレポートは exit_code で分類でき、「その他」への取りこぼしが原理的に消える。

---

## v3.9.90 (2026-06-19) — 建玉トレールを既定ON（標準）に

### 背景
建玉トレール（建玉時からのトレール式損切り）は、固定の強制損切りより**1件あたりの損失がほぼ半分**（全期間：建玉トレール −$34.34 / 131件 vs 固定 −$66.52 / 624件）。18→92→131件と増えても一貫。最大の出血バケット（強制損切り全期間 −$41,507）を縮小できる最有力レバー。v3.9.86で peak を建値で floor/cap 済み＝下方向は固定損切りに劣らない（弱意味で優位）ため低リスク。

### 変更
- **`MOMENTUM_TRAIL_FROM_ENTRY` の既定を false → true（建玉トレール標準）**。未設定の環境はONになる（.envに明示 false がある環境は従来どおり）。従来の固定損切りに戻すなら `=false`。
- 起動バナーを INFO 化し、ON/OFF を明示（「検証用の任意設定」表記を廃止）。
- Wizard v1.23：[6]損切りの方式を **[1]建玉トレール（標準）/[2]従来固定** に入替、既定を[1]に。`.env` 書き出しの既定も true。

### 反映・確認
- Bot を v3.9.90 に差し替え（.env変更不要）。レポートの「建玉トレール」決済が主になり、固定「強制損切り」が減るはず。1〜2週間で損失縮小を継続確認。

---

## v3.9.89 (2026-06-17) — L/S転換の抑制（任意）＋ Alpaca無限再接続の停止

### (a) L/S転換の抑制 — `MOMENTUM_REVERSE_EXIT`（既定true・falseで抑制）
- 背景：反対方向シグナルで既存ポジを強制決済して向きを変える「転換」（決済理由「ロング前ショート強制決済」「ショート前ロング強制決済」）は、**反対シグナルが相場の逆行時に出るため、ほぼ必ず含み損で確定**。実データで **WR0%・6/17で −$749** の純出血。
- 変更：これまで定義のみで未使用だった `MOMENTUM_REVERSE_EXIT` を `place_buy`/`place_short` に正式配線。**false のとき、反対ポジ保有中は強制決済せず新規エントリーを"見送る"だけ**にし、既存ポジは自身の損切り/トレール/時間切れ/建玉トレールに委ねる。**★既定を false（転換抑制）に設定**＝全体で転換オフが標準（WR0%の純出血を回避）。従来挙動に戻すなら `MOMENTUM_REVERSE_EXIT=true`。起動バナーに「L/S転換 ON/OFF」を表示。
- 影響：モメンタム・AIニュース両方の発注経路に効く（共通処理）。

### (b) Alpaca News 無限再接続の停止
- 背景：Alpaca News の認証失敗（402無効/未購読キー・406キー併用）が指数バックオフでも**無限に再接続**し、ログ汚染の最大要因（過去最多級）。
- 変更：`_AUTH_FAIL_GIVEUP=20`。**認証失敗が20回連続でそのセッションのAlpaca News受信を停止**（RSS/Finnhubでニュースは継続）。再開はBot再起動。

### (c) レポートの建玉トレール分類（集計ルール側）
- 日次レポートが「建玉トレールストップ」を「その他」に誤分類していた（6/15・6/17）。`日次レポート_集計ルール指示文.txt` に独立分類ルールを明記済み。レポート生成時にこの指示文を必ず添付すること（Bot/GAS変更ではなく集計プロンプト側）。

---

## v3.9.88 (2026-06-17) — GAS送信の負荷削減＋優先度付け（A）

### 背景
PANの実ログ（6/15-6/17・約2日）解析で、送信失敗が**米国市場時間（夜間ET）に集中**し、GASがHTMLエラーページを返す＝**共有GASの過負荷（同時実行30枠超過）**と確定（OBSERVATION_EMPTYRESP 70・TIMEOUT 34・DATA_COLLECT_EMPTYRESP 7）。再送キューが約127件で滞留し、48回到達分が破棄＝observation_pnl（と一部トレード）が少しずつ欠落していた。/exec のGETは正常JSON（アクセス設定はOK・原因は過負荷）。方針「トレード・シャドー重要／エラー低／送信が多すぎ」に沿い、送信を減らし重要度で守る。

### 変更内容（すべてBot側・GAS変更不要・データ整合は維持）
- **① POST数の削減**：観察バッチ上限 40→100、フラッシュ間隔 45-60秒→**90-120秒**、起動オフセット 0-30秒→**0-60秒**。バッファは全件を1ペイロードで送るためデータ欠落はせず、送信回数（GAS実行数）を約半減。シャドーは緊急性が低く、多少の遅延は問題なし。
- **② 種別別の再送上限（重要度保護）**：`_GAS_RETRY_MAX_BY_KIND` を追加。**トレード=480（実質破棄しない）/ サマリ=240 / 観察=48 / 観察PnL=24（最低優先・最初に諦める）**。重要なトレード記録が48回上限で失われるのを防ぐ。
- **③ 退避超過時の破棄も重要度順**：`_GAS_QUEUE_MAX` 超過時、最古一律破棄→**重要度順破棄**（トレード/サマリ温存・observation_pnl から先に捨てる・同優先内は新しい方を残す）。
- 既存の永続キュー・dedup・バックオフ・ジッターはそのまま。

### 効果と注意
- POST数減＋優先度保護で、過負荷由来の失敗・滞留・データ破棄を緩和。とくに**トレードは事実上破棄されなくなる**。
- 根本解決は共有GASの分散（将来課題）。本変更は頻度と取捨選択の改善でBot側からの緩和。
- 注記：稼働中の /exec は **GAS v9.4**（v9.5/v9.6 はエディタ保存のみで未デプロイ）。本変更はBot単独で有効。GAS v9.6の二重登録ロックは過負荷時に直列化でレイテンシ増の恐れがあり、本件（負荷低減）を先行させ、ロックの再デプロイは閑散時に慎重に。

---

## v3.9.87 (2026-06-16) — エラーログのノイズ削減（B-1）

### 背景
6/15 のエラー集計を見ると、件数の多くが「エラー」ではなく**情報バナー/状態通知**（起動バナー・設定通知・定常的なセッション移行決済・送信側オートバックオフ等）がWARNINGで記録され、GAS「エラーログ詳細」に混入したもの。これが"エラー"件数を水増しし、本当のエラー（GAS空/非JSON・timeout等）が埋もれていた。

### 変更内容（ログの選別のみ・売買/データ送信に影響なし）
- `_ERROR_LOG_EXCLUDE_TAGS`（9種）を追加し、日次サマリのエラー収集（自身のログを解析してGASへ送る箇所）で、これらのタグを含む行を**エラーシート送信から除外**。除外対象：`[PCT単位]` / `[デモ空売り]` / `[実口座ショート]` / `[建玉トレール]` / `[起動時検証]` / `[移行前全決済]` / `[モメンタム監視整合]` / `実発注モード有効` / `[gas_cooldown]`。
- **端末（コンソール）表示は従来どおり**（WARNINGのまま見える）。エラーシートへの送信だけを止める。
- GAS空/非JSON・timeout・強制損切り失敗・サーキットブレーカー等の**本物のイベントは引き続き記録**（除外しない）。

### 補足（レポート側の対応とセット）
- 日次レポートの集計ルールに「建玉トレールストップ」を独立カテゴリとして追加（`その他`への誤分類を解消）。6/15は建玉トレール3件（計−$235.78）が`その他`に誤分類されていた。

---

## v3.9.86 / GAS v9.6 (2026-06-13) — コードレビュー指摘の最優先3件を修正

詳細レビュー（4観点並行）で挙がった事故リスクのうち、最優先3件を実装。

### ① Bot：建玉トレール（v3.9.85）の peak_price 未seed を修正
- 問題：`TickerState.peak_price` は建玉時に `avg_cost` でseedされず「最初に観測した市場価格」で初期化される。建玉直後に逆行（ギャップ/観測遅延）すると、PANトレールの損切り線が建値より深くなり、CHANGELOGが謳う「固定損切りに劣らない（弱意味で優位）」前提が崩れていた。
- 修正：PANモードの基準を建値で floor/cap（LONG=`max(peak, avg_cost)` / SHORT=`min(peak, avg_cost)`）。下方向では固定損切りと同値、有利方向では切り上がる、を保証。
- 併せて：`trail_active` 到達後は広い建玉トレール（④）を止め狭いトレール（⑤）に委ねるよう `not ts.trail_active` を追加（急落1tickで利確が損切り計上されA/B分類を汚す問題を解消）。
- 影響：`MOMENTUM_TRAIL_FROM_ENTRY` は既定OFFのため通常受講生に影響なし。**検証者がONにする前提の修正**。

### ② GAS v9.6：二重登録の根絶（並行書き込みのロック）
- 問題：doPost は最大30並行実行されるため、`appendTradeRow`/`appendObservationRow` の「`find...ById_`（探索）→`appendRow`（追記）」の間に別実行が割り込み、両方が「重複なし」と読んで二重追記する競合（check-then-act）があった。v9.4の常時dedupは逐次実行を暗黙前提にしていた（6/8-6/9 の二重計上 -$1,466 の残存要因）。
- 修正：`_withWriteLock_`（`getScriptLock`）で探索→追記を直列化。バッチは全体で1回だけロック。`tryLock` 不可時はデータ欠損回避のためロック無しで続行（まれな重複は `runDedupeTrades` で掃除）。

### ③ GAS v9.6：週次メンテの6分上限対策（分割＋上限）
- 問題：`weeklyMaintenance` は makeCopy（約3分）＋トリミングを1実行で行い、シート肥大で6分上限に接近。先日のデータ欠損（rewrite途中終了）と同クラスのリスク。
- 修正：(a) `weeklyBackup`（バックアップ＋検証＋古いバックアップ掃除）と `weeklyRolling`（保持トリミング）を**分離**し別トリガーで実行可能に。`weeklyRolling` は直近26時間に完全バックアップが在る時だけ削除する安全策付き（`_hasRecentBackup_`）。(b) `trimOldRowsByDate_` に1回あたり削除上限 `MAINT_TRIM_MAX_ROWS=8000` を導入（大量削除でも次回へ持ち越し）。旧 `weeklyMaintenance` は互換のため残置。

### 反映手順
- Bot：差し替えのみ（v3.9.86）。挙動は既定不変。
- GAS：保存＋**再デプロイ**（doPost の `appendTradeRow`/`appendObservationRow` を変更したため）。トリガーは `weeklyMaintenance` 単独のままでも可だが、**`weeklyBackup`（日曜5:00）と `weeklyRolling`（日曜6:00）の2トリガーに分離**を推奨。

---

## v3.9.85 (2026-06-12) — PAN案：建玉トレール（任意・既定OFF）

### 背景
週次でもトレイル+$20,475(WR99.5%) vs 強制損切り−$18,468(WR15.8%)の綱引きが続き、損益はほぼトントン。PAN案「建玉時から損切り幅と同じ幅のトレールをかけ、現行トレールのトリガー到達で幅を狭める」を検討。MFE推定（破損前データ・直近10日）では強制損切り+$5,536・時間切れ+$2,838の回収見込み（上限）。論理的にも下方向は固定損切りに劣らない（弱意味で優位）。「1日で全体ロジックを変えない」方針に沿い、まず任意フラグ＋検証者のみONで運用検証する。

### 変更内容（既定OFF・全受講生は従来どおり）
- `MOMENTUM_TRAIL_FROM_ENTRY`（env・既定false）を追加。
- **true かつモメンタム建玉**のとき、`risk_monitor_loop` の損失保護を「固定の強制損切り（建値基準 `pnl <= -max_loss_dollar`）」→「**建玉からのトレール（peak基準・幅=`MOMENTUM_STOP_LOSS_PCT`）**」に置き換え。`peak_price` は従来から毎ループ更新済みのため、有利側へ動けば損切り線も切り上がる。下方向一直線では `peak≒建値` で固定損切りと同値。
- `trail_active` 到達後は既存の狭いトレール（`TRAIL_DROP_PCT` < 損切り幅）が先に発火するため、自然に**2段階トレール**になる。
- 決済理由は **「建玉トレールストップ」** で記録（レポートで識別可能）。USD上限（`STOCK_MAX_LOSS_USD`）の保護はモードに関わらず併用。
- A/B対照群を汚さないため、**PAN案で決済した分は `forced_stop_ab` 観察を記録しない**（固定損切りの対照群のみ計測継続）。
- 起動時に状態をWARNINGで明示（検証者向け）。

### 検証方法
検証者の `.env` に `MOMENTUM_TRAIL_FROM_ENTRY=true` を設定しデモで1〜2週間、現行（固定損切り）と並走比較。トレード詳細の決済理由「建玉トレールストップ」の実損益・勝率を、対照群（強制損切り）と比較する。効果が確認できたら全体反映を別途判断。

### 注意
- 挙動を変えるのは**フラグONの環境のみ**。未設定（既定）の全受講生は完全に従来どおり。
- 損切り幅そのものの緩和は本変更に含まない（それは `forced_stop_ab` の蓄積で別途判断）。

---

## v3.9.84 (2026-06-11) — 強制損切りA/B計測（シャドー・挙動完全不変）

### 背景
直近10営業日（取引ID重複除去・全口座）の堅牢な傾向：**トレイル決済 +$44,155 / WR98.9%（利益源）** に対し **強制損切り −$35,324 / WR10.6%（出血源）** で、両者がほぼ相殺。とくにSHORTは「トレイルで+$41K稼ぎ、強制損切りで−$32K失う」＝同じ建玉を通常の上方向ノイズで早すぎる損切りが潰している。**強制損切り幅の緩和／トレール委任**が最大の利益レバー候補だが、「1日のレポートでロジックは変えない」方針に沿い、まず1〜2週間シャドーで反実仮想を計測する。

### 変更内容（売買は一切変更しない・計測のみ追加）
- `risk_monitor_loop` の強制損切り発動箇所に、**反実仮想を記録する観察ログ送信を1件追加**（`block_stage="forced_stop_ab"`）。
  - `price_at_decision` に**取得単価**、`eff_stop_loss_pct` に**実効損切り%**、`live_allowed=True` を渡す。
  - 既存の +60分 vexit パイプライン（`_PENDING_OBSERVATIONS` → `_compute_virtual_exits` → 観察ログ列31〜39）に相乗りし、**「損切りせず保有/トレールしていたら」の反実仮想**を後追い算出：
    - 列32 `基準PnL%(60分)`：損切り後も保有した場合の方向別PnL%
    - 列33 `仮想MFEトレールPnL%`：トレールに委ねた場合のPnL%
  - `block_reason` に realized/threshold/entry/stop_px を文字列で併記（クロスチェック用）。
- **A/B評価**：`forced_stop_ab` 行で「実現損 ≒ −実効損切り%（列43）」と「反実仮想（列32/33）」を比較。反実仮想が継続的に実現損を上回れば、強制損切りが早すぎる＝緩める/トレール委任の検証材料になる。
- **GAS変更なし**（v9.5 の既存列＋既存vexit列を再利用）。送信は1強制損切りあたり1件増のみ。`_is_data_collect_enabled()` ゲートで従来どおりデータ収集ON環境だけが対象。

### 使い方（運営）
観察ログを `block_stage = forced_stop_ab` で抽出し、`category`（MOMENTUM / TECH / MACRO …）と `取引方向` 別に「列32・列33 の平均」対「−列43 の平均」を1〜2週間集計。SHORT/MOMENTUM で反実仮想が明確に勝てば、`MOMENTUM_STOP_LOSS_PCT` の緩和や確認型損切りへの移行を別途検討する。

---

## v3.9.83 (2026-06-11) — 事故防止ガードレール（認定サポーターの利用者B提案の縮小採用）+ GAS v9.5

### 背景
認定サポーターの利用者Bから「新ロジックを足さず、まず見える化とガードレールで事故を減らす」提案。評価の上、既存機能と重複する大型項目（Bot Doctorフル版・通信エラー台帳の一括共通化・設定正本化の全面リファクタ・巨大関数分割）は見送り、**効果が高くリスクの低い3点に絞って**採用した。

### 変更内容
- **① OpenD到達性チェックをデモ起動でも実行**（従来は `--live` のみ）。大多数の受講生はデモ口座で、OpenD未起動のまま起動すると「ポジション取得失敗＋リトライ連発（リトライ嵐）」が発生していた。`main()` の起動時に `if live:` → 常時実行へ変更し、3秒タイムアウトで明示エラー＋アラート音→停止。エラーボックスの文言はデモ/実で出し分け。
- **② 観察ログに「発注判断の文脈」列を追加**（運営が「なぜ発注/見送りか」を後から検証）。新ログ系統は作らず、**既にGASへ送信され運営が見られる観察ログに列追加するだけ**（送信回数は不変・既存バッチに相乗り＝160人スケールの負荷増なし）。`_log_observation` に任意kwargsを追加：`live_allowed`（実発注可否）/`size_pct`（投入サイズ%）/`eff_stop_loss_pct`（実効損切り%）/`pct_5m`,`pct_15m`（モメンタム変化率）/`quote_sanity`（クォート健全性 1/0）。モメンタムシャドー判定とクォートガード遮断の各 `_log_observation` 呼び出しから値を渡す。
- **③ 安全関数の単体テストを追加**（`tests/test_safety.py`・**配布外**・追加依存なし＝標準 `unittest`）。バイブコーディングの回帰を数秒で検出。対象：`_pct_frac`（1=1%統一＋旧小数互換）/`_classify_gas_error`/`_quote_reference_price`・`_quote_sanity_ok`（異常クォート遮断・fail-open）/`_momentum_side_allowed`・`_DEFAULT_ENABLED_SIDES`（IWM/DRAMが実発注に紛れない）/`_vexit_trail_drop`・`_wilder_rsi`。**20件すべて green**。

### GAS v9.5（観察ログ 39→45列）
- `OBSERVATION_HEADERS` に 40〜45 列を追加（クォート健全性/実発注可否/投入サイズ%/実効損切り%/モメンタム5分%/15分%）。`appendObservationRow` で記入。
- 列追加は `ensureObservationSchema` が初回受信時に**自動マイグレーション**（既存行は空欄・破壊なし）。**GAS の再デプロイが必要**。

### 採用しなかった提案（理由）
- **Bot Doctorフル版**：Wizard設定警告・起動バナー・PCT警告・健全性ループで大半カバー済み。新規価値のうち実効のある「OpenD事前チェック」は①で単独採用。残りは重複のため見送り。
- **通信エラー台帳の7サービス一括共通化**：分類は既に実装済み。一括化は全エラー箇所への広域追記＝事故源化のため段階導入（後回し）。
- **設定正本化の全面リファクタ／巨大関数分割**：17,000行への広い改修でリスク大。後回し。

### 実行
```
cd /Users/pan/Desktop/moomoo_trade
.venv/bin/python -m unittest tests.test_safety -v
```

---

## v3.9.82 (2026-06-10) — 160人スケール対策：GAS送信エラーの低減（Bot側のみ・GAS変更なし）

### 背景
受講生160人化が目前。6/9 の過負荷（timeout＋空応答）は「市場イベントで全受講生が同時刻に1つのGASへ殺到する相関バースト」が主因（Apps Script の同時実行上限30に超過分が弾かれる）。データ損失・二重登録は既に対策済みのため、ここでは**失敗そのものの低減**を狙う。

### 変更内容（すべて Bot 側・GAS は v9.4 のまま）
- **① 送信ジッター**：`_gas_send_with_retry` の送信前に 0〜2 秒のランダム遅延。観察フラッシュループを 30秒固定 → **45〜60秒（ジッター）＋起動時ランダムオフセット 0〜30秒**に変更。160台の一斉着弾を時間的に分散。
- **② 送信側オートバックオフ**：`_gas_send_health` で連続失敗を追跡。**連続5回失敗で180秒間は直接送信せずキューへ退避**（過負荷GASへの追い打ちを停止）。回復は `gas_retry_loop`（プローブ）が検知し、**成功でクールダウン即解除**。
- **③ POST数・回復力の強化**：観察バッチ件数 **25→40**、再送上限 **24→48**、再送バックオフに**ジッター**（再送の同時バーストも回避）。

### 効果
- 混雑日のGAS送信エラー（timeout/空応答）が減少。失敗時もキュー＋クールダウンで追い打ちを避けつつ、回復後に自動再送。GAS は据え置きで適用可能。

---

## v3.9.81 (2026-06-10) — セッション後のデータ回収を強化（朝レポート対策・GAS変更なし）

### 背景
朝レポートのため「セッション終了後に確実にデータを集めたい」。従来は以下が停止/再起動で取りこぼし得た：
- 再送キューはディスク保持だが**ボット停止中はドレインされない**。
- **+60分の観察PnL／Step0仮想exit** の対象 `_PENDING_OBSERVATIONS` が**メモリのみ**で、停止で消失（+60分未経過分は永久に未送信）。

### 変更内容（すべて Bot 側のみ・GAS は v9.4 のまま）
- **P1-1 pending観察のディスク永続化＋起動時リカバリ**：`_PENDING_OBSERVATIONS` を `pending_observations.jsonl` にも保存（`_persist_pending_obs`）。処理に回したら除去（`_unpersist_pending_obs`）。`observation_pnl_check_loop` 起動時に `_load_pending_observations()` で復元し、+60分経過済み分を**履歴K線から算出して送信**。→ 停止/翌朝再起動でも観察PnL・Step0仮想exitを回収。
- **P1-2 終了フック**：`main()` の finally で、観察バッファの最終フラッシュ＋未送信キューを1回ドレイン（best-effort）。送れない分はローカルに残り次回起動で回収。
- **P1-3 起動直後の即ドレイン**：`gas_retry_loop` が120秒待たず起動直後に1回ドレイン。翌朝の再起動で未送信分を素早く回収。

### 運用上の最善
- セッション後もボットを**最後の取引から+90分ほど起動維持**すると、+60分PnL算出・キュー・バッチが自然に全て流れる。朝レポート生成は「最後の取引から+60分以降かつキュー空」を確認してからが安全。

---

## v3.9.80 (2026-06-10) — GAS 空/非JSON応答の明示化（受講生報告：6/9 記録送信失敗）

### 背景
受講生報告（6/9・v3.9.76・デモ）：記録送信が109回失敗。①timeout 61回 ②`Expecting value: line 1 column 1 (char 0)` 48回（=GASが空/非JSONを返しJSON解析失敗）。trade23・observation86が再送キューへ退避するも当日中の再送成功ログが見当たらず。原因は**共有GASエンドポイントの過負荷**（多数の受講生が1件ずつ送信＋Googleの同時実行/クォータ制限でtimeout＋HTMLエラーページ/空応答）。

### 変更内容
- `_gas_post`：**空応答・非JSON応答を明確な例外**に変換（従来は不透明な JSONDecodeError）。`_classify_gas_error` に `EMPTYRESP` 分類を追加し、`[OBSERVATION_EMPTYRESP]`/`[DATA_COLLECT_EMPTYRESP]` として「記録送信のみ失敗（売買影響なし・再送キューへ退避）」と表示。
- 応答が不達でも GAS 側で書込済の可能性があるため、**再送はキュー経由（retryフラグ付き）→ trade_id / observation_id で重複排除**（v3.9.78 / GAS v9.4 とセット）。よって再送で二重登録しない。

### 補足（恒久対策）
- 過負荷自体の緩和は v3.9.76 のバッチ送信（観察ログ/PnL を数件まとめ＋直列化）＋最新版の普及が本筋。記録の確実性は永続再送キューが担保（`gas_retry_queue.jsonl` はディスク保持・次セッションでドレイン）。

---

## v3.9.78 (2026-06-09) — P0修正2件：トレード二重登録の根絶 + 異常クォートガード（外部レビュー指摘より）

外部レビュー（Fable5）が集計データで指摘した2件を実データで確認し、最優先で対応。

### #1 トレード結果の二重登録を根絶（進行中のデータ汚染）
- **症状**：6/8-6/9 にトレード詳細で完全重複122行（生徒名・日時・価格・PnL一致）、二重計上 −$1,466.10。重複行は全て trade_id 付き。
- **真因**：GAS側の重複排除（v9.2, trade_id）は `retry=true` の時のみ発火する設計。一方 Bot の `_gas_send_with_retry` の**インライン再送（0/5/15秒）は retry フラグを立てずに同一 payload を再送**していた。初回送信がGASに書き込み成功したのに応答だけ read timeout で消失すると、2回目がフラグ無しで再送され二重登録。v3.9.75 の実装欠陥。
- **修正（Bot）**：`_gas_send_with_retry` の**2回目以降のインライン再送に `retry=true`（トップレベル＋data両方）を付与**。trades/observations/batch を一括で塞ぐ。
- **修正（GAS v9.4）**：保険として `appendTradeRow` を「**trade_id があれば常時重複チェック**」に変更（retry フラグ非依存）。トレードは件数が少なくスキャンコスト軽微。

### #2 異常クォートガード（テール損失の構造遮断）
- **症状**：最大損失が板薄プリマーケットの異常クォート由来。CRM `entry $868.54→exit $174.18`（0分・premarket・**−$8,332**）、TSLA（**−$2,900**）等。「強制損切り −$39K」の相当部分がこの種の事故で、戦略損ではない。
- **修正**：発注直前（`place_buy`/`place_short` の get_quote 直後）に `_quote_sanity_ok()` を追加。
  - `_INDEX_PRICE_HISTORY` の**直近価格の中央値**を基準に、現値が **±`QUOTE_SANITY_DEVIATION_PCT`%（既定15）超**乖離なら発注見送り＋WARNING＋観察ログ（block_stage=quote_sanity）。
  - スプレッドガード（`QUOTE_SANITY_SPREAD_PCT`, 既定0=無効）は任意。
  - 参照価格が無い（履歴<3件）場合は **fail-open**（発注を止めない）。正常取引は一切制限せず、A-1見送り方針とも矛盾しない。

### 反映
- Bot を v3.9.78 へ更新、GAS を v9.4 にデプロイ（管理→編集→新バージョン／URL据置）。env 追加は任意（既定で有効）。

---

## v3.9.77 (2026-06-09) — エラー監視の強化 + 通信ノイズ削減（日次/シャドーレポート＋ログ分析より）

6/8 の集計・レポートおよびログ（PAN実ログ＋受講生集計）分析にもとづくエラー対応。A-1（モメンタム発注の絶対上限）は「good取引も金額を抑える」ため見送り。

### B-1. Alpaca News 接続不全の警告強化
- 受講生集計でエラーログ最多級（auth failed 連発 ≈ 2,700件）。多くはキー未設定/購読無効のまま再接続を反復。
- `health_warning_loop` の Alpaca 区画を**端末・赤字で5分おき**＋**Discord 30分おき**に変更（従来は1行WARNING/Discordなし）。文言に「APIキー/購読を確認・未設定でもRSS/Finnhubで継続」を明示。`_alpaca_news_health["last_discord_at"]` で30分間引き。

### B-4. sync_positions 連続失敗の警告強化
- 集計で「Network interruption / 全パターン失敗」≈ 2,000件（OpenD/ネットワーク不安定）。
- `_sync_health`（連続失敗数・最終成功時刻・直近エラー）を新設。`sync_positions` 成功でリセット・失敗で加算。連続3回以上で `health_warning_loop` が**端末赤字5分**＋**Discord 30分**で「OpenD起動/接続・ネットワーク確認」を警告。

### B-3. get_order_book 失敗のセッションキャッシュ
- `ret=-1`→`get_stock_quote` フォールバックは v3.9.31 で既に DEBUG 降格済み。本版で **セッション内で失敗した銘柄を記憶**（`_orderbook_fail_symbols`）し、以降は ORDER_BOOK 購読/取得を**スキップして get_stock_quote に直行**。セッション変化でリセット。無駄な失敗呼出とログを削減。

### B-2. Anthropic invalid_request の切り分けと対応
- 受講生集計の invalid_request(400) ≈ 1,600件を分解：**約1,460件＝「Your credit balance is too low / spend limit到達」（課金側）**、**約80–140件＝`stop_sequences` 拒否（実バグ）**。PAN実環境はAnthropicエラー皆無（健全）。
- `_anthropic_create_with_retry` に **stop_sequences 400拒否時のフォールバック**を追加：エラーに `stop_sequence` を含む場合は `stop_sequences` を外して即リトライ（出力削減用の最適化なので除外しても安全・将来のAnthropic仕様変更にも頑健）。
- 残高切れ分は既存のクレジット切れ検知＋30分Discord再通知（v3.9.71）でカバー済み（課金対応が必要）。

### 見送り
- A-1 モメンタム実発注の絶対上限（USD/株）：方針により見送り。

---

## v3.9.76 (2026-06-07) — 観察ログ/PnL のバッチ送信（GAS負荷・timeout頻度の低減）

### 背景
v3.9.75 で再送キュー化したが、観察ログ・観察PnLは発生数が多く1件ずつ送るとGAS側POST数・同時接続が増え `read timeout` の温床になっていた（認定サポーター指摘④）。

### 変更内容（Bot 側）
- 観察エントリ・観察PnL更新を**バッファに溜めて一括送信**する方式に変更：
  - `_buffer_obs_entry()` / `_buffer_obs_pnl()` がバッファへ追加。**25件到達 or 30秒間隔**でフラッシュ。
  - `observation_batch_flush_loop()`（単一タスク）が定期フラッシュ。送信が**この1タスクに集約＝直列化**され、1ボットからの同時接続が抑制される（送信間隔も自然に空く）。
  - フラッシュは `observation_batch` / `observation_pnl_batch` として `_gas_send_with_retry` 経由で送信 → 失敗は v3.9.75 のローカル再送キューへ。
- シャドーSHORTの entry/exit 送信も `_gas_send_with_retry`（再送/退避つき）に統一。
- 再送時はペイロード**トップレベルにも `retry=true`** を付与（`data` を持たない batch 型に対応）。
- バッファはメモリ上のみ（クラッシュ時は最大30秒/25件分が消えうるが、観察データかつ+60分遅延処理のため許容）。トレード結果は従来どおり**即時送信＋再送キュー**で欠落防止（バッチ化しない）。

### 変更内容（GAS v9.3 側）
- `observation_batch` / `observation_pnl_batch` を受信し、`items` を1件ずつ既存の `appendObservationRow` / `updateObservationPnlRow` へ委譲（`payload.retry` を各 item に伝播）。初回は即追記、再送時のみ `observation_id` で重複排除。

### 効果
- 1ボットあたりの GAS POST 数を最大25分の1に削減し、同時接続も直列化で抑制 → `read timeout` の発生頻度を低減。記録の確実性は再送キューで担保。

---

## v3.9.75 (2026-06-07) — GAS送信の堅牢化（分類ログ + リトライ + 永続再送キュー + 重複排除）

### 背景
認定サポーター報告：リアル/デモ両方で `The read operation timed out` が多発。切り分けの結果、**注文・約定ではなく記録系（GAS送信）の外部通信timeout**と確認。トレード結果送信は別スレッド実行で監視ループは非ブロックだが、**永続的な再送キューが無く timeout時にトレード記録が欠落**しうる点が弱点だった。

### 変更内容（Bot 側・送信は別スレッド/別タスクで売買本体は非ブロック）
- **失敗の分類ログ**：`_classify_gas_error()` で `DATA_COLLECT_TIMEOUT` / `OBSERVATION_TIMEOUT` / `*_CONN` 等に分類し、「記録送信のみ失敗（売買影響なし・再送キューへ退避）」と明示。本物の注文系timeoutと混同しない文言に。
- **インライン再送**：`_gas_send_with_retry()` が 0/5/15 秒の最大3回送信。
- **永続再送キュー**：全失敗時は `gas_retry_queue.jsonl`（スクリプト同階層）へ退避。`gas_retry_loop()` が120秒間隔で再送（指数バックオフ・最大24回で破棄・上限3000件）。同一keyは退避時に置換して重複退避を防止。
- **重複排除キー**：トレードに決定論的 `trade_id`（student/symbol/exit時刻/qty/価格/方向のmd5）を付与。観察は既存 `observation_id`。再送ペイロードには `retry=true` を立てる。
- 配線：トレード結果送信・観察ログエントリ・観察PnL更新の3経路を `_gas_send_with_retry` 経由に変更。`gas_retry_loop` を主 gather に登録（DATA_COLLECT 無効環境は即 return でアイドル）。

### 変更内容（GAS v9.2 側・受け皿）
- 再送（`data.retry=true`）の時のみ重複検索：トレードは新設 38列目「取引ID」(`findTradeRowById_`)、観察エントリは `observation_id`（`findObservationRowById_`）で既存があればスキップ（冪等）。**初回送信は従来どおり即追記（探索コストなし）**。観察PnL更新は元々冪等。
- トレード詳細を 37→38 列へ自動マイグレーション（`ensureTradeSchema`）。

### 効果
- read timeout で**トレード記録が欠落しなくなる**（自動再送）。read timeout の二重登録も防止。ログから「注文系か記録系か」が一目で判別可能に。

---

## v3.9.74 (2026-06-07) — Step0 シャドー計測（仮想exit列の追加・実決済は不変）

### 目的
モメンタムの出口改良（MFE可変トレール / RSI反転 / 15分撤退 / 金曜停止）を、**実決済を一切変えずに**「もし～で決済していたら」の仮想 PnL として数日記録し、**実績/基準を上回った案だけを後から本実装**するための計測基盤（Step0）。

### 実装（Bot 側・観測ログ専用）
- `_compute_and_send_observation_pnl()`（+5/15/60分PnLを算出する既存パス）に仮想exit算出を上乗せ。**発注・決済ロジックには一切触れない**（観測ログ=GAS送信のみ）。
- K線取得範囲を `decision -5分` → `decision -120分` に拡張（RSI(5分) のウォームアップ確保）。`max_count` 100→200。
- 新関数 `_compute_virtual_exits()` が1分足リプレイで4種を算出（`_wilder_rsi()` 併設）:
  - **① MFE可変トレール**：含み益%に応じて戻り許容を段階的に縮小（+0.30→0.20% / +0.45→0.15% / +0.70→0.10% / +1.00→0.08%）。
  - **② RSI(5分)反転**：5分足リサンプル＋Wilder RSI(14)。SHORTは≤10で起動→底+5pt反発で返済、LONGは≥90で起動→ピーク−5pt反落で利確。
  - **③ 15分撤退**：15分以内のMFEが+0.20%未満なら撤退、その時点PnL%。満たせば保有継続（基準PnL%）。
  - **④ 金曜15:30以降フラグ**：該当エントリーを 1/0 で記録（集計時に除外可）。
  - すべて方向別 PnL%（正=利益）。基準（+60分保有のPnL%）も併記。データ不足や例外は None + メモ（`vexit_note`）で安全に黙殺。
- 数値（段階トレール幅・15分閾値・RSI水準）は **Step0計測用の候補値**（採用前提ではない）。

### 実装（GAS v9.1 側）
- 観察ログを 30 → **39 列**に拡張（31〜39：金曜1530以降 / 基準PnL%(60分) / 仮想MFEトレールPnL%・保有分 / 仮想RSI反転PnL%・保有分 / 仮想15分撤退PnL%・発火 / 計測メモ）。
- `updateObservationPnlRow` が、Bot v3.9.74+ の同梱フィールドを 31〜39 へ記入（PnL%セルは赤緑色分け）。列追加は `ensureObservationSchema` が初回受信時に自動マイグレーション。

### 留意
- 仮想exitは「K線終値ベース」の近似（実約定とは別基準）。方向判断には十分。
- 数日〜1週間ぶん貯めて「総PnLが基準/実績を上回った案のみ」を段階導入する前提。

---

## v3.9.73 (2026-06-07) — PCT 単位の「1=1%」統一 + デモ(ネッティング)SHORT 決済の堅牢化

### ① PCT 系 env 単位の「1 = 1%」統一（後方互換つき半自動移行）

#### 背景
`.env` の % 系項目に **2 通りの表記が混在**していた：
- **A群（1 = 1%）**：`MAX_LOSS_PCT` `STOCK_MAX_PCT` `MOMENTUM_*` など
- **B群（0.01 = 1%）**：`TRAIL_TRIGGER_PCT` `TRAIL_DROP_PCT` `LIMIT_BUFFER_PCT(_ETF/_STOCK)` `EARNINGS_TRAIL_*` `EARNINGS_MAX_LOSS_PCT`

混在は受講生の設定ミス（桁違い）の温床。今回 **全て「1 = 1%」へ統一**する。

#### 実装（Bot 側・後方互換）
- 新ヘルパー `_pct_frac(key, default_frac, lo, hi)` を追加。env を読み、**0.05 を境に二重解釈**：
  - `値 ≥ 0.05` → 新表記（パーセント数値）とみなし `÷100` して fraction 化（例 `0.30` → 0.003）
  - `0 < 値 < 0.05` → 旧表記（小数）とみなしそのまま fraction（例 `0.003` → 0.003）＝**旧 .env もそのまま動く**
- 旧表記を検出したら `_pct_unit_legacy_warnings` に蓄積し、起動時に `_emit_pct_unit_warnings()` が **「新表記は『1=1%』です／Wizard 再実行で自動統一」** を1回だけ警告。
- B群8項目（`EARNINGS_TRAIL_TRIGGER_PCT` `EARNINGS_TRAIL_DROP_PCT` `EARNINGS_MAX_LOSS_PCT` `TRAIL_TRIGGER_PCT` `TRAIL_DROP_PCT` `LIMIT_BUFFER_PCT` `_ETF` `_STOCK`）のパースを `_pct_frac` に置換。
- ※ 境界 0.05 の補足：旧 5%（= 0.05）のような極端設定だけは新表記 0.05% と解釈される。トレール/バッファでは現実的に使われない値のため実害なし。

#### 実装（Wizard v1.16 側・半自動移行）
- `_pct_to_percent_str()` を追加：既存 `.env` の旧小数（<0.05）を **×100 してパーセント表記の既定値**として提示。
- STEP3（トレール）/ STEP7（指値バッファ）の入力・選択肢・確認画面を **すべてパーセント表記**（例「0.30%」「1.0%」）に統一。
- `_collect_warnings` の単位誤りしきい値・メッセージ・再入力 validate をパーセント基準に更新（旧小数が残っていても内部で×100正規化して判定）。
- `_build_env_lines` の出力既定値を新表記に（`TRAIL_TRIGGER_PCT=0.30` 等）。
- **受講生は Wizard を再実行するだけで新表記へ自動移行**。再実行しなくても旧 .env はそのまま動作（警告のみ）。

### ② デモ（ネッティング）SHORT 決済の堅牢化（DRAM/SMH 残留2件対策）

#### 背景
デモ口座（ネッティング）の SHORT は sync 反映が遅く、`place_close_all` で **position_id を取り切れずに残留**する事例（DRAM/SMH）があった。

#### 変更内容
- `place_close_all` の position_id 取得リトライを **3回（計6秒）→ 5回（漸増 2/2/3/3/4 = 計14秒）** に強化。
- `_notify_close_failed_no_pid()` を強化：
  - `trd_env` / `qty` を受け取り、**口座種別（デモ/実）と残留株数**を明示。
  - デモ SHORT 残留時は **「手動の買い(BUY)で同株数を買い戻すと相殺(ネッティング)で解消」** の具体手順を案内。
  - **symbol+side ごとに15分に1回**へ Discord 通知を間引き（リスク監視ループ60秒毎の再試行による通知連投を防止）。ログ(error)は毎回出力。

---

## v3.9.72 (2026-06-05) — SMH の強シグナル上限を 0.80%→1.20% に引き上げ（発注機会の取りこぼし是正）

### 背景
SMH は 15分変動率の中央値 0.52% と高ボラのため、共通の強シグナル上限 `MOMENTUM_MAX_SIGNAL_PCT=0.80%` だと **信号の約17%が「強(発注なし)」に弾かれ**、発注機会を不必要に削っていた（SPY/QQQ/IWM はほぼ0%）。DRAM には v3.9.62 で別枠cap(2.40%)を入れたが、SMH(同じ高ボラ)は未対応だった。

### 検証（観察60分・方向調整後）
1.6%案と比較し、新たに解禁される帯の成績を確認：
- **0.80–1.20%（新規解禁）**：SHORT +0.36%/WR75%（n=192）・LONG +0.13%/WR70%（n=367）＝明確にプラス。
- 1.20–1.60%：SHORT 平均 −0.40%（中央+だが大負けの尾）・LONG +0.17% ＝微妙。
- ≥1.60%：−1.1%/WR24% ＝据え置き除外が妥当。
→ **明確にプラスの 0.80–1.20% 帯のみ解禁する 1.20% を採用**（微妙な 1.20–1.60% は除外）。

### 変更内容
- `MOMENTUM_SMH_MAX_SIGNAL_PCT`（既定 **1.20**）を新設し `_MOMENTUM_MAX_SIGNAL_OVERRIDE` に SMH を追加。env で上書き可。
- 起動バナーに SMH の強シグナル上限を併記。
- 効果：SMH の発注機会が増え（受講生の「発注が少ない」不満の緩和にも寄与）、新規帯は観察上プラス期待。※観察値で、実発注は早期決済で目減りする可能性あり（方向はプラス）。

---

## v3.9.71 (2026-06-05) — 注文系 no-event-loop 例外の根絶 + Anthropicクレジット切れ30分Discord再通知

### ① 注文系のイベントループ例外を根絶（6/4 DRAM SHORT 対策）
- 症状：`【DRAM】[ORDER] SHORT API例外: There is no current event loop in thread 'X'`（6/4 に 4 件）。
- 原因：モメンタム実発注は `await asyncio.to_thread(place_short/place_buy,…)` で**ワーカースレッド**実行され、そのスレッドにイベントループが無い。moomoo SDK（`OpenSecTradeContext`/`place_order`）が内部で `asyncio.get_event_loop()` を呼ぶと Python 3.10+ で例外。
- 修正：`_ensure_thread_event_loop()` を新設し、全トレードSDK呼び出しの共通入口 `_trade_ctx()` 冒頭で呼ぶ。running loop が無く current loop も無ければ空ループを用意（メインスレッドでは何もしない・例外は握りつぶし）。→ DRAM以外（SMH/IWM/QQQ）のショートを含む全注文経路で同種エラーを根絶。
- 補足：moomoo/futu サポートへの確認は任意（本ワークアラウンドは標準的で即効）。

### ② Anthropic クレジット切れの Discord 30分再通知
- 従来：端末は 5 分おきに赤字ボックス（health_warning_loop）だが、Discord は初回＋復旧時の1回ずつのみ。
- 追加：クレジット切れ継続中、**30分おきに Discord へ再通知**（`_ANTHROPIC_CREDIT_DISCORD_INTERVAL_SEC=1800`）。端末を見ていなくても気づけるように。停止継続時間・スキップ件数・追加購入URLを含む。
- 既存の検知（"credit balance is too low"）・5分赤字表示・30分プローブ復帰はそのまま。

### ③ Wizard v1.15 — 銘柄ごとの実発注サイド（買い/売り）個別選択
- `[2-c]` を「プリセット選択」から「**銘柄ごとに 買い(LONG)/売り(SHORT) を個別選択**」へ変更（SPY/QQQ/SMH/IWM の各々で 両方/買いのみ/売りのみ/なし）。選択結果を `MOMENTUM_ENABLED_SIDES` に書き出し。
- 推奨既定（直近成績準拠）：**SPY/QQQ/IWM=売りのみ、SMH=両方**（SHORTが全銘柄優位・LONGはSMHのみプラス傾向）。
- DRAM は観察専用のため選択肢に含めない。

---

## v3.9.70 (2026-06-04) — DRAM を既定の実発注対象から除外 + 実発注サイドを Wizard で明示選択

### 背景
「設定していないのに DRAM の注文が出た」という指摘。原因は v3.9.66 で `DRAM:SELL_SHORT` を**コード既定**の実発注対象に追加し、v3.9.68 で実発注・デモショートが既定 ON になったため、`.env` に何も書かなくても DRAM が自動で実発注化されていたこと（仕様どおりだが想定外）。

### 変更内容
1. **`_DEFAULT_ENABLED_SIDES` から `DRAM:SELL_SHORT` を除外**。新既定：
   `SPY:SELL_SHORT, QQQ:SELL_SHORT, QQQ:BUY, SMH:BUY, SMH:SELL_SHORT, IWM:SELL_SHORT`
   - DRAM は `MOMENTUM_SYMBOLS` に残り**シャドー観察は継続**（実発注はしない）。
2. **Wizard `[2-c] 実発注対象サイド` を新設**。選んだ組合せを `MOMENTUM_ENABLED_SIDES` に**明示書き出し**＝「Wizard で設定したものだけが取引対象」になる。
   - プリセット：①標準（SPY売り/QQQ両建て/SMH買い）②ショート広め（+SMH/IWM売り）③買いのみ（QQQ/SMH買い）。**いずれも DRAM を含まない**。
   - `.env` に `MOMENTUM_ENABLED_SIDES` を出力。コード既定より .env が優先されるため、Wizard 設定が常に取引対象になる。

### 効果
- 更新後は DRAM の実発注注文は出ない（観察のみ）。
- 実発注対象は Wizard の選択（または `.env` の `MOMENTUM_ENABLED_SIDES`）で決まり、コード既定による“想定外の自動実発注化”が起きない。

### 戻す/広げる場合
- DRAM を実発注に戻したい場合のみ `.env` で明示：`MOMENTUM_ENABLED_SIDES=...,DRAM:SELL_SHORT`。

---

## v3.9.69 (2026-06-04) — 実口座の買い専用オプション（REAL_SHORT_ENABLED）

### 背景（受講生要望）
デモのシャドーSHORT勝率が振るわない環境があり、実口座は空売り規制が無いぶん PnL 低下が懸念される。**実口座でも「買い専用（ショート停止）」を選べるオプション**が欲しい、という声に対応。

### 変更内容
- 新 env **`REAL_SHORT_ENABLED`（既定 true）** を追加。
  - `true`：実口座でも空売りを実発注（従来動作）。
  - `false`：**実口座のショートを一括停止**。`place_short` の共通ゲートで遮断するため、**ニュース駆動・モメンタムの両方**の実口座ショートが対象。SHORT 判定はシャドー記録（`_shadow_short_open`）のみに転送。
- デモ口座の挙動は従来どおり `DEMO_SHORT_ENABLED` で制御（本フラグは**実口座専用**）。
- 起動ログに状態を明示（`[実口座ショート] REAL_SHORT_ENABLED=…`）。
- Wizard 生成 `.env` に `REAL_SHORT_ENABLED` 行＋コメントを出力（既定 true・上級者向け）。

### env パターン（買い専用運用）
```
# 完全買い専用（実口座・モメンタム＋ニュースともショートなし）
REAL_SHORT_ENABLED=false
MOMENTUM_ENABLED_SIDES=QQQ:BUY,SMH:BUY   # デモでのモメンタム空売りも止める場合に併用
DEMO_SHORT_ENABLED=false                  # デモのショートも記録のみにする場合
```

### 補足
- 既定 true のため、明示設定しない限り従来動作（ショート可）。
- モメンタム空売りだけを止めるなら `MOMENTUM_ENABLED_SIDES` から `:SELL_SHORT` を外すだけでも可（env のみ）。`REAL_SHORT_ENABLED=false` は**ニュース駆動も含めた実口座の全ショート**を1スイッチで止める用途。

---

## v3.9.68 (2026-06-03) — デモ空売り・モメンタム実発注を既定 ON 化 + Wizard トグル追加

### 変更内容
1. **`MOMENTUM_LIVE_TRADING` 既定 false→true**：`.env` に記載が無ければモメンタム実発注 ON。
2. **`DEMO_SHORT_ENABLED` 既定 false→true**：`.env` に記載が無ければデモ口座でもショート実発注化（ネッティング: SELL で建て / BUY で決済）。
3. **Wizard でオンオフ可能に**：
   - `[2] 動作モード`（シャドー / 実発注）の既定選択を「実発注」に。
   - `[2-b] デモ口座のショート`（ON / OFF）トグルを新設（既定 ON）。
   - 選択結果を `.env`（`MOMENTUM_LIVE_TRADING` / `DEMO_SHORT_ENABLED`）へ書き出し。

### 既定 ON にできる安全前提（既出の修正で担保済み）
- デモのショート決済 = プレーン BUY（`_cover_trd_side`・v3.9.63）
- 新規決済前の cancel-replace で二重決済→売り過ぎ反転を防止（v3.9.64）
- flat 時のみ新規ショート（place_short のガード・v3.9.65）
- 実発注対象＝必ずリスク監視対象（v3.9.61）
- 日次/週次サーキットブレーカー配線済み（v3.9.67）

### 影響
- 更新後、`.env` に該当行が無い環境では**モメンタムが実発注 ON・デモショート ON** になる。
- 従来動作（シャドーのみ）に戻す場合は `.env` に `MOMENTUM_LIVE_TRADING=false`（および/または `DEMO_SHORT_ENABLED=false`）を明示、または Wizard で選択。

---

## v3.9.67 (2026-06-03) —【重大バグ修正】モメンタム・サーキットブレーカーが未配線だった件

### 指摘（認定サポーター・6/3）
`momentum_record_trade_result()`（モメンタム損益を日次/週次に集計し損失上限で新規発注を止める関数）が**定義のみで呼び出し箇所が存在せず**、日次/週次のサーキットブレーカーが機能していなかった。v3.9.66 で実発注対象に SHORT 側（SMH/IWM/DRAM）が増えたため、負けが続いても新規発注が止まらないリスクがあった。

### 検証結果（事実）
- `momentum_record_trade_result()` は未呼出 → `_momentum_daily_pnl` / `_momentum_weekly_pnl` が増えない → `_momentum_daily_stopped` / `_momentum_weekly_stopped` が永遠に False。
- 新規発注前のガード `_momentum_is_daily_stopped()`（momentum_shadow_loop 内）は正しく配置されていたが、上記により常に False を返していた。
- → **サーキットブレーカーは「見かけ上あるが効いていない」状態だった。** 指摘は正しい。

### 修正
決済確定の共通経路 `_send_trade_result()`（LONG/SHORT・即時/リトライの全 4 経路から呼ばれる）に、確定損益をサーキットブレーカーへ反映する処理を追加：
```python
if ai_category == "MOMENTUM":
    with _momentum_pnl_lock:
        momentum_record_trade_result(realized_pnl, _BUDGET_USD)
```
- `DATA_COLLECT` ゲートより**前**に実行 → データ収集の有無に関わらず必ず反映。
- **LONG/SHORT 両方**の確定損益が対象（同経路を通るため）。
- 競合防止に `_momentum_pnl_lock`（新規）で保護。
- `momentum_record_trade_result()` は内部で `_momentum_reset_daily_if_needed()`（04:00 ET / 月曜リセット）を呼ぶため、日跨ぎ・週跨ぎも正しく扱われる。

これにより以下が機能する：
- `MOMENTUM_DAILY_LOSS_PCT`（RiskLevel）到達 → 当日のモメンタム新規発注を停止。
- `MOMENTUM_DAILY_LOSS_BUDGET_PCT`（BUDGET比）到達 → 当日停止。
- `MOMENTUM_WEEKLY_LOSS_BUDGET_PCT`（BUDGET比）到達 → 週内停止（月曜 04:00 ET リセット）。
- 停止後は `_momentum_is_daily_stopped()` が True を返し、`momentum_shadow_loop` の新規発注を `continue` でスキップ。

### 起動ログの可視化（サポーター要望6）
起動時に以下を明示出力：
- `[モメンタム監視整合]` 実発注対象銘柄＝`_momentum_live_symbols()` と、それらが監視対象へ取込済である旨、SHORT 決済サイド（実=BUY_BACK／デモ=BUY）。
- `[モメンタム サーキットブレーカー]` 日次（RiskLevel/BUDGET比）・週次の停止額（USD換算）と現在の稼働/停止状態。

### サポーター確認項目への回答（1〜6）
1. 実発注対象＝リスク監視対象：一致（`_momentum_live_symbols()` を全監視系統に合流・v3.9.61）。起動ログで可視化。
2. IWM/SMH/DRAM の SELL_SHORT も損切り/時間切れ/週末決済が動作：`place_close_all` 経由で動作（監視対象に取込済）。
3. SHORT 決済サイド：実=BUY_BACK／デモ=BUY（`_cover_trd_side`・v3.9.63）。
4. 確定損益がサーキットブレーカーに反映：**本修正で配線**（LONG/SHORT両対応）。
5. 上限到達後の新規停止：本修正で機能。
6. 起動ログに実発注対象/監視対象/停止状態：本修正で出力。

---

## v3.9.66 (2026-06-03) — ブレークスルー3施策：勝ち筋(SHORT/ETF)へ反転、個別株テール損失を遮断

### 背景（6/2 レポート分析）
日々の成績が「地味」な構造的要因を特定：**プログラムが勝てる四象限ではなく負ける四象限で取引していた**。
- 観察データで **SHORT が BUY を一貫して圧倒**（6/2: SHORT +2.25%/WR87.3% vs BUY +0.55%/WR50.6%。SMH-SHORT +3.34%・QQQ-SHORT +2.97%(WR100%)・IWM-SHORT +1.56%(WR100%)・DRAM-SHORT +0.73%(WR98%)）。なのにデモは空売り不可で勝ち筋を実発注化できていなかった。
- **個別株のテール損失が好成績を毎回相殺**（6/2 NVDA -$240.57 単独で日次 -$155 を超過、5/27 BAC -$549、5/29 DELL -$153）。ETF(QQQ-LONG +$25.89)は機能。**6/2 は個別株を除けば +$139 だった**。

### 施策A — 強い SHORT 側を実発注対象に追加
`_DEFAULT_ENABLED_SIDES` に **`SMH:SELL_SHORT,IWM:SELL_SHORT,DRAM:SELL_SHORT`** を追加（QQQ/SPY:SELL_SHORT は既存）。v3.9.65 の `DEMO_SHORT_ENABLED=true` と組み合わせ、デモ生も WR87〜100% のショートを実発注化。SMH は TRIGGER_TICKERS、IWM/DRAM は `_momentum_live_symbols()` 経由で全監視系統に自動取込（v3.9.61 原則）。
※ デモでは `DEMO_SHORT_ENABLED=false`（既定）の間はシャドーのまま。実発注化は同フラグ有効時のみ。

### 施策B' — 個別株テール損失の遮断（2段）
1. **`STOCK_MAX_PCT` 既定を 0（上限なし）→ 10%** に変更。1 銘柄の建玉を BUDGET の 10% に制限し、悪材料の単独大損を抑制（env 明示で上書き可、0 で従来どおり）。
2. **`STOCK_MAX_LOSS_USD`（新規・既定 0=無効）**：個別株の含み損が `-STOCK_MAX_LOSS_USD` を下回ったら % 損切りラインに関係なく即決済。ギャップ/スリッページで % 損切りが間に合わない大損の安全網。BUDGET 依存のため講師判断で .env 設定推奨。

### 施策C — 勝ち筋へ資金集中（サイド別配分）
モメンタム発注サイズにサイド係数を導入：**`MOMENTUM_BUY_SIZE_MULT`（既定 0.60）/ `MOMENTUM_SHORT_SIZE_MULT`（既定 1.00）**。WR50% の BUY を 60% に圧縮し、相対的に WR87% の SHORT へ資金を寄せる。`effective_size_pct = base × 銘柄係数 × 連敗係数 × 強度係数 × サイド係数`。

### 期待効果
「BUY×個別株（負け筋）」から「SHORT×ETF（勝ち筋）」へ重心を移し、個別株テール損失を断つことで、地味な±数十ドルを構造的にプラス方向へ。6/2 の counterfactual では個別株除外で -$155→+$139。

### 段階導入の注意
- 施策A の実発注化はデモでは `DEMO_SHORT_ENABLED=true` が前提（今夜のテスター稼働で検証）。
- 施策B' の `STOCK_MAX_PCT=10%` は全環境に効く既定変更（個別株サイズが縮小）。
- 施策C の BUY 圧縮も全環境に効く既定変更（モメンタム BUY のサイズが 60% に）。
- いずれも env で従来値へ戻せる。

---

## v3.9.65 (2026-06-02) — デモ口座のネッティング空売り対応（DEMO_SHORT_ENABLED）

### 背景
全受講生がデモ口座で、デモは信用区分（`SELL_SHORT`/`BUY_BACK`）を受け付けないため、これまで `place_short` はデモのショートをスキップ→シャドー記録のみにしていた。結果、デモ生が実発注で取れるのは BUY 側だけ（しかも観察上 BUY は不振）で、**勝ち筋のショート側を一切実発注検証できなかった**。

5/27 受講生ログで判明したとおり、**moomoo デモはネッティング口座として「プレーン `SELL` で保有を超えて売る = ショート新規」「プレーン `BUY` で決済」ができる**（`SELL_SHORT` だけが拒否される）。これを正式対応する。

### 変更内容
- 新 env `DEMO_SHORT_ENABLED`（既定 **false**）を追加。
  - `true`: デモで `SELL_SHORT` シグナル発生時、`place_short` が**プレーン `TrdSide.SELL`** で新規ショートを発注（`jp_acc_type` なし）。
  - `false`: 従来どおりスキップ→シャドー SHORT 記録のみ。
- `place_short` の発注サイドを環境別に切替：実口座=`SELL_SHORT`+`JP_TOKUTEI_SHORT` / デモ=プレーン `SELL`。
- 既存の安全ガードを活用：**ロング残存時は先に決済**（`SELL` がロング決済に化けるのを防止）、**既存ショート保有時はスキップ**（ピラミッディング防止）。到達時は必ず flat のため `SELL`=ショート新規が保証される。
- 決済は v3.9.63 の `_cover_trd_side(SIMULATE)=BUY` で実行（追加実装不要）。建玉の二重決済は v3.9.64（cancel-replace）で防止済み。
- 起動時に `DEMO_SHORT_ENABLED` の状態を明示ログ。
- Wizard v1.12: 生成 `.env` に `DEMO_SHORT_ENABLED` とコメントを出力（上級者向け・既定 false）。

### 期待効果
- デモ生も SMH/QQQ/SPY 等のショート側を実発注化でき、**信用口座に近い成績検証**が可能に（観察ベースで SHORT 勝率約58%）。
- v3.9.63 B-1（実発注が QQQ-BUY のみに偏る構造問題）の本質的解決につながる。

### 段階導入（重要）
- **既定 false**。まずテスター環境で `DEMO_SHORT_ENABLED=true` を設定し、ショートの建玉・決済・損切り・週末決済・パニックが正しく回るかを検証してから全体展開する想定。
- ネッティング口座ではロング決済 SELL と新規ショート SELL が同一サイドのため、ポジション判定の取り違えに注意（本実装は flat 限定で新規ショートを建てることで曖昧さを排除）。

---

## v3.9.64 (2026-06-02) —【重大バグ修正】二重決済レースによるデモ・ショート化を防止

### 症状（受講生ログ 5/25-31・デモ口座 v3.9.42 で実害）

デモ口座（API 空売り非対応・place_short はシャドー転送）にもかかわらず、**QQQ が −8 株のショートに反転**し、以後 `BUY_BACK` 拒否で決済不能のまま残留した。

### 根本原因（ログで特定・5/27 07:27-28）

保有していた **QQQ ロング +8株（avg $729.13）** に対し、**2 つの決済経路が同時に SELL を発注**した：

```
07:27:46  ⏰時間切れ決済 SELL 8 @ $722.02 → orderId=299110（時間外の指値・SUBMITTEDのまま未約定）
07:27:46  内部追跡を先行クリア（fill前に0株扱い）
07:27:46  デモ日次決済(15:45 ET)が同時発火 → QQQ に決済SELLを連投
07:27:47〜 日次決済SELLは「Not enough positions」連発
07:28:16  日次決済SELL 8 @ $722.06 → orderId=299111 発注成功
07:28:27  299111 FILLED_ALL @ $729.40 → ロング +8→0（実現+$2.17）
 (直後)   まだ生きていた 299110 も約定 → 0株に対する売り → -8 ショート新規
          ("A SELL order without holdings will open a short position")
07:28:34  sync: QQQ qty=-8 avg $729.40 を検出
```

`place_short`（SELL_SHORT）は一切関与せず（デモスキップ5回・SHORT発注0件を確認）。**ロング決済の SELL が2本約定して売り過ぎ**、ネッティング口座のデモで −8 ショートに反転したもの。

### 原因の構造
1. **時間切れ決済とデモ日次決済が同一建玉に同時に SELL**（決済経路間の排他制御なし）。
2. **約定前に内部ポジションを 0 クリア**したため 2 本目の発注を抑止できなかった。
3. **時間外の指値 SELL が即約定せず**、その間に 2 本目が先に約定 → 遅れて 1 本目も約定 → 売り過ぎ。

### 修正

新ヘルパ `_cancel_pending_closes_for_symbol(symbol, trd_env, reason)` を追加し、`place_close_all` の建玉取得前に呼ぶ。**新たな決済を出す前に、同銘柄の未約定決済注文をすべてキャンセル（cancel-replace）してから再同期 → 決済注文を常に 1 本だけに保つ**。これにより、時間切れ決済とデモ日次決済が重なっても 2 本目が出る前に 1 本目が取り消され、売り過ぎ（ショート反転）が発生しない。

### v3.9.63 との関係
- v3.9.63 A-1（デモ決済を `BUY` に分岐）で「できてしまったショートを自動決済できる」状態にはなっていた（残留の解消）。
- 本 v3.9.64 は「**ショートが作られること自体**」を断つ恒久対策。両者で「発生防止＋万一発生時の自動解消」の二重化となる。

### 補足（moomoo デモのショート挙動・判明事項）
- `SELL_SHORT`（信用の空売り新規）はデモでは拒否される（従来の理解どおり）。
- 一方、**プレーン `SELL` で保有を超えて売るとデモはショートを開く**／**プレーン `BUY` で閉じる**（ネッティング口座挙動）。
- → デモを「ネッティング(BUY/SELL)」でショート運用すれば信用口座に近い検証が可能。ただしロング決済 SELL とショート新規 SELL が同一サイドで曖昧になるため、ポジション管理（is_short/position_ids/監視・パニック・週末決済）のネッティング対応が必要。**別バージョン（v3.9.65 予定）で設計する**。

---

## v3.9.63 (2026-06-01) — デモ決済バグ修正 + 構造的パフォーマンス是正 + エラー多発の解消

6/1 の3レポート（MOMENTUMシャドー日次・実発注日次・エラーログ24版＝直近1週で 4,867 件のエラー）の突き合わせ分析に基づく、エラー解消（A）とパフォーマンス（B）の一括改善。

### A. エラー解消

**A-1【重大】デモ口座のショート決済を `BUY` に分岐（`BUY_BACK` 拒否の解消）**
- 症状: デモ口座でショート決済に `TrdSide.BUY_BACK` を送ると moomoo が「Order side must be BUY or SELL. A BUY order when short will close the short position.」を返して**全失敗→ショート残留→entry_time 累積**。5/25-6/1 のログで **QQQ 56 回・AMZN 20 回**等を確認。
- 修正: 新ヘルパ `_cover_trd_side(trd_env)` を追加し、**実口座=`BUY_BACK` / デモ=`BUY`** に分岐。決済3経路（`place_close_all`・損切り再発注 watchdog・損切りチェイス）と `_close_one_position_id` の整合検証を環境別に更新。
- デモはネッティング口座のためプレーン `BUY` で建玉が相殺される。実口座(FUTUJP 信用)は moomoo 仕様どおり `BUY_BACK` を維持。

**A-2 イベントループ例外の防御（`_threadsafe_future` / `_threadsafe_discord` 強化）**
- 旧版で「`[ORDER] BUY 例外: There is no current event loop`」「`[セッション監視] no current event loop`」が発生（worker thread からの asyncio 直叩き。v3.9.50/52 で本体は解消済み）。
- 念押しとして両ヘルパーを **try/except で完全防御**し、ループ未起動/終了間際でも発注経路へ例外を伝播させない + 投入できなかった coroutine を明示 close。

**A-3 Anthropic クレジット切れ時の API バックオフ**
- 旧版で「credit balance too low」が無駄に連発（197 件）。現行コードは graceful だが**毎ニュースで API を叩いて**いた。
- `_anthropic_credit_probe_due()` を追加し、検知後は **30 分ごとに 1 回だけ復旧プローブ**を許可（それ以外は API を叩かず即中立）。プローブ成功時は即バックオフ解除で高速復帰。キー未設定は従来どおり起動時 `sys.exit(1)` で明示ブロック。

**A-4 Alpaca 認証失敗ログの間引き + 接続数上限(406)の専用案内**
- 「同一キーを複数 PC で同時使用」で認証失敗が 757 件。指数バックオフは既存だが**毎リトライ WARNING を出力**していた。
- code 406 / connection limit を専用判定し、**初回・2回目以降は 5 分に 1 回**へ間引き。406 時は「1 キー 1 PC に分けてください」と原因を明示。

**A-5 sync_positions ログ間引き + 「Not enough positions」の解決済み扱い**
- 「Network interruption」のパターン試行ログ（555 件）を **WARNING→DEBUG** に降格、全パターン失敗の最終警告は **5 分に 1 回**へ間引き。
- 決済時の「Not enough positions」（QQQ 20・SPY 12 件）は**既決済（二重決済レース）**とみなし、内部 ret `_RET_ALREADY_CLOSED` で**失敗扱いせず state をクリア**（無限リトライ・ERROR スパムを停止）。

### B. パフォーマンス

**B-1【最重要・構造問題】`SMH:BUY` を実発注対象に追加**
- 全受講生がデモ口座 → デモは API 空売り不可（`place_short` がシャドー転送）→ `SPY/QQQ:SELL_SHORT` を有効化しても**実際に約定する実発注は `QQQ:BUY` のみ**で、QQQ:BUY は観察で最悪サイド（6/1: WR22.8%/-1.72%、実発注 WR14.3%）。
- 一方デモで約定可能かつ好調な **SMH-BUY（6/1: +3.30%/WR80.2%）はシャドー止まり**だった。`_DEFAULT_ENABLED_SIDES` に `SMH:BUY` を追加（SMH は TRIGGER_TICKERS 監視対象のため v3.9.61 原則に適合・orphan 懸念なし）。QQQ:BUY は実発注継続だが要観察。

**B-2 モメンタム建玉の損切りに `MOMENTUM_STOP_LOSS_PCT` を実適用（Wizard で選択可）**
- 従来モメンタム建玉も共通 `MAX_LOSS_PCT`(0.30%) で損切りされ、6/1 実発注でも QQQ 0.20-0.31% で被弾が頻発、60 分タイムアウト前に刈られていた（寄り高値掴みの早期損切り連発）。
- `risk_monitor_loop` の強制損切りで `entry_ai_category=="MOMENTUM"` のとき **`MOMENTUM_STOP_LOSS_PCT`/100（標準 0.50%）**を適用。Wizard v1.11 のモメンタム STEP に [5] 損切りラインを追加。

**B-3 寄り高値掴みガード（BUY 実発注のみ）**
- 6/1 実発注の高値掴み（QQQ $744-745 でマイナス大）対策。直近 `MOMENTUM_HIGH_CHASE_LOOKBACK_MIN`(30分) の高値から `MOMENTUM_HIGH_CHASE_GUARD_PCT`(標準 0.05%) 以内の BUY 実発注を見送る（シャドー観察は継続）。SHORT には適用しない。保守的デフォルトで開始しシャドーデータで調整。

**B-4 固定時間帯フィルタは廃案で最終確定**
- v3.9.25 の「ET 11 時以降のみ発火」フィルタは 7 日中 5 日で逆効果（6/1 も「フィルタ無 +1.20% → 有 -0.30%」、寄り付き ET 9-10 時の黄金時間帯を逃す）。v3.9.41 で既に撤去済み・`MOMENTUM_RTH_*` は無視。本バージョンで**恒久廃案を確定**（再導入しない）。

### 留意点
- A-1 の決済バグは、最新版へ更新すればデモで新規にショートが建たない（`place_short` がシャドー転送）ため再発しないが、**既存・手動・旧版残りのデモショートも本修正で自動決済できる**ようになった。
- B-1/B-3 のしきい値はシャドーデータでの再キャリブレーション前提の暫定値。

---

## v3.9.62 (2026-05-30) — モメンタムシャドーに DRAM（Roundhill Memory ETF）を追加

### 概要

メモリ半導体テーマの高ボラ ETF **DRAM**（Roundhill Memory ETF・2026-04-02 上場）をモメンタムの**シャドー観察専用銘柄**として追加。`MOMENTUM_ENABLED_SIDES` には含めないため**実発注対象外**（観察ログのみ）。SPY/QQQ/SMH/IWM に続く 5 銘柄目。

### 決定事項と根拠

| 項目 | 決定 | 根拠 |
|---|---|---|
| 発火閾値（5分/15分%）| **SPY プリセット × 3.0** | DRAM のボラは ≈ SPY の約3倍（前回評価）。標準 Lv3 = SPY 0.10/0.22 → DRAM **0.30/0.66**。新規上場で実績が浅いため IWM 同様 1-2 週間のシャドーデータで再キャリブレーション予定 |
| 配布範囲 | **デフォルト追加**（全受講生が観察）| `MOMENTUM_SYMBOLS` 既定を `QQQ,SPY,SMH,IWM` → `QQQ,SPY,SMH,IWM,DRAM`。再キャリブレーション用データを最速で蓄積 |
| 強シグナル上限ガード | **DRAM 別枠 ≈2.4%**（既定 0.80% × 3）| 3倍ボラのため共通 0.80% だと通常のトレンドが頻繁に「ノイズ(発注なし)」扱いで弾かれる。`MOMENTUM_DRAM_MAX_SIGNAL_PCT`（既定 2.40）で制御。env 上書き可 |
| 実発注 | **しない（シャドー専用）**| `MOMENTUM_ENABLED_SIDES` に含めないため `_momentum_side_allowed("DRAM", …)=False`。v3.9.61 の「実発注銘柄＝必ず監視対象」原則とも整合 |

### 変更点

- `_MOMENTUM_LEVEL_PRESETS`（Lv1-5）に DRAM 行を追加（SPY×3 スケール: 0.45/0.99・0.39/0.84・0.30/0.66・0.24/0.54・0.21/0.45）
- `MOMENTUM_SYMBOLS` 既定に `DRAM` を追加 → `_INDEX_PRICE_HISTORY` への登録・価格スナップショット記録・`get_index_pct_change` での騰落率算出はすべて自動連動
- `MOMENTUM_DRAM_MAX_SIGNAL_PCT`（既定 `MOMENTUM_MAX_SIGNAL_PCT × 3` = 2.40%）+ `_momentum_max_signal_for(symbol)` を新設。`_momentum_strength_multiplier(pct_15, symbol)` が銘柄別上限を参照
- `MOMENTUM_SIZE_MULTIPLIER` に `DRAM=1.00`（env `MOMENTUM_SIZE_MULTIPLIER_DRAM` で上書き可）
- 起動バナーに DRAM 専用の強シグナル上限を併記

### 留意点（運用後に確認）

- DRAM は新規上場のため、**moomoo OpenD が quote / K線を返せるか未確認**。返せない場合はシャドーログに「データ不足」と出るだけで害はないが、起動後にログで取得可否を確認する
- シグナル強度の山型バンド（0.30/0.50/0.80%）は SPY/QQQ スケール由来のまま据え置き。DRAM 専用バンドの最適化は今後のシャドーデータで検討（今回は上限ガードのみ別枠化）

---

## v3.9.61 (2026-05-30) —【重大バグ修正】IWM 実発注の監視漏れ（orphan position）を解消

### 症状（認定サポーター報告・実害あり）

5/29 23:25 (JST) に **IWM SELL_SHORT 27株（約 $7,800）が実発注・約定**したが、その後の監視がすべて漏れていた:

```
5/29 23:25:46  IWM SELL_SHORT 27株 約定 @ $288.53  (_tracked_position_cost=$7,752)
5/29 23:30〜    [状況] ポジションなし  ← QQQ/SMH/SPY のみ表示・IWM 不在
5/30 04:45:06  [週末決済] 決済対象ポジションなし → スキップ  ← IWM 検出されず
5/30 04:55:07  [移行前全決済] 決済対象ポジションなし → スキップ
```

→ IWM ショートが **損切り・トレール・タイムアウト・パニックセル・週末決済すべての監視から漏れ**、週末を越えて放置された。

### 根本原因

v3.9.54 で `IWM:SELL_SHORT` を `MOMENTUM_ENABLED_SIDES`（実発注対象）に追加したが、IWM は `TRIGGER_TICKERS`（標準 SPY,QQQ,SMH）にも `EXECUTION_MAP` にも含まれないため、以下の監視系統すべてから除外されていた:

| 機能 | 監視対象の構築式 | IWM |
|---|---|---|
| `risk_monitor_loop`（損切り/トレール/タイムアウト）| `EXECUTION_MAP ∪ STOCK ∪ EARNINGS` | ❌ |
| `_close_all`（週末/日次決済）| 同上 | ❌ |
| `panic_sell_all`（急落決済）| `EXECUTION_MAP` のみ | ❌ |
| `sync_positions`（同期）| `ALL_TICKERS` | ❌ |
| `MOMENTUM_ENABLED_SIDES`（実発注）| **IWM:SELL_SHORT を含む** | ✅ 発注のみ可 |

「発注はできるが監視されない」構造的欠陥。

### 変更内容

**① IWM を実発注対象からシャドー専用に戻す**

```
旧 (v3.9.54): _DEFAULT_ENABLED_SIDES = "SPY:SELL_SHORT,QQQ:SELL_SHORT,QQQ:BUY,IWM:SELL_SHORT"
新 (v3.9.61): _DEFAULT_ENABLED_SIDES = "SPY:SELL_SHORT,QQQ:SELL_SHORT,QQQ:BUY"
```

IWM は `MOMENTUM_SYMBOLS` でシャドー観察を継続。実発注は監視対象である SPY/QQQ のみに限定。

**② システム的修正: 実発注銘柄を必ず監視対象に含める（再発防止）**

新規ヘルパ `_momentum_live_symbols()` を追加（MOMENTUM_LIVE_TRADING=true 時に MOMENTUM_ENABLED_SIDES から実発注銘柄を抽出）。これを以下すべてに合流:

- `ALL_TICKERS`（sync_positions / _tracked_position_cost の母集合）
- `risk_monitor_loop` の `exec_syms`
- `_close_all`（週末/日次決済）の `exec_syms`
- `panic_sell_all` の `exec_syms`

→ 万一 env で監視外銘柄を実発注対象にしても、**自動的に全監視系統へ取り込まれる**。

**③ 起動時の整合性検証**

起動バナーで「実発注対象 ⊆ 監視対象」を検証:
- 整合 → `✅ [整合性] モメンタム実発注対象 [...] はすべてリスク監視・週末決済の対象に含まれています`
- 不整合（理論上は②で発生しないが最終防衛線）→ `⚠️ [整合性警告] ... 損切り/週末決済が効かない可能性`
- 方針（質問で確認）: **警告を出して起動継続**（受講生が起動できなくなるのを避ける）

**④ Wizard 表示の整合性**

`setup_wizard.py` のモメンタム説明は「SPY/QQQ=実発注、SMH/IWM=シャドー観察のみ・実発注対象外」と既に明記されており、v3.9.61 で IWM をシャドーに戻したことで表示とコードが再び一致した（Wizard 側の修正は不要）。

### ⚠️ 受講生・サポーターへの緊急連絡事項

**v3.9.54 〜 v3.9.60 で MOMENTUM_LIVE_TRADING=true かつ IWM が実発注された環境では、監視されない IWM ショートポジションが残っている可能性があります。**

1. moomoo アプリでポジション一覧を確認
2. IWM のショートポジションがあれば**手動で決済**
3. v3.9.61 に更新（以降は IWM 実発注されない + 万一の実発注も監視される）

### 検証

- 構文チェック OK
- `_momentum_live_symbols()` ロジックテスト: デフォルト(SPY/QQQ)・旧IWM含む・シャドーのみ の 3 ケース確認
- 旧 v3.9.54 設定が env に残っていても IWM が監視対象に自動取込されることを確認

---

## v3.9.60 (2026-05-29) — プロンプト v1.13.0（政治批判中立）+ Anthropic 529 リトライ + AI 散文応答からの score 抽出

### 背景（3 つの独立した改修）

**① プロンプト v1.13.0**: 5/28 で Trump 大統領の Walmart 関税批判記事が Alpaca(AMZN) で AMZN-LONG 判定 → 2 件 -$94.77 の損失。政治家・大統領の批判系発言は値動きの方向が読みにくいため絶対中立化が必要。

**② Anthropic 529 リトライ**: ユーザーログ 5/27 21:57 で `Anthropic API エラー: Error code: 529 - Overloaded` を観測。1 回失敗で即「中立扱い」となり発注機会を逃した。一時的なサーバー過負荷は数秒待てば復旧することが多い。

**③ AI 散文応答 score 抽出**: 5/28 03:48 で AI が「複数のニュース...本プロンプトの出力規則に従い、最も重要な1件に絞って判定します。...各件 score = 0」のような散文応答を返し、JSON 抽出が完全に失敗 → confidence=0.00 中立扱い。

### 変更内容

**① プロンプト v1.13.0: 政治家・大統領の批判系は中立**

`UNIFIED_SYSTEM_PROMPT_TEMPLATE` に絶対ルールを追加（`_PROMPTS_VERSION` 1.12.0 → 1.13.0）:

- 政治家・大統領が特定企業を批判（criticizes / bashes / slams / warns / complains 等）→ 必ず `score = 0`
- 関税の検討・批判段階（"considers new tariffs" / "criticizes tariff handling"）→ `score = 0`
- 例外: 確定発動（executive order / sign / impose）は通常判定可

`HEADLINE_NOISE_PATTERNS` に正規表現 2 種追加:
- `political-criticism-company`: 大統領/政治家による特定企業批判
- `political-tariff-consideration`: 関税検討・批判段階

**② Anthropic API リトライ (`_anthropic_create_with_retry`)**

新規ヘルパ関数で `client.messages.create` をラップ:

```python
def _anthropic_create_with_retry(client, max_retries=3, **kwargs):
    """529/503/overloaded/timeout で指数バックオフ再試行 (1s → 2s → 4s)"""
    for attempt in range(max_retries):
        try:
            return client.messages.create(**kwargs)
        except Exception as e:
            err_str = str(e).lower()
            is_retryable = any(kw in err_str
                for kw in ("529", "503", "overloaded", "unavailable",
                           "rate_limit", "timeout"))
            if not is_retryable or attempt >= max_retries - 1:
                raise
            time.sleep(2 ** attempt)
            log.warning(f"[Anthropic API] 再試行 {attempt+1}/{max_retries} ...")
```

既存の `client.messages.create` を呼ぶ 2 箇所（semaphore 直列化版・フォールバック版）両方に適用。

**③ AI 散文応答からの score 抽出（フォールバック ⑤）**

既存の JSON パース戦略 ①〜④ が全て失敗した場合の新フォールバック:

```python
# ⑤ 散文中の "score = N" / "score: N" / '"score": N' を抽出
_score_matches = re.findall(
    r'\bscore["\s\']*[:=][\s\']*(-?[01])\b', cleaned, re.IGNORECASE
)
if _score_matches:
    if all(s == 0 for s in _score_matches):
        # 全件中立 → 確定的に score=0 (WARNING を出さず INFO で通知)
        result = {"score": 0, "confidence": 0.0, ...,
                  "reason": f"AI散文応答 (全 {len(_score_matches)} 件 score=0)"}
    elif _nonzero is not None:
        # 散文中に score=±1 検出 → confidence=0.30 (発注閾値未達) で抑制
        result = {"score": _nonzero, "confidence": 0.30, ...}
```

### 効果

- **政治批判記事の誤判定防止**: AMZN/AAPL/TSLA 等の大型株が政治発言で誤発注される事象を解消（プロンプト + 正規表現の二重防御）
- **API 529 で機会損失ゼロ**: サーバー過負荷時も 3 回まで自動再試行・合計最大 7 秒の遅延で復旧
- **散文応答の中立判定**: WARNING ログを抑え、AI の意図（全件中立）を正しく汲み取る
- **散文中の非0スコア**: confidence 0.30 に抑えて発注閾値未達としつつ、判定情報は保持

### 検証

- 構文チェック OK
- プロンプト assertion OK (`v1.13.0` を含む)
- 既存パース戦略 ①〜④ は変更なし（後方互換）
- 正規表現は score=±1, ±0 のみマッチ（false positive 抑制）

---

## v3.9.59 (2026-05-29) — モメンタム + トレール ON 時はタイムアウトをスキップ（利益取り逃がし防止）

### 背景

5/29 00:16 のログで以下を確認:

```
00:16:32 【QQQ】 [ポジション] $733.78  PnL=+$25.81(+0.27%)  trail=ON  peak=$734.19 下落=0.06%
00:16:48 【QQQ】 ⏰ 時間切れ決済！ エントリから 60.2分経過（上限: 60分）
00:16:56 【QQQ】 LONG決済益 $+28.52 (+0.30%)
```

トレールが **ON**（peak 到達済み・利益確定モード）にもかかわらず、60 分タイムアウトが先に発火して決済された。トレールに任せれば:
- 価格が更に上昇 → peak 更新 → 利益拡大
- 価格が peak から 0.15% 戻し → 自動利確 (より大きな利益確定)

のいずれかで、タイムアウトより期待値の高い決済になる。

### 変更内容

`risk_monitor_loop` のタイムアウト判定に bypass を追加:

```python
if elapsed >= _t_timeout:
    # ★ v3.9.59: モメンタム + トレール ON 中はタイムアウト見送り
    if ts.entry_ai_category == "MOMENTUM" and ts.trail_active:
        # 1分に1回だけログ (連発防止)
        log.info(
            f"{tag} ⏳ モメンタム + トレール ON → タイムアウト見送り "
            f"({elapsed:.1f}分経過・peak=${ts.peak_price:.2f}・トレールで利確待ち)"
        )
        continue   # 次サイクルへ
    # ... 既存タイムアウト処理 ...
```

### 適用条件

- `entry_ai_category == "MOMENTUM"`（モメンタム発注のポジション）
- `trail_active == True`（既にトレール発動済み）

両方を満たす場合のみタイムアウトをスキップ。

### 適用されないケース

- ニュース駆動トレード（`entry_ai_category != "MOMENTUM"`）→ 既存通り 10 分タイムアウト
- 決算銘柄 → 既存通り `EARNINGS_TIMEOUT_EXIT_MINUTES`
- モメンタムでも **trail_active=False**（未発動）→ 通常通りタイムアウト 60 分で決済
  - peak まで届かなかった場合は時間切れで切り上げる

### 5/29 ケースの再現

旧挙動 (v3.9.58 以前):
```
00:15:48 trail=ON peak=$734.19 (entry 60 分経過)
00:16:48 ⏰ 時間切れ決済 → +$28.52 確定
```

新挙動 (v3.9.59):
```
00:15:48 trail=ON peak=$734.19 (entry 60 分経過)
00:16:48 ⏳ モメンタム + トレール ON → タイムアウト見送り (60.2分経過・peak=$734.19)
00:25:30 (peak が $735 に伸びる → 利益拡大)
00:32:15 price = $733.90 (peak から 0.15% 戻し) → トレール利確 +$40 程度
```

### リスクと安全装置

- **理論的リスク**: 価格が peak から 0.15% 戻し以内で永遠に振動 → 位置が長期保有
- **実際の影響**: TRAIL_DROP_PCT=0.0015 (0.15%) は非常にタイト → 数十分以内に必ず発火
- **安全装置**: MOMENTUM 日次/週次サーキット (BUDGET 比 3%/5%) と SPY 急変動ガードは引き続き有効

### 互換性

- 既存 .env への変更不要
- bypass 機能は無効化 env なし（理論的に無害なため）
- 必要ならコード側で `ts.trail_active` チェックを外せば旧挙動に戻せる

---

## v3.9.58 (2026-05-28) — モメンタムにセッション切替後 5 分クールダウン（持続性疑問の急変動回避）

### 背景

OVN / WEEKEND / 休日から PREMARKET 等の稼働セッションへ切り替わる瞬間は、**急変動が起きやすいが持続性に乏しい**ことが受講生から指摘された。

5/27 シャドー観察データでも:
- ET 9:00 (寄り) 125件・-7.04%・WR 28.8%（壊滅）
- ET 10:00 218件・-2.67%・WR 17.4%（寄り後継続して逆転）
- ET 11:00 51件・+1.78%・WR 96.1%（落ち着いてから好調）

セッション開始直後はモメンタムロジックの前提（「方向が継続する」）が崩れやすく、シグナルが立っても 60 分後には逆行している確率が高い。

### 変更内容

**新規 env `MOMENTUM_SESSION_CHANGE_COOLDOWN_SEC`（デフォルト 300 秒 = 5 分）**

```env
MOMENTUM_SESSION_CHANGE_COOLDOWN_SEC=300    # 0 で無効化
```

`momentum_shadow_loop` 内に判定を追加:
- `_last_session_change_at`（v3.9.56 で導入）からの経過秒を測定
- N 秒未満なら `continue` でループスキップ
- 1 分ごとに 1 回だけログ出力（連発防止）

```
[モメンタム] ⏳ セッション切替クールダウン中: 残り 240秒 (持続性確認のため発火見送り)
```

### 既存ガードとの併用構造

```
loop iteration:
  ① 市場停止セッション中 → await (待機)
  ② デイ/ウィークリーストップロス → continue
  ③ セッション切替クールダウン中 (5分) → continue   ← v3.9.58 新規
  ④ 急変動ガード (SPY 5m |Δ| ≥ vola_pct) → continue
  ⑤ シンボル毎の判定・発火
```

### 互換性・調整

- 0 設定で無効化（旧挙動）
- 短くしたい場合: `MOMENTUM_SESSION_CHANGE_COOLDOWN_SEC=120`（2 分）等
- 長くしたい場合: `=600`（10 分）等
- 既存の `MOMENTUM_COOLDOWN_MIN`（同方向シグナルのクールダウン）とは独立

### 対象セッション切替

- ✅ OVERNIGHT → PREMARKET（最も発動頻度が高い）
- ✅ WEEKEND → 月曜 PREMARKET
- ✅ HOLIDAY → 翌営業日
- ✅ デモ日次決済後 → 翌日 PREMARKET
- ✅ disabled セッション → 稼働セッション
- ⏭ PREMARKET → RTH（連続セッション間は作動しない）
- ⏭ RTH → AFTERHOURS（同上）

連続セッション間は同一日内の流れなので、シグナルの持続性は通常通り。

---

## v3.9.57 (2026-05-28) — K線バックフィル範囲を 80分→20時間 に拡張 + セッション切替時にも再bootstrap

### 背景

5/28 17:08 のログで以下を確認:

```
17:00:29 [セッション] プリマーケット開始（オーバーナイト終了・デモ）
17:08:55 ❓ QQQ │ 5分 +0.03% │ 15分 データ不足 │ 60分 データ不足
17:08:55 ❓ SPY │ 5分 +0.01% │ 15分 データ不足 │ 60分 データ不足
```

JST 17:00 = ET 04:00（PREMARKET 開始）に起動した場合、`_backfill_index_price_history` が要求する「過去 80 分」は **ET 02:40〜04:00 の OVERNIGHT 期間** であり、K 線データが存在しない。結果として bootstrap が 0 件取得となり、起動後 60 分以内のすべての時間で「15分/60分 データ不足」が出続け、モメンタム・トレンドフィルタが無効化されていた。

### 変更内容

**① バックフィル取得範囲を 80 分 → 20 時間に拡張**

```python
旧: start = now_et - timedelta(minutes=80)    # OVN を遡れず空振り
新: start = now_et - timedelta(hours=20)      # 前営業日 AFTERHOURS まで到達
```

合わせて `max_count` を 200 → 1000 に増やし、20 時間分の 1 分足を取得可能にした。

OpenD は extended_time=True で PREMARKET (04:00-09:30) / RTH (09:30-16:00) / AFTERHOURS (16:00-20:00) を返すが OVERNIGHT (20:00-04:00) は返さない。20 時間遡れば必ず前営業日の AFTERHOURS をカバーし、`_INDEX_PRICE_HISTORY_MAX_SEC = 4500` (75 分) のカットオフで結局直近 75 分のみが残るため、メモリ使用量は実質変わらない。

**② セッション切替時にも再 bootstrap を実行**

`market_schedule_loop` で OVN/WEEKEND/休日/disabled から稼働セッションへ復帰する瞬間に `_backfill_index_price_history()` を `_threadsafe_future` 経由で再実行。

これにより:
- 起動時に bootstrap が空振り（OVN 中の起動等）→ 失敗してもセッション切替時に自動再試行
- PREMARKET 開始時点で前営業日 AFTERHOURS データが新たに使えるため、15分/60分 判定を即座に有効化

### 期待される挙動

```
17:00:29 [セッション] プリマーケット開始（オーバーナイト終了・デモ）
17:00:29 [起動時バックフィル] 開始: ET 04:00 ⤴ 過去 20時間
17:00:31 [起動時バックフィル] QQQ: 350 件追加 / 履歴 75 件
17:00:31 [起動時バックフィル] SPY: 348 件追加 / 履歴 75 件
17:00:31 [起動時バックフィル] SMH: 345 件追加 / 履歴 75 件
17:00:31 [起動時バックフィル] IWM: 340 件追加 / 履歴 75 件
17:08:55 ✅ QQQ │ 5分 +0.03% │ 15分 +0.12% │ 60分 -0.30%   ← 即座に判定可能
```

### 副次的な改善

- モメンタムシグナル発火条件（5min + 15min の同時閾値超え）がセッション開始直後から判定可能に
- 急変動ガード (`MOMENTUM_VOLATILITY_GUARD_PCT`) も即座に有効化
- 個別株トレンドフィルタ（構成 D / D'）も同様にデータ即時利用可能

### 互換性

- OpenD への追加負荷は 1 起動あたり 1〜2 秒程度（K線数本×銘柄数）
- `max_count=1000` は moomoo OpenD の上限 1000 と一致（拡張余地なし）
- 取得失敗時は静かにスキップ（既存と同じ挙動）

---

## v3.9.56 (2026-05-28) — セッション切替直後の蓄積ニュース発注を防止（クワイエットウィンドウ 60 秒）

### 背景

5/28 17:00 (JST) = ET 04:00 PREMARKET 開始タイミングで、**わずか 6 秒**で「セッション切替 → 蓄積ニュース処理 → AI 判定 → SHORT 発注」が発生:

```
17:00:29 [セッション] PREMARKET 開始（オーバーナイト終了）
17:00:29 [ニュース] 新着16件 (Yahoo Finance)        ← OVN 中の蓄積分
17:00:31 [ニュース] 新着19件 (Finnhub: Iran 関連)    ← OVN 中の蓄積分
17:00:33 【GOOGL】 個別株ニュース (Alpaca WebSocket) ← OVN 中の蓄積分
17:00:34 [個別株AI判定] 📉 ネガティブ confidence=0.78 → SHORT
17:00:35 ▼▼ SHORT GOOGL 25株 @ $385.46
```

「Google Engineer Insider Trading」記事は実際は**数時間前**から流れていた可能性が高く、市場ではすでに織込み済。発注後の PnL は −$2 〜 −$17 で推移し、ニュースに反応した動きではなかった。

### 原因

- Alpaca News WebSocket と RSS フィードは OVERNIGHT / WEEKEND 中も**受信を継続**（蓄積される）
- `market_open_event` は OVN 中クリアされニュース判定もスキップされるが、セッション切替で `set()` した瞬間に**蓄積された記事が一斉に処理される**
- 結果: 数時間前の既に織込済ニュースで発注 → 直後の動きとは無関係

既存の `STARTUP_NEWS_IGNORE_SEC` (起動時 60 秒の無視期間) は **Bot 起動時のみ**に作用し、セッション切替には反応しなかった。

### 変更内容

**① 新規 env `SESSION_CHANGE_QUIET_SEC`（デフォルト 60 秒）を導入**

```env
SESSION_CHANGE_QUIET_SEC=60     # 0 で無効化
```

**② `market_schedule_loop` で `_last_session_change_at` を記録**

停止セッション（OVN / WEEKEND / 休日 / disabled）から稼働セッションへの**再開時点**で `_last_session_change_at = datetime.now()` をセット。

**③ `process_headlines` / `process_stock_news` でクワイエットウィンドウを判定**

切替後 N 秒以内なら:
- 新着ニュースを「**既読登録のみ**」（重複排除のため記録）
- AI 判定・発注は完全スキップ
- ログに「[セッション切替待機中] N秒後に発注開始」表示

`STARTUP_NEWS_IGNORE_SEC` と同じパターンの実装（既存ロジック流用）。

### 例（v3.9.56 適用後の挙動）

```
17:00:29 [セッション] PREMARKET 開始
17:00:29 [セッション切替待機] 60秒後に発注開始 → 16件を既読登録のみ
17:00:31 [セッション切替待機] 60秒後に発注開始 → 19件を既読登録のみ
17:00:33 【GOOGL】 [セッション切替待機中] 60秒後に発注開始 → 個別株1件を既読登録のみ
17:01:30 [セッション切替待機] 終了 → 通常処理再開
17:01:35 (新規ニュースを通常通り判定・発注)
```

GOOGL の蓄積記事は**既読登録のみ**で発注されない → 5/28 のような「OVN 中の古いニュースで瞬時に発注」が解消。

### 効果

- セッション切替直後 (PREMARKET 開始の 04:00 ET、ATH 移行の 16:00 ET 等) の**蓄積ニュース誤発注を完全防止**
- 新規（切替後）のニュースは引き続き通常処理 (60 秒経過後)
- WebSocket / RSS 受信は中断しない（既読登録は継続）

### 既存機能との整合性

- `STARTUP_NEWS_IGNORE_SEC` (起動時): 影響なし・並列動作
- `_BOT_START_TIME`: 影響なし
- 重複排除 (`state.is_new_article`): 影響なし（既読登録は継続）
- `STARTUP_NEWS_IGNORE_SEC` と `SESSION_CHANGE_QUIET_SEC` は独立 env (両方ともデフォルト 60 秒)

### 互換性

- 0 設定で機能無効化（旧挙動）
- 短くしたい場合は `.env` で `SESSION_CHANGE_QUIET_SEC=30` 等に調整可能
- セッション内での切替（PREMARKET → RTH 等の連続セッション間）は **影響なし**（OVN/WEEKEND からの復帰時のみ作動）

---

## v3.9.55 (2026-05-28) — 個別株株数キャップ撤回 + MOMENTUM サーキットを BUDGET 比 % に変更

### 背景

v3.9.54 でリリースした 2 つの改修について、ユーザーフィードバックを反映:

1. **個別株絶対株数キャップ (`STOCK_MAX_SHARES_PER_ORDER`) は不要**
   → 既存の `calc_order_size()` ロジックを精査した結果、計算ミスなく `BUDGET_USD × STOCK_MAX_PCT` の設計通り動作している。絶対株数キャップは設計の二重化になるため撤回。

2. **MOMENTUM サーキットを USD ベース → BUDGET 比 % ベースに変更**
   → 異なる `BUDGET_USD` を持つ受講生（$10,000 / $50,000 など）でも公平に機能するよう、固定 USD 値 ($300 / $500) ではなく BUDGET 比 % で表現すべき。デフォルトは **日 3% / 週 5%**。

### 変更内容

**① `STOCK_MAX_SHARES_PER_ORDER` を撤回**

- env 定義（`moomoo_trade_v1.py`）を削除
- `place_buy` / `place_short` 内の qty クランプ処理を削除
- 既存の `STOCK_MAX_USD` (BUDGET × STOCK_MAX_PCT) によるドル基準上限のみで運用

**② MOMENTUM サーキットを %ベースに変更**

旧 (v3.9.54):
```env
MOMENTUM_DAILY_LOSS_USD=300       # 固定 USD
MOMENTUM_WEEKLY_LOSS_USD=500      # 固定 USD
```

新 (v3.9.55):
```env
MOMENTUM_DAILY_LOSS_BUDGET_PCT=3.0   # BUDGET の 3% (デフォルト)
MOMENTUM_WEEKLY_LOSS_BUDGET_PCT=5.0  # BUDGET の 5% (デフォルト)
```

### BUDGET 別の停止額

| BUDGET | 日次停止 (3%) | 週次停止 (5%) |
|---|---|---|
| $5,000 | -$150 | -$250 |
| **$10,000** | **-$300** | **-$500** |
| $25,000 | -$750 | -$1,250 |
| $50,000 | -$1,500 | -$2,500 |

%ベースなら BUDGET にかかわらず「資金の 3%/5%」という直感的な保護が機能する。

### 二段防御の構造（変更なし）

```
1日損失上限 = min(
  RISK_LEVEL プリセット (L3: 0.50% = $50/日 @ BUDGET $10,000),  ← 細かめ
  MOMENTUM_DAILY_LOSS_BUDGET_PCT (3.0% = $300/日)                ← 大枠
)
```

通常は RISK_LEVEL の細かい値が先に発火するが、何らかの理由でそれを通過しても **絶対に BUDGET × 3% で止める**設計。週次は新規追加のため BUDGET × 5% のみ。

### 互換性

- 旧 `MOMENTUM_DAILY_LOSS_USD` / `MOMENTUM_WEEKLY_LOSS_USD` を `.env` に設定していた場合は無視される（短期間しか存在しなかった env のため影響は限定的）
- 新 env を明示設定していない受講生は自動的に新標準（3% / 5%）が適用される
- 0 設定で個別に無効化可能

---

## v3.9.54 (2026-05-28) — 5/27 Phase 1 初日壊滅 (-$566) への 4 点対応

### 背景

5/27 日次レポート分析で 4 つの構造的問題を確認:

| 問題 | 損失 | 原因 |
|---|---|---|
| ① BAC 923 株 × 16 分 = -$549 単独損失 | -$549 | 絶対株数キャップ未実装 |
| ② MOMENTUM Phase 1 初日 -$504 (WR 12.5%) | -$504 | USD ベースのサーキットブレーカー不在 |
| ③ Siemens × Mai-Liao 海外×海外×米国ETF 誤判定 | -$47 | プロンプト v1.10.0 で取りこぼし |
| ④ Phase 1 BUY-only 設計 | 機会損失 | SHORT 圧倒的好調 (SPY-SHORT WR 95.5%, IWM-SHORT WR 92.9%) も実発注対象外 |

### 変更内容

**① 個別株の絶対株数キャップ `STOCK_MAX_SHARES_PER_ORDER` を新規導入**

新規 env (デフォルト 500 株・0 で無効化):
```env
STOCK_MAX_SHARES_PER_ORDER=500
```

`place_buy` / `place_short` の qty 計算後に `_is_stock_sym and qty > 上限` で WARNING ログ + qty クランプ。低価格株 (BAC ~$45) で BUDGET × STOCK_MAX_PCT が大きな株数になるケースを防止。ETF / モメンタムには非適用。

5/27 BAC ケースに新ロジックを適用すると: 923 株 → 500 株にクランプ → 損失 -$549 → -$295 程度に抑制（株価変動率は同じため約 54% 削減）。

**② MOMENTUM USD ベース サーキットブレーカー**

既存の `MOMENTUM_DAILY_LOSS_PCT` (% ベース) に加えて USD ベースの上限を導入:

```env
MOMENTUM_DAILY_LOSS_USD=300       # 1日 -$300 で当日停止 (新規・デフォルト)
MOMENTUM_WEEKLY_LOSS_USD=500      # 1週 -$500 で月曜まで停止 (新規・デフォルト)
```

`% ベース と USD ベースのいずれかが先に発火`した時点で停止。週次は月曜 04:00 ET で自動リセット (`_momentum_weekly_pnl` / `_momentum_weekly_reset_date` 追加)。

5/27 ケース: -$504 で発火 → 3 連敗時点 (約 -$150) で停止していたはず → -$354 削減。

**③ プロンプト v1.12.0: 海外企業 × 海外プロジェクト → 米国 ETF 完全禁止**

`UNIFIED_SYSTEM_PROMPT_TEMPLATE` に v1.12.0 ルール追加 (`_PROMPTS_VERSION 1.11.0 → 1.12.0`):

「3 段階の地理的非関連」(海外企業 × 海外プロジェクト × 米国 ETF) を絶対禁止。例外として「海外企業の米国内投資」「米国企業の海外展開」は通常判定。

加えて `HEADLINE_NOISE_PATTERNS` に 2 種の正規表現追加:
- `foreign-co-foreign-project`: 主要海外企業名 (Siemens / Honda / TSMC 等) × 海外国・地域
- `foreign-co-foreign-project-specific`: Mai-Liao 等の具体プロジェクト名パターン

**④ MOMENTUM_ENABLED_SIDES に IWM:SELL_SHORT を追加**

旧: `SPY:SELL_SHORT,QQQ:SELL_SHORT,QQQ:BUY`
新: `SPY:SELL_SHORT,QQQ:SELL_SHORT,QQQ:BUY,IWM:SELL_SHORT`

5/27 シャドー実績: IWM-SHORT 56件・WR 92.9%・+0.59% → BUY-only 設計の偏りを是正し、下落相場でも利益確保。

### 期待される改善

5/27 のような壊滅日を再現すると:
- BAC: -$549 → **-$295**（株数キャップで 54% 削減）
- MOMENTUM: -$504 → **-$150 〜 -$300**（サーキット発火で連敗 3 で停止）
- Siemens-Mai-Liao: -$47 → **$0**（プロンプト + 正規表現で完全遮断）
- 機会損失（IWM-SHORT）: 56件 × WR 93% を Phase 1 で活用可能に

合計の損失抑制効果: 約 **-$1,100 → -$450 程度**（60% 削減）

### 互換性

- 既存 `.env` に新 env が無い場合は新標準値 (500 / 300 / 500) が適用される
- `MOMENTUM_ENABLED_SIDES` を `.env` で明示設定している場合はそちらが優先
- 各サーキットブレーカーは 0 設定で無効化可能（後方互換）

---

## v3.9.53 (2026-05-27) — SOXL を `_HIGHVOL_SYMBOLS` から削除（moomoo JP 信用で取扱不可）

### 背景

受講生からの指摘で、`_HIGHVOL_SYMBOLS`（高ボラ銘柄の損切り幅自動拡大対象）に含まれていた **SOXL（Direxion Daily Semiconductor Bull 3X Shares）** が **moomoo JP の信用取引（特定口座・信用枠）で取扱対象外**であることが判明。

3x レバレッジ ETF は一般的に日本の証券会社の信用取引対象外のため、リストに残しておく意味がない（実発注時に moomoo 側でリジェクトされる）。

### 変更内容

**① `_HIGHVOL_SYMBOLS` から SOXL を削除**

```python
旧:
_HIGHVOL_SYMBOLS = {
    "NVDA", "TSLA", "AMD", "MU", "AVGO", "SMH", "MRVL", "ARM",
    "SMCI", "COIN", "MSTR", "PLTR", "SOXL", "SOXX",
}

新:
_HIGHVOL_SYMBOLS = {
    "NVDA", "TSLA", "AMD", "MU", "AVGO", "SMH", "MRVL", "ARM",
    "SMCI", "COIN", "MSTR", "PLTR", "SOXX",
}
```

**② Wizard STEP 2 の表示文言を更新**

`setup_wizard.py` の自動調整ガイドボックス内の例示銘柄から SOXL を削除し、SOXX に置換:
```
旧: 対象: NVDA / TSLA / AMD / MU / AVGO / SMH / SOXL など値動きの激しい銘柄
新: 対象: NVDA / TSLA / AMD / MU / AVGO / SMH / SOXX など値動きの激しい銘柄
```

### 残置するもの

- **SOXX**: iShares Semiconductor ETF（1x・通常 ETF）。信用取引対象なので維持
- **既知 ETF リスト（setup_wizard.py の `STOCK_TICKERS` バリデーション用）**: SOXL は引き続き「ETF として個別株リストに入れさせない」用途で残置（ETF であること自体は事実）

### 影響

- 受講生の `.env` で `STOCK_TICKERS` に SOXL が含まれることは想定されない（Wizard バリデーションで拒否済み）
- `_HIGHVOL_SYMBOLS` から削除しても他の銘柄の挙動には影響なし
- 3x レバ ETF（TQQQ / SQQQ / SPXL / SPXS 等）は元々リスト未掲載のため対応不要

---

## v3.9.52 (2026-05-27) — place_buy/short 例外ハンドリングを2段階分離（API送信 vs 後処理）

### 背景

5/26 ログで以下の事象を観測:

```
22:50:02 【QQQ】 [ORDER] BUY  orderId=...           ← place_order 成功
22:50:02 【QQQ】 [注文管理] 登録 orderId=...        ← watchdog 登録も成功
22:50:02 [ERROR] [ORDER] BUY 例外: ...              ← 後処理で例外 → 全体を ERROR 扱い
22:51:58 【QQQ】 ⏱ 未約定タイムアウト orderId=...   ← watchdog 2分後タイムアウト確認
22:51:58 【QQQ】 [キャンセル] orderId=... は約定済みのため削除  ← 実は約定していた
```

旧 v3.9.51 までは `place_buy` / `place_short` の API 呼出と後処理 (`_register_pending_order` / `_threadsafe_future(_check_order_filled)` 等) が同一の `try` ブロックに包まれていた。後処理で例外が出ると:

1. ログ全体が ERROR 扱い
2. 関数が `return False` → 呼び出し元 (モメンタム等) は「発注見送り」と誤認
3. `ts_state` (entry_time / peak_price 等) が未設定 → `risk_monitor_loop` がトラッキングできない
4. 注文自体は moomoo サーバへ送信済みなので watchdog の救済で 2 分後に拾われるが、その間ポジション管理は空白

v3.9.50 で `asyncio.ensure_future` → `_threadsafe_future` 全置換により**根本原因（イベントループ未取得エラー）は解消済み**だが、構造的に「後処理の例外で注文成功が打ち消される」リスクは残っていた。

### 変更内容

`place_buy` / `place_short` の例外ハンドリングを **2 段階に分離**:

```python
# 段階1: API 送信 (place_order) の例外
order_id = None
try:
    ret, data = ctx.place_order(...)
    if ret != RET_OK:
        return False    # 注文未送信 → ERROR + 終了
    order_id = str(data["order_id"][0])
    log.info(f"[ORDER] BUY  orderId={order_id} ...")
except Exception as e:
    log.error(f"[ORDER] BUY API例外: {e}")    # 重大: 注文送信失敗
    return False

# ── ここから API 送信成功確定 ──
# 段階2: 後処理 (watchdog 登録・約定確認スケジュール) の例外
try:
    _register_pending_order(order_id, symbol, trd_env)
    if _trade_locked:
        _trade_locked = False
        log.info("[ロック解除] ...")
    _threadsafe_future(_check_order_filled(...))
except Exception as e:
    log.warning(f"[ORDER] BUY 後処理例外 (注文 {order_id} は送信済み): {e}")
    # WARNING に降格 + 処理継続 → ts_state 設定へ
```

### 効果

- 注文成功 + 後処理例外のケースで:
  - **ログレベル**: ERROR → WARNING に降格（「注文は送信済み」と明示）
  - **戻り値**: False → True (継続) → `ts_state.total_orders += 1` 等の後続処理も実行
  - **ポジション追跡**: entry_time / peak_price が正しく設定 → risk_monitor が即座にトラッキング開始
- API 送信そのものが失敗した場合は引き続き ERROR + return False（変更なし）
- 同じ改修を `place_buy` と `place_short` 両方に対称適用

### 関連修正

- v3.9.50: `asyncio.ensure_future` → `_threadsafe_future` 全 59 箇所置換（根本原因解消）
- v3.9.52: 構造的堅牢化（後処理の例外が注文成功を打ち消さないように分離）

両改修により、5/26 の QQQ ケースが今後発生しても:
1. v3.9.50 によりそもそも `_threadsafe_future` 経路で例外発生しない
2. 万一なんらかの後処理例外が出ても、v3.9.52 により WARNING + 処理継続 → 即座にポジション管理開始

の二段防御になります。

---

## v3.9.51 (2026-05-27) — Alpaca News 警告を非RTHは60分間隔・1行表示に変更（ログ汚染緩和）

### 背景

受講生ログで Alpaca News 接続不全警告が「ET 19:00 から 5 分おきに 10 回」表示された事象を確認:

```
2026-05-26 21:35:47  ボット起動 v3.9.44
...
2026-05-27 08:00:32  [ERROR] ╔══════ Alpaca News 接続不全 ...
                     ║  最終正常受信: 23分前  認証失敗 0回  ...
                     (10 行以上の枠付き ERROR が 10 回連続)
```

調査の結果:
- 期間: ET 19:00〜19:45（**AFTERHOURS 後半**）
- Alpaca News 流量がもともと少ない時間帯
- 接続自体は生きており、ET 20:50 から正常受信再開
- → **本物の接続不全ではない**（時間帯由来の沈黙）

問題点:
1. **誤警告で受講生を不安にさせる**（接続正常なのに 10 回連続 ERROR）
2. **表示が大きすぎて他のログが埋もれる**（1 警告あたり 15 行の ASCII アート枠）
3. **5 分間隔は RTH 前提**で、ニュース流量の少ない時間帯には頻度過剰

### 変更内容

**① 警告頻度をセッション別に分岐**

| セッション | 警告間隔 | 理由 |
|---|---|---|
| RTH（09:30-16:00 ET） | **5 分**（既存） | ニュース流量大、本物の異常を素早く検知 |
| プリマーケット / アフターアワーズ | **60 分** | ニュース流量小、沈黙は正常範囲が多い |

実装: `_alpaca_news_health["last_warning_at"]` で最終発火時刻を記録し、非 RTH では 60 分経過後のみ再発火。

**② 表示をコンパクトな 1 行に変更**

```
旧 (15 行・ERROR レベル):
╔════════════════════════════════════════════════════════════════╗
║  ⚠️  Alpaca News 接続不全 — ニュースの一部が取得できていません  ║
╠════════════════════════════════════════════════════════════════╣
║  連続認証失敗: 0  回   最終正常受信: 23分前                 ║
║  直近エラー: (詳細不明)                                              ║
║                                                                ║
║  よくある原因と対処:                                            ║
║    ・同じ Alpaca キーを複数 PC で使用 → 1 台のみに絞る         ║
... (省略) ...
╚════════════════════════════════════════════════════════════════╝

新 (1 行・WARNING レベル):
[Alpaca News] ⚠ 接続不全 (セッション=ath): 最終受信 23分前 / 認証失敗 0回 /
直近: 詳細不明 — 復旧で自動解除
```

- レベル: ERROR → **WARNING** に変更（接続不全は致命的バグではないため）
- 表示: 15 行枠 → **1 行**に圧縮
- 情報量: 連続認証失敗回数 / 最終受信からの経過分 / 直近エラー / セッション名は維持

### 影響

- 受講生ログのノイズ大幅減少（5/26 のケースでは 10 行 × 10 回 = 100 行以上が 1 行 × 1 回 = 1 行に）
- ET 19 時台の AFTERHOURS で「沈黙 = 接続不全」と誤判定して警告が連発する事象が解消
- 本物の RTH 中の接続不全は引き続き 5 分間隔で素早く通知（検知精度は維持）
- Anthropic クレジット切れ警告（同 health_warning_loop 内）は変更なし（課金問題は緊急性が高いため枠付き 5 分間隔を維持）

---

## v3.9.50 (2026-05-27) — AI 抽象ニュース判定禁止ルール (v1.11.0) + place_buy/short の event loop バグ修正

### 背景

**① AI 関連抽象ニュースの誤判定**（5/26 報告）

5/26 単日で AI 関連の「具体的銘柄に紐付かない抽象記事」が累計 -$68 の損失源:

- "Workers over 60 are the least worried AI will take their jobs" → SEMI_STRONG-LONG → 4 件 -$29.84
- "Wall St gains at open as AI optimism overshadows Middle East risks" → TECH-LONG → 3 件 -$38.39

労働市場の世代調査記事や「AI 楽観論」という抽象表現で TECH/SEMI ETF 上昇を期待する判定が頻発していた。

**② place_buy / place_short の event loop バグ**（5/26 ログで確認）

```
2026-05-26 22:50:02 [ERROR] 【QQQ】 [ORDER] BUY 例外:
  There is no current event loop in thread 'asyncio_0'.
  File "moomoo_trade_v1.py", line 8931, in place_buy
    asyncio.ensure_future(...)
  RuntimeError: There is no current event loop in thread 'asyncio_0'.
```

モメンタム発注は `asyncio.to_thread(place_buy, ...)` でワーカースレッドから呼ばれるため、`place_buy` 内の `asyncio.ensure_future` が「現在のスレッドにイベントループがない」エラーを発生させていた。結果として QQQ 発注が失敗（5/26 22:50 のモメンタム発注がスキップ）。

### 変更内容

**① プロンプトに v1.11.0 ルール追加**

`UNIFIED_SYSTEM_PROMPT_TEMPLATE` の FP 実例セクションに、AI 抽象ニュースを米国 ETF 判定材料にしないルールを追加:

- 当該記事が AI 業界の動向・楽観論・社会現象・規制論議など抽象的テーマで、具体的な企業の業績・契約・規制発動を伴わない場合 → 必ず `score = 0`
- 過去実例 4 件を明示（5/26 の 2 件 + 将来想定 2 件）
- 例外: 具体的銘柄 + 確定事象（NVIDIA chip shipment / OpenAI deal / EU AI Act 発動等）は通常判定
- `_PROMPTS_VERSION` 1.10.0 → 1.11.0

**② `HEADLINE_NOISE_PATTERNS` に正規表現 8 種追加**

AI 呼出前に明確なパターンを遮断（API コスト削減も兼ねる）:

| パターン | ラベル | 該当実例 |
|---|---|---|
| `AI will take/replace jobs/workers` | `ai-labor-abstract` | "AI will take their jobs" |
| `workers over N ... AI` | `ai-labor-demographics` | "Workers over 60 ... AI" |
| `least/most worried AI` | `ai-anxiety-abstract` | "least worried AI" |
| `AI optimism/enthusiasm` | `ai-optimism-abstract` | "AI optimism" |
| `overshadow ... AI` / `AI ... overshadow` | `ai-overshadow-abstract` | "AI optimism overshadows" |
| `how/why AI is changing/transforming` | `ai-society-abstract` | "How AI is changing the workplace" |
| `AI ethics/debate intensifies` | `ai-ethics-abstract` | "AI ethics debate" |
| `future/impact of AI on/in/for` | `ai-future-abstract` | "future of AI on jobs" |

**③ `asyncio.ensure_future` → `_threadsafe_future` への全置換（59 箇所）**

`_threadsafe_future` は既に v2.x で定義済み:
```python
def _threadsafe_future(coro) -> None:
    if _main_loop and _main_loop.is_running():
        asyncio.run_coroutine_threadsafe(coro, _main_loop)
    else:
        log.warning("[スレッド] イベントループ未起動のためコルーチンをスキップ")
```

`asyncio.run_coroutine_threadsafe` は**メインスレッド / ワーカースレッド両方で動作**する正規 API。`place_buy` / `place_short` / `place_close_all` 等の同期関数がワーカースレッドから呼ばれた場合でも安全にコルーチンを発火できる。

### 影響

- **AI 抽象ニュース誤判定 -$68/日の削減**を期待（プロンプト + 正規表現の二重防御）
- **モメンタム発注エラーの解消**: ワーカースレッドからの `place_buy` 呼出が安定動作
- 正規表現遮断分の Claude API コスト削減
- 構文チェック OK

### 例外（通常判定継続）

- `NVIDIA announces new AI chip ...` （具体的銘柄 + 確定事象）
- `OpenAI signs $X deal with Microsoft` （具体的取引）
- `EU passes AI Act with $X fine` （規制発動）

---

## v3.9.49 (2026-05-26) — 個別株のソース別ディスカウントを撤去（Finnhub/Alpaca 平等扱い）

### 背景

`process_stock_news()` 内で v2.93 から実装されていた個別株向けの confidence ディスカウント:

```
旧:
  Alpaca:  × 0.95 (5% 減)
  Finnhub: × 0.85 + 追加 × 0.95 = × 0.81 (19% 減)
```

このため例えば AI が confidence 0.85 を出しても:
- Alpaca 個別株: 0.85 → **0.81** （閾値 0.75 をぎりぎり）
- Finnhub 個別株: 0.85 → **0.69** （閾値 0.75 未満 → 発注スキップ）

結果として **Finnhub 個別株は事実上ほぼ発注されない状態** になり、判定データの収集も止まっていた。

### 撤去の根拠

導入時の根拠 (730 件のデータで Finnhub 個別株勝率 35.6%) は v3.8.x 時代のもの。以後の改善:

- **v3.9.41**: 海外単一企業 → 米国 ETF 誤判定の禁止ルール（プロンプト v1.10.0）
- **v3.9.46**: 損切り 0.50% → 0.30% / トレール 1.0% → 0.30% に改訂（損失幅縮小）
- **v3.9.47**: モメンタム専用タイムアウト分離

5/26 報告では強制損切り 0 件・MACRO 系で +$69 と全体構造が改善。旧ディスカウントは現状にそぐわない。

### 変更内容

`process_stock_news()` 内の以下 2 ブロックを削除:
- `_STOCK_SOURCE_WEIGHTS = {"Alpaca": 0.95, "Finnhub": 0.85}` による減衰
- Finnhub 個別株への追加 `× 0.95` 減衰

AI が出した confidence をそのまま `STRONG_BUY_CONFIDENCE` 閾値と比較するように。
ETF 判定 (`process_headlines`) 側のソース重み付けは継続（別途検証中）。

### 影響・期待効果

- **Finnhub/Alpaca 個別株の発注が増加**: 同条件で AI が 0.75+ と判定したらそのまま発注
- **判定データの収集量増加**: 個別株経路の実発注データが揃い、今後のチューニング材料に
- **個別株勝率の再評価が可能**: 改善版プロンプト + タイト損切りでの真の勝率を計測
- 1-2 週間のデータで個別株判定の妥当性を再判断（必要なら銘柄別の調整等を検討）

---

## v3.9.48 (2026-05-24) — 成績自動バックアップが月曜以降の起動で前週末分を拾えない不具合を修正

### 症状

受講生から「成績の自動バックアップが機能していない、月曜以降の起動で実行されない」との報告。前週末（金曜）のトレード集計が GAS Webhook に送信されないまま残るケースが発生していた。

### 原因

`find_unsent_trading_dates()`（未送信日検出）と `run_daily_data_collect()`（集計・送信）は両方とも **現在のログファイル `moomoo_trade_v1.log` のみ** をスキャンしていた。

v3.8.7 以降、`WeeklyLogbackupHandler` が**毎週月曜 00:00** に前週のログを `logbackup/` へローテーションする。このため月曜以降の起動時には:

1. 前週末のログは `logbackup/moomoo_trade_v1.YYYY-MM-DD_to_YYYY-MM-DD.zip` へ移動済み
2. 現在の `moomoo_trade_v1.log` は月曜以降の分のみ
3. 起動時キャッチアップ (`catchup_data_send_on_startup`) は現在ログのみスキャン → 前週末のデータが見つからない → 送信されない

タイミング図:
```
金 5/22  正常取引・ログ記録
土 5/23  Bot停止 (週末・OVERNIGHT)
日 5/24  Bot停止
月 5/25 00:00  WeeklyLogbackupHandler が rollover → 5/18-22 のログを ZIP 化
月 5/25 09:00  Bot起動 → catchup 走査 → 現在ログには 5/25 のデータしかない
              → 5/22 のデータは "未送信" として検出されず → GAS 送信されない
```

### 変更内容

**① 統合ログスキャナ `_iter_log_lines(log_path, cutoff_date)` を新設**

現在のログと `logbackup/` 内のアーカイブを**順に開いて行を yield** するジェネレータ。`cutoff_date` 以前のデータしか含まないアーカイブはスキップして無駄な読み込みを避ける。

- 現在のログ: そのまま open
- `.zip` アーカイブ: `zipfile.ZipFile` で展開して内部 `.log` を `io.TextIOWrapper` 経由で読む
- `.log` アーカイブ（旧形式・v3.9.42 以前）: そのまま open
- ファイル名の `_to_YYYY-MM-DD` 部分から終了日を取り出し、`cutoff_date` より前なら skip

**② 2 つの関数を新スキャナ経由に変更**

- `find_unsent_trading_dates()`: 過去 7 日以内に取引ログがあり未送信の ET 日付を返す
  → 現在ログ + バックアップから日付を集計するように改修
- `run_daily_data_collect(target_date)`: 特定日の集計を実行
  → 同じく統合スキャナ経由に変更（`target_date` 以前のアーカイブは自動で除外される）

### 影響

- **月曜（祝日明け含む）の起動時に前週末（金曜）の集計が自動送信されるようになった**
- 起動時キャッチアップは過去 7 日以内まで遡るため、複数日休止していた場合も全部回収
- 既存の重複送信防止 (`.data_sent_history.json`) はそのまま機能
- バックアップ走査のオーバーヘッドは無視できる（lookback 7 日なので対象アーカイブは最大 2 ファイル程度）

### 検証

- 構文チェック OK
- 統合スキャナは現在ログ → 新しい順にバックアップを走査
- `.zip` / `.log` 両形式を対応（v3.9.43 移行期間も問題なし）

---

## v3.9.47 (2026-05-24) — モメンタム専用タイムアウトを実配線（60分・旧10分はニュース用）

### 背景

`TIMEOUT_EXIT_MINUTES`（標準 10 分）は**ニュース駆動トレード**の「ニュースの鮮度」を前提として導入された設定。一方、モメンタム取引は**トレンド継続**を狙う戦略であり、シャドー観察データのベンチマークも **60 分 PnL** で評価している。

しかし v3.9.45 までは Phase 1 の実発注ポジションも `place_buy/place_short` 経由でリスクモニタに登録され、**同じ `TIMEOUT_EXIT_MINUTES = 10 分` が適用されていた**。これにより:
- モメンタムシグナルが伸びる前に強制決済される
- 5/18-22 実績で `タイムアウト決済 119 件・-$125.74`（ほぼフラット）= モメンタムが伸び切る前に切られている可能性大
- シャドー観察データ（60 分 PnL ベース）と実発注の前提が乖離

`MOMENTUM_TIMEOUT_MIN`（v3.9.25 で定義済み・デフォルト 15）は変数だけ存在し、決済ロジックには未配線だった。

### 変更内容

**① `MOMENTUM_TIMEOUT_MIN` を実配線**

`risk_monitor_loop` のタイムアウト選択を 3 分岐に変更:

```python
_is_momentum = (ts.entry_ai_category == "MOMENTUM")
if _is_earn_sym:
    _t_timeout = EARNINGS_TIMEOUT_EXIT_MINUTES
elif _is_momentum:
    _t_timeout = MOMENTUM_TIMEOUT_MIN   # ← 新規
else:
    _t_timeout = TIMEOUT_EXIT_MINUTES
```

`entry_ai_category` はモメンタム発注時に `place_buy/place_short` が `"MOMENTUM"` を `ts.entry_ai_category` にセットするため、識別は確実。

**② `MOMENTUM_TIMEOUT_MIN` のデフォルトを 15 → 60 に変更、範囲を 0-240 に拡大**

```python
# 旧
MOMENTUM_TIMEOUT_MIN = _parse_momentum_int("MOMENTUM_TIMEOUT_MIN", 15, 1, 120)
# 新
MOMENTUM_TIMEOUT_MIN = _parse_momentum_int("MOMENTUM_TIMEOUT_MIN", 60, 0, 240)
```

- **60 分**: シャドー観察データの 60 分ベンチマークに整合
- **0 を許可**: タイムアウト無効化（トレール / 損切り / 反転に委ねる）
- **最大 240 分**: 必要十分な上限

**③ 起動ログにモメンタムタイムアウトを表示**

```
[モメンタム個別設定] ... / タイムアウト 60分
```

`MOMENTUM_TIMEOUT_MIN=0` のときは「無効（トレール/損切りで管理）」と表示。

### 影響

- **Phase 1 実発注のモメンタムポジション**: タイムアウトが 10 分 → 60 分に拡大（または 0 で無効）。トレンドの伸びを取りやすくなる
- **ニュース駆動トレード**: 影響なし（`TIMEOUT_EXIT_MINUTES = 10 分` を継続使用）
- **決算銘柄**: 影響なし（`EARNINGS_TIMEOUT_EXIT_MINUTES` を継続使用）
- **シャドー観察**: 観察ログのみのため影響なし
- 既存 `.env` に `MOMENTUM_TIMEOUT_MIN` が設定されていれば既存値が優先される

### 期待される改善

5/18-22 実績のタイムアウト 119 件 -$125 の多くは「モメンタムが 10 分以内に伸び切らず切られた」可能性が高い。60 分に拡大することで:
- トレール決済（既に v3.9.46 で発動を早めた）に流れる件数が増加
- 早期切られによる機会損失の減少
- シャドー実績（QQQ-SHORT WR 80%, +3.16%）に近い実発注成績への接近

---

## v3.9.46 (2026-05-24) — 損切り / トレール標準値を改訂（非対称性 5:1 → 2:1）

### 背景

旧設定の損益非対称性を計算で確認:

| 設定 | 旧値 | 最悪ケース |
|---|---|---|
| MAX_LOSS_PCT | 0.50% | -0.50% |
| TRAIL_TRIGGER_PCT | 1.0% | 利益 1.0%（トレール発動）|
| TRAIL_DROP_PCT | 0.5% | -0.5% → 最小利益 +0.5% |

5/18-22 実績の決済理由内訳:
- トレイル決済: わずか **2 件 +$612.76**（全体の 1.2%）
- タイムアウト: **119 件 -$125.74**（時間切れ・ほぼフラット）
- 強制損切り: 12 件 -$302.02

「トレール発動が稀すぎてタイムアウトが大半 → 大勝 2 件で全体を支える高分散運用」になっていた。

### 変更内容

3 つの値を引き下げて **頻繁な小勝でコツコツ積み上げる** 設計へ移行:

| 設定 | 旧値 | **新標準** | 効果 |
|---|---|---|---|
| `MAX_LOSS_PCT` | 0.50% | **0.30%** | 1 トレード最大損失を 40% 縮小 |
| `TRAIL_TRIGGER_PCT` | 1.0% | **0.30%** | トレールを早く発動 → 多くのトレードで作動 |
| `TRAIL_DROP_PCT` | 0.5% | **0.15%** | 利益取りこぼし減 |

**損益分岐 WR**: 83% → **67%** に大幅改善。

### リスク / リワード比較

| | 旧 | 新 |
|---|---|---|
| 最悪利益 | +0.5% | **+0.15%** |
| 最悪損失 | -0.5% | **-0.30%** |
| 損益比 | 1:1 だが TRIGGER 未到達で timeout 大半 | **2:1（明確）** |
| 損益分岐 WR | 83% (実勢) | **67%** |
| 高ボラ銘柄 (× 2.5) | -1.25% | **-0.75%** |

### 注意点

- 旧 `.env` の TRAIL_TRIGGER_PCT / TRAIL_DROP_PCT / MAX_LOSS_PCT が明示設定されている場合は既存値が優先される（既存ユーザの設定は維持）
- Wizard 再実行時の標準選択肢として新値が「← まずここから」マークになる
- 旧標準値（1.0% / 0.5% / 0.50%）も Wizard の選択肢として残存（後方互換）
- 高ボラ銘柄は `STOCK_HIGHVOL_LOSS_MULT=2.5` により損切り 0.75%、サイズ 1/2.5 でリスクパリティ維持
- 想定される挙動変化: トレール決済件数の大幅増、タイムアウト決済の減少、強制損切り平均額の縮小

### Wizard 連動

`setup_wizard.py` を v1.10 に更新し、STEP 2（損切り）/ STEP 3（トレール）の標準選択肢に新値を設定。旧標準値も選択肢として残し、後方互換性を維持。

---

## v3.9.45 (2026-05-24) — モメンタム銘柄別サイズ係数を廃止（全銘柄 1.0 統一・手数料負け解消）

### 背景

`MOMENTUM_SIZE_MULTIPLIER` は v3.9.32 から銘柄ごとにサイズを調整する設計だった (`SPY=1.0 / QQQ=0.5 / SMH=0.0 / IWM=0.85`):
- QQQ=0.5 は変動が大きいためサイズ縮小（リスク調整）
- SMH=0.0 / IWM=0.85 はシャドー専用銘柄（実発注対象外）

しかし運用で以下の問題が確認された:

1. **手数料負けの主因**: QQQ=0.5 のため発注額が半分になり、BUDGET $10,000・MAX_PCT 80% で QQQ 発注額が $4,000 → 1 ピップ動いても手数料込で利幅縮小
2. **シャドーデータの精度低下**: SMH=0.0 はシャドー観察ログの想定発注額を $0 にしてしまい、PnL 推定が破綻
3. **リスク調整の二重制御**: 銘柄別リスク調整は `MAX_LOSS_PCT × _HIGHVOL_SYMBOLS` の自動調整で既にカバー済 (v3.9.32 で導入)

### 変更内容

`MOMENTUM_SIZE_MULTIPLIER` のデフォルト値を全銘柄 **1.00** に統一:

```python
MOMENTUM_SIZE_MULTIPLIER = {
    "SPY": 1.00,  # 変更なし
    "QQQ": 1.00,  # 0.50 → 1.00 (手数料負け解消)
    "SMH": 1.00,  # 0.00 → 1.00 (シャドーデータ精度向上)
    "IWM": 1.00,  # 0.85 → 1.00
}
```

環境変数 `MOMENTUM_SIZE_MULTIPLIER_<銘柄>` で個別上書きは引き続き可能（上級者向け・後方互換維持）。

### 影響

- **QQQ 発注額が 2 倍**に。$10,000 × 80% × 1.0 × 強度 1.0 = $8,000（旧 $4,000）。手数料負けが大幅に改善
- **シャドー観察データ**（SMH/IWM）の想定発注額が現実的な値になり、PnL 推定の精度が向上
- **実発注の対象銘柄**は `MOMENTUM_ENABLED_SIDES` で引き続き制御（標準: SPY:SHORT, QQQ:BUY/SHORT のみ）。SMH/IWM は実発注対象外のまま
- **銘柄別リスク調整**は `_HIGHVOL_SYMBOLS` (MU/NVDA/SMH/AVGO 等) の損切り幅 2.5 倍に集約（重複制御の解消）

---

## v3.9.44 (2026-05-24) — ログから Alpaca キー / Secret 等の機密漏洩を遮断（二段防御）

### 症状（重大なセキュリティ問題）

受講生環境の `moomoo_trade_v1.log` を点検した結果、**Alpaca API Key / Secret が平文で記録**されているのを確認した:

```
2026-05-18 23:02:32,151 [DEBUG] > APCA-API-KEY-ID: PKAPZ............................
2026-05-18 23:02:32,151 [DEBUG] > APCA-API-SECRET-KEY: DDSnr3......................
2026-05-18 23:02:32,328 [DEBUG] > TEXT '{"action": "auth", "key": "PKAPZ...DDSnr3..."}'
```

各受講生のローカルログ・logbackup（最大 8 週分）に蓄積されており、トラブル時にログを共有した瞬間に Alpaca 資格情報が運営側に渡る状態だった。

### 原因

ボットは Alpaca News WebSocket に **`websockets` ライブラリ**（モダンな async 版）を使用しているが、ログ設定で抑制していたのは `websocket`（旧 `websocket-client`・末尾 s 無し）だけで、**末尾 s 付きの `websockets` ロガーは抑制対象から漏れていた**。

`websockets` は DEBUG レベルで全送受信フレームを `> APCA-API-KEY-ID: ...` 形式で出力する仕様のため、ファイルログ（DEBUG 出力）にそのまま流れていた。

### 変更内容（二段防御）

**第 1 層: 漏洩源のロガーを WARNING に昇格**

`websockets` および以下のサブロガーを WARNING 以上に制限し、機密含有の可能性がある DEBUG/INFO 出力をそもそも生成しない:

- `websockets`, `websockets.client`, `websockets.server`
- `websockets.protocol`, `websockets.legacy.client`, `websockets.legacy.protocol`
- `anthropic`, `httpx`, `httpcore`（Anthropic SDK 経路の生 HTTP ペイロード対策）
- `asyncio`（イベントループ内部の詳細抑制）

**第 2 層: 機密マスキング・フィルタを全ハンドラに追加**

新クラス `_SecretMaskingFilter` を導入し、`_stream_handler` と `_file_handler` の両方にアタッチ。万一機密文字列がログレコードに含まれていても、ハンドラへの出力直前にマスクする:

| 検出方式 | マスク対象 |
|---|---|
| 既知 env 値の完全一致 | `ANTHROPIC_API_KEY` / `FINNHUB_API_KEY` / `ALPACA_API_KEY_ID` / `ALPACA_API_SECRET_KEY` / `DISCORD_WEBHOOK_URL`（および webhook token 部分） |
| パターン1 | `sk-ant-api03-XXX...` |
| パターン2 | `discord.com/api/webhooks/NUMERIC/TOKEN` |
| パターン3 | `PK...` / `AK...`（Alpaca paper / live key プレフィックス + 大文字英数 16 文字以上） |
| パターン4 | JSON `"key":"..."` / `"secret":"..."` / `"api_key":"..."` / `"token":"..."` |
| パターン5 | `APCA-API-KEY-ID:` / `APCA-API-SECRET-KEY:` HTTP ヘッダ |

マスクは `XXXX***YY`（先頭 4 + アスタリスク + 末尾 2）形式で、運営側がトラブル調査時にキーを識別できるが完全に再現はできない最小開示にとどめる。

フィルタは例外時も `True` を返す設計で、フィルタ自身の失敗がログ全体を止めない安全側のフォールバックを確保。

### 既存ログへの対応

- 過去に書き出された `moomoo_trade_v1.log` および `logbackup/*.log` / `logbackup/*.zip` には既に平文の Alpaca キーが含まれている可能性が高い
- **受講生 / 運営側ともに、既存ログファイルは Alpaca キー再発行後に削除することを強く推奨**（別途案内が必要）

### 影響・効果

- v3.9.44 以降は WebSocket auth ヘッダ / フレームがログに出ない
- 万一未知の経路で漏洩しても、フィルタが自動マスクする
- 既存運用との互換性: ログ出力先 / フォーマット / 保持期間に変更なし

---

## v3.9.43 (2026-05-23) — logbackup アーカイブを ZIP 圧縮（容量 約 1/10）

### 背景

`WeeklyLogbackupHandler`（v3.8.7 で導入）は毎週月曜 00:00 にログを `logbackup/` へ移動する設計だが、形式は非圧縮の `.log` だった。

ログはタイムスタンプ・ログレベル・キーワード等が大量に繰り返されるテキストで、**ZIP 圧縮で約 90% の容量削減が可能**。8 週分の保持で 1.5〜2 GB を占有していた状況を、約 150〜200 MB まで削減する。

### 変更内容

**① アーカイブ形式を `.log` → `.zip` に変更**

- `doRollover()` を改修。週次ローテーション時にログを `zipfile.ZIP_DEFLATED` + `compresslevel=9` で圧縮し、`logbackup/moomoo_trade_v1.YYYY-MM-DD_to_YYYY-MM-DD.zip` として保存する。
- ZIP 内部にはこれまでと同じ `.log` ファイル名で 1 ファイル格納（ダブルクリックで開いた時の体験は従来と同じ）。
- 圧縮は `.zip.tmp` に書いてから `os.replace` で rename することでクラッシュ耐性を確保。
- 圧縮失敗時は非圧縮 `.log` への移動に**自動フォールバック**し、次回起動時の移行処理で再試行される。

**② 起動時 1 回限りの旧形式 `.log` → `.zip` 自動移行**

- `WeeklyLogbackupHandler.__init__()` で `_migrate_existing_logs_to_zip()` を呼び、`logbackup/` 内に残る v3.9.42 以前の非圧縮 `.log` を順次 `.zip` に変換する。
- 同名 `.zip` が既に存在する場合（前回中断等）は重複 `.log` を安全に削除。
- 失敗時は元 `.log` を残し、次回起動で再試行。
- 変換結果は `print(stderr)` で「○MB → ○MB, -○%」と表示。

**③ ファイル名正規表現の拡張**

`_ARCHIVE_NAME_RE` のサフィックス部分を `(\.log|\.zip)` の両方を受け付けるように更新。`_cleanup_old_archives()`（8 週超過分の削除）は変更不要で `.zip` も含めて自動的に保持期間管理される。

### 影響・効果

- **容量削減**: 1 週分 約 200 MB → 約 20 MB（実測値の比は受講生環境で多少前後）
- **保持期間は変更なし**: 8 週間維持
- **既存環境の互換性**: 起動時に旧 `.log` が自動で `.zip` に変換されるため、受講生は何もしなくてよい
- **依存追加なし**: Python 標準ライブラリ `zipfile` のみ使用
- **ZIP の開き方**: Mac / Windows ともダブルクリックで解凍可能。トラブル時にログを共有する場合もそのまま添付できる

### 検証

- 構文チェック OK
- フォールバック設計: 圧縮失敗時に非圧縮 `.log` 移動 → 次回起動で再試行という二段構え

---

## v3.9.42 (2026-05-23) — .env API キーの非ASCII文字を自動除去 (AI 呼出全失敗の対策)

### 症状

ある受講生環境（v3.9.26 で稼働）で 5/18-22 の 5 日間連続稼働中、**一度も発注が行われない**重大な不具合を確認。
ログ調査の結果:

- RSS ニュース取得: 27,178 回 (正常)
- ニュース新着 → AI 判定キュー投入: 3,959 件 (正常)
- セッション遷移 (PREMARKET → RTH): 正常
- スリープ: 発生していない (`caffeinate` 機能正常)
- **AI 呼出 (POST /v1/messages): 3,924 回試行 → 全失敗**
- **`UnicodeEncodeError: 'ascii' codec can't encode characters in position 108-125`**

すべての失敗が同じエラー (`position 108-125`、17 文字の非 ASCII 文字) で、`httpx/_models.py:82` の `value.encode("ascii")` で発生。

### 原因

`.env` の `ANTHROPIC_API_KEY` 末尾にコピペで混入した非 ASCII 文字 (全角スペース・スマートクォート・日本語コメント等) が残っており、anthropic SDK がそれを `x-api-key` HTTP ヘッダに入れて送信 → `httpx` がヘッダ値を ASCII エンコードする箇所で毎回失敗していた。

正規の `sk-ant-api03-...` キーは 108 文字前後で、`position 108-125` は **キー本来の長さを超えた末尾 17 文字** に非 ASCII が混入していたことを示す。受講生が Anthropic コンソールから API キーをコピーした際、または `.env` を編集した際に:

- `ANTHROPIC_API_KEY=sk-ant-...XXX　← APIキー` のように全角スペース＋日本語コメント
- `ANTHROPIC_API_KEY="sk-ant-..."` のスマートクォート ("" U+201C / U+201D, python-dotenv が剥がさない)
- 末尾の BOM・改行・制御文字

等が付加された状態で保存されていたと推定される。

### 変更内容

**① ボット本体 — `_sanitize_api_key()` ヘルパを新設し、起動時に必ず通す**

`os.environ.get(...)` で読み込んだ機密値から:
- 非 ASCII 文字 (`ord(ch) >= 128`)
- 前後の囲みクォート (`'`, `"`, `` ` `` 各種)
- 前後の空白・制御文字

を除去し、何か除去した場合は WARNING ログで受講生に通知する (キー本体は機密のためログに出さない)。

適用箇所:
- モジュールレベル定数: `DISCORD_WEBHOOK_URL` / `FINNHUB_API_KEY` / `ALPACA_API_KEY_ID` / `ALPACA_API_SECRET_KEY`
- `get_anthropic_client()` — `ANTHROPIC_API_KEY`
- `_get_ai_error_fix()` — `ANTHROPIC_API_KEY` (urllib 直接呼出経路)
- 既存の `os.environ.get("ALPACA_API_KEY_ID", "").strip()` 5 箇所をモジュールレベル定数参照に統一

**② フェールセーフ — `sk-ant-` プレフィックスチェック**

サニタイズ後も `ANTHROPIC_API_KEY.startswith("sk-ant-")` でなければ起動時に明確なエラーメッセージを出して `sys.exit(1)` する。サニタイズで救えない致命的な書き間違いは、AI 呼出が始まる前に止める。

**③ Wizard 側 — 保存前サニタイズ (v1.9)**

`step_save()` で `.env` 書き出し前に同じ `_sanitize_api_key()` を全機密キー (`ANTHROPIC_API_KEY` / `FINNHUB_API_KEY` / `ALPACA_API_KEY_ID` / `ALPACA_API_SECRET_KEY` / `DISCORD_WEBHOOK_URL`) に適用。混入があれば WARNING を表示してから保存する。

### 影響

- AI 呼出が 0 回 → 正常稼働へ復旧
- 過去の `.env` 汚染状態でもボット起動時に自動修復 + 警告通知
- Wizard 再実行で `.env` が clean な状態に上書き保存される
- 構文チェック (両ファイル) OK

### 受講生への一時対応（バージョン更新前に今すぐ復旧したい場合）

1. `.env` をテキストエディタで開く
2. `ANTHROPIC_API_KEY=` の行を一旦削除
3. Anthropic コンソールからキーをブラウザのコピーボタンで取得 (手動範囲選択は不可)
4. `.env` に半角・クォート無し・行末スペース無し・1 行で貼り付け
5. ボット再起動

---

## v3.9.41 (2026-05-23) — 海外単一企業→米国ETF誤判定の禁止ルール + モメンタム時間帯フィルタ廃案

### 背景

5/20-22 の日次・週次レポートで以下 2 つの構造的問題が確認された:

1. **海外単一企業のローカル材料が米国 ETF 発注材料に誤判定 (累計 -$222 / 5/20-22)**
   - AJ Bell H1 Earnings Call (英国独立金融サービス, 3 件 -$65.73)
   - Deloitte UK to raise bonuses (英国会計事務所, 4 件 -$41.27)
   - Australia's SPC Global opens share issue (豪州食品, 3 件)
   - Zambia central bank / Angola construction (アフリカ単一国マクロ, 3 件 -$75.04)
2. **モメンタム時間帯フィルタ (RTH 11:00-15:30 ET) が 3 日中 2 日で逆効果**
   - 5/20: +1.55% / WR 78.6% → -0.17% / WR 60% (大幅劣化)
   - 5/22: +0.745% / WR 66.1% → -1.16% / WR 0% (完全壊滅)
   - 寄り付き 9-10 時台が黄金時間帯になる日が多く、固定時間帯フィルタは害となる

### 変更内容

**① プロンプト本文に v1.10.0 ルール追加**

`UNIFIED_SYSTEM_PROMPT_TEMPLATE` の FP 実例セクションに、海外単一企業のローカル材料を絶対に米国 ETF 判定で扱わないルールを追加:

- 当該企業が SPY/QQQ/SMH 構成銘柄でなく、米国主要市場への直接的因果関係が説明できない場合は必ず `score = 0`
- 過去実例 5 件 (AJ Bell・Deloitte UK・SPC Global・Zambia/Angola・GridStor) を明示
- 例外: 米国大型株を直接対象とした EU 規制・中国制裁、米中貿易戦争・FRB 利下げ等のグローバルマクロは通常判定
- `_PROMPTS_VERSION` 1.9.0 → 1.10.0

**② HEADLINE_NOISE_PATTERNS に正規表現 4 種追加**

AI 呼出前に明確な海外単一企業パターンを遮断 (API コスト削減も兼ねる):

| パターン | ラベル | 該当実例 |
|---|---|---|
| `[Company] UK to [verb]` | `foreign-co-uk-internal` | Deloitte UK to raise bonuses |
| `^AJ Bell` | `foreign-co-uk-ajbell` | AJ Bell H1 Earnings Call |
| `^[国]'s [Company] [verb]` | `foreign-co-local-news` | Australia's SPC Global opens... |
| `^[新興国] central bank/rate/...` | `foreign-macro-emerging` | Zambia central bank cuts rate |

国名は新興国・小型先進国に限定 (中国・日本・ECB・英国 BoE 等の主要国は除外し、プロンプト本文で判定させる)。

**③ モメンタム時間帯フィルタを完全撤去**

- `_momentum_is_in_trading_hours()` 関数を削除
- `MOMENTUM_RTH_START_ET` / `MOMENTUM_RTH_END_ET` の env 読み込みを削除
- `_parse_et_time` ヘルパも合わせて削除
- `momentum_shadow_loop` のゲーティング呼び出しを削除
- 起動ログから「時間帯 X:XX-Y:YY ET /」表示を削除
- Wizard の .env コメントから `MOMENTUM_RTH_*` の言及を削除
- 旧 .env に `MOMENTUM_RTH_*` が残っていても無視される (壊れない)

代替制御として既存の急変動ガード (`MOMENTUM_VOLATILITY_GUARD_PCT`, SPY 5 分変動率) を継続使用。「相場状態」ベースの制御に統一。

### 影響・期待効果

- 海外単一企業誤判定 -$222/週の削減を期待 (プロンプト + 正規表現で二重防御)
- モメンタム寄り付き黄金時間帯 (9-10 時 ET) のシグナルを取り逃さず、シャドー実績との整合性が回復
- 正規表現遮断分の AI 呼出削減 (推定 0.5-1.5% 程度のコスト削減)

---

## v3.9.40 (2026-05-23) — 週末・取引不可セッション中の Alpaca News 誤警告を停止

### 症状

v3.9.39 で「正常受信中の誤警告」は解消されたが、その後に**週末や取引不可セッション（OVERNIGHT 等）に入って `market_open_event` がクリアされた状態**では、5 分おきに同じ警告が出続け、「最終正常受信」の値が `25 分前 → 30 分前 → … → 115 分前` と増え続ける現象が残っていた。

```
09:14:58 [ERROR] ⚠️ Alpaca News 接続不全 — 最終正常受信: 25分前
09:19:58 [ERROR] ⚠️ Alpaca News 接続不全 — 最終正常受信: 30分前
...
10:44:58 [ERROR] ⚠️ Alpaca News 接続不全 — 最終正常受信: 115分前
```

### 原因

`alpaca_news_loop` は週末・取引不可セッション中、ループ先頭の `await market_open_event.wait()` で**意図的に WebSocket を停止**している（無駄なサーバ接続を避ける設計）。停止中はメッセージを 1 件も受信しないため `last_success_at` が更新されず、`health_warning_loop` は「正常受信が止まった = 接続不全」と誤判定していた。

つまり「ニュース取得を意図的に停止しているだけ」と「本物の接続障害」を区別できていなかった。

### 変更内容

`health_warning_loop` の Alpaca 警告条件に `market_open_event.is_set()` を追加。取引可能セッション中のみ「接続不全」判定を行うようにした。

```python
if (
    ALPACA_API_KEY_ID and ALPACA_API_SECRET_KEY
    and market_open_event.is_set()      # ← 追加
):
    ...
```

### 影響

- 週末・OVERNIGHT・取引不可セッション中は Alpaca News 警告が出なくなった（意図的停止なので正常）
- 取引可能セッション中に本当に接続が切れた場合は従来どおり警告が出る
- Anthropic クレジット警告（①）は副条件を追加していないので、停止中も引き続き表示される（クレジット切れは取引再開時にも影響するため）

---

## v3.9.39 (2026-05-22) — Alpaca News「接続不全」の誤警告を修正

### 症状

Alpaca News が正常にニュースを受信しているにもかかわらず、起動 25 分後あたりから 5 分おきに次の警告が端末に表示される:

```
⚠️  Alpaca News 接続不全 — ニュースの一部が取得できていません
連続認証失敗: 0 回   最終正常受信: 25分前
直近エラー: (詳細不明)
```

5/22 のログでは `22:24:04 [Alpaca News] 新着 1件`（受信成功）の **1 秒後** に `22:24:05 [ERROR] Alpaca News 接続不全` が出ており、明らかな誤検知だった。

### 原因

`health_warning_loop`（v3.9.31 追加）は `_alpaca_news_health["last_success_at"]` を見て「20 分以上正常受信なし」なら警告を出す。しかし `last_success_at` は **WebSocket 認証成功時にしか更新されず**、その後のニュース受信ループ（`async for raw_msg in ws:`）では一度も更新されていなかった。

このため WebSocket が再接続されずに 20 分以上接続が継続すると、ニュースが正常に流れていても `last_success_at` が認証時刻のまま古くなり、`auth_fail_count = 0`（認証失敗ゼロ）でも誤って「接続不全」警告が発火していた。v3.9.31 以来の潜在バグ。

※ AlphaVantage（ニュース REST API・v3.9.37 で削除）と Alpaca（Benzinga リアルタイム WebSocket）は別サービスであり、本不具合は AlphaVantage 削除とは無関係。

### 変更内容

- Alpaca News WebSocket の受信ループで、メッセージを 1 件受信するたびに `_alpaca_news_health["last_success_at"]` を現在時刻へ更新し、`last_error` をクリアするよう変更
- メッセージを受信できた事実そのものが「接続・購読が生きている」証拠であるため、これにより正常稼働中の誤警告が解消される

### 影響

- 正常受信中に「Alpaca News 接続不全」警告が出なくなった
- 認証失敗（`auth_fail_count >= 3`）や本当の接続断は従来どおり検知・警告される

---

## v3.9.38 (2026-05-22) — Ctrl+C 終了時にトレード集計が表示されない不具合を修正

### 症状

Ctrl+C でボットを停止すると停止バナー（`🛑 プログラムを停止しました`）は表示されるが、その後に出るはずの当日トレード集計（`📊 本日のトレード集計`）が端末に一切表示されない。Discord への終了メッセージも送信されていなかった。

### 原因

SIGINT ハンドラ `_sigint_handler` が、停止メッセージ表示後に以下を実行していた:

```python
signal.signal(signal.SIGINT, signal.SIG_DFL)   # デフォルト処理に戻す
os.kill(os.getpid(), signal.SIGINT)             # SIGINT を再送
```

`SIG_DFL`（OS デフォルト）の SIGINT は `KeyboardInterrupt` を送出せず**プロセスを即座に終了**させる。このため `main()` の `finally` ブロックにある `_print_daily_summary_to_terminal()`（集計の端末表示）と Discord 終了通知が一度も実行されなかった。

### 変更内容

**① `_sigint_handler` を「KeyboardInterrupt 送出」方式に変更**

- 1 回目の Ctrl+C: 停止バナーを表示し `raise KeyboardInterrupt`
- → asyncio がメインタスクをキャンセル → `main()` の `finally` が走り、トレード集計の端末表示 + Discord 送信が実行される
- 2 回目の Ctrl+C（シャットダウン処理が固まった場合の保険）: 従来どおり `SIG_DFL` + `os.kill` で強制終了

**② エントリポイントで `KeyboardInterrupt` を捕捉**

`asyncio.run(main(...))` を `except KeyboardInterrupt: pass` で囲み、終了時に不要なトレースバックが出ないようにした。

### 影響

- Ctrl+C 停止時に当日のトレード集計（取引ゼロなら「本日のトレードなし」）が端末に表示されるようになった
- Discord にも終了メッセージ + 集計が送信されるようになった
- 集計表示中にもう一度 Ctrl+C を押せば即座に強制終了できる（バナーに案内を追記）

---

## v3.9.37 (2026-05-22) — AlphaVantage 連携を全廃 (Finnhub に一本化)

### 背景

AlphaVantage は Finnhub の「フォールバック専用」(Finnhub 未設定時のみ呼ばれる) として残していたが、運用上 Finnhub が常に設定されており実際には一度も呼び出されない死蔵コードになっていた。

加えて起動バナーに `ALPHAVANTAGE_API_KEY: ✅ 設定済み（待機中: Finnhub 優先のため呼出されない）` と表示され、受講生に「設定が必要なキー」と誤解させる原因になっていた。不要な機密キー項目は持たない方が安全なため、関連コードを全廃した。

### 変更内容（moomoo_trade_v1.py）

- ファイル冒頭 docstring の `.env 設定項目` から `ALPHAVANTAGE_API_KEY` 行を削除、外部ニュース説明を「Finnhub」のみに修正
- `env_snapshot` の `news_sources` から `AlphaVantage` エントリを削除
- `ALPHAVANTAGE_API_KEY` env 読み取り・`_AV_URL` 定数を削除
- `_fetch_alphavantage()` 関数を削除
- `fetch_external_news()` を Finnhub のみの単純分岐に簡素化
- `external_news_loop` の起動ガードを `if not FINNHUB_API_KEY:` に簡素化、`source` ラベルを `Finnhub` 固定に
- 起動バナー（外部ニュースソース一覧 / `.env 読み込み状態`）から AlphaVantage 表示行を削除

### 変更内容（setup_wizard.py）

- `_SENSITIVE_KEYS` / `_WIZARD_MANAGED_KEYS` から `ALPHAVANTAGE_API_KEY` を削除
- STEP 9 の「Alpha Vantage 既存値引き継ぎ」処理を削除
- `.env` 書き出しから `ALPHAVANTAGE_API_KEY=` 行を削除
- バリデーション（クォート囲み検査リスト・形式チェック ⑥ AlphaVantage 形式）を削除し、後続の番号を繰り上げ
- 更新履歴コメント（v1.x 行）は当時の記録としてそのまま保持

### 影響・互換性

- 既存 `.env` に `ALPHAVANTAGE_API_KEY` が残っていてもボット・Wizard 共に無視するだけで害はない（再実行時に Wizard 管理対象外となり、行はそのまま温存される）
- 外部ニュース取得は Finnhub（未設定なら RSS）に一本化。Finnhub を設定済みの受講生に機能上の変化はない

---

## v3.9.36 (2026-05-22) — CLOSE_BEFORE_INACTIVE を「移行 N 分前」決済に改修

### 背景

v3.9.35 で実装した CLOSE_BEFORE_INACTIVE は「取引不可セッションへ移行を**検知した時点**」(=移行後) で決済する設計だった。レビューで以下の問題が判明:

1. **不利な価格での決済** — 移行後は既に低流動性セッションに入っており、決済価格が悪化する
2. **OVERNIGHT 移行後はデモ口座が決済不能** — moomoo デモは OVERNIGHT 取引非対応のため、移行後に決済注文を出しても約定しない（ポジションが取り残される）

→ まだ取引可能な現セッション内（境界の数分前）に決済する設計へ修正。

### 変更内容

**① 決済タイミングを「移行後」→「移行 N 分前」に変更**

- 新規 env `CLOSE_BEFORE_INACTIVE_MIN`（デフォルト 5・範囲 1-60 分）
- セッション境界の `CLOSE_BEFORE_INACTIVE_MIN` 分前に、まだ取引可能な現セッション内で全決済

**② セッション境界判定ヘルパーを新設**

- `_next_session_boundary()` — 現在 ET 時刻から次のセッション境界時刻と次セッション名を返す（早期クローズ日対応）
- `_should_preclose_before_inactive()` — 次セッションが「取引不可」(OVERNIGHT または発注しない設定) かつ境界まで N 分以内なら True

**③ market_schedule_loop に preclose チェックを追加**

- 毎分ポーリングで `_should_preclose_before_inactive()` を判定
- 発火時は `close_all_for_weekend()` で全決済 + Discord 通知
- boundary をキーに dedup（同一境界で 1 回のみ発火）

**④ v3.9.35 の「移行時決済」ブロックを削除**

`market_schedule_loop` の `_cur_disabled` 分岐にあった移行後決済コードを撤去（preclose へ置き換え）。

### 動作例

```
平日 AFTERHOURS 19:55 ET (CLOSE_BEFORE_INACTIVE=true / MIN=5)
  → 次セッション = OVERNIGHT 20:00 (取引不可)
  → 境界まで 5 分 → preclose 発火
  → AFTERHOURS 内 (まだ取引可能) で全決済
  → 20:00 OVERNIGHT 移行時にはポジションゼロ
```

### 後方互換

- 既存 .env 変更不要（`CLOSE_BEFORE_INACTIVE` 未設定なら従来どおり何もしない）
- 新規 env `CLOSE_BEFORE_INACTIVE_MIN` は任意（未設定で 5 分）
- `BOT_VERSION` v3.9.35 → v3.9.36

---

## v3.9.35 (2026-05-22) — CLOSE_BEFORE_INACTIVE 実装 + モメンタム Phase 1 実発注対応

### 背景

Wizard↔プログラムの整合性レビューで、Wizard が書き込むが本体が参照しない設定キーが 2 件判明:
- `CLOSE_BEFORE_INACTIVE` — 本体での参照が 0 件（「移行前全決済」STEP が機能していなかった）
- `MOMENTUM_LIVE_TRADING` — 変数に読み込むだけで参照する処理がなかった（「実発注モード」が機能していなかった）

デモ取引期間中で実害がないため、両方を本体に実装して整合性を解消。

### ① CLOSE_BEFORE_INACTIVE の実装

「発注しない設定」(`CONFIDENCE_*=2.00` sentinel) のセッションへ切り替わった瞬間に、保有ポジションを全決済する。

- 新規 env `CLOSE_BEFORE_INACTIVE` (デフォルト false) を本体で読み取り
- `market_schedule_loop` の `_cur_disabled` 移行検知ブロックで、true なら `close_all_for_weekend()` を呼び出して全決済
- 「N 分前」予測ではなく「移行を検知した時点」での決済 (毎分ポーリングで検知)
- `close_all_for_weekend()` に `log_prefix` 引数を追加し、ログを「移行前全決済」プレフィックスで出力

### ② モメンタム Phase 1 実発注の実装

`MOMENTUM_LIVE_TRADING=true` のとき、`momentum_shadow_loop` が live-eligible シグナルを実発注する。

- `momentum_shadow_loop(trd_env)` に変更 (実発注に trd_env が必要)
- 起動条件を調整: 実発注モードなら `DATA_COLLECT` 不問で起動
- シグナル検知後、`_will_live_order` 判定 (LIVE_TRADING + live-eligible + 強度>0 + 価格取得済) が真なら `place_buy` / `place_short` を `qty` 明示指定で呼出
- 発注額 = BUDGET × MOMENTUM_MAX_PCT × シグナル強度係数 (50/75/100%) で `calc_order_size` を回避
- **実発注対象は `MOMENTUM_ENABLED_SIDES` のみ** (SPY:SHORT / QQQ:SHORT / QQQ:BUY)。SMH / IWM はシャドー専用のまま
- 発注後のポジションは既存の `risk_monitor_loop` が損切り/トレール/時間切れで自動管理
- デモ口座での SHORT は `place_short` 内でシャドー SHORT に自動転送
- 起動バナーに「⚡ 実発注モード (Phase 1)」/「シャドー観察モード (Phase 0)」を表示

### ③ setup_wizard.py — モメンタム発注対象の明記

モメンタム STEP の概要に発注対象を明記:
- SPY / QQQ … 実発注の対象
- SMH / IWM … シャドー観察のみ（実発注対象外・データ収集検証中）

### ④ その他の Wizard 表示修正 (同時リリース)

- 「移行前全決済」STEP の「5分前」表記 → 本体実装に合わせて記述を整合化
- モメンタム最大発注比率ガイドの括弧書き（手数料の技術的説明）を簡潔化
- アラート音 STEP: 「はい」を選ぶ前に「テスト音が再生される」旨を事前案内

### ⑤ ボット本体の冗長な版履歴ヘッダを整理

`moomoo_trade_v1.py` 冒頭のインライン版履歴 (v3.7.2〜v3.9.34 の約 675 行) を、最新 6 版の 1 行要約に圧縮。詳細は本 CHANGELOG.md に集約 (重複の解消)。

### 後方互換

- 既存 .env 変更不要 (`CLOSE_BEFORE_INACTIVE` / `MOMENTUM_LIVE_TRADING` 未設定なら従来動作)
- `BOT_VERSION` v3.9.34 → v3.9.35

---

## v3.9.34 (2026-05-22) — モメンタム発注額を MOMENTUM_MAX_PCT 方式に変更 + Wizard v1.8

### 背景

受講生の多くは BUDGET $10,000 前後。旧 RISK_LEVEL の投入率 10-25% では 1 回の発注が $1,000-$2,500 にとどまり、moomoo の往復手数料 (約定代金の 0.495%×2、最大 $44) を吸収できず**手数料負け**が多発していた。$5,000 未満の発注は損益分岐に約 1% 以上の値動きが必要。

### ① MOMENTUM_MAX_PCT 新設 (Wizard v1.8 で設定・既定 80)

発注額の決定方式を変更:

```
最終発注額 = BUDGET × MOMENTUM_MAX_PCT × シグナル強度係数
シグナル強度係数 = 50% / 75% / 100% / 0%（弱 / 中 / 最良帯 / 強は発注なし）
```

優先順位:
1. `MOMENTUM_MAX_PCT` (Wizard v1.8・新方式) が最優先
2. 旧 `MOMENTUM_ORDER_SIZE_PCT` が .env にあれば後方互換で使用
3. どちらもなければ `RISK_LEVEL` プリセットの `order_pct` にフォールバック

既定 80% で BUDGET $10,000 なら最大 $8,000、中シグナル時 $6,000 を確保し手数料を吸収できる水準に。

### ② RISK_LEVEL を投入額から切り離し

`MOMENTUM_RISK_LEVEL` は「1 回損失上限 / 1 日損失上限 / 急変動ガード」のみを制御。投入率は `MOMENTUM_MAX_PCT` に分離。RISK_LEVEL プリセットの `order_pct` は後方互換フォールバックとしてのみ残置。

### ③ setup_wizard.py v1.8 — モメンタム STEP を 4 項目構成に再編

| 項目 | 内容 |
|---|---|
| [1] シグナル発火頻度 | MOMENTUM_LEVEL (1-5) |
| [2] 動作モード | シャドー観察のみ / 実発注も行う (新規) |
| [3] リスク許容度 | MOMENTUM_RISK_LEVEL (投入% 表示を削除・損失上限のみ) |
| [4] 最大発注比率 | MOMENTUM_MAX_PCT (50-100%・BUDGET 連動の金額表示 + 選び方ガイド) |

[4] では BUDGET から各 % の発注額を計算表示し、$5,000 未満には手数料負け警告を表示。

### setup_wizard.py v1.8 のその他の変更 (同時リリース)

- 表示文言「推奨」→「標準」に統一 (投資助言と受け取られないため)
- API キー漏洩防止: FINNHUB_API_KEY / ALPACA_API_KEY_ID 入力欄を secret 化、設定警告のキー表示をマスク
- データ収集の説明をニュース見出し送信の実態に修正 (見出しは送信される旨を明記)
- モメンタム STEP の「上級者向け（任意）」.env 案内を削除
- アラート音 STEP: macOS 音源パス選択を削除、有効化選択後に実際にテスト音を再生
- 高ボラ銘柄の損切り自動調整の説明を STEP 2 に追加 (v1.7)

### 後方互換

- 既存 .env の `MOMENTUM_ORDER_SIZE_PCT` は引き続き有効 (優先順位 ②)
- `BOT_VERSION` v3.9.33 → v3.9.34
- `setup_wizard.py` v1.7 → v1.8

---

## v3.9.33 (2026-05-22) — シャドーモメンタムに IWM (小型株 ETF) を追加

### 背景

既存のモメンタム観察銘柄 SPY / QQQ / SMH は **すべて大型株・テック偏重**で相関が高い。分散検証のため、Russell 2000 連動の **IWM (米国小型株 ETF)** をシャドーモメンタム観察に追加。小型株は値動きの源泉が異なる (金利感応度・国内経済依存) ため、大型株とは違うモメンタム挙動が観測できる可能性がある。

5/21 シャドーレポートのレビューに基づく判断:
- IWM 追加は **シャドー専用で開始** (リスクゼロ・データ取得のみ)
- SMH-BUY が「ボラ高い銘柄は日替わりで不安定」と実証済 → IWM も実発注はデータ検証後に判断

### 実装内容

| # | 変更 | 詳細 |
|---|---|---|
| ① | `MOMENTUM_SYMBOLS` デフォルト | `QQQ,SPY,SMH` → `QQQ,SPY,SMH,IWM` |
| ② | `_MOMENTUM_LEVEL_PRESETS` | 全 5 レベルに IWM の (5m, 15m) 閾値を追加 |
| ③ | `_INDEX_PRICE_HISTORY` | IWM を価格履歴トラッキング対象に追加 |
| ④ | `_backfill_index_price_history` | `_INDEX_PRICE_HISTORY.keys()` 経由で IWM も自動バックフィル |
| ⑤ | `MOMENTUM_SIZE_MULTIPLIER` | IWM=0.85 を追加 (新 env `MOMENTUM_SIZE_MULTIPLIER_IWM`) |
| ⑥ | `[市場状態]` ログ表示 | QQQ/SPY/SMH に IWM を追加 (4 銘柄表示) |
| ⑦ | `MOMENTUM_ENABLED_SIDES` | **変更なし** → IWM はシャドー専用 (実発注対象外) |

### IWM 閾値の初期推定値

IWM のボラは SPY の約 1.2-1.3 倍 ≒ QQQ をやや上回る水準。QQQ 基準で少し広げた推定値で開始:

| Level | QQQ (参考) | **IWM (初期推定値)** |
|---|---|---|
| 1 (超厳格) | 0.22 / 0.45 | **0.24 / 0.48** |
| 2 (厳格) | 0.18 / 0.36 | **0.20 / 0.40** |
| 3 (標準) | 0.15 / 0.30 | **0.16 / 0.33** |
| 4 (緩和) | 0.12 / 0.24 | **0.13 / 0.26** |
| 5 (最緩和) | 0.10 / 0.20 | **0.11 / 0.22** |

→ あくまで推定。1-2 週間のシャドー観察データで実測ボラに基づき再キャリブレーション予定。

### IWM の位置づけ

- **シャドー専用** — SMH と同じく `MOMENTUM_ENABLED_SIDES` に含めない
- 観察ログには `live可` / `シャドー専用` のタグが付く (v3.9.32 で追加済)
- Phase 1 (実発注) 移行後も、IWM は実発注対象外。データ検証完了まで観察のみ

### 後方互換

- 既存 .env 変更不要 (`MOMENTUM_SYMBOLS` 未設定なら IWM 込みで動作)
- 新規 env `MOMENTUM_SIZE_MULTIPLIER_IWM` は任意 (未設定で 0.85)
- `BOT_VERSION` v3.9.32 → v3.9.33

---

## v3.9.32 (2026-05-22) — 高ボラ銘柄のリスク調整 + モメンタム銘柄方針の整理

### 背景

5/21 日次レポート: NVDA 利用者I -$41.80 / 利用者L -$28.99 — NVDA $223 の 0.5% 損切り = わずか $1.12 で、日中ノイズで強制損切りが誤発動していた。「高ボラ銘柄は損切り幅を広げるべき」だが、幅を広げると 1 トレードの損失額が増える。

### ① 高ボラ銘柄の損切り幅拡大 + ポジションサイズ縮小 (リスクパリティ)

**設計:** 損切り幅を `N` 倍に拡げる代わりにポジションサイズを `1/N` に縮小し、1 トレードの想定最大損失額 (ドル) を ETF と同水準に保つ。

```
例: MAX_LOSS_PCT=0.50% / STOCK_HIGHVOL_LOSS_MULT=2.5
  ETF  : 損切り 0.50% / サイズ 100%  → 最大損失 = サイズ × 0.50%
  NVDA : 損切り 1.25% / サイズ  40%  → 最大損失 = (サイズ×0.4) × 1.25%
                                       = サイズ × 0.50%  ← ETF と同じ
```

**対象銘柄** `_HIGHVOL_SYMBOLS`: NVDA / TSLA / AMD / MU / AVGO / SMH / MRVL / ARM / SMCI / COIN / MSTR / PLTR / SOXL / SOXX

**新規 env**: `STOCK_HIGHVOL_LOSS_MULT` (デフォルト 2.5、0 で無効化)

**適用箇所:**
- `risk_monitor_loop` の強制損切り判定 (`_effective_max_loss_pct`)
- `place_buy` / `place_short` の発注額計算 (`_symbol_loss_mult` で ÷)
- シャドー SHORT の損切り判定 (`_shadow_short_check_exit`)

**ヘルパー新設:** `_symbol_loss_mult(symbol)` / `_effective_max_loss_pct(symbol, base)`

### ② モメンタム: シャドー観察と実発注対象の分離

**旧 (v3.9.25):** `MOMENTUM_ENABLED_SIDES` に含まれないサイドは `continue` でスキップ → シャドー観察ログにも記録されなかった。

**新 (v3.9.32):** 「シャドー観察は**全銘柄・全サイド記録**、Phase 1 実発注対象は `MOMENTUM_ENABLED_SIDES` のみ」に分離。

- SMH は `MOMENTUM_ENABLED_SIDES` に含めないことで **シャドー専用** として動作
  - SMH の観察データ (シグナル検知・60min PnL) は引き続き取得
  - SMH は Phase 1 でも実発注対象外
- 5/13-5/21 データで SMH-BUY は累計マイナス (約 -0.56% / 勝率 45%)、SMH-SHORT は安定 (4日中3日プラス) と判明。両方ともシャドーで観察継続、実発注は見送り
- 観察ログ・サマリに `live可` / `シャドー専用` のタグを記録 (`live_eligible`)

### ③ setup_wizard.py v1.7

STEP 2 (リスク管理) に高ボラ銘柄の損切り自動調整の説明ボックスを追加:
- 「NVDA 等は損切り幅 2.5 倍 + サイズ 1/2.5 倍 → 最大損失額は ETF と同水準」
- 「プログラムが自動調整・設定不要」「無効化は `STOCK_HIGHVOL_LOSS_MULT=0`」
- 入力した `MAX_LOSS_PCT` から高ボラ銘柄の実効損切り% を計算して表示

### 後方互換

- 既存 .env 変更不要 (新規 env `STOCK_HIGHVOL_LOSS_MULT` は任意・未設定で 2.5)
- `BOT_VERSION` v3.9.31 → v3.9.32
- `setup_wizard.py` v1.6 → v1.7

---

## v3.9.31 (2026-05-22) — 5/21 レポート分析を受けた 5 件の修正

### 背景

5/21 の MOMENTUM シャドーレポート + 日次レポート + エラーログ (524 件) を分析し、構造的問題 5 件に対応。

### ① ショートカバー無限リトライの停止ロジック

**5/21 利用者T事例:**
```
22:31〜22:42 (11分間) QQQ ショートカバーが moomoo に拒否され続け、
時間切れ決済が 60 秒ごとに無限リトライした:
  【QQQ】[ショートカバー] 発注失敗: Order side must be BUY or SELL...
  【QQQ】⏰ 時間切れ決済の発注が全失敗 → 60秒後に再試行  ← 無限ループ
```

**修正:**
- `TickerState` に `close_fail_count` フィールド追加
- 決済発注が `_CLOSE_FAIL_GIVEUP_LIMIT` (=3) 回連続失敗 → `sync_positions` 強制実行
- sync 後に `position_qty == 0` → 既に決済済みと判断、`entry_time` クリアでリトライ停止
- sync 後もポジション残存 → Discord で手動決済を促す + カウント部分リセット

### ② get_order_book ret=-1 のログレベル降格

`WARNING → DEBUG`。`get_stock_quote` フォールバックが正常動作するため実害なし。5/21 エラーログ 524 件のうち 293 件 (56%) がこれで、受講生が誤ってエラーと認識する問い合わせが多発していた。ファイルログには引き続き記録。

### ③ health_warning_loop 新設 (5 分おきの重大問題再警告)

初回しか目立たず WARNING に埋もれる 2 つの重大問題を、5 分間隔で赤字ボックス枠で端末に再表示:

| 対象 | 警告条件 |
|---|---|
| Anthropic クレジット残高ゼロ | `_anthropic_credit_exhausted=True` の間ずっと |
| Alpaca News 接続不全 | 連続認証失敗 3 回以上 / または 20 分以上正常受信なし |

`_alpaca_news_health` 状態を `alpaca_news_loop` 内で更新 (認証成功/失敗/接続エラー時)。

### ④ 経済指標プロンプトルール強化 (PMI/ISM 追加)

**5/21 事例:** `USA S&P Global Manufacturing PMI For May 55.3 Vs 53.8 Est` (予想超過) を AI が「景気堅調 → 株高」と解釈し SPY-LONG を 4 件発火 → 全敗 -$5.16。

- 既存の経済指標ルール (CPI/PPI/PCE) に **成長・景況感指標 (GDP/小売/PMI/ISM)** を明示追加
- PMI/ISM の予想超過時は「インフレ再加速 → FRB タカ派 → 株安」連想が優勢 → **SPY-LONG (score=+1) 禁止**、score=0 基本
- カテゴリルーティングにも PMI/ISM/PPI を追加 (→ MACRO)
- `_PROMPTS_VERSION` 1.8.0 → **1.9.0**

### ⑤ 市場コメンタリー記事フィルタ新設 (`_MARKET_COMMENTARY_REGEX`)

**5/21 事例:** `Sector Update: Tech Stocks Advance Thursday Afternoon` を Finnhub(NVDA) 経由で AI が NVDA-STOCK 判定 (conf=0.66) → 強制損切り -$28.99。

- `_OBSERVATION_HEADLINE_REGEX` は ETF モード専用だが、本 regex は **ETF・個別株 両モード**でブロック
- パターン: `Sector Update` / `{セクター} Stocks {値動き動詞}` / `Stocks/Markets Edge/Mixed` 型
- 純粋な市況サマリはどの銘柄にとっても売買材料にならないため両モードで除外
- `block_stage="market_commentary"` で観察ログ記録
- テスト 12 ケース全合格 (ブロック 7 / 通過 5)

### 後方互換

- 既存 .env 変更不要
- Wizard 変更不要
- `_PROMPTS_VERSION` 1.8.0 → 1.9.0 (起動バナーで確認可)
- `BOT_VERSION` v3.9.30 → v3.9.31

### 実装規模

| ファイル | 追加行数 |
|---|---|
| moomoo_trade_v1.py | +約 160 行 |
| CHANGELOG.md | +約 75 行 |

---

## v3.9.30 (2026-05-21) — シャドー成績を日次サマリに統合表示

### 背景

受講生からの要望:
> 自分の Bot のシャドーSHORT / シャドーモメンタムの結果が見られない。
> GAS スプレッドシートには PAN しかアクセスできないし、PDF レポートは全体集計だけ。

→ Discord 通知と Ctrl+C 終了時の端末表示の **両方** に「自分のシャドー成績」を統合。

### ① 当日シャドー集計バッファ新設

```python
_today_shadow_short: list[dict] = []      # 決済確定したシャドー SHORT を集計
_today_shadow_momentum: list[dict] = []   # モメンタム シグナル検知をカウント

def _record_today_shadow_short(rec): ...
def _record_today_shadow_momentum(rec): ...
```

ET 日付変化で `_maybe_reset_today_trades()` が実トレードと同時にリセット。

### ② 既存処理にフック追加

| 場所 | フック内容 |
|---|---|
| `_shadow_short_close()` | 決済確定時に `_record_today_shadow_short()` 呼出 |
| `momentum_shadow_loop()` | シグナル検知時に `_record_today_shadow_momentum()` 呼出 |

### ③ サマリ表示

`_format_daily_summary()` 末尾に「★ シャドー成績」セクションを追加。Discord 通知と端末出力の両方に自動反映。

**表示例:**

```
📊 本日のトレード集計  2026-05-21 (Wed) 15:50 ET
========================================================
合計 5件   勝率 80% (4勝 1敗)
PnL  +12.34   平均勝ち +5.20 / 平均負け -8.46
...
────────────────────────────────────────────────────────
★ シャドー成績 (もし発注していたら…の集計)
────────────────────────────────────────────────────────

▼ シャドーSHORT  8件  勝率 75% (6勝 2敗)
  仮想 PnL  +28.50
    SPY      3件  勝率 67%   +15.20
    QQQ      3件  勝率 100%  +10.30
    SMH      2件  勝率 50%    +3.00

▼ シャドーモメンタム  シグナル検知 12件
    SPY-SHORT       5件
    QQQ-SHORT       4件
    QQQ-BUY         3件
```

### ④ 実トレードゼロでもシャドーは表示

旧版: 実トレードゼロなら `_format_daily_summary()` は None を返して通知なし
新版: 実トレードゼロでもシャドー集計があれば簡易ヘッダ + シャドー成績を表示

これにより、ニュース判定で発注に至らなかった日でも「シャドーで何件検知されたか」を確認可能。

### 後方互換

- 既存 .env 変更不要
- Wizard 変更不要
- プロンプト変更なし (v1.8.0 据置)
- 既存日次サマリのフォーマット (実トレード部分) は完全保持
- `BOT_VERSION` v3.9.29 → v3.9.30

### 実装規模

| ファイル | 追加行数 |
|---|---|
| moomoo_trade_v1.py | +約 90 行 (バッファ + サマリブロック + 2 箇所フック) |
| CHANGELOG.md | +約 70 行 |

---

## v3.9.29 (2026-05-21) — 5/20 全受講生エラー分析を受けた 3 件の修正

### 背景

5/20 の Excel エラーログ全受講生集計 (494 件) で構造的問題が判明:

| エラー種別 | 件数 | 影響受講生 | 構造性 |
|---|---|---|---|
| websockets 未インストール | 4 件 | 2 名 (利用者J / 利用者I) | 🔴 永久スキップ |
| sync_positions: Asset Category 不足 | 29 件 | 1 名 (利用者K) | 🔴 ポジション同期完全停止 |
| 「既存ショートあり or 余力不足」誤解 | 多数 | 全受講生 | 🟡 受講生問合せ多発 |

### ① websockets 未インストール時の 5 分間隔リカバリ警告

**旧版の問題:**
```python
except ImportError:
    log.warning("websocketsライブラリが未インストール...")
    return  # ← 永久スキップ。受講生が気づかず Alpaca News 来ない状態継続
```

**新版:** 赤文字ボックス枠の警告を log.error() で出力し、5 分後に再試行 (インストール後の自動復旧可能)。

```
╔════════════════════════════════════════════════════════════════╗
║  🚨  Alpaca News 起動失敗: websockets ライブラリが未インストール  ║
╠════════════════════════════════════════════════════════════════╣
║  対処: pip install websockets                                 ║
║  影響: Alpaca News のニュース取得が停止                          ║
║        Yahoo Finance / Finnhub は引き続き動作                   ║
║  ※ この警告は 5 分ごとに表示されます (インストールで自動解除)    ║
╚════════════════════════════════════════════════════════════════╝
```

ANSI 赤文字 + ボックス描画文字で視認性を最大化。終端ターミナルが ANSI 非対応でも文字情報は読める設計。

### ② sync_positions の asset_category 必須化対応

**旧版の問題:**
- 新版 moomoo SDK で `position_list_query` の `asset_category` 引数が**必須化**
- 旧パターン順は `asset_category` なしを先に試行 → 新 SDK 環境で即時エラー
- 利用者K の環境で 29 件すべて失敗 → ポジション同期不能

**新版:** パターン順を変更 + TypeError 自動 fallback 追加 (両 SDK 対応):

```python
# 新パターン順 (実口座)
_patterns = [
    (REAL_ACC_ID, False, _asset_cat_us),  # ★ 新 SDK 優先
    (0,           False, _asset_cat_us),
    (REAL_ACC_ID, False, None),           # 旧 SDK fallback
    (0,           False, None),
]

# TypeError 自動 fallback
try:
    ret, data = ctx.position_list_query(**_kwargs)
except TypeError as _te:
    if "asset_category" in str(_te) and _asset_cat is not None:
        _kwargs.pop("asset_category", None)
        ret, data = ctx.position_list_query(**_kwargs)
    else:
        raise
```

これで新 SDK / 旧 SDK 両方の環境で `position_list_query` が成功。

### ③ 「既存ショートあり or 余力不足」ログメッセージの分離

**旧版:**
```
全銘柄スキップ（既存ショートあり or 余力不足）
```

→ 受講生が「証券口座の残高不足?」と誤解。5/20 PAN ログの「余力不足」2 件はすべて「既存ショート保有中」が原因。

**新版:** スキップ理由を分類して明示:

```python
_skipped_existing_short  # 既存 SHORT 保有中: SPY,QQQ
_skipped_with_long       # LONG 保有中・SHORT 不可: SMH
_skipped_etf_reentry     # ETF 再エントリー禁止: QQQ
# どれにも該当しなければ "place_short() 内でブロック (詳細は直前ログ参照)"
```

ログ例:
```
【SPY】 → 空売り試行: 全銘柄スキップ (既存 SHORT 保有中: SPY,QQQ)
```

### 後方互換

- 既存 .env 変更不要
- Wizard 変更不要
- プロンプト変更なし (v1.8.0 据置)
- `BOT_VERSION` v3.9.28 → v3.9.29

### 実装規模

| ファイル | 追加行数 |
|---|---|
| moomoo_trade_v1.py | +約 60 行 (3 件の修正) |
| CHANGELOG.md | +約 90 行 |

---

## v3.9.28 (2026-05-21) — シャドー SHORT + 起動ログ表現改善

### 背景

受講生からの 2 件のフィードバックに対応:

1. **「デモでも SHORT 戦略の PnL を見たい」** — moomoo Japan のデモ口座は API 経由の空売り非対応のため、これまで `place_short()` は即スキップ。デモ環境では LONG 側のみ集計され、SHORT 戦略の有効性検証ができなかった。
2. **「起動ログの『LV1 株価取得確認』が紛らわしい」** — OpenD 画面の市場データ権限「LV3」と混同し、サポート AI が誤って「LV1 のままだと問題」と回答する事例が発生。

### ① シャドー SHORT ライフサイクル シミュレーション (新規)

`place_short()` が SELL_SHORT 判定で呼ばれたとき、デモ口座なら実発注はせず **仮想ポジションとして記録**。LONG と対称の決済ルールで PnL をシミュレートし、観察ログ (GAS) に記録する。

**エントリー** (`_shadow_short_open()`):
- 既存の `get_quote()` で エントリー価格取得
- 既存の `calc_order_size(confidence)` で発注額算出 (山型サイズ配分)
- `_SHADOW_SHORT_POSITIONS[obs_id]` に仮想ポジション登録
- GAS に `block_stage="shadow_short_entry"` で送信

**決済ルール** (LONG と対称):

| 決済理由 | LONG 条件 | SHORT 条件 (本機能) |
|---|---|---|
| 強制損切り | 価格が entry の -MAX_LOSS_PCT 下落 | 価格が entry の **+MAX_LOSS_PCT 上昇** |
| トレール作動 | 高値が entry の +TRAIL_TRIGGER_PCT 到達 | **安値が entry の -TRAIL_TRIGGER_PCT 到達** |
| トレール決済 | 高値から TRAIL_DROP_PCT 戻り | **安値から TRAIL_DROP_PCT 戻り (上昇)** |
| 時間切れ | TIMEOUT_EXIT_MINUTES 超過 | 同左 |

**監視ループ** (`shadow_short_exit_loop()`):
- 30 秒間隔で各オープン仮想ポジションの決済条件チェック
- 決済発動時に PnL 計算 → GAS に `type="shadow_short_exit"` で送信
- PnL 計算: `(entry_price - exit_price) × qty` (SHORT 損益)

**動作条件**:
- `SHADOW_SHORT_ENABLED=true` (デフォルト・全ユーザ ON)
- `trd_env == TrdEnv.SIMULATE` (デモ口座のみ)
- `DATA_COLLECT` の有無は不問 (v3.9.28 で独立化・全デモ環境で動作)

**設計判断**: 既存の `_log_observation()` 系は `DATA_COLLECT=true` ゲートで動作するが、本機能は受講生全員に SHORT 検証データを提供する目的で**独立 GAS 送信**を採用。STUDENT_NAME が未設定でも空文字で送信される (GAS 側で「anonymous」として処理可能)。

**GAS 受信 type** (新規):
- `observation` の `block_stage="shadow_short_entry"` (既存 type に新 stage 追加)
- `shadow_short_exit` (新規 type)

### ② 起動ログ「LV1 株価取得確認」→「気配値取得確認」

```python
# 旧
log.info(f"[OK] moomoo 接続成功・LV1 株価取得確認（{_test_symbol}: ${_test_price:.2f}）")
# 新
log.info(f"[OK] moomoo 接続成功・気配値取得確認（{_test_symbol}: ${_test_price:.2f}）")
```

コメント行も「US Securities LV1 権限確認」→「株価ストリーミング権限確認」に変更。機能変更なし。

### 後方互換

- 既存 .env 変更不要 (`SHADOW_SHORT_ENABLED` 未設定なら true で動作)
- Wizard 変更不要
- プロンプト変更なし (v1.8.0 据置)
- 実発注ロジックへの影響ゼロ (デモ分岐内のみで完結)
- `BOT_VERSION` v3.9.27 → v3.9.28

### 実装規模

| ファイル | 追加行数 |
|---|---|
| moomoo_trade_v1.py | +約 220 行 (シャドー SHORT モジュール + place_short 連携 + ループ登録) |
| CHANGELOG.md | +約 90 行 |

---

## v3.9.27 (2026-05-21) — 5/20 損失分析を受けた regex 拡張 2 件

### 背景

5/20 受講生日次レポートの分析結果、v3.9.21-26 のフィルタを通り抜けた **2 つの新パターン** が当日損失の主因と判明。

| パターン | 損失例 | 件数 | PnL 合計 |
|---|---|---|---|
| 新興国マクロ → 米テック ETF | "Zambia and Angola announce rate cuts, boosting the construction industry" → QQQ-LONG | 3 名 | **-$75.04** |
| 自動車個別企業 → TECH ETF | "GM to shift some production from Asia to Mexico from 2027" → TECH-LONG | 3 名 | -$8.42 |

特に Zambia/Angola 案件は **5/20 損失 -$70.51 の 79%** を占めるピンポイント被害。
構造的に再発する性質なので regex 拡張で根絶。

### ① 新興国マクロ regex 追加 (`_OBSERVATION_HEADLINE_REGEX` 拡張)

28 か国の金融政策アナウンスを AI 呼出前にハードブロック。

```python
# v3.9.27 追加: 新興国の金融政策ニュース
# 中国 / 日本 / EU / UK / 米国は US 連動性が高いため意図的に除外
r'\b(?:zambia|angola|argentina|venezuela|nigeria|kenya|egypt|ghana|'
r'pakistan|bangladesh|sri\s+lanka|vietnam|indonesia|philippines|'
r'turkey|south\s+africa|chile|peru|colombia|brazil|mexico|'
r'thailand|malaysia|morocco|tunisia|kazakhstan|uzbekistan)\b'
r'(?:\s+and\s+\w+(?:\s+\w+)?)?'   # "Zambia and Angola" 連結対応
r'\s+(?:announces?|announced|cuts?|cutting|raises?|raising|'
r'hikes?|hiking|lowers?|lowering|holds?)\s+'
r'(?:interest\s+|policy\s+|key\s+)?(?:rates?|interest)'
```

- **対象国 28 ヵ国**: 米国と高い相関がない新興国に限定
- **意図的除外**: 中国 (Apple サプライチェーン)、日本 (USD/JPY)、EU/UK (キャリートレード)、米国 (FOMC)、インド (テック関連)
- **動詞**: announces / cuts / raises / hikes / lowers / holds (現在/過去/進行形)

### ② 自動車個別企業の生産移管 regex 追加

22 メーカーの生産地移管ニュースをハードブロック。

```python
# v3.9.27 追加: 自動車個別企業の生産地移管
r'\b(?:gm|general\s+motors|ford|stellantis|toyota|honda|nissan|hyundai|kia|'
r'volkswagen|vw|bmw|mercedes|audi|mazda|subaru|mitsubishi|chrysler|jeep|'
r'rivian|lucid|polestar|tesla|byd|nio|xpeng|li\s+auto)\s+'
r'(?:to\s+|plans?\s+to\s+|will\s+|is\s+)?'
r'(?:shifts?|moves?|relocates?|transfers?|migrates?|shifting|moving|'
r'relocating|transferring|migrating)\s+'
r'(?:some\s+|all\s+|partial\s+|its\s+|the\s+)?'
r'(?:production|manufacturing|assembly|operations|factory|plant)'
```

- **対象 22 メーカー**: 米系 / 欧州系 / 日系 / 韓国系 / 中国 EV / EV 専業
- **動詞 9 種**: shift / move / relocate / transfer / migrate + 各活用形
- **名詞 6 種**: production / manufacturing / assembly / operations / factory / plant

### 検証 (30 ケース全合格)

| カテゴリ | 件数 | 結果 |
|---|---|---|
| 新興国マクロ 新規ブロック (Zambia/Argentina/Turkey/Brazil/South Africa 等) | 7 | ✅ 全件ブロック |
| 自動車生産移管 新規ブロック (GM/Ford/Toyota/Tesla/VW/BMW/Honda/Stellantis/Nissan) | 9 | ✅ 全件ブロック |
| 既存パターン継続ブロック (Tech stocks today / Analyst Report / Stock market today / 訴訟) | 5 | ✅ 継続ブロック |
| 通常市場ニュース通過 (FRB / Fed / BOJ / ECB / China / UK / Apple iPhone) | 7 | ✅ 通過 (US連動性高) |
| 通常自動車ニュース通過 (GM Q1 beat / Tesla Model Y / Ford 投資 / Toyota 生産量) | 5 | ✅ 通過 |
| メキシコ通過 (Mexico approves trade agreement) | 1 | ✅ 通過 (国名のみで動詞なし) |
| 自動車関連通過 (BMW launches EV / VW ID.4 award) | 2 | ✅ 通過 |

→ **誤検出 (False Positive) ゼロ**、全 30 ケース期待通り。

### 5/20 想定改善

| 対策 | 想定改善 |
|---|---|
| Zambia/Angola 3 件 ブロック | **+$75.04** |
| GM Asia→Mexico 3 件 ブロック | **+$8.42** |
| **正味改善** | **+$83.46** |

5/20 当日 -$70.51 → 改善後 **+$13** 〜 **+$30** に転換見込み。

### 見送り項目 (5/20 レビューで提案も今回は対応せず)

| # | 提案 | 状況 |
|---|---|---|
| 1 | v3.9.25 時間帯フィルタの撤回 (5/20 適用シミュで逆効果) | ⏳ 継続検討 (あと 2-3 日のデータで再検証) |
| 2 | v3.9.26 MOMENTUM_MAX_SIGNAL_PCT の引き上げ/無効化 | ⏳ 継続検討 (相場局面依存と判明) |
| 3 | Alpaca プレマーケット停止 (NVDA/MU で 2 日連続 -$65) | ⏳ 継続検討 |
| 4 | Lv5 完全廃案 | ⏳ ユーザ要望 (preset slide) で対応済とみなす |

### 後方互換

- 既存 .env 変更不要
- プロンプト変更なし (regex のみ拡張)
- `_OBSERVATION_HEADLINE_REGEX` パターン数: 15 → 17
- `BOT_VERSION` v3.9.26 → v3.9.27
- プロンプトバージョンは v1.8.0 のまま

---

## v3.9.26 (2026-05-20) — モメンタムプリセット上方スライド + 強シグナル上限 + JV regex

### 背景

5/19 モメンタムシャドートレードレポート + 5/19 日次レポートの分析結果:

| 指標 | 値 |
|---|---|
| 5 日累計シャドー PnL | **-$101,594** (Phase 1 移行は当面保留) |
| ベスト LEVEL | **Lv1** (+1.19% / 勝率 61.5%) |
| ワースト LEVEL | **Lv5** (-0.36% / 勝率 41.2%) — 唯一マイナス |
| 強シグナル 0.8%+ | **2 日連続マイナス** (ノイズ域) |
| 新パターン損失 | Blackstone/Google AI JV 6 件 -$23.93 |

ユーザ要望「LEVEL 番号は維持し、しきい値だけ上にスライド (旧 Lv1 → 新 Lv3)」を採用。
ユーザの .env 書き換え不要で実績最良の閾値に切替可能。

### ① モメンタム LEVEL プリセット上方スライド

| Level | v3.9.25 (QQQ 5m/15m) | v3.9.26 (QQQ 5m/15m) | 位置 |
|---|---|---|---|
| 1 | 0.15 / 0.30 | **0.22 / 0.45** | 超厳格 (新規) |
| 2 | 0.12 / 0.24 | **0.18 / 0.36** | 厳格 (新規) |
| **3 ⭐推奨** | 0.10 / 0.20 | **0.15 / 0.30** | 標準 (= 旧 Lv1) |
| 4 | 0.08 / 0.16 | **0.12 / 0.24** | 緩和 (= 旧 Lv2) |
| 5 | 0.06 / 0.12 | **0.10 / 0.20** | 最緩和 (= 旧 Lv3) |

SPY と SMH も同じ比率で上方スライド。`_MOMENTUM_LEVEL_PRESETS_HISTORY` で旧プリセットを保持し、レポート集計のバージョン跨ぎ一貫性を担保。

### ② MOMENTUM_RISK_LEVEL プリセットも同方針でスライド

| Level | v3.9.25 (1回 / 1日 / 投入 / 急変動) | v3.9.26 |
|---|---|---|
| 1 | 0.20% / 0.50% / 10% / 0.70% | **0.10% / 0.25% / 5% / 0.50%** |
| **3 ⭐推奨** | 0.30% / 1.00% / 15% / 1.00% | **0.20% / 0.50% / 10% / 0.70%** (= 旧 Lv1) |
| 5 | 0.40% / 1.50% / 25% / 1.50% | **0.30% / 1.00% / 15% / 1.00%** (= 旧 Lv3) |

`_MOMENTUM_RISK_LEVEL_PRESETS_HISTORY` を併設。

### ③ MOMENTUM_MAX_SIGNAL_PCT 新設 (デフォルト 0.80)

```python
# 5/19 観察で 0.8%+ シグナルが 2 日連続マイナス → 過剰ボラ域は逆効果
if MOMENTUM_MAX_SIGNAL_PCT > 0 and abs(pct_5) >= MOMENTUM_MAX_SIGNAL_PCT:
    log.info(f"[モメンタムシャドー] {symbol} 5m {pct_5:+.2f}% は上限超 → スキップ")
    continue
```

- 0 設定で無効化 (上限なし)
- 起動バナーに「強シグナル上限 ±0.80%」表示

### ④ _STOCK_SURGE_REGEX に JV パターン追加

```python
# 5/19 "Blackstone Commits $5B Equity To Launch Joint Venture With Google" 6件 -$23.93
r'\b(?:commits?|invests?|pledges?)\s+\$?\d+(?:\.\d+)?\s*(?:b|billion|bn|m|million|mm)?\s+'
r'(?:equity\s+)?(?:to\s+|in\s+|for\s+)?'
r'(?:launch|form|create|establish|forge|expand|build)\s+'
r'(?:a\s+|the\s+|new\s+)?(?:joint\s+venture|jv|partnership|alliance)\b|'
r'\b(?:joint\s+venture|jv)\s+with\s+[A-Z]\w'
```

`_MARKET_WIDE_REGEX` が同居していない単一企業 JV ニュースを ETF score=1 から除外。

### ⑤ 観察ログ payload にモメンタム実プリセット値を併記

`block_stage="momentum_shadow"` のときに以下を追加:

```json
{
  "momentum_level": 3,
  "momentum_risk_level": 3,
  "signal_threshold_5m": 0.15,
  "signal_threshold_15m": 0.30,
  "stop_loss_pct": 0.50,
  "per_trade_loss_pct": 0.20,
  "daily_loss_pct": 0.50,
  "volatility_guard_pct": 0.70,
  "max_signal_pct": 0.80,
  "size_multiplier": 1.0,
  "order_size_pct": 10.0
}
```

→ レポート側は `bot_version` + `signal_threshold_*` で「実効プロファイル」基準の連結集計が可能。

### ⑥ 見送り項目

| 提案 | 状況 |
|---|---|
| Lv5 完全廃止 (Wizard 1-5 → 1-4) | ❌ 見送り → ユーザ要望によりプリセット上方スライドで対応 |
| Analyst Report: GE HealthCare 素通り調査 | ⏳ 該当ログ未取得・継続調査 |
| Alpaca(NVDA) プレマーケット停止 | ⏳ データ蓄積継続 |

### 後方互換

- 既存 .env 変更不要 (LEVEL 番号は維持、中身だけ更新)
- setup_wizard.py 変更不要 (Wizard の選択肢 1-5 はそのまま)
- プロンプト変更なし (v1.8.0 据置)
- `_STOCK_SURGE_REGEX` パターン数: 20 → 22
- `BOT_VERSION` v3.9.25 → v3.9.26

### 実装規模

| ファイル | 追加行数 |
|---|---|
| moomoo_trade_v1.py | +約 80 行 (プリセット履歴 + max_signal + JV regex + payload 拡張) |
| CHANGELOG.md | +約 110 行 |

---

## v3.9.25 (2026-05-19) — モメンタム機能 Phase 1 移行準備 + リスク管理一括設定

### 背景

認定サポーターからの 5/18 レポート提案 (リスク管理整備) + 7 日間シャドー観察データ (2,406 件) を統合。Wizard では「**LEVEL を 2 つ選ぶだけ**」で完結する 3 層構造を採用し、受講生の認知負荷を最小化。

### 設定の 3 層構造

| 層 | 数 | 受講生が触る? | 設定方法 |
|---|---|---|---|
| **Wizard 必須 (一括 LEVEL)** | **2 個** | ◯ 必須 | Wizard で 1-5 から選択 |
| 個別 .env 設定 | 5 個 | △ 上級者のみ | .env を直接編集 |
| コード固定値 | 6 個 | ✕ 触れない | 全員共通の最適値 |

→ 受講生が意識すべき設定は **2 個のみ**。残り 11 項目はデフォルトで安全運用。

### ① 新設: `MOMENTUM_RISK_LEVEL` (1-5)

1 つの LEVEL で 4 つのリスク管理値が連動:

| Level | 名前 | 1 回損失 | 1 日損失 | 投入額 | 急変動ガード |
|---|---|---|---|---|---|
| 1 | 超慎重 | 資金 0.20% | 0.50% | 10% | 5m ±0.7% |
| 2 | 慎重 | 0.25% | 0.75% | 12% | ±0.85% |
| **3** | **標準 (推奨)** | **0.30%** | **1.00%** | **15%** | **±1.0%** |
| 4 | 積極 | 0.35% | 1.25% | 20% | ±1.25% |
| 5 | 高リスク | 0.40% | 1.50% | 25% | ±1.5% |

例: BUDGET=$1M で RISK_LEVEL=3 → 1 回 -$3,000 / 1 日 -$10,000 で自動停止。

### ② 新規ガード機構 4 件

#### デイストップロス
- 1 日累計 PnL ≤ -X% で当日の新規発注停止
- 04:00 ET (プリマーケット開始) に自動リセット
- `_momentum_reset_daily_if_needed()` / `_momentum_is_daily_stopped()`
- 取引結果は `momentum_record_trade_result(pnl, budget)` で集計

#### 急変動ガード
- SPY 5m 変動率が ±X% を超えたら新規発注を **10 分停止**
- 上昇反発・急落どちらの方向にも反応
- `_momentum_check_volatility_guard()` で発火直前に判定

#### 連敗時サイズ縮小
- 3 連敗で次回以降のサイズを **50% に縮小**
- 勝ちが出ればカウンタリセット (60 分以内)
- 「全停止」ではなく「縮小」とすることで黄金時間帯 (14-15 ET) を取り逃さない設計

#### 時間帯フィルタ
- RTH 11:00-15:30 ET のみ発注
- データで最悪だった寄り付き 90 分 (09:30-11:00 ET, -$2.24/share) を完全回避

### ③ 個別 `.env` 設定 (上級者向け 5 項目)

すべてデフォルト値で安全動作。受講生は通常触らない。

```env
# 銘柄サイド (デフォルト: 7 日シャドーで累計プラスの組合せ)
MOMENTUM_ENABLED_SIDES=SPY:SELL_SHORT,QQQ:SELL_SHORT,QQQ:BUY

# 銘柄別サイズ係数 (SPY=基準 / QQQ=半分 / SMH=観察のみ)
MOMENTUM_SIZE_MULTIPLIER_SPY=1.00
MOMENTUM_SIZE_MULTIPLIER_QQQ=0.50
MOMENTUM_SIZE_MULTIPLIER_SMH=0.00

# 発注時間帯 (ET)
MOMENTUM_RTH_START_ET=11:00
MOMENTUM_RTH_END_ET=15:30

# 個別損切りライン
MOMENTUM_STOP_LOSS_PCT=0.50
```

### ④ コード固定値 (env なし)

| 項目 | 固定値 | 理由 |
|---|---|---|
| 連敗時サイズ縮小率 | 50% | データ的に妥当 |
| 連敗カウント基準 | 3 連敗 | 業界標準 |
| 連敗リセット時間 | 60 分 | 勝ち後の自然リセット |
| 急変動ガード クールダウン | 10 分 | 沈静化待ち |
| peak トレール基準 | +$1.50 / 戻り 35% | データから最適値 |
| デイストップ リセット時刻 | 04:00 ET | プリマーケット開始 |

### ⑤ setup_wizard.py v1.6: 新 STEP `step_momentum()`

`step_data_collect` の直後に新 STEP を挿入。受講生に対して:

1. `MOMENTUM_LEVEL` (1-5) を選択 — シグナル発火頻度
2. `MOMENTUM_RISK_LEVEL` (1-5) を選択 — リスク許容度

の 2 問のみ。デフォルトは推奨値 (3, 3)。

### 認定サポーター提案 8 項目への対応

| # | 提案 | 判定 | 実装 |
|---|---|---|---|
| 1 | SPY 中心 / SMH 観察継続 | ✅ 採用 | `.env デフォルト` |
| 2 | QQQ は SPY より小さめ | ✅ 採用 | `SIZE_MULTIPLIER_QQQ=0.50` |
| 3 | Level 1〜2 厳格化 | ✅ 採用 | Wizard `MOMENTUM_LEVEL` |
| 4 | ボラティリティ連動サイジング | ⭐ 強く採用 | RISK_LEVEL から自動 |
| 5 | 1 日損失上限 | ⭐ 強く採用 | デイストップ機構 |
| 6 | 急変動 ±1% で新規停止 | ⭐ 採用 | 急変動ガード |
| 7 | 3 連敗でその日停止 | 🟡 修正採用 | **全停止 → 50% サイズ縮小** |
| 8 | 成績悪い時間帯除外 | ✅ 採用 | RTH 11:00-15:30 ET |

⑦ を修正した理由: データで 09 時台は -$2.24/share (最悪) だが、14-15 ET は +$1.27/share (最高効率)。「3 連敗で全停止」だと黄金時間帯を取り逃すリスクが大きいため、「サイズ縮小」にした上でデイストップ ⑤ と組み合わせる二段構え設計に変更。

### Phase 1 移行ロードマップ

```
Step 1: v3.9.25 リリース (MOMENTUM_SHADOW_ENABLED=true 継続)
Step 2: 1 週間のシャドー観察でリスク管理ロジックの妥当性検証
Step 3: 認定サポーター 2-3 名のみで Phase 1 (実発注) 開始
Step 4: 1 週間データを集計して全体への展開を判断
```

### 後方互換

- 既存 .env 変更不要 (新 env 未設定なら推奨値で動作)
- 旧 `MOMENTUM_ORDER_SIZE_PCT` が .env にある場合は優先 (後方互換)
- `BOT_VERSION` v3.9.24 → v3.9.25
- `setup_wizard.py` v1.5 → v1.6
- プロンプトバージョンは v1.8.0 のまま

### 実装規模

| ファイル | 追加行数 |
|---|---|
| moomoo_trade_v1.py | +180 行 (リスク管理ロジック) |
| setup_wizard.py | +75 行 (新 STEP) |
| **合計** | **約 255 行** |

---

## v3.9.24 (2026-05-17) — 5/18 損失分析を受けた regex 拡張 2 件

### 背景

5/18 月曜 (US 取引日) の損失 -$130 超を Excel データ + 受講生レポートで詳細分析。**v3.9.21 で対応済 (Brady/ReNew の Earnings Call) は構造的にブロック** されたが、5 つの新パターンが未対応のまま損失を生んでいた:

| 受講生 | 損失 | ヘッドライン | 既存対応 |
|---|---|---|---|
| 利用者A | **-$71.00** | "Kioxia plans U.S. ADS listing amid AI memory chip boom" | ❌ ADS listing 型 |
| 利用者C | **-$28.22** | "U.S. Jury Finds OpenAI, Sam Altman Not Liable" | ❌ 訴訟ニュース |
| 利用者N/利用者M/利用者L | -$31.20 (3件) | "Tech stocks today: Samsung extends talks" | ❌ Tech stocks today |
| 利用者M | -$1.14 | "Mattel (MAT) Slipped Due to Unexpected Spending" | ❌ 値動き解説 |
| 利用者S | +$22.97 | "Analyst Report: GE HealthCare" | ❌ アナリスト集約記事 (ラッキー勝ち) |

5/18 単日で **約 -$130 の機会損失** + 5 週連続で同類パターン累計 -$700 以上。

### ① `_OBSERVATION_HEADLINE_REGEX` 拡張 (4 パターン追加)

```python
# 「Tech stocks today: ...」プレフィックス
# 5/8/13/14/18 累計 -$80+ の損失源
r'tech\s+stocks?\s+today\b|'

# 「Analyst Report: {企業名}」型 (アナリストレポート集約記事)
# 5/18 で +$22.97 のラッキー勝ち。長期的には方向性なし → 運に依存
r'analyst\s+report\s*:\s*\w+|'

# 訴訟・判決ニュース (株価への直接的影響は限定的)
# 5/18 利用者C -$28.22 "U.S. Jury Finds OpenAI Not Liable" 型
r'(?:u\.?\s*s\.?\s+|federal\s+|state\s+|district\s+|supreme\s+)?'
r'(?:jury|court|judge)\s+'
r'(?:finds?|rules?|orders?|sides?\s+with|dismisses?|throws?\s+out|tosses?)|'
r'(?:found|ruled|deemed)\s+not\s+liable\b|'
r'verdict\s+in\s+(?:lawsuit|case|trial)'
```

### ② `_STOCK_SURGE_REGEX` 拡張 (4 パターン追加)

```python
# 個別企業の上場/IPO/ADS 計画ニュース
# 5/18 利用者A -$71.00 "Kioxia plans U.S. ADS listing" 型
r'\bplans?\s+(?:u\.?\s*s\.?\s+|american\s+|nasdaq\s+|nyse\s+)?'
r'(?:ads|adr|ipo|listing|dual\s+listing|public\s+offering)\b|'
r'\b(?:ads|adr)\s+listing\b|'
r'\bto\s+list\s+(?:on|via)\s+(?:nasdaq|nyse|otc)\b|'

# 個別企業の値動き解説型 (中小型企業のスリップ/ドロップ)
# 5/18 利用者M -$1.14 "Mattel (MAT) Slipped Due to Unexpected Spending" 型
r'\bslip(?:s|ped|ping)?\s+(?:due\s+to|on|after|amid|because\s+of)\b|'
r'\b(?:dropped?|fell|tumbled|slid)\s+(?:due\s+to|on|after|amid|because\s+of)\s+'
r'(?:unexpected|disappointing|weak|lower)\b'
```

### 検証 (22 ケース、22/22 合格)

5/18 損失全 5 ケース + v3.9.21-23 既存対応 5 ケース + 通常通過 6 ケース + 訴訟追加 6 ケースをテスト:

| カテゴリ | 結果 |
|---|---|
| 5/18 新規ブロック対象 (5) | ✅ 全 5 件ブロック |
| 既存対応 (5/13 MPWR / Brady / Eos / Stock market today 等) | ✅ 全 5 件継続ブロック |
| 通常ニュース通過 (PPI / NVIDIA H200 / FRB / Trump Iran 速報 等) | ✅ 全 6 件通過 |
| 訴訟系追加テスト (Court dismisses / Federal court rules 等) | ✅ 全 6 件適切処理 |

特に **「Trump cancels planned Iran strike」型の政治速報は通過** することを確認 (5/19 04:11 JST 質問で議論されたケース)。

### 想定効果

5/18 データで再試算:

| 対策 | 想定改善 |
|---|---|
| Kioxia ADS listing → ブロック | +$71.00 |
| Jury Finds OpenAI → ブロック | +$28.22 |
| Tech stocks today × 3 → ブロック | +$31.20 |
| Mattel Slipped → ブロック | +$1.14 |
| Analyst Report → ブロック (機会損失) | -$22.97 |
| **正味改善** | **+$108.59** |

5/18 当日 -$28.45 → 改善後 **+$80 ~ +$100** に転換見込み。

### スキップした項目 (検証結果)

| # | 提案 | 検証結果 | 状況 |
|---|---|---|---|
| MAX_LOSS_PCT 0.50% → 0.70% | 22 件の強制損切りで定量試算: ケース B (現実的) で **-$126** | ❌ **見送り** |

強制損切り 22 件中、9 件が「peak ≤ 0 (一度も含み益にならず)」 = ニュース判定そのものが逆方向。これらに対して 0.70% まで深く待つことは **損失拡大** のみ。

代替案として「peak > X% 到達後のトレール型損切り」が筋がいいが実装中。当面は **現状 0.50% 据置 + ニュース判定強化 (v3.9.21-24) で構造的に対応**。

### 後方互換

- 既存 .env 変更不要
- プロンプト変更なし (regex のみ拡張)
- `_OBSERVATION_HEADLINE_REGEX` パターン数: 11 → 15
- `_STOCK_SURGE_REGEX` パターン数: 16 → 20
- `BOT_VERSION` v3.9.23 → v3.9.24
- プロンプトバージョンは v1.8.0 のまま

---

## v3.9.23 (2026-05-16) — 日次サマリ「最高勝ち」誤表示バグ修正 + Ctrl+C 端末出力改善

### バグ報告

5/15 受講生から Discord 通知への質問:

```
📊 本日のトレード集計  2026-05-15 (Fri) 15:50 ET
合計 1件   勝率 0% (0勝 1敗)
PnL  -3.28   平均勝ち +0.00 / 平均負け -3.28

💰 最高勝ち  【SPY】 -3.28  (SHORT, 10.4分)   ← ❌ 負けトレードが勝ちと誤表示
💸 最大負け  【SPY】 -3.28  (SHORT, 10.4分)
```

「勝率 0%」「0 勝 1 敗」と整合しない「最高勝ち -3.28」表示。

### 原因

`_format_daily_summary()` 内の以下が原因:

```python
best  = max(_today_trades, key=lambda t: t['pnl'])   # ← 全件マイナス時も実行
worst = min(_today_trades, key=lambda t: t['pnl'])
```

全件マイナスの日に「最もマシな負けトレード」が `best` として抽出 → 「最高勝ち」欄に表示されていた。

### 修正 ①: wins/losses から抽出

```python
# v3.9.23
best  = max(wins,   key=lambda t: t['pnl']) if wins   else None
worst = min(losses, key=lambda t: t['pnl']) if losses else None

# 表示も None チェック
if best is not None:
    lines.append(f"💰 最高勝ち  【{best['symbol']}】 {best['pnl']:+.2f}  ...")
else:
    lines.append("💰 最高勝ち  -- (勝ちトレードなし)")
if worst is not None:
    lines.append(f"💸 最大負け  【{worst['symbol']}】 {worst['pnl']:+.2f}  ...")
else:
    lines.append("💸 最大負け  -- (負けトレードなし)")
```

### 修正後の表示

```
📊 本日のトレード集計  2026-05-15 (Fri) 15:50 ET
合計 1件   勝率 0% (0勝 1敗)
PnL  -3.28   平均勝ち +0.00 / 平均負け -3.28

💰 最高勝ち  -- (勝ちトレードなし)
💸 最大負け  【SPY】 -3.28  (SHORT, 10.4分)
```

### 修正 ②: Ctrl+C 時の端末表示を見やすく

ユーザー質問「同じ表示を Ctrl+C で止めた時にプロンプトに出せますか」への対応:

**実は既に出力されていた** (`finally` ブロックの `_print_daily_summary_to_terminal()`) が、`log.info()` 経由でログ行に埋もれて気づきにくかった。

修正:

```python
def _print_daily_summary_to_terminal() -> None:
    summary = _format_daily_summary()
    if summary is None:
        # トレードゼロでも明示
        sys.stderr.write("\n" + "─" * 56 + "\n")
        sys.stderr.write("📊 本日のトレード集計  (本日のトレードなし)\n")
        sys.stderr.write("─" * 56 + "\n\n")
        sys.stderr.flush()
        return
    # 通常: 枠付きで端末表示 + ログにも記録
    sys.stderr.write("\n" + "═" * 56 + "\n")
    sys.stderr.write(summary + "\n")
    sys.stderr.write("═" * 56 + "\n\n")
    sys.stderr.flush()
    log.info(f"[終了サマリ]\n{summary}")
```

**stderr に直接 print()** で出すため、ANSI 色付きログとは別行になり Ctrl+C 後に確実に目に入る。

### Ctrl+C 動作の完全な流れ (確認)

```
1. ユーザーが Ctrl+C
2. _sigint_handler() が「🛑 プログラムを停止しました」を表示
3. signal.SIG_DFL に戻して再度 SIGINT 発行
4. KeyboardInterrupt が main() の try ブロックに到達
5. except KeyboardInterrupt → finally ブロック
6. _print_daily_summary_to_terminal() で枠付き集計を stderr に表示
7. Discord にも同じ集計を送信
```

### 後方互換

- Discord 通知の整形ロジックも同じ関数 (`_format_daily_summary`) なので、Discord 側でも修正反映される
- 既存 .env 変更不要
- `BOT_VERSION` v3.9.22 → v3.9.23

---

## v3.9.22 (2026-05-16) — ネガセンチメント検出パターン強化

### 背景

5/11-15 週間レポートでワースト 1 位だった「Keel Infrastructure posts $145M net loss in Q1」(11 件 -$201) を含む、ネガセンチメント記事で AI が誤って LONG (score=1) を返す事例への対策強化。

| ヘッドライン | 損失 | 既存検出 |
|---|---|---|
| "Keel Infrastructure posts $145M net loss" | -$201 | ✅ `net loss` + `business shrinks` (既存) |
| "Q1 revenue miss disappoints investors" | (例) | ❌ 未対応 |
| "Company faces headwinds in 2026" | (例) | ❌ 未対応 |
| "Weak Q1 results from XYZ" | (例) | ❌ 未対応 |

既存 v3.9.7 の `HEADLINE_NEGATIVE_TONE_PATTERNS` (22 パターン) で Keel ケースは既に検出可能だったが、レポート提案の「net loss / risk / concerns / headwinds」のうち **headwinds と revenue miss 系が未カバー** だったため拡張。

### 変更内容: HEADLINE_NEGATIVE_TONE_PATTERNS を 22 → 41 パターンへ拡張

#### 損失系の追加 (6 パターン)

```python
r'\bloss(?:es)?\s+(?:widen|deepen|mount|balloon)(?:ed|ing|s)?\b',  # losses widen
r'\b(?:reports?|reported)\s+(?:a\s+)?(?:\$?[\d.]+[bmk]?\s+)?loss\b',  # reports loss
r'\bswings?\s+to\s+(?:a\s+)?loss\b',              # swings to loss
r'\bq[1-4]\s+loss\b',                             # Q1 loss
r'\bfiscal\s+(?:year\s+)?loss\b',                 # fiscal year loss
r'\boperating\s+loss\b',                          # operating loss
```

#### ミス・下方修正系の追加 (6 パターン)

```python
r'\b(?:revenue|sales|earnings|eps)\s+miss(?:es|ed)?\b',
r'\bmiss(?:es|ed)?\s+(?:on\s+)?(?:revenue|sales|earnings|eps|top[- ]?line|bottom[- ]?line)\b',
r'\bdisappoint(?:s|ed|ing)?\s+(?:investors|wall\s+street|market)\b',
r'\bguid(?:es?|ance)\s+(?:weaker|lower|softer|below)\b',
r'\b(?:weak|soft|poor)\s+(?:q[1-4]\s+)?(?:results?|outlook|guidance|demand|sales|earnings)\b',
r'\bdowngrade(?:s|d|ing)?\s+(?:to|by|from)\b',
```

#### 退潮系の追加 (5 パターン) — **週間レポート最優先提案**

```python
r'\bheadwinds?\b',                                # headwinds
r'\b(?:revenue|sales|earnings)\s+(?:fell|declined|decreased|dropped)\b',
r'\b(?:facing|faces)\s+(?:headwinds?|challenges?|pressure|slowdown)\b',
r'\brestructuring\s+plan\b',
r'\bcost[- ]?cutting\s+measures?\b',
```

#### 急落系の追加 (2 パターン)

```python
r'\bfalls?\s+to\s+(?:multi[- ]?year|record|fresh|new)\s+low\b',
r'\bsink(?:s|ing)?\s+to\s+(?:multi[- ]?year|record|fresh|new)\s+low\b',
```

### 検証 (17 テストケース、全合格)

ネガ判定すべき 13 ケース + 通常通過すべき 4 ケースで完全合格を確認:
- ✅ Keel / revenue miss / headwinds / weak demand / earnings miss / downgrade / restructuring plan / weak Q1 / losses widen / swings to loss / operating loss / falls to multi-year low / reports $50M loss
- ✅ NVIDIA H200 shipping / Apple Q3 earnings beat / FRB cuts rates / CPI 通常値 → 通過

### 動作

ETF モード (target_symbol 指定なし) で:
- AI が score=1 (LONG) を返した場合のみ発動
- ヘッドラインに 38 パターンのいずれかが含まれていれば **score=0 強制**
- 観察ログ `block_stage="meta_negtone"` で記録

個別株モード (target_symbol 指定あり) は影響なし — 例えば AAPL の「AAPL revenue miss」判定は通常通り行う。

### 後方互換

- 既存 .env 変更不要
- パターン数増加によるパフォーマンス影響: 各 AI 呼出後の正規表現マッチを 22 → 41 へ増加 (誤差レベル、1ms 未満)
- `BOT_VERSION` v3.9.21 → v3.9.22
- プロンプトバージョンは v1.8.0 のまま (regex のみ拡張、プロンプト変更なし)

---

## v3.9.21 (2026-05-16) — 単一企業 → ETF 発注禁止の体系的強化 + Alpaca 互換修正

### 背景

5/15-5/16 の分析で、過去 1 週間連続で発生していた「個別企業のニュースを ETF (SPY/QQQ/SMH) BUY と誤判定」型損失の **新パターン** が判明:

| 受講生 | PnL | ヘッドライン |
|---|---|---|
| 利用者Q | **-$47.88** | "Eos Energy Enterprises Q1 2026 Earnings Call Summary" |
| 利用者F | -$23.00 | "Innovate (VATE) Q1 2026 Earnings Transcript" |
| 利用者R | -$26.78 | "Alphabet AI Cloud Surge ... Anthropic Commits $200B" |
| 利用者O/利用者P/利用者M | -$37 計 | "Cerebras stock opens at $350 per share in biggest IPO of 2026" |
| (再掲 5/14) | -$58.80 | "Chip-Gear Firm Nova Lights Up On First-Quarter Beat" |

**累計 -$200 以上が 5 日連続発生**。v3.9.17 の `_STOCK_SURGE_REGEX` (Surged/Soared 系) では拾えなかった以下のパターンを追加:

- Earnings Call Summary / Transcript / Highlights
- 個別企業の IPO (opens at $N / biggest IPO of YYYY)
- 提携・投資ニュース ($200B 規模)
- 「Lights Up On First-Quarter Beat」型 (Nova / Camtek)

加えて 5/16 受講生環境で 21 件発生していた **Alpaca News websockets エラー** (Python 3.13 + 古いライブラリの非互換) も対応。

### 変更内容

#### ① `_STOCK_SURGE_REGEX` の拡張 (post-AI ハードガード)

```python
# v3.9.17 既存
\bsurged?\s+(?:on|after|to|amid|following)\b|
\bjumps?\s+(?:on|after|to|amid|following)\b|
\brobust\s+quarterly\s+results?\b|

# v3.9.21 追加: 個別企業の決算電話/Transcript
\bq[1-4]\s+\d{4}\s+earnings\s+(?:call\s+)?(?:summary|highlights|transcript|recap)\b|
\bearnings\s+call\s+(?:summary|highlights|transcript|recap)\b|
\bq[1-4]\s+earnings\s+call\b|

# v3.9.21 追加: 個別企業の IPO
\b(?:stock\s+)?opens?\s+at\s+\$\d+(?:\.\d+)?\s+per\s+share\b|
\bbiggest\s+ipo\s+of\s+\d{4}\b|
\b(?:debuts?|launched?)\s+(?:at|with)\s+\$\d+(?:\.\d+)?\b|

# v3.9.21 追加: 提携・大型契約 (個別企業主導)
\bcommits?\s+\$\d+(?:\.\d+)?\s*(?:b|billion)\b|
\binvests?\s+\$\d+(?:\.\d+)?\s*(?:b|billion)\s+in\b|

# v3.9.21 追加: Nova/Camtek 型 "On Q1 Beat"
\b(?:lights?\s+up|jumps?|leaps?)\s+on\s+(?:first|second|...)[\-\s]*quarter\s+beat\b|
\bon\s+(?:first|second|third|fourth)[\-\s]*quarter\s+beat\b
```

正規表現テスト 11 ケースで全合格を確認 (個別企業 NG パターン × 7、通常ニュース通過 × 4)。

#### ② SYSTEM_PROMPT v1.7.0 → v1.8.0 (単一企業 → ETF 発注禁止 明文化)

旧 v1.7.0 の「個別株サージ語彙の絶対禁止リスト」を体系化:

**新セクション**: 「単一企業ニュース → ETF 発注禁止 (大型株以外)」

```
▼ 単一企業のニュースで ETF score=1 を出してよい条件 (すべて満たすこと):
1. 大型株 (時価総額 $500B 以上、NVDA / AAPL / MSFT / GOOGL / AMZN / META /
   TSLA / LLY / AVGO / TSMC レベル) のニュースである
2. かつ「Surge / Jump / Soar / Beats / Robust」等の動詞単独ではなく、
   ETF 全体への波及メカニズム (供給チェーン / セクター指標 / 大型契約) が明示
3. かつ「市場全体」のキーワード (broader market / S&P 500 / Nasdaq / 業界全体)
   がヘッドライン内に同居

▼ 上記を満たさない場合は ETF モードで強制的に score = 0
```

**大型株リストの明示**:
- TECH 大型: NVDA / AAPL / MSFT / GOOGL / AMZN / META / TSLA / AVGO / NFLX
- 半導体大型: NVDA / TSMC / AMD / AVGO / MU
- ヘルスケア大型: LLY / NVO / UNH / JNJ
- 金融大型: JPM / BAC / BRK.B / V / MA

これら以外の「{中小型企業} Q1 Beat」型は **ETF への波及力なし → score = 0** と明文化。

#### ③ Alpaca News websockets ライブラリ互換性対応

```python
# 旧: 常に additional_headers (websockets 13.0+ 想定)
async with websockets.connect(
    _ALPACA_WS_URL,
    additional_headers={...},
) as ws:

# 新: バージョン判定で extra_headers / additional_headers を切替
try:
    _ws_ver = tuple(int(x) for x in websockets.__version__.split(".")[:2])
except Exception:
    _ws_ver = (13, 0)
_headers_kw = "additional_headers" if _ws_ver >= (13, 0) else "extra_headers"

async with websockets.connect(
    _ALPACA_WS_URL,
    ping_interval=20,
    ping_timeout=60,
    **{_headers_kw: _headers_dict},
) as ws:
```

これで `websockets 12.x` (Python 3.10-3.12 環境) と `websockets 13.x+` (Python 3.13 環境) の両方で動作する。

### モメンタムシャドー試算結果

参考: 5/15-5/16 で「認定サポーター 5 名がモメンタムシグナルで毎回 $5,000 ずつ発注していたら」を試算:

| 日 | PAN | 利用者B | 利用者F | 利用者D | 利用者E | 5名合計 |
|---|---|---|---|---|---|---|
| 5/15 | +$113.65 | +$73.45 | +$49.88 | +$13.17 | +$67.37 | **+$317.52** |
| 5/16 | -$92.09 | -$100.49 | -$168.65 | -$64.32 | -$68.87 | **-$494.43** |
| 計 | +$21.56 | -$27.04 | -$118.77 | -$51.16 | -$1.50 | **-$176.91** |

**1 日で +$317 → -$494 にひっくり返る挙動**を確認。Phase 1 実発注移行は**当面保留**を推奨。

### 後方互換

- 既存 .env は変更不要 (新 env なし)
- `_PROMPTS_VERSION` 1.7.0 → 1.8.0 (起動バナーで確認可能)
- `BOT_VERSION` v3.9.20 → v3.9.21

---

## v3.9.20 (2026-05-15) — ピラミッディング撤去 + confidence 山型サイズ配分

### 背景

Excel 1,293 件 (5/6-5/15) + PAN ログを分析した結果、2 つの構造的な問題が判明:

1. **ピラミッディングが実運用で 1 回も発動していなかった**
   - PAN ログ 4 日間で「ピラミッド追加条件」のヒット = 0
   - 受講生 1,293 件で同一×銘柄×10分以内の追加発注 = 20 件 (1.5%、しかも別ニュース起点の可能性)
   - 原因: タイムアウト 10 分 / ETF 再エントリー禁止 30 分 / AI キャッシュ 15 分 / トピックダブり 30 分 と仕組みが衝突

2. **confidence と勝率/効率は「山型」だった** (線形ではない)

| conf 帯 | 件数 | 平均 PnL | ペイオフ | 効率 |
|---|---|---|---|---|
| 0.60-0.65 | 29 | -$23.13 | 0.37 | 🔴 大損 |
| 0.66-0.68 | 110 | +$4.05 | 1.71 | 🟢 |
| 0.69-0.70 | 30 | +$2.73 | 2.99 | 🟢 |
| 0.71-0.72 | **743** | +$1.38 | 1.14 | 🟡 主力だが効率最低 |
| 0.73-0.74 | 232 | +$2.05 | 1.57 | 🟡 |
| 0.75-0.78 | 100 | +$3.83 | 1.98 | 🟢 |
| **0.79-0.82** | **32** | **+$11.02** | **3.56** | 🟢🟢 **最高効率 (6 倍)** |
| 0.83-0.85 | 9 | -$4.13 | 0.57 | 🔴 |
| 0.86-0.88 | 4 | -$25.58 | 1.05 | 🔴 大負け |

→ 「conf 高いほど大きく」は嘘。**0.78-0.82 がスイートスポット**で、それ以上は AI が個別株サージ (MPWR/NVDA/AMD) に過剰反応して逆に負ける傾向。

### 実装内容

#### ① `calc_order_size()` を山型配分に変更

```python
def calc_order_size(confidence: float) -> float:
    """v3.9.20: 山型配分で発注額決定"""
    if confidence < 0.70:
        size_pct = 0.30   # 弱シグナル帯
    elif confidence < 0.78:
        size_pct = 0.60   # 主力帯
    elif confidence < 0.83:
        size_pct = 1.00   # ★スイートスポット
    else:
        size_pct = 0.40   # 超強気帯 (過剰反応リスク管理)
    return max(ORDER_SIZE_MIN_USD, min(ORDER_SIZE_MAX_USD, _BUDGET_USD * size_pct))
```

#### ② ピラミッディング撤去 (3 つの env 削除)

- `PYRAMID_MAX_ENTRIES` (撤去)
- `PYRAMID_ALLIN_THRESHOLD` (撤去・「一気全額モード」削除)
- `PYRAMID_STEP_PCT` (撤去)
- `place_long` / `place_short` の多段エントリー分岐を削除
- 既存ポジション保有中の同方向シグナル再送は**シンプルに「スキップ」**に統一

#### ③ 起動バナー更新

```
変更前:
  予算上限: $50,000  注文額: $20,000〜$50,000 (confidence 連動)
  ピラミッド追加条件: 前回エントリーから +0.3% 上昇 (PYRAMID_STEP_PCT)
  決済FAILED時新規BUYブロック: 30分  ピラミッド上限=2回

変更後:
  予算上限: $50,000
  サイズ配分: 弱30% / 主力60% / ★ｽｲｰﾄ100% / 強気40% (confidence 山型)
              範囲: $15,000〜$50,000
  決済FAILED時新規BUYブロック: 30分
```

#### ④ Discord 通知更新

```
変更前: 発注回数: 1/3回目  ピラミッティング (最大3回)
変更後: サイズ配分: ★スイートスポット 100% (conf=0.80)
```

#### ⑤ setup_wizard STEP 7 ピラミッド設定撤去 (v1.4 → v1.5)

旧 STEP 7 に存在した 2 つの選択画面を削除:
- 「ピラミッディング回数 (PYRAMID_MAX_ENTRIES)」
- 「一気全額しきい値 (PYRAMID_ALLIN_THRESHOLD)」

代わりに「山型サイズ配分」の解説を表示 (設定不要・コード組み込み)。

### 期待効果

| | 旧 (線形補間) | 新 (山型) |
|---|---|---|
| 投下総額 | $10,455K | **$7,156K (32% 削減)** |
| 累計 PnL | +$1,044 (試算) | +$1,003 (試算) |
| 資金効率 | +0.0100% | **+0.0140% (1.4 倍)** |

**同じ利益を約 70% の投下資金で稼ぐ**設計。残り 30% は安全マージン or モメンタム発注 Phase 1 などの他戦略に振り向け可能。

### 高 conf でサイズを減らす理由 (受講生向け)

> AI が異常に高い自信を示すケースには、個別企業の急騰ニュース (MPWR / NVDA / AMD など) に過剰反応している事例が含まれており、ETF 全体への波及は限定的でした。v3.9.17 で対策ルールを入れていますが、データのばらつきが大きい領域です。ロットを 40% に抑えることで安全マージンを確保します。

### 後方互換

- 既存 .env の `PYRAMID_MAX_ENTRIES` / `PYRAMID_ALLIN_THRESHOLD` / `PYRAMID_STEP_PCT` は残っていても無視される (削除推奨だが副作用なし)
- `ORDER_SIZE_MIN_USD` のデフォルトを 40% → 30% に微調整 (30% 帯を正しく適用するため)
- setup_wizard.py v1.5 で .env の PYRAMID_* 行は書き出されなくなる
- `BOT_VERSION` v3.9.19 → v3.9.20

---

## v3.9.19 (2026-05-15) — 重大エラー時のアラート音（上級者向け設定）

### 背景

利用者からの要望。6 つの致命的イベント発生時に OS の音を再生して即座に気づけるようにする。Discord 通知は履歴用、音は「今すぐ席に戻ってほしい」用途として併用する。

### 音を鳴らす 6 イベント

| # | イベント | 既存ログ | 影響 |
|---|---|---|---|
| 1 | Anthropic クレジット切れ検知 | `_handle_credit_exhausted()` | AI 判定全件失敗 |
| 2 | moomoo SDK 古すぎる (起動拒否) | `hasattr(TrdSide, "BUY_BACK")` 検証失敗 | Bot 起動不可 |
| 3 | OpenD 接続失敗 (起動拒否) | `create_connection` タイムアウト | Bot 起動不可 |
| 4 | 取引ロック検出 (unlock エラー) | `_warn_trade_locked()` | 全発注停止 |
| 5 | 強制損切り発注が全失敗 | `⚠ 強制損切りの発注が全失敗` | 損失拡大リスク |
| 6 | ショートカバー全 position_id 発注失敗 | `[ショートカバー] 全 position_id 発注失敗` | 損失拡大リスク |

### 鳴らない条件 (意図的に除外)

- 通常の損切り発動・トレイル発動・パニックセル発動 (これらは正常動作)
- Anthropic 529 Overloaded (既存リトライで吸収)
- `[ETF影響ガード]` 系の警告 (正常動作)
- LONG ポジションの決済全失敗 (損失拡大リスクは低いため除外。SHORT のみ対象)

### 技術仕様

| 項目 | 内容 |
|---|---|
| macOS | `afplay /System/Library/Sounds/Sosumi.aiff` (subprocess) |
| Windows | `winsound.MessageBeep(MB_ICONHAND)` (標準ライブラリ) |
| Linux | `paplay /usr/share/sounds/freedesktop/stereo/dialog-warning.oga` |
| クールダウン | 同一 `event_key` は 60 秒以内は再生しない (連発防止) |
| 夜間ミュート | `ALERT_SOUND_QUIET_HOURS=23:00-06:00` で JST 時間帯指定 (日跨ぎ可) |
| 失敗時 | SSH リモート / 音声デバイス無しは try/except で無視 |
| 別スレッド | `threading.Thread(daemon=True)` で再生 (ロガーをブロックしない) |

```python
def _play_alert_sound(event_key: str) -> None:
    if not ENABLE_ALERT_SOUND:
        return
    if _is_alert_quiet_hours():
        return
    # 同一 event_key のクールダウン
    now = datetime.datetime.now()
    last = _alert_sound_last_played.get(event_key)
    if last and (now - last).total_seconds() < ALERT_SOUND_COOLDOWN_SEC:
        return
    _alert_sound_last_played[event_key] = now
    # 別スレッドで再生
    threading.Thread(target=_play, daemon=True).start()
```

### 設定方法 (デフォルト無効)

**ENABLE_ALERT_SOUND=false がデフォルト**。setup_wizard.py の新 STEP「🔔 重大エラー時のアラート音（上級者向け・任意）」で対話的に有効化可。

```env
ENABLE_ALERT_SOUND=false                                    # ON/OFF
ALERT_SOUND_COOLDOWN_SEC=60                                 # 連発抑制 (秒)
ALERT_SOUND_QUIET_HOURS=23:00-06:00                         # 夜間ミュート (JST)
ALERT_SOUND_FILE_MACOS=/System/Library/Sounds/Sosumi.aiff   # macOS 音源カスタム
```

### setup_wizard.py 変更点 (v1.4)

- 新 STEP `step_alert_sound()` を `step_data_collect()` の後に追加
- `import platform` を追加 (macOS 検出用)
- 有効化選択時は対話的に cooldown / quiet hours / macOS 音源パスを設定
- 無効化選択時は新 env 4 つを既存値で .env に書き込み (or デフォルト値)

### 後方互換

- 既存 .env は変更不要 (新 env 未設定なら従来通り無音)
- 既存 setup_wizard 利用者は再実行すれば新 STEP のみ追加表示
- `BOT_VERSION` v3.9.18 → v3.9.19

---

## v3.9.18 (2026-05-15) — 過去 4 連続日次レポート指摘の継続課題 3 件

5/8 - 5/14 の日次レポートで繰り返し指摘されていた未着手項目のうち、優先度の高い 3 件を実装。

### ① 観測記事ヘッドラインのプレフィルタ (P0)

**背景**: 4 連続レポートで指摘されながら未着手だった最大の継続課題。

| 日付 | 損失 | ヘッドライン |
|---|---|---|
| 5/14 | -$130.13 (11件) | "Stock market today: Dow retakes 50,000 level, Nasdaq surge" |
| 5/13 | -$12.60 (2件) | "Barbarians meet their new gatekeepers: 'wealthtech' middlemen" |
| 5/12 | -$74.27 (22件) | "Consumer spending is healthy, but gas prices remain a risk" |
| 5/11 | 記録 | "OpenAI new unit" |
| **累計** | **推定 -$220+** | 4 日連続で継続発生 |

**実装**: `analyze_news()` 冒頭で AI 呼出前に regex マッチをかけ、ETF モードで該当すれば即 score=0 を返す。

```python
_OBSERVATION_HEADLINE_REGEX = re.compile(
    r'^(?:'
    r'stock\s+market\s+today\b|'
    r'(?:dow|s&p\s*500?|nasdaq|wall\s+street)\s+(?:retakes?|reclaims?|hits?|tops?)|'
    r'(?:dow|s&p\s*500?|nasdaq)\s+(?:jones\s+)?(?:closes?|finishes?|ends?|opens?)\s+(?:higher|lower)|'
    r'(?:bitcoin|ethereum|crypto|gold|oil|stocks?)\s+(?:and\s+\w+\s+)?prices?\s+today\b|'
    r'live\s+updates?\b|'
    r'market\s+(?:digest|wrap|recap|roundup|update|brief)\b|'
    r'(?:explained|explainer)[:\s]|'
    r'what\s+is\s+(?:a\s+)?(?:perpetual|defi|crypto|bitcoin|ethereum|nft|dao|dex)\b|'
    r'(?:barbarians|middlemen|gatekeepers|insiders)\s+(?:meet|face|battle|fight)'
    r')',
    re.IGNORECASE
)
```

**副次効果**:
- Anthropic API 呼出が 1 件あたり ~$0.0005 削減
- ログ汚染も削減 (AI レスポンス解析エラーが減る)
- 観察ログに `block_stage="observation_headline"` として記録 → 後で集計可能

### ② Bitcoin / Crypto 系ヘッドライン除外 (P2)

**背景**: 上昇相場での「ラッキー勝ち」を構造的に防ぐ。

| 日付 | PnL | ヘッドライン |
|---|---|---|
| 5/12 | +$171 (13件) | "Bitcoin and ethereum prices today" |
| 5/14 | +$287.52 (13件) | "Explained: What is a perpetual DEX?" |

上昇相場では偶然勝てるが、同じ仕組みで下落相場では大損する可能性が高い。長期的には勝率 50% 以下に収束する見込み。

**実装**: ETF モードのみ score=0 強制 (個別株モードの Coinbase Q3 等の判定には影響しない厳格パターン)。

```python
_CRYPTO_HEADLINE_REGEX = re.compile(
    r'(?:'
    r'\bbitcoin\s+(?:and\s+\w+\s+)?prices?\b|'
    r'\bethereum\s+prices?\b|'
    r'\b(?:btc|eth)\s+(?:price|prices|surge|surges|jumps|drops|plunges)\b|'
    r'\b(?:bitcoin|ethereum)\s+(?:surges?|jumps?|soars?|plunges?|crashes?)\s+(?:past|above|below|to)\b|'
    r'\bperpetual\s+dex\b|'
    r'\bdecentralized\s+(?:exchange|finance|defi)\b|'
    r'\b(?:defi|crypto)\s+(?:primer|explained|explainer|guide|overview|deep\s+dive)\b|'
    r'\bstablecoins?\s+(?:tokenization|driving|adoption|surge)\b|'
    r'\b(?:bitcoin|ethereum|crypto)\s+etf\b|'
    r'\b(?:bitcoin|ethereum)\s+(?:halving|halvening)\b'
    r')',
    re.IGNORECASE
)
```

観察ログには `block_stage="crypto_headline"` として記録。

### ③ Anthropic クレジット切れ警告を起動バナーに追加 (P1)

**背景**: 5/13-5/14 のエラーログ分析で発覚した問題。

- 受講生 65 名のうち **10-15 名で「credit balance is too low」エラー発生**
  - じゅうべい 76 件 / YUNOI 51 件 / pochi 48 件 / ー 47 件 / えつ 43 件 等
- Bot は `_is_credit_exhausted_error` で検知して中立スキップ → 誤発注はしない
- しかし **「Bot は動作中に見えるが実発注は完全停止」状態** に本人が気づきにくい
- Discord 通知は初回検知時のみ → 数日経つと埋もれて見落とす

**実装**: 起動時に直近 24 時間の moomoo_trade_v1.log を走査し、クレジット切れ検知件数があれば WARNING 罫線ブロックを表示。

```python
def _check_recent_credit_exhaustion(log_path: str = "moomoo_trade_v1.log",
                                     hours: int = 24) -> tuple[int, Optional[datetime.datetime]]:
    # ファイル末尾 5 MB のみ読んで効率化
    # "credit balance is too low" または "you have reached your specified" を検出
    # 直近 24 時間以内の件数と最終発生時刻を返す
    ...

# 起動バナーで:
if _credit_cnt > 0:
    log.warning("╔═══════════════════════════════════════════════════════╗")
    log.warning("║  ⚠️  Anthropic API クレジット切れ警告 (過去 24 時間)  ║")
    log.warning(f"║  発生件数: {_credit_cnt} 件    最終発生: {_last_disp}   ║")
    log.warning("║  [対応] https://console.anthropic.com/settings/billing ║")
    log.warning("╚═══════════════════════════════════════════════════════╝")
```

### スキップした項目 (継続検討)

5/14 レポート提案のうち本リリースから外したもの:

| # | 提案 | 状況 |
|---|---|---|
| プレマーケット閾値 0.78 引き上げ | 既存 `CONFIDENCE_PREMARKET` env で各自設定可能 → 受講生案内のみで対応 |
| 個別株 STOCK_CONFIDENCE=0.85 | 検討中 (Finnhub 個別株は実質停止状態のため緊急度低) |
| ATR 動的損切り | Phase 1 (観察のみ) 実装は今後検討 |

### 後方互換

- 既存 .env は変更不要 (新 env なし)
- 観察ログは既存スキーマで `block_stage` 列に新タグが入るだけ
- `BOT_VERSION` v3.9.17 → v3.9.18

---

## v3.9.17 (2026-05-14) — 5/13 全体分析を受けた 7 件の改善

5/13 受講生 65 名のトレードデータ ($+1,776 / 65 件、BE 単独 $+1,810 を除けば $-34 / 64 件) と PAN ログを総合分析した結果に基づく改善 7 件。

### ① 約定確認待ち中のリスク監視を強制スキップ (P0 バグ修正)

**症状**: 5/13 22:49 SPY SHORT 発注の 2.7 秒後 (約定確認前) に強制損切り誤発動。

```
22:49:28.873  SPY SHORT 発注 (指値 $732.32, 7株)
22:49:31.879  [ポジション] PnL=$-26.11(-0.51%)  ← 指値を avg_cost として誤算
22:49:31.880  ⚠ 強制損切り発動！(まだ約定確認前!)
22:49:32.547  BUY_BACK 発注 ($739.78)
22:49:34.471  ← ここで初めて元の SHORT が FILLED_ALL ($736.01) 確認
22:49:40.569  BUY_BACK 約定 ($736.09) → 結果: -$0.54
```

**原因**: `place_short()` で `avg_cost = limit_price` を仮設定 (v3.1.3) → 監視ループの max_loss チェックが指値を起点に PnL 計算 → 実約定価格との乖離で誤発動。

**修正**: `TickerState.entry_pending_fill: bool` フラグを追加。

```python
# place_long / place_short (発注成功後)
ts_state.entry_pending_fill = True

# _check_order_filled FILLED_ALL ハンドラ (BUY/SHORT 約定確定後)
ts_upd.avg_cost = filled_price  # 実約定均価で上書き (既存)
ts_upd.entry_pending_fill = False  # 監視解放 (新規)

# sync_positions (moomoo がポジション認識 = 約定済み)
ts.entry_pending_fill = False

# 監視ループ (avg_cost <= 0 ガード直後)
if ts.entry_pending_fill:
    log.debug(f"{tag} 約定確認待ち → リスク判定スキップ")
    continue
```

これにより約定確認前は強制損切り・トレール・パニック・peak追跡を全停止。FILLED_ALL / sync_positions で `entry_pending_fill=False` に戻り通常監視再開。

### ② SYSTEM_PROMPT v1.6.0 → v1.7.0 (経済指標サプライズ非対称ルール)

**症状**: 5/13 PPI MoM 1.4% Vs 0.5% Est. (予想の 2.8 倍上振れ) を AI が SPY BUY 判定 → えつ -$70.81 / hma -$23.78 (パニックセル発動)。

**修正**: プロンプトに「経済指標サプライズの非対称ルール」セクションを追加:

- インフレ指標 (CPI/PPI/PCE/Core) 上振れ (1.5 倍以上 or +0.3pt以上): **MACRO 売り強制** (score=-1, conf ≥ 0.78)
- 「上振れ → 景気強い → 株高」と解釈して score=+1 を出すのは **禁止**
- 雇用指標・GDP も非対称ルールを明文化
- 例外: "Dow Falls / Stocks Drop" 同居なら conf +0.05 加点

### ③ SYSTEM_PROMPT v1.7.0 個別株サージ語彙の絶対禁止リスト

**症状**: 5/13 09:48-09:52 "Monolithic Power Systems (MPWR) Surged on Robust Quarterly Results" を AI が SEMI_STRONG QQQ BUY と誤判定 → 5 名同時被弾 (-$24.14 / -$21.87 / -$18.50)。

**修正**: 以下の語彙パターンを ETF モードで強制 score=0:

- "{TICKER} Surged on / Soared / Jumps After / Spikes After / Rallies on"
- "Robust Quarterly Results" + 単一企業名
- 例外: ヘッドラインに ETF 名 (SPY/QQQ/SMH) や "broader market / s&p 500 / nasdaq" が同居している場合のみ ETF 強気判定可

### ④ post-AI 個別株サージガード (コード側最終防衛線)

プロンプト遵守だけに頼らず、`_apply_meta_filter_to_ai_result()` に regex ベースのハードガードを追加:

```python
_STOCK_SURGE_REGEX = re.compile(
    r'\b(?:surged?|soared?|jumps?|spikes?|rallies?|climbs?)'
    r'\s+(?:on|after|to|amid|following)|'
    r'robust\s+quarterly\s+results?|'
    r'shares?\s+(?:are\s+)?trading\s+higher\s+after\b',
    re.IGNORECASE
)
_MARKET_WIDE_REGEX = re.compile(
    r'\b(?:spy|qqq|smh|broader\s+market|s&p\s*500|nasdaq|dow|'
    r'wall\s+street|stocks?\s+(?:rally|surge|jump))\b',
    re.IGNORECASE
)
# ETF モード × score=1 × 個別株サージあり × 市場全体KWなし → score=0 強制
```

### ⑤ Anthropic 400 invalid_request_error の詳細ロギング

**症状**: 5/13 認定サポーター環境で 49 件発生 (PAN 環境では 0 件) → 受講生固有の問題で原因特定が必要。

**修正**: `anthropic.APIError` 捕捉時に 400 を分離して詳細ロギング:

```python
if _status == 400 or "invalid_request_error" in str(e).lower():
    log.error(
        f"[{trigger_symbol}] Anthropic 400 詳細: "
        f"err_type={type(e).__name__} status={_status} "
        f"model={CLAUDE_MODEL} max_tokens=1000 "
        f"stop_sequences={_stop_seqs_repr} "
        f"prompt_ver={_PROMPTS_VERSION} prompt_len={len(SYSTEM_PROMPT)} "
        f"target_symbol={target_symbol or 'ETF'} headlines_cnt={len(headlines)} "
        f"user_msg_head={_user_head!r} "
        f"body={_body}"
    )
```

次回の受講生環境発生時に問題部位が特定できる。

### ⑥ peak リトレース 微利確シミュレーション (Phase A-2)

5/13 観察データで peak ≥ +$5 を一度触れて戻りで損失 or 微利になった 11 件あり。Phase A-1 (TIMEOUT 延長 sim) と同様、実トレード非破壊で「peak の 60% で利確していたら」を観察ログに記録:

```python
# _send_trade_result() の末尾 (TIMEOUT_SIM の前)
if peak_pnl >= PEAK_RETRACE_SIM_MIN_USD and realized_pnl < peak_pnl * PEAK_RETRACE_SIM_TAKE_RATIO:
    _log_observation(
        block_stage="peak_retrace_sim",
        block_reason=f"peak=${peak_pnl:.2f}  actual=${realized_pnl:+.2f}  "
                    f"sim_take(60%)=${peak_pnl * 0.6:+.2f}  delta=${...}",
        price_at_decision=exit_price,
    )
```

1 週間のデータ蓄積後、「peak ≥ +$5 のケースで peak * 0.6 利確ルールを導入した場合、累計 PnL は何 % 改善するか」を定量評価可能。

### ⑦ panic_sell_all に peak リトレース ガード

**症状**: 5/13 22:49 hma SPY -$23.78 パニックセル発動。直後の SHORT 組は 60 分後 -$3〜6 程度に収束 → 急落の底でパニック決済して傷を拡大。

**修正**: `PANIC_RETRACE_SKIP_PCT` (env, デフォルト 0.0 = 無効) で「想定底からの戻り率が閾値以上ならスキップ」を追加:

```python
_theoretical_bottom = ts.avg_cost * (1 - MAX_LOSS_PCT)
_recovery_range = ts.peak_price - _theoretical_bottom
_recovery_ratio = (cur_price - _theoretical_bottom) / _recovery_range
if _recovery_ratio >= _PANIC_SKIP_PCT:  # 例: 0.3 で 30% 戻し
    continue  # この銘柄は panic 対象から除外
# 含み益域 (cur_price >= avg_cost) なら無条件スキップ
```

### スキップした項目

| # | 提案 | 理由 |
|---|---|---|
| §2-a | 強制損切り -0.5% → -0.7% | `MAX_LOSS_PCT` env で各自調整可、本リリース除外 |
| §1-C | 同時エントリ ≥3 で size 半減 (cluster risk) | Bot 単体では他受講生の発注を観測不能。§1-B 個別株サージガードで根本原因 (MPWR 型) は遮断済み。真の cross-Bot cluster 制御は GAS 集計側の機能追加が必要 (将来課題) |

### 新規 env (任意)

```env
# Phase A-2 sim (デフォルト有効・無感)
PEAK_RETRACE_SIM_MIN_USD=5.0       # peak がこの $ 以上で観察記録
PEAK_RETRACE_SIM_TAKE_RATIO=0.60   # peak * 60% で仮想利確

# panic_sell リトレース ガード (デフォルト 無効)
PANIC_RETRACE_SKIP_PCT=0.0          # 0.3 = 底から 30% 戻したら panic スキップ
```

### 後方互換

- 既存 .env は変更不要。すべて新 env はデフォルト動作で従来挙動を維持
- `_PROMPTS_VERSION` 1.6.0 → 1.7.0 (起動バナーに表示)
- `BOT_VERSION` v3.9.16 → v3.9.17

---

## v3.9.16 (2026-05-13) — TIMEOUT 延長 sim + モメンタム銘柄別 5 段階 LEVEL

### 含まれる 2 つの機能

#### ① TIMEOUT 延長シミュレーション (Phase A-1)

認定サポーター提案を受け、「もし時間切れで閉じずに延長していたら +5/+15/+60min PnL はどうなったか」を観察ログに自動記録する機能を実装。実トレードは変更せず、シャドーシミュレーションのみ。

```python
# _send_trade_result() の末尾に追加
if "時間切れ" in (exit_reason or ""):
    _side  = "SELL_SHORT" if trade_type == "SHORT" else "BUY"
    _score = -1 if trade_type == "SHORT" else 1
    _log_observation(
        symbol=symbol, side=_side, confidence=ai_conf, score=_score,
        category=ai_category or "TIMEOUT_SIM",
        headlines=[f"[TIMEOUT延長sim] {symbol} {trade_type} ..."],
        outcome="ordered",
        block_stage="timeout_extension",  # ← 識別タグ
        block_reason=f"actual_pnl=${realized_pnl:+.2f}/share=${pnl_per_share:+.3f}",
        price_at_decision=exit_price,
    )
```

既存の観察ログ Phase 2 インフラを流用するため:
- 追加 async ループ不要
- GAS 変更不要 (既存スキーマで識別可能)

集計方法: Google Sheets「観察ログ」タブで `block_stage="timeout_extension"` でフィルタし `pnl_5min/15min/60min` の平均値を見れば、「もし延長していたら平均 +$X だった」が定量判明。

#### ② モメンタムシャドー 銘柄別キャリブレーション + 5 段階 LEVEL

##### 背景
v3.9.12 では全銘柄共通の単一閾値 (`MOMENTUM_TREND_5M_PCT=0.20%` / `MOMENTUM_TREND_15M_PCT=0.40%`) を使っていたが:
- SPY (σ=0.05%) では 0.20% = 4σ → 発火率ほぼ 0%
- SMH (σ=0.13%) では 0.20% = 1.5σ → 過剰発火
- 3 銘柄でアンバランスなデータ

##### 実装
銘柄別の 6 閾値を **5 段階プリセット** で制御:

```python
_MOMENTUM_LEVEL_PRESETS: Dict[int, Dict[str, tuple]] = {
    1: {"QQQ": (0.15, 0.30), "SPY": (0.10, 0.22), "SMH": (0.22, 0.45)},  # 最厳格
    2: {"QQQ": (0.12, 0.24), "SPY": (0.08, 0.18), "SMH": (0.18, 0.36)},  # 厳格
    3: {"QQQ": (0.10, 0.20), "SPY": (0.07, 0.15), "SMH": (0.15, 0.30)},  # 標準 ⭐
    4: {"QQQ": (0.08, 0.16), "SPY": (0.06, 0.12), "SMH": (0.12, 0.24)},  # 緩和
    5: {"QQQ": (0.06, 0.12), "SPY": (0.04, 0.09), "SMH": (0.09, 0.18)},  # 最緩和
}

MOMENTUM_LEVEL = _parse_momentum_int("MOMENTUM_LEVEL", 3, 1, 5)

def _get_momentum_thresholds(symbol: str) -> tuple:
    """① 個別 env オーバーライド → ② LEVEL プリセット の順で閾値解決。"""
    ...
```

##### env 変数 (3 つの独立次元)

```bash
MOMENTUM_SHADOW_ENABLED=true     # 機能 ON/OFF
MOMENTUM_LEVEL=3                 # 閾値レベル (1-5)
MOMENTUM_COOLDOWN_MIN=30         # クールダウン (LEVEL と独立)
MOMENTUM_ORDER_SIZE_PCT=70       # 想定発注額% (LEVEL と独立)
```

最小 4 行で全設定完了。クールダウン/サイズは LEVEL とは無関係に自由に設定可能。

##### 上級者向け 個別オーバーライド

```bash
MOMENTUM_QQQ_5M_PCT=0.12   # MOMENTUM_LEVEL プリセットを部分上書き
MOMENTUM_QQQ_15M_PCT=0.24
MOMENTUM_SPY_5M_PCT=0.06
MOMENTUM_SPY_15M_PCT=0.13
MOMENTUM_SMH_5M_PCT=0.18
MOMENTUM_SMH_15M_PCT=0.32
```

未設定の項目は LEVEL の preset 値が自動適用。

##### 起動バナー表示

```
モメンタムシャドー観察: 有効 (実発注なし・観察ログのみ)  Level 3 (標準)  対象=QQQ/SPY/SMH
  QQQ: 5min≥±0.1% AND 15min≥±0.2%
  SPY: 5min≥±0.07% AND 15min≥±0.15%
  SMH: 5min≥±0.15% AND 15min≥±0.3%
    クールダウン: 30分  想定発注額: 70.0% (=$7000)
```

### 認定サポーター 5 名分散検証 (5/14 〜 5/20)

| サポーター | MOMENTUM_LEVEL | 性格 | 想定発火 |
|-----------|----------------|------|---------|
| Supporter 1 | 1 | 最厳格 | 1-2 件/日 |
| Supporter 2 | 2 | 厳格 | 2-3 件/日 |
| Supporter 3 (PAN) | 3 | 標準 ⭐ | 3-5 件/日 |
| Supporter 4 | 4 | 緩和 | 5-8 件/日 |
| Supporter 5 | 5 | 最緩和 | 8-15 件/日 |

CD と Size は全員 30分/70% で固定 → Level 以外の変数を統制して純粋な閾値効果を比較。

### 後方互換

- 旧 `MOMENTUM_TREND_5M_PCT` / `MOMENTUM_TREND_15M_PCT` は MOMENTUM_SYMBOLS 外銘柄のフォールバックのみで使用
- 既存 env 設定があっても警告なしで動作 (LEVEL のプリセットが優先)
- Word ドキュメントで設定変更手順を案内

### 検証

5 レベル × 3 銘柄の全 30 組合せで `_get_momentum_thresholds` を呼出し、プリセット値が正しく返ることを確認:

```
[Level 1 (最厳格)] QQQ:5min=0.15% 15min=0.30%  SPY:5min=0.10% 15min=0.22%  SMH:5min=0.22% 15min=0.45%
[Level 2 (厳格)]   QQQ:5min=0.12% 15min=0.24%  SPY:5min=0.08% 15min=0.18%  SMH:5min=0.18% 15min=0.36%
[Level 3 (標準)]   QQQ:5min=0.10% 15min=0.20%  SPY:5min=0.07% 15min=0.15%  SMH:5min=0.15% 15min=0.30%
[Level 4 (緩和)]   QQQ:5min=0.08% 15min=0.16%  SPY:5min=0.06% 15min=0.12%  SMH:5min=0.12% 15min=0.24%
[Level 5 (最緩和)] QQQ:5min=0.06% 15min=0.12%  SPY:5min=0.04% 15min=0.09%  SMH:5min=0.09% 15min=0.18%
```

個別オーバーライドも検証済 (例: LEVEL=3 + MOMENTUM_QQQ_5M_PCT=0.18 で QQQ 5min のみ上書き)。

---

## v3.9.15 (2026-05-13) — SHORT 決済失敗バグ修正 (panic_sell の論理矛盾 + sync 遅延リトライ)

### 認定サポーター環境で観測された事象

```
22:49:26  Yahoo「Dow Falls 200 Points; US PPI Surge」到着
22:49:28.051  AI 判定: 📉 ネガ conf=0.74 victims=['SPY']
22:49:28.487  SPY SHORT 注文送信 (6 株 @ $733.77)
22:49:28.765  orderId 受領 (約定待ち)
22:49:28.766  ✅ ショートエントリー完了
22:49:28.766  🆘 panic_sell_all 発動 (← 同じ AI 判定の高 conf で連鎖)
22:49:28.780  ❌ ERROR: SHORT 建玉の position_id が取得できない
              (注文受付から 14 ミリ秒後・sync まだ追いついていない)

22:49:34.029  約定確認 FILLED_ALL (発注から 5.5 秒後・以降は通常管理)
```

### 根本原因 (2 つの問題が重なった)

#### 🐛 Bug 1: 論理矛盾 (Design Flaw)

`process_headlines` の SHORT 分岐で:
```python
if confidence >= _thresh:
    place_short(...)                    # ← 新規 SHORT
    if confidence >= PANIC_CONFIDENCE:
        await panic_sell_all(...)        # ← 全 SPY を決済 (= 今 SHORT した分も対象)
```

→ AI が「ネガニュース + 高 conf」と判定すると **「SHORT 発注」と「全決済」が同時発火**。14 ミリ秒後に自分で立てた SHORT を閉じようとする paradox。

`panic_sell_all` の旧設計 (v2.98):
```python
# 旧: SHORT (qty<0) も全決済対象に含める
targets = [sym for sym in exec_syms if state.get(sym).position_qty != 0]
```

#### 🐛 Bug 2: タイミング競合 (Race Condition)

`place_short` 成功 → `ts_state.position_qty -= 6` で内部状態は即座に -6 になるが、`ts_state.position_ids` は `sync_positions()` 経由でしか populate されない。moomoo は注文受付から約定確認 (新ポジション報告) まで 1-5 秒のラグがあるため、14 ミリ秒後の panic では position_ids が空 → `_get_position_ids_for_close` 失敗 → `_notify_close_failed_no_pid` 発火。

### 実害評価 (今回のケース)

| 項目 | 状態 |
|------|------|
| SHORT 6 株が moomoo に成立 | ✅ 約定済 (avg $736.01) |
| Bot による以降の管理 | ✅ 5.5 秒後から通常稼働 (損切り/トレール/タイムアウト全機能) |
| 現在の含み損益 | ≈ -$6 (-0.15%) で安定管理 |
| 致命的損失 | なし |

### 修正内容

#### 修正 1: panic_sell_all を LONG 限定に変更

```python
async def panic_sell_all(trd_env, trigger_symbol, reason):
    exec_syms = {sym for syms in EXECUTION_MAP.values() for sym in syms}
    # ★ v3.9.15: LONG (position_qty > 0) のみを panic sell 対象に
    targets = [sym for sym in exec_syms if state.get(sym).position_qty > 0]
    ...
```

**設計意図**: panic_sell_all は「悪材料で LONG を急いで切る」用途。SHORT は既にネガティブシグナルへの応答ポジションのため panic で閉じる必要なし。これにより「新規 SHORT 発注直後に自分を閉じる」paradox を**構造的に**解消。

#### 修正 2: place_close_all に position_id 取得失敗時のリトライ追加

```python
pids = _get_position_ids_for_close(symbol, side)
if not pids:
    # 内部 state.position_qty が 0 以外 = sync 遅延の可能性
    _state_qty = state.get(symbol).position_qty
    _has_state_position = (
        (side == "LONG"  and _state_qty > 0) or
        (side == "SHORT" and _state_qty < 0)
    )
    if _has_state_position:
        import time as _time_retry
        log.info(f"{tag} [{side_label}] position_id 取得失敗・内部状態は {_state_qty}株 "
                 f"保有中 → sync 遅延の可能性 → 2 秒×3 回リトライします")
        for _attempt in range(3):
            _time_retry.sleep(2)
            sync_positions(trd_env)
            pids = _get_position_ids_for_close(symbol, side)
            if pids:
                log.info(f"{tag} [{side_label}] ✅ リトライ {_attempt+1}回目で取得成功")
                break
    if not pids:
        _notify_close_failed_no_pid(symbol, side, reason)
        return False
```

**効果**: 修正 1 で本件は再発しないが、他経路 (リスク監視ループの panic 発火等) からの呼出も保護。moomoo 約定遅延への defensive 対策。

### 検証 (シナリオテスト 3 件全合格)

| シナリオ | 結果 |
|---------|------|
| LONG (QQQ +10) + SHORT (SPY -5) → panic 対象は ['QQQ'] のみ | ✅ OK |
| SHORT (SPY -6) のみ → panic 対象は [] (5/13 バグの再発防止) | ✅ OK |
| ポジションなし → panic 対象は [] | ✅ OK |

### 影響範囲

- 既存 LONG 主体運用には影響なし (旧版同様 LONG panic は機能)
- SHORT を運用している実口座でのみ修正効果あり
- 受講生環境への影響: なし (デフォルト挙動の縮小方向)

---

## v3.9.14 (2026-05-13) — [市場状態] ログの視認性向上 (3 行展開 + ANSI 色付き)

### 背景

5 分ごとに出力される [市場状態] ログが 1 行に QQQ/SPY/SMH 全部詰め込まれていて、「どの銘柄がどの方向に動いているか」が一目で分からない問題があった。

```
変更前 (1 行詰め込み):
[市場状態] QQQ: 5分 -0.02% / 15分 -0.04% / 60分 -0.22% / SPY: 5分 -0.02% / 15分 -0.05% / 60分 -0.16% / SMH: 5分 -0.03% / 15分 -0.06% / 60分 -0.30%
```

### 修正内容

#### ① 3 行展開 + 銘柄方向絵文字 + ANSI 色付き

```
変更後 (端末表示・色付き):
[市場状態]
  📉 QQQ  │  5分  -0.02%  │  15分  -0.04%  │  60分  -0.22%
  📉 SPY  │  5分  -0.02%  │  15分  -0.05%  │  60分  -0.16%
  📉 SMH  │  5分  -0.03%  │  15分  -0.06%  │  60分  -0.30%
```

(端末では各 % が赤色で表示・60min が太字赤)

#### ② 色分けルール (色覚多様性に配慮した濃淡併用)

| 騰落率 | 色 | 強調 | 絵文字 |
|--------|-----|------|------|
| > +0.10% | 緑 | 太字 | 📈 (60min ベース) |
| 0〜+0.10% | 緑 | 通常 | ➡️ (60min が ±0.10% 内) |
| 0 | 無色 | 通常 | ➡️ |
| -0.10〜0% | 赤 | 通常 | ➡️ |
| < -0.10% | 赤 | 太字 | 📉 (60min ベース) |
| データ不足 | 灰色 | 薄字 | ❓ |

#### ③ ログファイルでは ANSI 自動剥離

既存 `_PlainFormatter` が ANSI コードを自動剥離するため:

```
変更後 (ファイル保存形式・プレーンテキスト):
[市場状態]
  📉 QQQ  │  5分  -0.02%  │  15分  -0.04%  │  60分  -0.22%
  📉 SPY  │  5分  -0.02%  │  15分  -0.05%  │  60分  -0.16%
  📉 SMH  │  5分  -0.03%  │  15分  -0.06%  │  60分  -0.30%
```

→ Excel / VSCode / テキストエディタで開いても文字化けゼロ。

### 既存機能への影響

| 項目 | 影響 |
|------|------|
| ログ regex parser | なし (`[市場状態]` プレフィックスのみで判別) |
| 個別株セクション (5/12 から続く) | 変化なし |
| 「下落トレンド: ...」「データ蓄積: ...」行 | 従来通り保持 |
| ログサイズ | 5 分ごと 1 行 → 4 行 (週次 +70KB 程度・許容範囲) |

### 想定環境別の表示

| 環境 | 表示 |
|------|------|
| macOS Terminal / iTerm2 | 緑・赤・太字でカラフル表示 ✅ |
| Windows Terminal / PowerShell 7+ | カラフル表示 ✅ |
| Windows cmd (旧) | ANSI 未対応版は ESC コードが表示される可能性 (旧版 cmd を避ける案内推奨) |
| ログファイル (.log) | プレーンテキスト ✅ |
| Excel / VSCode で .log 開く | プレーンテキスト ✅ |

### 多様なパターンでの表示例

```
[市場状態]
  📈 QQQ  │  5分  +0.35%  │  15分  +0.42%  │  60分  +0.65%     ← 強い上昇 (太字緑)
  ➡️ SPY  │  5分  +0.05%  │  15分  +0.08%  │  60分  -0.03%     ← ほぼ横ばい
  📉 SMH  │  5分  -0.55%  │  15分  -0.62%  │  60分  -0.85%     ← 強い下落 (太字赤)
  ➡️ ABC  │  5分  データ不足 │  15分  +0.10%  │  60分  -0.05%   ← 一部データなし
```

---

## v3.9.13 (2026-05-13) — Anthropic API 並列呼出シリアライズ (529 連発対策)

### 背景

5/13 04:44 認定サポーター環境で、異なるニュースループから AI 呼出が並列に同時発火 → サーバー側で 529 (Overloaded) を 2 連発した事例を報告いただいた。

```
04:44:35  Alpaca → ① Rigel Pharma  → 529
04:44:38  Yahoo  → ② Consumer spending → 529  ← 並列
04:44:39  ① リトライ送信
04:44:41  ① 200 OK ✅
04:44:41  ② リトライも 529 ❌ (ERROR 記録)
```

v3.9.8 正規化 dedup は「同じヘッドラインの繰り返し」対象だが、今回のように「異なるヘッドラインの並列発火」はカバー対象外だった。SDK 自動リトライは機能したが、根本対策として呼出経路自体の直列化が必要と判断。

### 修正内容

#### ① asyncio.Semaphore + 最小間隔ガード

```python
# モジュールレベル (main で初期化)
_anthropic_api_lock: Optional[asyncio.Semaphore] = None
_last_anthropic_call_time: Optional[datetime.datetime] = None
ANTHROPIC_MIN_INTERVAL_SEC: float = 0.5  # env で調整可

# main() で初期化
async def main(live: bool) -> None:
    global _anthropic_api_lock
    _anthropic_api_lock = asyncio.Semaphore(1)
    ...

# analyze_news() の API 呼出を semaphore で wrap
async def analyze_news(...):
    if _anthropic_api_lock is not None:
        async with _anthropic_api_lock:
            global _last_anthropic_call_time
            if _last_anthropic_call_time is not None and ANTHROPIC_MIN_INTERVAL_SEC > 0:
                elapsed = (datetime.datetime.now() - _last_anthropic_call_time).total_seconds()
                if elapsed < ANTHROPIC_MIN_INTERVAL_SEC:
                    await asyncio.sleep(ANTHROPIC_MIN_INTERVAL_SEC - elapsed)
            _last_anthropic_call_time = datetime.datetime.now()
            response = await asyncio.to_thread(client.messages.create, ...)
```

#### ② env 変数

```bash
ANTHROPIC_MIN_INTERVAL_SEC=0.5   # デフォルト 0.5 秒
                                 # 0 にすると semaphore のみ機能 (間隔ガード無効)
                                 # 上限 10.0 秒 (それ以上は clamp)
```

### 効果

#### 変更前 (v3.9.12)

```
04:44:35  Alpaca → ① 送信 (即時)
04:44:38  Yahoo  → ② 送信 (即時) ← 並列で詰まる → 529
```

#### 変更後 (v3.9.13)

```
04:44:35.0  Alpaca → ① ロック取得 → 送信 → 200 OK (1.8秒で完了)
04:44:38.0  Yahoo  → ② ロック待ち
04:44:38.3  ② ロック取得 (0.5秒間隔ガード経過後) → 送信 → 200 OK
```

5 並列発火テスト結果 (実測):
- 5 件すべて 0.5 秒間隔で完全直列化
- 総経過時間 約 2.0 秒 (= 4 × 0.5 + API 処理時間)
- サーバー側同時着弾: 完全に排除

### 3 重防御スタック (累積)

| 層 | 機能 | 対象 |
|----|------|------|
| ① SDK 自動リトライ | Anthropic SDK 内蔵 (max_retries=2) | 単発の 529 救済 |
| ② v3.9.8 正規化 dedup | check_headline_seen | 同一ヘッドライン繰り返し排除 |
| **③ v3.9.13 並列直列化** | asyncio.Semaphore | **異なるヘッドラインの同時発火排除** |

3 層を組合せると 529 の発生確率は **ほぼゼロ** になる見込み。

### 想定影響

- **遅延**: 連続発火時は (件数 - 1) × 0.5 秒の追加待機
  - 例: 一気に 5 件来たら最後の判定まで 2 秒余分に待つ
  - 但し AI 判定自体が 1-2 秒/件かかるので体感差は微小
- **既存キャッシュ効率**: 維持 (cache_read/write は API レベルなので影響なし)
- **既存機能**: すべて保持 (analyze_news の中身を 1 層 wrap するだけ)

### 受講生環境への影響

- 起動バナー変化なし
- Discord 通知変化なし
- 既存の発注/決済挙動変化なし
- 並列発火時の遅延 (最大数秒) のみが見える変化 — ログに `[API直列化]` DEBUG として記録

---

## v3.9.12 (2026-05-13) — モメンタムシャドー観察モード (Phase 0)

### 背景

5/12 PAN ログ分析 (73 件) で、エントリー時の preEntry (5 分前の QQQ 変動) と勝率の相関を確認:

| preEntry | 件数 | 勝率 | 平均 PnL |
|---------|------|------|---------|
| -0.1〜-0.3% (下落中) | 4 | **100%** | $+10.13 |
| ±0.1% (横ばい) | 50 | 36% | $+1.47 |
| +0.1〜+0.3% (上昇中) | 15 | **93%** | $+9.21 |

**ニュース判定の質に関わらず、トレンドの強さがリターンを支配**しているという強い示唆。「ニュースなしのトレンドフォロー」戦略の有効性を 2 週間で検証する Phase 0 を実装。

### 機能概要

QQQ/SPY/SMH の 5min/15min 騰落率の絶対値が両方しきい値を超えた瞬間に、シャドー発注として観察ログに記録 (実発注なし)。Phase 2 PnL 機能が +5/+15/+60min 後の真の PnL を自動取得。

### 実装内容

#### ① モメンタムシャドー観察ループ (約 130 行)

```python
async def momentum_shadow_loop() -> None:
    """強トレンド検知でシャドー発注を観察ログに記録 (実発注なし)。"""
    if not MOMENTUM_SHADOW_ENABLED:
        return
    if not _is_data_collect_enabled():
        return
    if MOMENTUM_ORDER_SIZE_PCT <= 0:
        return
    
    while True:
        await asyncio.sleep(MOMENTUM_CHECK_INTERVAL_SEC)  # 5 分間隔
        await market_open_event.wait()
        
        for symbol in MOMENTUM_SYMBOLS:
            pct_5  = get_index_pct_change(symbol, 5)
            pct_15 = get_index_pct_change(symbol, 15)
            if pct_5 is None or pct_15 is None: continue
            
            if pct_5 >= MOMENTUM_TREND_5M_PCT and pct_15 >= MOMENTUM_TREND_15M_PCT:
                direction = "LONG"  # ...
            elif pct_5 <= -MOMENTUM_TREND_5M_PCT and pct_15 <= -MOMENTUM_TREND_15M_PCT:
                direction = "SHORT"  # ...
            else:
                continue
            
            # クールダウン + 観察ログ記録 (block_stage="momentum_shadow")
            _log_observation(...)
```

#### ② 全 env 変数 (12 個・すべて調整可能)

```bash
MOMENTUM_SHADOW_ENABLED=true       # Phase 0 機能 ON/OFF
MOMENTUM_LIVE_TRADING=false        # Phase 1 (実発注) は 2 週間後の検証後
MOMENTUM_TREND_5M_PCT=0.20         # 5min ±0.20% 以上
MOMENTUM_TREND_15M_PCT=0.40        # 15min ±0.40% 以上
MOMENTUM_CHECK_INTERVAL_SEC=300    # 5 分間隔
MOMENTUM_COOLDOWN_MIN=30           # 同方向クールダウン 30 分
MOMENTUM_ORDER_SIZE_PCT=20         # BUDGET_USD の 20% を想定発注
MOMENTUM_MAX_OPEN_POSITIONS=1      # 同時保有上限
MOMENTUM_TIMEOUT_MIN=15            # (Phase 1 用) タイムアウト
MOMENTUM_STOP_PCT=-0.30            # (Phase 1 用) 損切り
MOMENTUM_TRAIL_TRIGGER_PCT=0.30    # (Phase 1 用) トレール発動
MOMENTUM_TRAIL_DROP_PCT=0.20       # (Phase 1 用) トレール幅
MOMENTUM_SYMBOLS="QQQ,SPY,SMH"     # 監視銘柄
```

#### ③ 認定サポーター 5 名 × 5 パターン分散検証

| パターン | 5min | 15min | クールダウン | 予算% | 想定発火 |
|---------|------|-------|------------|------|---------|
| A: 厳格・大口 | 0.30% | 0.50% | 60 分 | 30% | 1-2 件/日 |
| **B: PAN 推奨** | 0.20% | 0.40% | 30 分 | 20% | 2-4 件/日 |
| C: 緩和・小口 | 0.15% | 0.30% | 15 分 | 15% | 5-10 件/日 |
| D: 中間 (厳格寄り) | 0.25% | 0.45% | 45 分 | 25% | 1-3 件/日 |
| E: 中間 (緩和寄り) | 0.18% | 0.35% | 20 分 | 20% | 3-6 件/日 |

#### ④ 起動バナー表示 (DATA_COLLECT=true 環境のみ)

```
モメンタムシャドー観察: 有効 (実発注なし・観察ログのみ)
  5m≥±0.20%/15m≥±0.40%  対象=QQQ/SPY/SMH  クールダウン=30分
  想定発注額=20.0% (=$2000)
```

### 受講生への影響

- **DATA_COLLECT=false** の受講生は **完全に無感** (ループ自体が起動しない)
- バナー表示・Discord 通知・ログ出力 すべてなし
- 既存のニュース戦略は通常通り動作 (両者は完全独立)

### GAS 変更

**不要**。既存の観察ログスキーマで `block_stage="momentum_shadow"` として識別可能。

### 検証スケジュール

- 期間: 2026/5/14 〜 5/27 (2 週間)
- 蓄積目安: 5 名 × 5 パターンで 168-350 件
- 振り返り会議: 5/28 (Discord 通話 60 分)

### Phase 1 移行判定基準

- ✅ 平均総 PnL > +$5/件 → Phase 1 (実発注) GO
- 🟡 +$0〜+$5/件 → 閾値再調整して再観察
- 🔴 < $0/件 → 廃案

### 配布物

- `moomoo_trade_v1.py` v3.9.12
- `モメンタムシャドー観察_認定サポーター案内.docx` (PAN/サポーター向け案内)

---

## v3.9.11 (2026-05-13) — Alpaca 認証失敗の指数バックオフ + _MACRO_KEYWORDS 大幅拡充

### 背景

5/11-13 のログ (Mac PAN 187k 行 + Windows PAN 57k 行) を分析し、2 件の改善点を特定。

### 観測された問題

#### 🚨 問題 1: Alpaca 認証失敗 738 件 (5/11 17:00-19:25・2.5h)

```
2026-05-11 17:00:26 [ERROR] [Alpaca News] 認証失敗:
  [{'T': 'error', 'code': 406, 'msg': 'connection limit exceeded'}]
...
(1 分間に 6 回・約 2.5 時間連続)
```

**原因**: 同じ Alpaca API キーを Mac と Windows で同時使用 → Alpaca News WebSocket は同時 1 接続のみ許可のため `connection limit exceeded` (code 406) が連発。

**現状の挙動**: 認証失敗 → 数秒間隔で即リトライ (固定間隔) → ログ汚染膨大化。

#### 🚨 問題 2: ETF影響ガード _MACRO_KEYWORDS の不足

```
5/12 23:07 ヘッドライン:
  "Consumer prices surged 3.8% in April from a year ago, driven by energy costs"
AI 判定: 📈 conf=0.74 MACRO bene=['SPY']  ← CPI 関連を正しく判定
ETF影響ガード: SPY を除外
  (理由: マクロKW(Fed/CPI/金利等)も SPYセクター語も登場しないため)
最終結果: bene=[] → 発注ゼロ
```

「CPI」という単語は使われず「Consumer prices surged 3.8%」と書かれていたため、既存 `_MACRO_KEYWORDS` の `"cpi"` `"inflation"` 検出に失敗。CPI 発表日 (月 1 回) の最重要シグナルを取り逃がした。

### 修正内容

#### ① Alpaca News 認証失敗 指数バックオフ

```python
_AUTH_BACKOFF_BASE_SEC = 10
_AUTH_BACKOFF_MAX_SEC = 600
_auth_fail_count = 0

# 認証失敗時
_auth_fail_count += 1
_backoff_sec = min(
    _AUTH_BACKOFF_BASE_SEC * (2 ** (_auth_fail_count - 1)),
    _AUTH_BACKOFF_MAX_SEC
)
_log_fn = log.error if _auth_fail_count == 1 else log.warning
_log_fn(
    f"[Alpaca News] 認証失敗 ({_auth_fail_count}回連続): {auth_msg} "
    f"→ {_backoff_sec}秒後に再接続 "
    f"(同じ Alpaca キーを複数 PC で同時使用していないかご確認ください)"
)
await asyncio.sleep(_backoff_sec)
continue

# 認証成功時
if _auth_fail_count > 0:
    log.info(f"[Alpaca News] ✅ 認証復旧 ({_auth_fail_count}回失敗の後) → バックオフカウンタをリセット")
    _auth_fail_count = 0
```

| 回数 | 待機時間 |
|-----|---------|
| 1   | 10 秒 |
| 2   | 20 秒 |
| 3   | 40 秒 |
| 4   | 80 秒 |
| 5   | 160 秒 |
| 6   | 320 秒 |
| 7+  | 600 秒 (10 分・上限) |

**効果**:
- 5/11 のケース (2.5h で 738 件失敗) なら新挙動では **約 20 回の試行に圧縮** = ログ汚染 -97%
- 初回失敗のみ ERROR、2回目以降は WARNING (受講生 Discord 混乱抑制)
- 受講生にも親切なメッセージで「複数 PC 同時使用の可能性」を示唆

#### ② _MACRO_KEYWORDS 大幅拡充 (21 → 83 件)

**追加カテゴリ別** (合計 +62 キーワード):

```python
# FED 関連追加 (+8)
"dot plot", "fomc minutes", "fed minutes", "rate decision",
"basis points", "bps cut", "bps hike", "fed funds",
"hawkish", "dovish", "tightening", "easing cycle",

# CPI/PPI シノニム (+12)  ← 5/12 取り逃がしの主因対策
"consumer prices", "consumer price index", "core prices", "core inflation",
"core cpi", "headline inflation", "headline cpi",
"wholesale prices", "producer prices", "producer price index",
"pce", "core pce", "personal consumption",

# 雇用統計シノニム (+9)
"nonfarm payrolls", "nfp", "unemployment rate", "jobless claims",
"initial claims", "weekly claims", "continuing claims",
"labor market", "wage growth", "average hourly earnings",
"jolts", "job openings",

# 主要マクロ指標 (+13)
"retail sales", "ism manufacturing", "ism services", "ism index",
"pmi", "manufacturing pmi", "services pmi",
"consumer confidence", "consumer sentiment", "michigan sentiment",
"housing starts", "existing home sales", "new home sales",
"industrial production", "capacity utilization",
"durable goods", "factory orders",
"trade deficit", "current account",

# 地政学・財政 (+5)
"soft landing", "hard landing", "debt ceiling", "fiscal cliff",
"government shutdown", "credit rating",
```

### 検証 (11 ケース全合格)

| ヘッドライン | 期待 | 実測 |
|------------|------|------|
| Consumer prices surged 3.8% in April | **MACRO** | ✅ ヒット |
| Existing Home Sales 4.02M Vs 4.05M Est. | MACRO | ✅ |
| Powell hints at hawkish stance | MACRO | ✅ |
| Nonfarm payrolls 175K vs 200K expected | MACRO | ✅ |
| Retail sales jumped 0.7% | MACRO | ✅ |
| Consumer confidence falls 14-month low | MACRO | ✅ |
| PCE inflation rises 2.8% YoY | MACRO | ✅ |
| CPI shows 3.2% annual inflation (既存) | MACRO | ✅ |
| FED cuts rates by 25 bps (既存) | MACRO | ✅ |
| Apple earnings beat | 非 MACRO | ✅ |
| Tesla stock plunges | 非 MACRO | ✅ |

### 想定効果

- **5/12 CPI 発表で SPY 発注機会を逃した事例** が新版では発注対象になる (機会獲得)
- 月に 1 回の CPI / FOMC / NFP / Retail Sales 等の重要日に確実に SPY/QQQ 発注経路が開く
- Alpaca 認証失敗時のログ汚染 -97% → 受講生 Discord での混乱抑制

---

## v3.9.10 (2026-05-13) — 5/12 分析発見 6 件の一括修正 (終盤エントリーブロック ほか)

### 背景

5/12 のトレード 73 件 (総 PnL $+256.48) を分析し、**構造的損失要因 6 件** を特定。本 v3.9.10 で全てを修正。

### 観測された問題

#### 🚨 問題 1: デモ日次決済 (15:45 ET) 直前のラスト 1 分エントリーで 22 件損失

```
ニュース: "Consumer spending is healthy, but gas prices remain a risk"
発注時刻: 15:45:11 〜 15:46:06 ET (55 秒間に集中)
件数:    22 件
結果:    1 勝 21 敗 (勝率 4.5%) / PnL -$74.27
内訳:    買って数秒〜数分後に強制決済で全件損切り
```

`place_buy()` / `place_short()` の冒頭に「強制決済時刻まで N 分以内ならエントリースキップ」のガードが**ない**ことが原因。

#### 🟡 問題 2: SMH 0.80 厳格化が過剰保守

5/12 観察ログで `smh_strict` が 6 件発火・**もし通していたら平均 +$6.20/share の勝ち**。閾値を見直す余地あり。

#### 🟡 問題 3: BTC/altcoin 価格記事が QQQ 判定材料化

```
Bitcoin and ethereum prices today, Tuesday, May 12
→ AI が QQQ 上昇材料と誤連想 → 4 件で偶然 +$60 程度の勝ち
→ 構造的にノイズ (BTC/ETH は QQQ 構成銘柄ではない)
```

#### 🔴 問題 4: エネルギー価格関連ヘッドラインで TECH/SEMI 誤分類

```
Case A (QQQ 誤マッチ):
  "Consumer spending is healthy, but gas prices remain a risk"
  → AI category=TECH, beneficiaries=['QQQ'], conf=0.74
  → 22 件発注 -$74.27 (問題 1 と同一)

Case B (SMH 誤マッチ):
  "Think $6 Gas Is Bad? It's About to Get Even Worse in California"
  → AI category=SEMI_STRONG, beneficiaries=['SMH']
  → カリフォルニアのガソリン価格 = 半導体? という構造的にあり得ない誤マッチ
```

#### 🔴 問題 5: 反語形 + 悲観未来形が既存 QUESTION ガードを素通り

```
"Think $6 Gas Is Bad? It's About to Get Even Worse"
  → 既存 QUESTION 正規表現 ^(will|are|is|does|...) には該当しない
  → 反語 "Think X? Y" 形式・悲観未来形 "about to get worse" がガード対象外
```

#### 🟡 問題 6: 横ばい (preEntry ±0.1%) エントリーの勝率 36% (50 件中 32 件負け)

「明確なトレンドがある時は勝てるが、横ばい時に撃つと負ける」傾向。今期は対処見送り (機会損失リスクとのバランス検証中)。

### 修正内容

#### ① 終盤エントリーブロック (新規・最重要)

```python
ENTRY_BLOCK_BEFORE_CLOSE_MIN: int = 15   # 強制決済の何分前から新規エントリー禁止か

def _is_late_session_entry_blocked() -> tuple[bool, float]:
    """強制決済発火時刻 (close_trigger_time) の N 分前以内なら True を返す。"""
    now_et = datetime.datetime.now(_ET).replace(tzinfo=None)
    early_close = get_early_close_time(now_et.date())
    if early_close:
        close_minutes = early_close.hour * 60 + early_close.minute - _EARLY_CLOSE_PRECLOSE_MINUTES
        close_t = datetime.time(close_minutes // 60, close_minutes % 60)
    else:
        close_t = _FRIDAY_CLOSE_TIME
    close_dt = now_et.replace(hour=close_t.hour, minute=close_t.minute, second=0, microsecond=0)
    mins_to_close = (close_dt - now_et).total_seconds() / 60.0
    block = (0 < mins_to_close <= ENTRY_BLOCK_BEFORE_CLOSE_MIN)
    return block, mins_to_close
```

`place_buy()` / `place_short()` の早期 exit で各 1 箇所追加。観察ログにも `block_stage="late_session"` で記録。

#### ② SMH_CONFIDENCE_THRESHOLD: 0.80 → 0.78

`0.78 ≥ v2.94 当初の最悪帯 0.75` で安全範囲。観察ログを 1 週間蓄積後に再評価。

#### ③ HEADLINE_NOISE_PATTERNS に暗号資産系 5 パターン追加

```python
# BTC/ETH 価格速報
(r'\b(?:bitcoin|ethereum|btc|eth)\s+(?:price|prices)\s+(?:today|move|moves|jump|...)',
 'crypto-price-update'),
# BTC + ETH の同時言及
(r'\b(?:bitcoin|ethereum|btc|eth)\s+and\s+(?:bitcoin|ethereum|btc|eth)\s+price',
 'crypto-price-pair'),
# Altcoin 銘柄
(r'\b(?:dogecoin|cardano|solana|polkadot|chainlink|polygon|matic|avax|near\s+protocol)\b',
 'altcoin-specific'),
# "Where Will Cardano Be in 2030" 系
(r'\bwhere\s+will\s+(?:bitcoin|ethereum|cardano|solana|dogecoin)\b',
 'crypto-future-prediction'),
# Crypto ETF
(r'\b(?:bitcoin|ethereum)\s+etf\b',
 'crypto-etf-product'),
```

#### ④ HEADLINE_NOISE_PATTERNS にエネルギー価格 + リスク組合せ 3 パターン

```python
# "gas price ... risk" 等の組合せ
(r'\bgas\s+price.*\b(?:risk|worse|surge|spike|concern|threat|warning|bad)\b',
 'macro-gas-risk-combo'),
# "$X gas"
(r'\$\d+\s+gas\b', 'macro-dollar-gas'),
# "consumer spending ... risk" 等
(r'\bconsumer\s+spending.*\b(?:risk|worse|concern|threat|headwind|warning)\b',
 'macro-consumer-risk-combo'),
```

#### ⑤ SYSTEM_PROMPT 強化 (`_PROMPTS_VERSION: 1.5.0 → 1.6.0`)

エネルギー価格 = MACRO カテゴリ固定ルール、「ポジ but ネガ」「反語 + 悲観未来」ヘッジ構造の conf ≤ 0.55 抑制ルールを `UNIFIED_SYSTEM_PROMPT_TEMPLATE` に追加。

#### ⑥ HEADLINE_QUESTION_PATTERNS 拡張

```python
# "Think X? Y" 反語形
r'^\s*Think\s+[^?]{3,}\?\s+(?:It|That|This|We|You|Here|There)',
# 悲観未来形
r'\babout\s+to\s+get\s+(?:worse|harder|tougher|tighter|uglier|messier)\b',
# "Bad? ... Worse" 比較ヘッジ
r'\b(?:bad|tough|hard|painful)\?\s+(?:it|that|this|we|you|here|there)',
```

#### ⑦ カテゴリ × キーワード整合性チェック (post-AI 第 4 防衛線)

```python
_SEMI_KEYWORD_REGEX = re.compile(r'\b(?:chip|semiconductor|wafer|fab|nvda|...)\b', re.I)
_ENERGY_PRICE_REGEX = re.compile(r'\b(?:gas\s+price|...|\$\d+\s+gas|...)\b', re.I)

# _apply_meta_filter_to_ai_result 内で:
# (a) SEMI_STRONG カテゴリだが半導体キーワード皆無 → score=0 強制
# (b) エネルギー価格ヘッドラインで QQQ/SMH が bene/victims に含まれる → 該当 ETF 除外
```

### 4 重防御の効果

#### 5/12 Case A (Consumer spending) 再発防止

```
ヘッドライン: "Consumer spending is healthy, but gas prices remain a risk"
  防御層 1: HEADLINE_NOISE_PATTERNS (macro-gas-risk-combo) → AI 呼出前カット
  防御層 2: SYSTEM_PROMPT 強化 → AI が conf ≤ 0.55 抑制
  防御層 3: カテゴリ整合性 → エネルギー価格で QQQ 除外
  防御層 4: 終盤エントリーブロック → 15:45 ET の 15 分前以降は BUY 自体スキップ
→ 4 重防御で再発防止
```

#### 5/12 Case B (Think $6 Gas Is Bad?) 再発防止

```
ヘッドライン: "Think $6 Gas Is Bad? It's About to Get Even Worse in California"
  防御層 1: HEADLINE_NOISE_PATTERNS (macro-dollar-gas) → AI 呼出前カット
  防御層 2: HEADLINE_QUESTION_PATTERNS (Think...? It + about to get worse) → score=0 強制
  防御層 3: カテゴリ整合性 (SEMI_STRONG なのに半導体 KW 皆無) → score=0 強制
  防御層 4: SYSTEM_PROMPT (反語 + 悲観未来形) → conf ≤ 0.55 抑制
→ 4 重防御で再発防止
```

### 想定効果 (5/12 観測ベース試算)

| 損失要因 | 5/12 実損 | v3.9.10 後 |
|---------|---------|----------|
| 終盤エントリー集中 (22 件) | -$74.27 | $0 |
| Gas tax holiday 連続判定 | -$3.50 | $0 |
| BTC 偶然勝ち (機会損失だが構造的にノイズ) | +$60 | $0 |
| **正味改善** | -$74 | **+$74/日 ≒ +$1,500/月** |

### 検証 (全 6 項目テスト合格)

- 終盤エントリーブロック関数: 存在・定数 = 15 ✅
- SMH 0.78 確認 ✅
- BTC/altcoin ノイズパターン: 4/4 ヒット ✅
- エネルギー価格ノイズ: PAN 5/12 22件損失の元ヘッドラインを捕捉 ✅
- QUESTION 拡張: Think $6 Gas Is Bad? → ダブルヒット (Think...? It + about to get worse) ✅
- カテゴリ整合性: SEMI_STRONG + ガス価格 = score=0 強制 / 正常な NVDA SEMI = 通過 ✅

---

## v3.9.9 (2026-05-12) — 確定損益ログの視認性向上 + AlphaVantage バナー誤解修正

### 観測された問題

#### 問題 1: 確定損益ログがフラットで埋もれる

```
2026-05-12 21:40:34,514 [INFO] 【SPY】 [確定損益(SC)]  realized_pnl=-4.58  cover_avg=737.04  short_avg=736.12  qty=5
```

PnL のマイナス符号や金額が他のログと同じトーンで表示され、「実際に -$4.58 損したのか」が一目で分からない問題。

#### 問題 2: AlphaVantage バナーが誤解を招く

`fetch_external_news()` は Finnhub 優先で AlphaVantage はフォールバック専用設計。だが起動バナーが「✅ 設定済み」とだけ出すため、「設定したのに使われていないのでは?」という受講生からの問い合わせを招く状態だった。

### 修正内容

#### ① 確定損益ログの視認性向上

LONG決済 / SHORT決済 (SC) の前置きに **絵文字 + ANSI 色付き損益サマリ + フロー** を追加:

```
変更前 (フラット):
  【SPY】 [確定損益(SC)] realized_pnl=-4.58 cover_avg=737.04 short_avg=736.12 qty=5

変更後 (端末では太字赤):
  【SPY】 📉 SHORT決済損 $-4.58 (-0.12%)  │  5株  │  売 $736.12 → 買 $737.04  │
  [確定損益(SC)] realized_pnl=-4.58 cover_avg=737.04 short_avg=736.12 qty=5
```

4 パターン (LONG/SHORT × 益/損) を以下で色分け:
- 利益: `📈 ◯◯決済益` 太字緑 (`\033[1;32m`)
- 損失: `📉 ◯◯決済損` 太字赤 (`\033[1;31m`)
- 同値: `➡️ ◯◯決済 同値` デフォルト色

#### ② ログファイルでは ANSI 自動剥離

既存 `_PlainFormatter` が ANSI コードを自動剥離するため、ログファイルにはプレーンテキストのみ保存される。Excel/VSCode で開いても文字化けゼロ。

#### ③ regex parser 互換性 100% 保持

`realized_pnl=... sell_avg=... buy_avg=... qty=...` の構造化部分は完全に維持されており、既存の report parser がそのまま動作。

#### ④ AlphaVantage バナー誤解修正

`fetch_external_news()` の動作実態を反映した 2 段階表示に変更:

```
変更前:
  ALPHAVANTAGE_API_KEY: ✅ 設定済み

変更後 (Finnhub 設定済み環境):
  ALPHAVANTAGE_API_KEY: ✅ 設定済み（待機中: Finnhub 優先のため呼出されない）

変更後 (Finnhub 未設定環境):
  ALPHAVANTAGE_API_KEY: ✅ 設定済み（Finnhub 未設定のため使用中）
```

ニュースソース一覧 (`_ext_sources`) でも、AlphaVantage は Finnhub 未設定時のみ表示する。

### 検証

- 4 パターンの色付け正常動作 (端末) ✅
- ログファイルは ANSI 自動剥離 ✅
- report parser regex マッチ成功 ✅
- バナー表示が実態を反映 ✅

---

## v3.9.8 (2026-05-12) — 正規化ヘッドライン dedup (AI コスト削減 + ログ汚染抑制)

### 背景

5/11 PAN ログ (18 時間分・v3.9.6 運用) で、同一ヘッドラインが繰り返し AI 判定されている問題が判明:

| ヘッドライン | AI 判定回数 (同一文字列) |
|------------|---------------------|
| `600 OpenAI employees scored $6.6 billion in a single day...` | **11 回** |
| `Tech stocks today: AI chipmaker Cerebras to stage blockbuster IPO...` | **9 回** |
| `OpenAI creates new unit with $4 billion investment...` | **6 回** |
| `Trump backs gas tax holiday as pump prices rise...` | **3 回連続 AI 判定** |
| `USA Existing Home Sales For April 4.02M Vs 4.05M Est.` | **3 回** |

### 原因

既存の重複検知層が以下の理由で素通り:
- **URL/GUID dedup**: Yahoo Finance は同一記事の GUID を 5 分おきに更新するため失効
- **TOPIC_KEYWORDS dedup**: `iran/fed/tariff/inflation` 等の固定キーワードのみで、企業ニュース・マクロ指標・業界記事は対象外
- **AI cache (ai_cache)**: ハッシュキーが厳密一致のため、1 文字でも違うとミス

### 修正内容

#### ① 正規化ヘッドライン dedup 層を追加

`GlobalState` に 2 メソッド + 1 ヘルパーを追加:

```python
@staticmethod
def _normalize_headline_key(headline: str) -> str:
    """ヘッドラインを正規化して dedup キー (sha1[:16]) を返す。"""
    h = html.unescape(headline)
    # 金額・億/百万単位・割合を # に
    h = re.sub(r'\$?\d[\d.,]*\s*(?:billion|million|trillion|B|M|K|%|cents?|bps)?',
               '#', h, flags=re.I)
    h = re.sub(r'\d+', '#', h)
    h = re.sub(r'\s+', ' ', h.lower().strip())
    return hashlib.sha1(h.encode('utf-8')).hexdigest()[:16]

def check_headline_seen(self, headline) -> tuple[bool, int, int]:
    """正規化キーで 60 分以内の既出を判定。戻り値: (is_dup, 経過秒, 累計件数)"""
    ...

def mark_headline_seen(self, headline) -> None:
    """ヘッドラインを記録 (or hit_count をインクリメント)。"""
    ...
```

#### ② 正規化の方針 (誤マージ回避)

| 正規化対象 | やる/やらない | 理由 |
|-----------|-------------|------|
| HTML エンティティ復号 (`&#39;` → `'`) | ✅ やる | 表示差吸収 |
| 大文字小文字統一 | ✅ やる | "TESLA" と "Tesla" 等 |
| 連続空白圧縮 | ✅ やる | フォーマット差吸収 |
| 金額・割合・数値の `#` 化 | ✅ やる | 速報の数値更新を吸収 |
| 動詞 (beats/misses/cuts) の維持 | ✅ やる | **方向情報の保持・最重要** |
| 同義語マージ | ❌ やらない | AI 判定領域 |

`Beats Estimate` と `Misses Estimate` は別キーとして扱う (キー検証: それぞれ `23d9...` と `8388...` で衝突しない)。

#### ③ 統合先

```python
# process_headlines (マクロニュース)
_topic_blocked, _topic_reason = state.check_topic_cooldown(texts)
if _topic_blocked: return

# ★ v3.9.8 追加
_hl_seen, _hl_elapsed, _hl_hits = state.check_headline_seen(texts[0])
if _hl_seen:
    log.info(f"[正規化重複] {_hl_elapsed//60}分前と同一 (累計 {_hl_hits+1} 件目) → スキップ")
    state.mark_headline_seen(texts[0])
    return
...
state.mark_headline_seen(texts[0])  # AI 呼出後に記録
```

`process_stock_news` (個別株モード) にも同じパターンを統合。

### TTL: 60 分 (既存 TOPIC_COOLDOWN_SEC と同じ)

1 日にすると 5/4 イラン情勢の 20 件全スキップ事案 (価格急変時の再判定機会喪失) と同種のリスク。60 分なら速報の繰り返し配信を吸収しつつ、相場変動による再判定機会を確保。

### 想定効果 (5/11 PAN ログベース試算)

| ヘッドライン | 現状 AI 呼出 | v3.9.8 後 | 削減 |
|------------|-----------|---------|------|
| OpenAI 600 employees | 11 回 | 1 回 | **-10** |
| Tech stocks today: Cerebras IPO | 9 回 | 1 回 | **-8** |
| OpenAI creates new unit | 6 回 | 1 回 | **-5** |
| Trump backs gas tax holiday | 3 回 | 1 回 | **-2** |
| USA Existing Home Sales | 3 回 | 1 回 | **-2** |

合計: 1 日で約 **27 回** の Anthropic 呼出が節約。月コスト換算: **-$5〜10**。

### 副次効果 (コスト削減以外)

- 「同じヘッドラインで 11 回連続 ETF ガード発火」のようなログ汚染を抑制
- 観察ログ (v3.9.7) にも重複行が入らない → サポーター集計が綺麗に
- 受講生環境の AI 呼出回数も削減 → レート制限到達リスク減

### 検証 (6 ケース全合格)

- 数字違いの同一性: Tesla EPS $1.05 / $1.07 / $1.10 → すべて同一キー `42fb8052`
- 方向違いは別キー: Beats vs Misses → 別キー `23d9...` vs `8388...`
- HTML エンティティ: `&#39;` と `'` を同一視
- 9 件の同一ヘッドライン入力 → AI 呼出 1 件のみ (削減 8)
- 末尾追加 (Sales $25.1B 等) は別記事扱い (期待通り)

---

## v3.9.7 (2026-05-12) — 観察ログ機能 + ネガ語幹×LONG倒錯ガード (A 案・Keel ケース対策)

### 背景

v3.9.x の累積ガード強化により、PAN 環境 (v3.9.6 観察期間 18 時間) で 1,611 件の AI 判定があったにもかかわらず **実発注がゼロ** という現象が発生。フィルタが多層に積み重なった結果、`conf_threshold` / `smh_strict` / `etf_guard_strong` / `meta_question` などのいずれかで遮断されて発注に至らないケースが大半を占めていた。

これが **受講生全員に伝播すると「観察モード状態」(発注ゼロ) が常態化** し、教育プログラムの実トレード経験が損なわれるリスクがある。フィルタを過度に緩めると 5/11 の Keel/OpenAI 損失パターン (-$256 / TECH カテゴリ) が再発するため、まずは **データで実態を可視化** して定量的に判断する必要があった。

### 機能概要

AI が `score=±1` を返したのに発注に至らなかった **near-miss ケース** を、ブロック理由 (`block_stage`) を含めて GAS スプレッドシートに自動収集する観察ログ機能を追加。

**Phase 1**: フィルタ別の発火統計を収集 (今すぐ稼働)
**Phase 2**: +5/+15/+60min 後の「もし発注していたら PnL」を自動計算 (60min 後にバックグラウンドで更新)

### 設計原則

| 原則 | 実装 |
|------|------|
| 受講生に可視化される変化はゼロ | バナー変化なし・Discord 通知なし・ログは全て DEBUG レベル |
| Bot 動作を一切阻害しない | GAS 送信は daemon thread・例外は全て黙殺 |
| 既存トレード送信と同じガード | `DATA_COLLECT=true` + `STUDENT_NAME` 設定済み時のみ動作 |
| 既存 GAS 集計に一切影響なし | 新タブ「観察ログ」を自動作成・既存タブ「トレード詳細」等には触らない |

### 実装内容

#### ① 観察ログヘルパー (約 250 行新規追加)

```python
_PENDING_OBSERVATIONS: Dict[str, Dict[str, Any]] = {}
_PENDING_OBSERVATIONS_MAX = 5000      # メモリ上限 (FIFO)
_OBSERVATION_PNL_DELAY_SEC = 3600     # +60min 後に PnL 計算

def _log_observation(*, symbol, side, confidence, score, category, headlines,
                     beneficiaries, victims, outcome, block_stage, block_reason,
                     price_at_decision=0.0) -> None:
    """observation event を GAS へ送信 + Phase 2 用に pending 登録。"""
    ...

async def observation_pnl_check_loop() -> None:
    """60 秒毎に pending observation をスキャン、+60min 経過したものを処理。"""
    ...

def _compute_and_send_observation_pnl(obs_id, info) -> None:
    """K 線から +5/+15/+60min の価格を取得 → PnL/share 計算 → GAS 更新。"""
    ...
```

#### ② フィルタ exit への instrumentation (各 1 行追加)

`place_buy()` / `place_short()` / `process_headlines()` の主要 filter exit に `_log_observation()` を 1 行追加:

- `risk_event_a` (構成 A: 銘柄別リスクイベントブロック)
- `macro_downtrend` (構成 B: QQQ 下落)
- `smh_strict` (SMH 0.80 厳格化)
- `smh_downtrend` (構成 C: SMH 下落)
- `spy_downtrend` (構成 S: SPY 下落, v3.9.5)
- `stock_downtrend` (構成 D: 個別株下落)
- `macro_uptrend` (構成 B': QQQ 上昇 SHORT ブロック)
- `smh_uptrend` (構成 C': SMH 上昇)
- `spy_uptrend` (構成 S': SPY 上昇, v3.9.5)
- `stock_uptrend` (構成 D': 個別株上昇)
- `conf_threshold` (RTH/Pre/After/OVN 閾値未満)
- `victim_in_beneficiary` (AI 内部矛盾)
- `ordered` (発注成功パス)

#### ③ main() に Phase 2 loop 登録

```python
_loop_guard(observation_pnl_check_loop, name="観察PnL"),
```

`DATA_COLLECT=true` 以外の環境では即 return してアイドル化 (リソース節約)。

### Phase 2 PnL/share の正規化

側 (BUY/SELL_SHORT) に応じた符号調整で「もし発注していたら 1 株あたり何ドル儲かっていたか」を計算:

```python
pnl_per_share = (price_at_T - price_at_decision) * side_sign
# side_sign = +1 (BUY) / -1 (SELL_SHORT)
```

株数を仮定しないため、集計時の予算スケール差問題が出ない。

### GAS 側の対応

GAS を v6 → v7 にアップデート要 (別途共有済み)。主な変更:
- 新タブ「観察ログ」を初回イベント受信時に自動作成
- `doPost` に分岐 2 行追加 (`type='observation'` / `type='observation_pnl_update'`)
- 既存タブ・既存集計には一切手を入れない

### 受講生への影響

- `.env` 変更不要
- バナー変化なし
- Discord 通知なし
- ローカルファイル変化なし
- すべて DEBUG ログ → 受講生からの問い合わせが増える要素ゼロ

### 認定サポーター運用

集計は Google Sheets のピボットテーブルで完結:

```
行:    block_stage
列:    日付
値1:   COUNT(observation_id)     ← フィルタ別の発火件数
値2:   AVG(pnl_60min)            ← もし通していたら平均 PnL (Phase 2)
値3:   AVG(ai_confidence)        ← 平均信頼度
```

「SMH 0.80 厳格化が日次 30 件発火していて、もし通していたら平均 -$0.50/share だった」のようにフィルタの有効性を統計化できる。3〜4 週間蓄積後にフィルタの過剰保守性を判断する材料になる。

---

### 追加機能 ②: ネガ語幹×LONG倒錯ガード (A 案・Keel ケース対策)

#### 観測された問題

5/11 サポーター環境 (v3.5.3) で以下の損失パターン発生 (週次レポートの BEST/WORST 3 より):

```
WORST 1: "Keel Infrastructure posts $145M net loss in Q1 as legacy mining business shrinks"
         → AI: beneficiaries=['QQQ'], score=1, conf=0.74
         → n=11 件発注  pnl=-$201.17  (当日損失の 203%)
```

AI が `mining` という単語に引きずられて「AI mining (TECH カテゴリ)」と誤連想し、明確なネガ語幹 (`net loss` / `business shrinks`) を見落として QQQ ロング判定を出した。

#### 設計

`HEADLINE_NEGATIVE_TONE_PATTERNS` を新設し、断定的なネガ語幹を含むヘッドラインで AI が `score=1` を返した場合に `_apply_meta_filter_to_ai_result` で `score=0` に強制する。

**既存 META フィルタとの棲み分け**:

| フィルタ | 検出対象 | 対策 |
|---------|---------|------|
| HEDGE_PATTERNS (既存) | `warns` / `could plunge` 等の **仮定法** | confidence を 0.10 減点 (弱い対策) |
| QUESTION_PATTERNS (既存) | `Will X Climb or Sink?` 等の **疑問形** | score=0 強制 |
| **NEGATIVE_TONE_PATTERNS (新)** | `net loss` / `misses estimates` 等の **断定形** | **score=1→0 強制 (倒錯ガード)** |

優先順位: ネガ語幹 (新) > 疑問形 > ヘッジ。`score=-1` には適用しない (弱気判定を支持するため)。

#### 検出パターン (22 件)

```
損失/赤字:    "net loss" / "posts $X loss" / "quarterly loss" / "widens losses" /
              "goes bust" / "bankruptcy filing" / "chapter 11"
業績ミス:     "misses estimates/expectations" / "double miss" / "cuts FY/guidance" /
              "lowers FY/guidance" / "guides down" / "withdraws guidance" / "slashes target"
規模縮小:     "business shrinks" / "massive layoffs" / "production halted" /
              "recall announced" / "FDA rejection"
急落動詞:     "stock plunges/tumbles/sinks/crashes/slumps" /
              "plunges on" / "tumbles after"
```

#### 検証 (実ヘッドライン 15 件で正規表現テスト)

| ヘッドライン | 期待 | 実測 |
|------------|------|------|
| Keel posts $145M net loss... business shrinks | **発火** | ✓ (net loss) |
| OpenAI creates new unit with $4B investment | 通過 | ✓ |
| Direct Digital Q1 EPS Misses Estimate | **発火** | ✓ (Misses Estimate) |
| Apple cuts FY 2027 guidance | **発火** | ✓ (cuts FY) |
| NVIDIA stock soars on strong earnings beat | 通過 | ✓ |
| Are Analysts Predicting Will Climb or Sink? | (疑問形側でカバー) | ✓ |

**15 件全て期待通り**。

#### 想定効果

5/11 サポーター環境ベースの試算:
- Keel 11 件 (-$201.17) → 完全ブロック
- 当日 -$99 → +$102 程度に転換可能 (Keel 損失のみ防御の場合)

観察ログ (Phase 1) と組み合わせて、本ガードの発火件数・有効性を 1 週間蓄積して評価する。

---

## v3.9.6 (2026-05-11) — 「発注しない」設定で Anthropic API が消費される問題を修正

### 認定サポーターからの指摘

> セットアップウィザードで「プレ・アフター・オーバーナイトは発注しない」と設定していても、
> 現状ではニュース取得と Anthropic によるニュース判定自体は継続される仕様。
> 発注は止まるが、ターミナル上で positive / negative / confidence などの判定結果が
> 表示されている時点で Anthropic API は消費されている。
> RTH 以外の時間帯で API 消費も止めたい場合は、Anthropic へ判定を投げる前に
> プレ・アフター・オーバーナイトの停止判定を入れる修正が必要。

### 根本原因

`setup_wizard.py` の STEP「発注しない」選択時に `.env` へ `CONFIDENCE_PREMARKET=2.00`
等の sentinel 値を書き込む設計だが、`moomoo_trade_v1.py` 側の `_parse_conf()` が
`max(0.5, min(1.0, float(v)))` で値を **clamp** しているため、2.00 → 1.0 に丸められ、
「sentinel が設定されている」事実が捨てられていた。結果:

```
ニュース到着 → analyze_news() → client.messages.create()  ← Anthropic 課金発生
            → AI が confidence=0.7 を返す
            → place_buy() で confidence < 1.0 → 発注ブロック
            → 「発注は止まるが API は消費」
```

### 修正内容

#### ① raw env 値による sentinel 検出 (clamp 前の値を保持)

```python
_DISABLED_CONF_SENTINEL: float = 2.0  # setup_wizard._DISABLED_CONF と一致

def _raw_conf_disabled(key: str) -> bool:
    v = os.environ.get(key, "").strip()
    if not v: return False
    try: return float(v) >= _DISABLED_CONF_SENTINEL
    except ValueError: return False

_DISABLED_RTH        = _raw_conf_disabled("CONFIDENCE_RTH")
_DISABLED_PREMARKET  = _raw_conf_disabled("CONFIDENCE_PREMARKET")
_DISABLED_AFTERHOURS = _raw_conf_disabled("CONFIDENCE_AFTERHOURS")
_DISABLED_OVERNIGHT  = _raw_conf_disabled("CONFIDENCE_OVERNIGHT")
```

#### ② `_should_stop` への組込 (ニュース取得自体を停止)

セッション監視ループ (`market_schedule_loop`) の `_should_stop` 判定に
`_is_session_disabled_by_name(session)` を追加。OVERNIGHT と同じ
`market_open_event.clear()` パスを通り、全ニュースループ (Finnhub / Alpaca WS /
RSS / 個別株 / 決算) が `market_open_event.wait()` で待機状態に入る。

```python
_should_stop = (
    (session == SESSION_WEEKEND) or
    (session == SESSION_OVERNIGHT) or
    _cur_disabled or                       # ★ v3.9.6 追加
    (is_demo and _demo_daily_close_done)
)
```

セッション切替時に Discord 通知:
```
💤 [実口座] プリマーケット: 発注しない設定により全停止
2026-05-12 04:00 ET
ニュース取得・AI判定・発注を停止 → Anthropic API 課金ゼロ
次セッションに切り替わると自動再開
```

#### ③ `analyze_news()` 冒頭の防御的 gate (二段構え)

万一 `market_open_event` チェックを通過するパスが残っていても確実に Anthropic
呼出を阻止するため、`analyze_news()` 冒頭にも sentinel 判定を追加:

```python
_disabled, _sess_name = is_current_session_disabled()
if _disabled:
    log.info(f"[{trigger_symbol}] [AI判定スキップ] セッション {_sess_name} は"
             f"発注しない設定 → Anthropic API 呼出回避")
    return {"score": 0, "confidence": 0.0, "horizon": "skip",
            "reason": f"session-disabled ({_sess_name})", ...}
```

#### ④ 起動バナーに「発注しない」セッション一覧を追加

```
  　発注しない設定: Pre/After/OVN (CONFIDENCE_*=2.00 sentinel)
       → ニュース取得・AI判定・発注を停止し Anthropic API 課金回避
```

### 受講生への影響

- **`.env` 変更不要** — 既存 `CONFIDENCE_PREMARKET=2.00` 等が自動的に新挙動のトリガーになる
- **ウィザード再実行不要** — sentinel 値はそのまま流用
- 「発注しない」設定のセッション中は **Anthropic 課金が完全にゼロ** になる
- RTH の動作は完全に不変

### 互換性

- `CONFIDENCE_RTH=2.00` を設定した場合は RTH も停止対象になる (対称性のため)。
  ただし setup_wizard は RTH に "disabled" 選択肢を提示しない設計のため、
  通常運用で誤発火することはない。
- 旧 `_parse_conf()` の clamp 動作 (≥0.5, ≤1.0) は維持。閾値ベースの発注ブロックは
  従来通り機能する (二重防御として残存)。

---

## v3.9.5 (2026-05-11) — 60分ルックアップの境界バグ修正 + SPY 専用トレンドフィルタ追加

### 観測された問題

#### 問題 1: バックフィル成功後も「60分 データ不足」が続く

v3.9.4 でバックフィルは正常動作するようになったものの、起動後の `[市場状態]` ログで 60 分騰落率が常に「データ不足」と表示される:

```
2026-05-11 21:27:59,285 [INFO] [市場状態] QQQ: 5分 +0.04% / 15分 -0.02% / 60分 データ不足 / ...
2026-05-11 21:27:59,286 [INFO]   データ蓄積: QQQ=60分 SPY=60分 SMH=60分 個別株12銘柄(データ有=0)
```

「データ蓄積: QQQ=60分」と件数自体は揃っているのに、`get_index_pct_change("QQQ", 60)` だけ `None` を返す。15 分版は問題なし。

##### 根本原因: 保持期間 cutoff と 60分ルックアップ target の完全一致

```python
# record_index_price (cutoff で古いデータを削除)
cutoff = now - timedelta(seconds=_INDEX_PRICE_HISTORY_MAX_SEC)  # = now - 60min
while _hist and _hist[0][0] < cutoff:
    _hist.pop(0)

# get_index_pct_change(symbol, 60) (60分前の最も近い候補を探す)
target = now - timedelta(minutes=60)
for t, p in _hist:
    if t <= target:
        _candidate = (t, p)
    else:
        break
```

`cutoff == target == now - 60min` のため、cutoff より「ちょうど 60 分前のデータ」は残りえず、`_hist[0][0]` は常に `target` より新しい時刻 (`target + ε`) になる。結果として `t ≤ target` を満たす候補が見つからず常に `None`。15 分版は cutoff (60分) が lookup (15分) より十分大きいので影響なし。

#### 問題 2: SPY が QQQ と同じ閾値を使うのが不適切

統計分析の結果 (受講生環境ログから):

| 銘柄 | 15分 σ | 60分 σ | 現在の閾値 (下落 / 上昇) | 60分上昇発火率の見積もり |
|------|--------|--------|------------------------|------------------------|
| QQQ  | 0.08% | 0.15% | -0.30%/-0.70% / +0.10%/+0.25% | 適切 (~1.7σ) |
| SMH  | 0.13% | 0.25% | -0.50%/-1.00% / +0.15%/+0.40% | 適切 (専用閾値) |
| **SPY** | **0.05%** | **0.07%** | **-0.30%/-0.70% / +0.10%/+0.25%** (QQQ 流用) | **3.6σ → 発火率ほぼ 0%** |

SPY は QQQ より約 40% 低ボラ。QQQ と同じ閾値を流用すると SPY では 6〜10σ 相当の極端な水準となり、特に SHORT 抑止用の上昇 60分閾値 (+0.25%) は P90=+0.20% を超える「ほぼ起きない条件」となっていた。

### 修正内容

#### 修正 1: `_INDEX_PRICE_HISTORY_MAX_SEC` 拡張 (60分 → 75分)

`record_index_price` の cutoff を 75 分前に変更。60 分ルックアップに対して 15 分のバッファを確保し、`target` 時刻周辺のデータが残るようにした。あわせてバックフィル range も 70 → 80 分に拡張し、起動直後でも 60 分ルックアップが空振らないようマージンを確保。

#### 修正 2: SPY 専用トレンド判定の追加

SPY の σ に合わせて約 QQQ の 60% の閾値に再キャリブレーション:

```python
SPY_DOWNTREND_15M_PCT: float = -0.20  # ~4σ
SPY_DOWNTREND_60M_PCT: float = -0.45  # ~6σ
SPY_UPTREND_15M_PCT:   float = 0.07   # ~1.4σ
SPY_UPTREND_60M_PCT:   float = 0.15   # ~2.1σ
```

`is_spy_downtrend()` / `is_spy_uptrend()` を追加し、`place_buy()` / `place_short()` の `symbol == "SPY"` 分岐で QQQ ベースの構成 B/B' に加えて二段で適用 (SMH と同じ二段構え)。

### 設計上のポイント

- SPY は **構成 B (QQQ macro)** にも引き続き含まれる。「QQQ 急落 → 全銘柄ブロック」と「SPY 自身の動きが鈍い時のブロック」の両方が効く。
- 個別株閾値の細分化はユーザー判断で保留 (将来の課題)。
- 起動バナーに `構成S` / `構成S'` 行を追加して受講生が SPY 専用閾値を確認できるようにした。

---

## v3.9.4 (2026-05-11) — 起動時バックフィル hotfix（v3.9.3 が「取得 0 件」で機能していなかった問題）

### 受講生環境で観測されたログ

```
2026-05-11 20:47:47,068 [INFO] [起動時バックフィル] 取得 0 件 (NYSE 休場時など) → 従来通り 60 分かけて蓄積
```

PREMARKET 時間中（ET 07:47）に起動したにもかかわらず、全銘柄で 0 件。

### 原因 (2 つの問題が重なっていた)

#### 問題 1: タイムゾーンずれ
`request_history_kline()` は US 株の場合 **start/end を ET (米国東部時間) で解釈**する。v3.9.3 は `datetime.now()` (JST) をそのまま渡していたため、moomoo OpenD が「JST 20:47」を「ET 20:47」と解釈 = **JST 換算で 12 時間先の未来**を要求していた。当然 0 件が返る。

```
v3.9.3 動作:
  Bot 起動時刻:        JST 20:47:39
  moomoo へ送信:       start="2026-05-11 19:37:39"  end="2026-05-11 20:47:39"
  moomoo が解釈:       ET 19:37 〜 ET 20:47
  JST 換算:            2026-05-12 08:37 〜 2026-05-12 09:47 (= 12時間先の未来)
  結果:                取得 0 件
```

#### 問題 2: PREMARKET/AFTERHOURS が除外
`request_history_kline()` の `extended_time` パラメータはデフォルト `False`。RTH (09:30〜16:00 ET) 以外のデータは除外される。受講生が PREMARKET (04:00〜09:30 ET) 中に起動する場合、RTH データは一切返らない。

### 修正内容 (3 点)

#### Fix 1: ET 基準で時刻パラメータを生成

```python
# 旧 (v3.9.3): JST → moomoo が ET 解釈 → 未来時刻でエラー
now = datetime.datetime.now()           # JST 20:47
start = (now - 70min).strftime(...)     # "2026-05-11 19:37:39" (JST)
end   = now.strftime(...)               # "2026-05-11 20:47:39" (JST)

# 新 (v3.9.4): ET を明示的に計算して送信
now_local = datetime.datetime.now()                              # JST 20:47
now_et    = datetime.datetime.now(_ET).replace(tzinfo=None)      # ET 07:47
start = (now_et - 70min).strftime(...)  # "2026-05-11 06:37:39" (ET)
end   = now_et.strftime(...)            # "2026-05-11 07:47:39" (ET)
et_to_local_offset = now_local - now_et  # JST 20:47 - ET 07:47 = +13h
```

#### Fix 2: `extended_time=True` を追加

```python
ret, klines, _ = ctx.request_history_kline(
    code=f"US.{sym}",
    start=start, end=end,
    ktype=KLType.K_1M,
    max_count=200,
    extended_time=True,   # ★ PREMARKET / AFTERHOURS データを含める
)
```

#### Fix 3: time_key (ET) をローカル時刻に変換して保存

`_INDEX_PRICE_HISTORY` 内の比較は `datetime.now()` (ローカル時刻) と行われるため、保存する時刻もローカルに揃える必要がある。

```python
ts_et = datetime.datetime.strptime(row["time_key"], "%Y-%m-%d %H:%M:%S")
ts_local = ts_et + et_to_local_offset    # ET → ローカル時刻
_INDEX_PRICE_HISTORY[sym].append((ts_local, price))
```

### 起動ログの変化

#### v3.9.3 (失敗時)
```
[起動時バックフィル] 取得 0 件 (NYSE 休場時など) → 従来通り 60 分かけて蓄積
```

#### v3.9.4 (成功時)
```
[起動時バックフィル] 開始: ET 2026-05-11 06:37:39 〜 ET 2026-05-11 07:47:39 (extended_time=True で PREMARKET/AFTERHOURS 含む)
[起動時バックフィル] QQQ: 60 件追加 / 履歴 60 件 (19:48〜20:47 ローカル)
[起動時バックフィル] SPY: 58 件追加 / 履歴 58 件 (19:48〜20:47 ローカル)
[起動時バックフィル] SMH: 60 件追加 / 履歴 60 件 (19:48〜20:47 ローカル)
[起動時バックフィル] AAPL: 60 件追加 / 履歴 60 件 (19:48〜20:47 ローカル)
... (各監視銘柄)
[起動時バックフィル] 15/15 銘柄完了 (失敗 0) → トレンドフィルタ即時稼働可能
```

### 確認方法 (受講生)

Bot を v3.9.4 で再起動すると:
1. 起動ログに `[起動時バックフィル] 開始: ET ...` が表示される
2. 各監視銘柄ごとに `15 件追加 / 履歴 N 件` のログが流れる
3. **5 分後の `[市場状態]` ログで「データ不足」が消える**

```
2026-05-11 20:53:00 [INFO] [市場状態] QQQ: 5分 -0.04% / 15分 -0.12% / 60分 -0.34% / ...
```

### 変更ファイル

- [moomoo_trade_v1.py:160-164](moomoo_trade_v1.py:160) — バナー + `BOT_VERSION = v3.9.4`
- [moomoo_trade_v1.py:6073-6180](moomoo_trade_v1.py:6073) — `_backfill_index_price_history()` を ET 基準に修正、`extended_time=True` 追加、診断ログ追加

### 互換性

- v3.9.3 → v3.9.4 への移行で追加の操作は不要
- 旧仕様 (`extended_time` パラメータが存在しない moomoo-api バージョン) は対象外。v3.5.2 で必須化済の moomoo-api 10.4.6408+ で動作確認済。

---

## v3.9.3 (2026-05-11) — 起動時の K 線バックフィルでトレンドフィルタ即時稼働

### 背景

受講生から: 「`データ不足` と表示される時間帯（起動直後）に注文が止まるのか？」

`_INDEX_PRICE_HISTORY` は Bot プロセスのメモリ上にのみ存在するため、**再起動するたびに空になり、最大 60 分間トレンドフィルタ（構成 B/B'/C/C'/D/D'）が無効化**される問題があった。
（注文自体は止まらないが、上昇/下落トレンド検知が「データ不足 → False」を返すため、保護機構が外れた状態でトレードする）

### 解決策

moomoo OpenD の `request_history_kline()` で **直近 60 分の 1 分足 K 線を取得** し、起動直後にメモリ上の履歴を埋める。

### 実装

新規関数 [moomoo_trade_v1.py:6073](moomoo_trade_v1.py:6073) `_backfill_index_price_history()`:

```python
def _backfill_index_price_history() -> None:
    """起動時に直近 70 分の 1 分足 K 線を取得して _INDEX_PRICE_HISTORY を埋める。"""
    now = datetime.datetime.now()
    start = (now - datetime.timedelta(minutes=70)).strftime("%Y-%m-%d %H:%M:%S")
    end = now.strftime("%Y-%m-%d %H:%M:%S")
    
    with _quote_ctx() as ctx:
        for sym in list(_INDEX_PRICE_HISTORY.keys()):
            ret, klines, _ = ctx.request_history_kline(
                code=f"US.{sym}",
                start=start, end=end,
                ktype=KLType.K_1M,
                max_count=200,
            )
            if ret == RET_OK and not klines.empty:
                for _, row in klines.iterrows():
                    ts = datetime.datetime.strptime(row["time_key"], "%Y-%m-%d %H:%M:%S")
                    _INDEX_PRICE_HISTORY[sym].append((ts, float(row["close"])))
                _INDEX_PRICE_HISTORY[sym].sort(key=lambda x: x[0])
```

`main()` の起動時、Discord 起動メッセージ送信直前で呼び出し。

### 採用したデータソース

| ソース | 採用 | 理由 |
|---|---|---|
| **moomoo OpenD `request_history_kline`** | ⭐ 採用 | 既存接続流用 / 追加 API キー不要 / ローカル通信で高速 / ライブ値と整合 |
| yfinance (Yahoo Finance) | 不採用 | 追加ライブラリ必要 / クラウド経由で遅延 |
| Finnhub `/stock/candle` | 不採用 | 60 req/min 制限 / 受講生キーへの負荷 |
| Alpaca `/v2/stocks/{sym}/bars` | 不採用 | 200 req/min 制限 / キー差異 |
| Alpha Vantage `TIME_SERIES_INTRADAY` | 不採用 | 5 req/min と厳しい |

### 効果比較

| 状況 | v3.9.2 以前 | v3.9.3 以降 |
|---|---|---|
| 起動直後（5 分以内） | 5/15/60 分すべて データ不足 → トレンドフィルタ無効 | **フル稼働** ✓ |
| 起動 15 分後 | 5/15 分 OK / 60 分 データ不足 | フル稼働 |
| 起動 60 分後 | フル稼働 | フル稼働 |
| **再起動時** | **ウォームアップ 60 分** | **0 分** ✓ |
| 受講生 Bot のアップグレード作業 | 都度 60 分の保護なし期間 | 保護期間ゼロ ✓ |

### エッジケース対応

| シナリオ | 挙動 |
|---|---|
| WEEKEND/OVERNIGHT 中の起動 | K 線取得は可能 (休場でも前日 RTH の終値が残る) |
| NYSE 休場日 | 取得 0 件 → 静かにスキップ ("[起動時バックフィル] 取得 0 件" ログ) |
| PREMARKET 直後の起動 | 前日 AFTERHOURS のデータも含めて 60 分埋まる |
| OpenD 接続失敗 | `WARNING` ログ + 通常起動 (後方互換) |
| 個別銘柄で K 線取得失敗 | `DEBUG` ログ + 他銘柄は継続 |

### 安全性

- バックフィル失敗時も Bot は通常通り起動する（後方互換）
- 取得した K 線の `close` 価格を使うため、現実のティック価格との誤差は最小
- 追加の API 呼出は起動時に 1 回だけ（受講生環境に新規負荷なし）

### 起動ログ例

成功時:
```
[起動時バックフィル] QQQ: 60 件 (06:35〜07:35)
[起動時バックフィル] SPY: 60 件 (06:35〜07:35)
[起動時バックフィル] SMH: 58 件 (06:36〜07:35)
[起動時バックフィル] AAPL: 60 件 (06:35〜07:35)
...
[起動時バックフィル] 15/15 銘柄完了 → トレンドフィルタ即時稼働可能
```

休場日:
```
[起動時バックフィル] 取得 0 件 (NYSE 休場時など) → 従来通り 60 分かけて蓄積
```

### 変更ファイル

- [moomoo_trade_v1.py:165-169](moomoo_trade_v1.py:165) — バナー + `BOT_VERSION = v3.9.3`
- [moomoo_trade_v1.py:6072-6155](moomoo_trade_v1.py:6072) — `_backfill_index_price_history()` 関数追加
- [moomoo_trade_v1.py:10632-10636](moomoo_trade_v1.py:10632) — `main()` 起動時に呼び出しを追加

---

## v3.9.2 (2026-05-11) — AI判定ログの中立絵文字をカラー絵文字に統一

### 背景

受講生から: 「ポジティブ・ネガティブはカラフルな絵文字なのに、中立だけ地味な矢印で統一感がない」

### 原因

3 種類の判定絵文字が Unicode 上で異なる分類を持っていた:

| score | 絵文字 | Unicode | デフォルト描画 |
|---|---|---|---|
| +1 | 📈 | U+1F4C8 | カラー絵文字 (`emoji_presentation=Yes`) |
| 0 | ➡ | U+27A1 | **テキスト字形** (`emoji_presentation=No`) |
| -1 | 📉 | U+1F4C9 | カラー絵文字 (`emoji_presentation=Yes`) |

`➡` (U+27A1) は標準ではテキスト字形として描画されるため、Windows ターミナルや一部のフォントでモノクロのシンプルな矢印として表示されていた。

### 修正

`➡` の後に **Variation Selector-16 (U+FE0F)** を追加し、カラー絵文字として強制描画:

```python
# 旧 (v3.9.1 以前)
score_label = {1: "📈 ポジティブ", 0: "➡ 中立", -1: "📉 ネガティブ"}.get(score, "?")

# 新 (v3.9.2 以降)
score_label = {1: "📈 ポジティブ", 0: "➡️ 中立", -1: "📉 ネガティブ"}.get(score, "?")
#                                       ^^ ➡ + U+FE0F (VS-16)
```

### ログ表示の変化

| 場面 | 旧表示 | 新表示 |
|---|---|---|
| ポジティブ判定 | `📈 ポジティブ` (カラー) | 同じ |
| **中立判定** | `➡ 中立` (モノクロ矢印) | **`➡️ 中立` (カラー絵文字)** |
| ネガティブ判定 | `📉 ネガティブ` (カラー) | 同じ |

ご利用者様の Windows ログでは中立判定が 95% を占めるため、視認性が大きく改善する。

### 変更ファイル

- [moomoo_trade_v1.py:165-167](moomoo_trade_v1.py:165) — バナー + `BOT_VERSION = v3.9.2`
- [moomoo_trade_v1.py:7555](moomoo_trade_v1.py:7555) — `process_headlines` 内 (詳細コメント付き)
- [moomoo_trade_v1.py:8138](moomoo_trade_v1.py:8138) — `process_stock_news` 内 (短縮コメント)
- [moomoo_trade_v1.py:8566](moomoo_trade_v1.py:8566) — 動的銘柄判定内 (短縮コメント)

### 影響範囲

- ログファイル: `INFO  [AI判定] ➡️ 中立 ...` がカラー絵文字で記録される
- Discord: 通知本文では絵文字未使用のため影響なし
- AI 判定ロジック・動作: 変更なし (表示のみ)

---

## v3.9.1 (2026-05-11) — 緊急: stop_sequences 空白拒否エラーの修正

### 受講生から報告されたエラー

```
2026-05-11 19:45:40,146 [ERROR] [SHARED] Anthropic API エラー:
Error code: 400 - {'type': 'error',
 'error': {'type': 'invalid_request_error',
 'message': 'stop_sequences: each stop sequence must contain non-whitespace'},
 'request_id': 'req_011CaveSqocRs8CmxKAm7WVH'}
```

**影響**: AI 判定が全件 400 エラーで失敗。Bot が新規ニュースに対して判定できない状態。

### 原因

v3.8.4 で導入した出力削減用の `stop_sequences` 設定:

```python
stop_sequences=["\n\n", "\n```"]
```

このうち `"\n\n"` (改行 2 つ) は **完全に空白文字のみ** で構成されている。Anthropic API は最近、空白文字のみで構成された stop sequence を `invalid_request_error` として拒否する仕様変更を行ったため、v3.8.4 以降の全リクエストがエラーになった。

### 修正内容

空白のみのパターンを削除し、非空白文字を含む 4 つのパターンに差し替え:

```python
# 旧 (v3.8.4 〜 v3.9.0): 400 エラー
stop_sequences=["\n\n", "\n```"]

# 新 (v3.9.1 以降): API 仕様に準拠
stop_sequences=["\n```", "\nNote:", "\nなお", "\n注:"]
```

| パターン | 用途 |
|---|---|
| `"\n```"` | 改行+コードフェンス (markdown 装飾の典型開始) |
| `"\nNote:"` | 英語の補足コメント開始 |
| `"\nなお"` | 日本語の補足コメント開始 |
| `"\n注:"` | 日本語の「注:」開始 |

### 効果

- AI 判定が再び正常動作 (400 エラー解消)
- 出力削減効果は維持: コメント生成の典型開始パターンを 4 種類カバー
- 旧 `"\n\n"` で漏れていたケース (Note:/なお/注:) もカバーされるため、むしろ削減効果が向上

### 受講生への影響

v3.8.4 〜 v3.9.0 のいずれかを稼働中の受講生は、すべて AI 判定が失敗していた状態。v3.9.1 へ更新後、次の起動から AI 判定が再開される。

### 変更ファイル

- [moomoo_trade_v1.py:165-170](moomoo_trade_v1.py:165) — バナー + `BOT_VERSION = v3.9.1`
- [moomoo_trade_v1.py:3993-4005](moomoo_trade_v1.py:3993) — `stop_sequences` 修正

---

## v3.9.0 (2026-05-11) — 表記統一: 「実発注」→「実口座」

### 背景

受講生から: 「ログや Discord に "実発注" と表示されるが、"実口座" のほうが分かりやすい」

「実発注」はトレード界隈の俗語 (Live ammunition = 本物のお金で取引) だが、受講生は技術的に初心者寄りのため、よりフォーマルな「実口座」に統一する。

### 変更箇所（6 件）

| 場所 | 旧 | 新 |
|---|---|---|
| `_live_last_sent_date` コメント | `実発注口座：日次データ送信済み...` | `実口座：日次データ送信済み...` |
| 当日分データ送信ブロック (3 箇所のコメント) | `実発注口座限定 / 実発注通常曜日 / 実発注は夜間取引...` | `実口座限定 / 実口座通常曜日 / 実口座は夜間取引...` |
| ログ出力 | `[実発注日次送信] HH:MM ET 到達` | `[実口座日次送信] HH:MM ET 到達` |
| OVERNIGHT 停止メッセージ | `🌙 [実発注] オーバーナイト: ...` | `🌙 [実口座] オーバーナイト: ...` |

### 変更後の表示例

**ログ:**
```
2026-05-11 13:53:50,439 [INFO] [セッション] オーバーナイト（実口座）: プリマーケット（04:00 ET）まで停止
2026-05-11 15:45:00,000 [INFO] [実口座日次送信] 15:45 ET 到達 → 当日分データ送信（取引は継続）
```

**Discord:**
```
🌙 [実口座] オーバーナイト: ニュース取得・AI判定・発注を停止
2026-05-11 20:00 ET
プリマーケット開始（04:00 ET）まで待機
```

### 変更ファイル

- [moomoo_trade_v1.py:165-167](moomoo_trade_v1.py:165) — バナー + `BOT_VERSION = v3.9.0`
- [moomoo_trade_v1.py:2458](moomoo_trade_v1.py:2458) — コメント
- [moomoo_trade_v1.py:2595-2597](moomoo_trade_v1.py:2595) — コメント (3 行)
- [moomoo_trade_v1.py:2607](moomoo_trade_v1.py:2607) — ログ出力タグ
- [moomoo_trade_v1.py:2654](moomoo_trade_v1.py:2654) — `mode_label` 表記

---

## v3.8.9 (2026-05-11) — Discord 起動メッセージから acc_id を除外

### 背景

受講生から指摘:

> 🚀 moomoo 監視を開始しました
> モード: 🔴 実口座（acc_id=********・特定口座・信用）
> ...
>
> Discord にこう送られました。acc_id は送らないでください。

旧仕様 (v3.8.8 以前) では `mode_label` 1 変数を「ローカルログ」と「Discord 通知」両方で使い回していたため、Discord メッセージにも `acc_id` が含まれていました。

### 問題点

- Discord Webhook URL が万一第三者に漏れた場合、moomoo の口座 ID も同時に漏えい
- 受講生が Discord メッセージのスクリーンショットを SNS や質問部屋に貼った際、acc_id が映り込む
- 口座 ID は moomoo にとっては内部識別子だが、ユーザ環境では機密情報として扱うべき

### 修正

ローカルログ用と Discord 用で表示文字列を分離:

```python
# ローカルログ用 (運営側の監査・トラブル調査用に acc_id 付き)
mode_label = f"🔴 実口座（acc_id={REAL_ACC_ID}・特定口座・信用）"

# Discord 用 (acc_id を含めない)
mode_label_safe = "🔴 実口座（特定口座・信用）"
```

起動時の Discord 通知では `mode_label_safe` を使用。

### 変更後の Discord メッセージ

```
🚀 moomoo 監視を開始しました
モード: 🔴 実口座（特定口座・信用）       ← acc_id 削除
トリガー: SPY, QQQ, SMH
発注対象: QQQ, SMH, SPY
注文: 指値（気配値基準+0.5%バッファ）時間外OK
外部ニュース: Finnhub+AlphaVantage（10秒間隔）  + Alpaca（WebSocketリアルタイム）
週末決済: 金曜 15:45 ET に全決済・停止
```

ローカルログ (`moomoo_trade_v1.log`) は引き続き acc_id 付きで記録 (運営による検証・サポート対応で必要):

```
2026-05-11 13:14:11 [INFO]   moomoo_trade_v1.py  v3.8.9  🔴 実口座（acc_id=...・特定口座・信用）
```

### 確認した Discord 送信箇所での acc_id 漏えい

| 場所 | 旧仕様 | 新仕様 |
|---|---|---|
| 起動メッセージ (line 10522) | acc_id 含む ❌ | acc_id 非表示 ✓ |
| OVERNIGHT 停止メッセージ | acc_id 含まず ✓ | 同じ |
| 週末決済通知 | acc_id 含まず ✓ | 同じ |
| 当日トレード集計 (v3.8.6) | acc_id 含まず ✓ | 同じ |
| 終了メッセージ | acc_id 含まず ✓ | 同じ |

### 変更ファイル

- [moomoo_trade_v1.py:165-167](moomoo_trade_v1.py:165) — バナー + `BOT_VERSION = v3.8.9`
- [moomoo_trade_v1.py:10110-10113](moomoo_trade_v1.py:10110) — `mode_label_safe` 追加
- [moomoo_trade_v1.py:10524](moomoo_trade_v1.py:10524) — Discord 送信で `mode_label_safe` を使用

---

## v3.8.8 (2026-05-11) — 週次アーカイブの起動時 catch-up 対応

### 背景

v3.8.7 でログアーカイブ機構を実装したが、**Bot が月曜 00:00 に稼働していないと永久にローテーションが発生しない**問題が判明。受講生フィードバックから発覚:

> 月曜 00:00 になれば自動アーカイブが走るという動作だと、この時間にプログラムを起動していない人は永久にログがバックアップされません。

### 旧仕様の問題点

v3.8.7 では `TimedRotatingFileHandler` の自動ローテーションのみに依存していた:

- ローテーション点 = 「毎週月曜 00:00」
- Bot が emit() を呼び出した瞬間に `time >= rolloverAt` であれば発火
- **しかし v3.8.0 以降は WEEKEND (土曜終日〜月曜 04:00 ET = JST 月曜深夜) は完全停止中**
  - JST 月曜 00:00 = ET 日曜 10〜11:00 = WEEKEND 停止中
- 結果として **「月曜 00:00 ぴったりに emit() が呼ばれない」のがデフォルト状態** となり、自動ローテーションは事実上発火しない設計だった

### 新仕様: 起動時 catch-up ローテーション

`WeeklyLogbackupHandler.__init__()` の末尾で `_maybe_catch_up_rollover()` を呼び出す。

```python
def _maybe_catch_up_rollover(self) -> None:
    """起動時に月曜境界を跨いだログを検出して即ローテーションする。"""
    if not os.path.exists(self.baseFilename):
        return
    first_date = self._read_first_log_date()
    if first_date is None:
        return
    today = datetime.date.today()
    current_monday = today - datetime.timedelta(days=today.weekday())
    if first_date < current_monday:
        # 先頭タイムスタンプが今週月曜 00:00 より前 → catch-up rotate
        print("[WeeklyLogbackup] 起動時 catch-up ローテーション ...", file=sys.stderr)
        self.doRollover()
```

ロジック:
- ログファイル先頭の YYYY-MM-DD を読み取り
- 今週の月曜 (`today - today.weekday()`) と比較
- 先頭が今週月曜より前ならローテーション実行

### 動作シナリオ

| シナリオ | 旧 (v3.8.7) | 新 (v3.8.8) |
|---|---|---|
| Bot 連続稼働 (週末も) | Mon 00:00 で正常ローテーション | 同じ (catch-up は不要) |
| Bot 週末停止 → 月曜朝起動 | **永久にアーカイブされない** | **起動時に即 catch-up** |
| Bot 火曜起動 (月曜終日停止) | 永久にアーカイブされない | 起動時に即 catch-up |
| 新規インストール | ローテーション動作 OK | 同じ |

### Windows 受講生環境での実例

ご利用者様の確認用 Windows 環境 (2026-05-05 から v2.92→v3.8.7 で累積運用):
- 2026-05-11 (Mon) 13:14 に v3.8.7 で初起動
- 旧仕様では次回ローテーション = 2026-05-18 (Mon) 00:00 (Bot 停止中の可能性高)
- 新仕様では起動時に即 catch-up:
  ```
  [WeeklyLogbackup] 起動時 catch-up ローテーション:
  ログ先頭 2026-05-05 が今週月曜 2026-05-11 より前 → logbackup/ へアーカイブ
  ```
  → `logbackup/moomoo_trade_v1.2026-05-05_to_2026-05-10.log` (40 MB) が即作成される

### 変更ファイル

- [moomoo_trade_v1.py:1326](moomoo_trade_v1.py:1326) — `__init__()` に `_maybe_catch_up_rollover()` 呼び出し追加
- [moomoo_trade_v1.py:1330-1369](moomoo_trade_v1.py:1330) — `_maybe_catch_up_rollover()` メソッド追加
- [moomoo_trade_v1.py:165-169](moomoo_trade_v1.py:165) — バナー & `BOT_VERSION = v3.8.8` 更新

### 互換性

- v3.8.7 から v3.8.8 への移行で追加の操作は不要
- Bot 起動時に既存の (大きい) ログが自動的に logbackup/ にアーカイブされる
- ローテーション動作後は通常通り月曜境界で自動ローテーション (起動中の場合) または次回起動時 catch-up (停止中の場合)

---

## v3.8.7 (2026-05-10) — ログファイル週次自動アーカイブ機能の追加

### 背景

v3.8.6 までは `logging.FileHandler` を使用しており、`moomoo_trade_v1.log` がローテーションされず無限に肥大化していた。

実測値（PAN 側の検証環境、24 時間運用）:

- 1 日あたり約 25 MB のログが蓄積
- 10 日間の運用で 243 MB に到達
- 1 ヶ月で約 750 MB、1 年で約 9 GB

この肥大化により以下のリスクが発生していた:

1. SSD 容量の圧迫（特に 256 GB 以下の MacBook で深刻）
2. ログファイルが大きすぎてバックアップを取りそびれる（重いから後回し問題）
3. 8 GB メモリの Mac でファイルシステムキャッシュを圧迫し、Bot 全体の動作が遅延
4. テキストエディタで開けないサイズになり、受講生が自分でログを確認・調査できない
5. 万一トラブル発生時、肥大化したログから問題箇所を特定するのに時間がかかる

これらを根本的に解消するため、ログを週単位で分割し、過去ログは別フォルダにアーカイブする仕様に変更。

### 新しい動作仕様

| 項目 | 値 |
|---|---|
| ローテーション周期 | 毎週月曜日 00:00（システムローカル時刻、JST 想定） |
| アーカイブ先 | `logbackup/`（Bot ディレクトリ直下、自動作成） |
| ファイル命名 | `moomoo_trade_v1.{週開始日}_to_{週末了日}.log` |
| 命名例 | `moomoo_trade_v1.2026-05-05_to_2026-05-11.log` |
| 保持期間 | 最新 8 週間（約 2 ヶ月分、最大ディスク使用量 約 1.4 GB） |
| 古いファイルの扱い | ローテーション時に自動削除 |
| 圧縮 | なし（テキストエディタで直接開ける） |

### ディレクトリ構成

```
~/Desktop/moomoo_trade/
├── moomoo_trade_v1.py
├── moomoo_trade_v1.log              ← 今週分（書き込み中）
└── logbackup/                       ← 自動作成（v3.8.7 以降）
    ├── moomoo_trade_v1.2026-05-05_to_2026-05-11.log
    ├── moomoo_trade_v1.2026-05-12_to_2026-05-18.log
    ├── moomoo_trade_v1.2026-05-19_to_2026-05-25.log
    └── （以降毎週 1 ファイル増える、最大 8 ファイル）
```

### 実装

新規クラス `WeeklyLogbackupHandler` を `logging.handlers.TimedRotatingFileHandler` の派生として実装（`when='W0'`、`atTime=00:00`）。`doRollover()` をオーバーライドして:

1. 現在のログファイルを `logbackup/` 直下へ週範囲付きファイル名で `shutil.move()`
2. `retention_weeks=8` を超えた古いファイル名を解析して自動削除
3. 新しい `moomoo_trade_v1.log` を 0 バイトから開始

ファイル名衝突 (同日 2 回ローテーション等の極稀ケース) は時刻 suffix で回避。

### 既存累積ログの初回アーカイブ動作

v3.8.6 までで蓄積された大きな `moomoo_trade_v1.log` が存在する場合、ハンドラ初期化時にファイル先頭のタイムスタンプを読み取り、それを「週開始日」として採用する。これにより v3.8.7 移行直後の初回アーカイブは **「累積開始日 〜 直前の日曜」** の範囲となる。

例:
- 累積ログが 2026-04-15 から続いており、v3.8.7 適用が 2026-05-15（木）の場合
  - 5/15 〜 5/17 は引き続き同じファイルに書き込み
  - **2026-05-18（月）00:00** にローテーション
  - アーカイブファイル名: `moomoo_trade_v1.2026-04-15_to_2026-05-17.log`
  - 以降は通常通り週単位で分割

### タイミングの安全性

ローテーションは月曜 00:00 ローカル時刻（JST 想定）に実行される。v3.8.0 以降、Bot は週末から月曜朝にかけて OVERNIGHT/WEEKEND 停止中のため、ログ書き込み中にローテーションが起きるリスクは極小。万一稼働中だった場合も `TimedRotatingFileHandler` 標準のロック機構で書き込み競合は発生しない。

### 受講生影響

- 特別な操作・設定変更は **一切不要**
- v3.8.7 を導入すれば自動的にこの動作になる
- `logbackup/` フォルダは初回ローテーション時に自動作成される
- 認定サポーター様向け案内文を別途準備済（運営側で配信）

### 変更ファイル

- [moomoo_trade_v1.py:67-78](moomoo_trade_v1.py:67) — `import shutil`, `from logging.handlers import TimedRotatingFileHandler` 追加
- [moomoo_trade_v1.py:165](moomoo_trade_v1.py:165) — `BOT_VERSION = v3.8.7`
- [moomoo_trade_v1.py:1287-1410](moomoo_trade_v1.py:1287) — `WeeklyLogbackupHandler` クラス定義
- [moomoo_trade_v1.py:1410-1430](moomoo_trade_v1.py:1410) — `_file_handler` を新クラスに置き換え

---

## v3.8.6 (2026-05-10) — 当日トレード集計の表示（端末 + Discord 共通フォーマット）

### 背景

受講生から「moomoo 証券アプリで取引履歴を見るのが手間」「今日の勝ち負けが一目で見たい」という声があり、Bot 側で当日の集計を自動生成する機能を追加。受講生が能動操作なしでも見られる Discord 自動送信と、ご利用者様が直接 Bot を見ているときの Ctrl+C 終了時端末表示の両方を、**1 つの整形関数 `_format_daily_summary()`** で共通化することでコード量を圧縮。

### 表示タイミング (案 A + 案 B 同時実装)

| トリガー | 配信先 | 用途 |
|---|---|---|
| 毎日 ET 15:50 | Discord (Webhook) | 受講生が能動操作なしで見られる |
| Ctrl+C 終了時 | 端末 + Discord 終了メッセージに添付 | ご利用者様用 + 通知履歴 |

両方とも `_format_daily_summary()` の出力をそのまま流用。Discord は ``` ``` で囲んで monospace 表示にする。

### 表示例

```
📊 本日のトレード集計  2026-05-10 (Sat) 15:50 ET
========================================================
合計 12件   勝率 75% (9勝 3敗)
PnL  +45.32   平均勝ち +8.42 / 平均負け -11.27

💰 最高勝ち  【NVDA】 +22.15  (LONG, 8.4分)
💸 最大負け  【SMH】  -18.43  (SHORT, 10.2分)

▼ 銘柄別
  NVDA    3件  勝率100%  +32.15
  QQQ     6件  勝率 67%  +23.40
  SMH     2件  勝率 50%  -10.23

▼ 決済理由別
  時間切れ      8件  +32.10
  トレール       2件  +18.32
  強制損切り    1件  -10.30

▼ AI カテゴリ別
  TECH         7件  +25.10
  SEMI_STRONG  3件  +32.15
  MACRO        2件  -11.93
```

### 実装

#### 新規関数 (4 つ、合計 ~110 行)

- `_maybe_reset_today_trades()` — ET 日付変更時にバッファ自動リセット
- `_record_today_trade(rec)` — `_send_trade_result` から呼ばれて 1 トレード追記
- `_format_daily_summary() -> Optional[str]` — **整形関数（共通）**
- `_send_daily_summary_if_needed()` — 毎日 ET 15:50 で 1 回だけ Discord 送信
- `_print_daily_summary_to_terminal()` — Ctrl+C 終了時の端末表示

#### フック箇所 (3 箇所)

- `_send_trade_result` 内: トレード成立時に `_record_today_trade` を呼ぶ
- `market_schedule_loop` 内: 毎分末に `_send_daily_summary_if_needed()` を呼ぶ
- `main()` の `finally` 節: Ctrl+C 時に端末表示 + Discord 終了メッセージにサマリを添付

### 集計対象

- ET 当日の `_send_trade_result` 経由で確定したトレードのみ
- bot 再起動するとメモリ上のバッファはクリアされる (前回までの取引は集計に含まれない)
  - ログから過去取引を読み込む実装は今回未対応 (要望があれば v3.8.7+ で追加可能)
- 取引ゼロの日は送信されない (Discord/端末ともスキップ)

### Discord 重複送信の防止

`_daily_summary_sent_date` で当日送信済みフラグを保持。日付が変わると自動でリセット。`market_schedule_loop` の毎分ポーリングで `now_et.time() >= 15:50` を満たした最初のループでのみ送信する。

### 変更ファイル

- [moomoo_trade_v1.py:165](moomoo_trade_v1.py:165) — `BOT_VERSION = v3.8.6`
- [moomoo_trade_v1.py:240-260](moomoo_trade_v1.py:240) — `_send_trade_result` に `_record_today_trade` フック追加
- [moomoo_trade_v1.py:2074-2200](moomoo_trade_v1.py:2074) — 集計バッファ + 整形関数 + 送信関数群
- [moomoo_trade_v1.py:2497](moomoo_trade_v1.py:2497) — `market_schedule_loop` に毎分の送信トリガー追加
- [moomoo_trade_v1.py:10240-10260](moomoo_trade_v1.py:10240) — `main()` finally 節に端末出力 + Discord 添付追加

---

## v3.8.5 (2026-05-09) — WARNING ログ整理（受講生問い合わせ削減）

### 背景

ログを集計したところ、`log.warning` 出力の多くが **正常フロー / 自動リカバリ動作** にもかかわらず ⚠️ 表示されており、受講生から「これは何？」「Bot が壊れているのか？」という問い合わせが多発していた。WARNING を「真に対応が必要なもの」に絞ることで、運用判断ミスを減らす。

### 高頻度 WARNING の集計（4/30〜5/9 ログ）

| 件数 | 内容 | 対応 |
|---|---|---|
| 859×3 (2,576) | `[sync_positions] moomoo OpenD 異常レスポンス qty=-N (negative)` | 既に v2.x で DEBUG 化済 (本対応不要) |
| 114+29 | `⏰ 時間切れ決済！ エントリから N分経過` | **WARNING → INFO** (通常運用フロー) |
| 44 | `成行拒否 → 指値フォールバック（バッファ%）` | **WARNING → INFO** (自動リカバリ成功) |
| 28+23+21+18+12+7+5 (114) | `[ETF影響ガード] X から Y を除外` | **WARNING → INFO** (フィルタ動作成功) |
| 27 | `[状況] ⚠️ ポジション詳細不明（建玉あり・発注ブロック中）` | KEEP (受講生に対応を促す表示) |
| 22 | `[起動時復元] ポジション取得が空 → N回目リトライ` | **WARNING → INFO** (リトライ過程) |
| 12 | `[SHORT前チェック] ロングポジション残存` | **WARNING → INFO** (正常フロー) |
| 11 | `デモ口座: ポジションが取得できませんでした` | **WARNING → INFO 1行に短縮** ⭐ ユーザ要請 |
| 11+9 (20) | `[sync_positions] X: API cost_price 乖離 → ボット追跡値を維持` | **WARNING → INFO** (防御的維持) |
| 12 | `実口座: ポジションが取得できませんでした` | KEEP (実口座は要確認) |
| 6 | `🔄 損切り再発注成功` | **WARNING → INFO** (リトライ成功) |
| 9 | `🚫 ショートスキップ: 貸株在庫なし` | KEEP (発注機会逸失) |
| 5 | `🚫 発注ブロック: 起動時のポジション確認が取れていません` | KEEP (要対応) |

### 降格した WARNING の一覧

#### ① デモ口座ポジション取得失敗 ⭐ ユーザ要請

[moomoo_trade_v1.py:10146-10148](moomoo_trade_v1.py:10146):
- 旧: 2 行の `⚠️` warning
- 新: 1 行の INFO（`[起動時復元] デモ口座: ポジション取得スキップ → クリーンスタート扱い`）
- 理由: デモ環境では position_list_query が頻繁に空を返し、その大半は実害なしのクリーンスタート相当
- 実口座 ([:10138-10145](moomoo_trade_v1.py:10138)) は引き続き WARNING + Discord 通知（要確認）

#### ② [ETF影響ガード] X から Y を除外

[moomoo_trade_v1.py:824](moomoo_trade_v1.py:824):
- フィルタ動作成功 = 不当な発注を防いだ＝望ましい挙動
- WARNING → INFO

#### ③ [SHORT前チェック] / [BUY前チェック] 反対方向ポジション残存

[moomoo_trade_v1.py:5467](moomoo_trade_v1.py:5467) (LONG→SHORT 切替), [moomoo_trade_v1.py:5092](moomoo_trade_v1.py:5092) (SHORT→LONG 切替):
- 正常なフロー (反対方向ポジションを先に決済して次サイクルで再試行)
- WARNING → INFO

#### ④ [sync_positions] cost_price 乖離 → ボット追跡値を維持

[moomoo_trade_v1.py:4332](moomoo_trade_v1.py:4332):
- API の昨日終値が現在の avg_cost と乖離した際の防御的維持動作
- WARNING → INFO

#### ⑤ [起動時復元] ポジション取得リトライ・accinfo フォールバック

[moomoo_trade_v1.py:9999](moomoo_trade_v1.py:9999) と [moomoo_trade_v1.py:10010](moomoo_trade_v1.py:10010):
- 起動時の通常リトライ過程 / 正常なフォールバック経路
- WARNING → INFO

#### ⑥ 成行拒否 → 指値フォールバック

[moomoo_trade_v1.py:6863](moomoo_trade_v1.py:6863):
- 自動リカバリ成功 (時間外で成行不可 → 指値で再発注)
- WARNING → INFO

#### ⑦ ⏰ 時間切れ決済！

[moomoo_trade_v1.py:9224](moomoo_trade_v1.py:9224):
- タイマー満了による通常決済（max_holdminutes 超過）
- 失敗時は別経路で `log.error` が出るため、WARNING で重複させる必要なし
- WARNING → INFO

#### ⑧ 🔄 損切り再発注成功

[moomoo_trade_v1.py:6371](moomoo_trade_v1.py:6371):
- リトライ成功 = 望ましい挙動（失敗は `log.error` で別表示）
- WARNING → INFO

### 維持した WARNING

以下は引き続き WARNING（受講生に対応を促す必要があるもの）:
- `🚫 発注ブロック: 起動時のポジション確認が取れていません` (要 moomoo アプリ確認)
- `[状況] ⚠️ ポジション詳細不明（建玉あり・発注ブロック中）`
- `🚫 ショートスキップ: 貸株在庫なし` (発注機会逸失の通知)
- `実口座: ポジションが取得できませんでした` (デモと違って実害あり)
- `🔄 損切り再発注: ...` (再発注の発生自体は注視に値する)
- `🆘 パニックセル発動` (重大)
- `[実口座モードが有効]` (起動確認プロンプト)
- API/Discord/Anthropic などのシステムエラー全般

### 想定効果

ログサンプルの WARNING 件数を 70% 程度削減見込み（実数: 4/30〜5/9 で約 1,500 件 → 約 450 件）。受講生サポート問い合わせの主因となっていた「⚠️ デモ口座: ポジションが取得できませんでした」は完全に消滅。

### 変更ファイル

- [moomoo_trade_v1.py:165](moomoo_trade_v1.py:165) — `BOT_VERSION = v3.8.5`
- [moomoo_trade_v1.py:824](moomoo_trade_v1.py:824) — ETF 影響ガード除外ログ
- [moomoo_trade_v1.py:4332](moomoo_trade_v1.py:4332) — sync_positions cost 乖離
- [moomoo_trade_v1.py:5092](moomoo_trade_v1.py:5092) — BUY 前 SHORT 残存チェック
- [moomoo_trade_v1.py:5467](moomoo_trade_v1.py:5467) — SHORT 前 LONG 残存チェック
- [moomoo_trade_v1.py:6371](moomoo_trade_v1.py:6371) — 損切り再発注成功
- [moomoo_trade_v1.py:6862](moomoo_trade_v1.py:6862) — 成行拒否 → 指値 FB
- [moomoo_trade_v1.py:9224](moomoo_trade_v1.py:9224) — 時間切れ決済
- [moomoo_trade_v1.py:9999](moomoo_trade_v1.py:9999) — 起動時 accinfo フォールバック
- [moomoo_trade_v1.py:10008](moomoo_trade_v1.py:10008) — 起動時ポジション取得リトライ
- [moomoo_trade_v1.py:10146](moomoo_trade_v1.py:10146) — デモ口座ポジション取得失敗

---

## v3.8.4 (2026-05-09) — 出力側コスト削減一括（process_stock_news 拡張 + 出力短縮）

v3.8.3 の入力側削減に続き、**出力側 (output tokens) の削減**を中心とした改修。ログ実測で出力 token の平均が 266 tok / max 1009 tok と max_tokens=1500 の 1/3 〜 2/3 を消費しているため、ここを圧縮することで月 $40-60 の追加削減を狙う。

### 4 つの主要変更

#### ① process_stock_news にもノイズフィルタを適用

v3.8.3 で `process_headlines` (ETF モード) のみに適用していた `_check_noise_pattern` を、`process_stock_news` (個別株モード) にも追加。個別株モードでもアナリスト PT 変更や transcript は常に score=0 を返すため、AI 呼出を節約できる。

[moomoo_trade_v1.py:7651](moomoo_trade_v1.py:7651) のフロー:

```
重複排除 (articleId / prefix)
↓
ノイズフィルタ (regex 26 パターン) ← v3.8.4 で追加
↓
全件ノイズ → スキップ / 一部残れば AI 呼出
```

#### ② トピック重複 TTL を 30 分 → 60 分に延長

[moomoo_trade_v1.py:1697](moomoo_trade_v1.py:1697) の `TOPIC_COOLDOWN_SEC` を `1800` → `3600` に変更。

- 既報トピック (Iran 情勢、関税、地政学リスク等) の再判定間隔が倍に
- 既存の `TOPIC_PRICE_DEVIATION_PCT=0.003` (QQQ ±0.3% 動的解除) はそのまま稼働するため、**急激な相場変動を伴うニュースは引き続き再判定可能**
- 横ばい相場での重複削減効果が増える見込み

#### ③ max_tokens 1500 → 1000 + stop_sequences 追加

[moomoo_trade_v1.py:3623-3633](moomoo_trade_v1.py:3623):

```python
max_tokens=1000,                       # 旧 1500 (ログ実測で max=1009 / avg=266)
stop_sequences=["\n\n", "\n```"],      # JSON 後のコメント生成を停止
```

- **max_tokens 1000**: ログ 5,535 件中 max=1009 (1 件のみ)、avg=266。99.98% は影響なし
- **stop_sequences `\n\n`**: JSON は 1 行記述ルールのため、改行 2 つ = AI が解説段落に入った合図
- **stop_sequences `\n```'**: コードフェンス装飾の検知

#### ④ プロンプト v1.5.0: reason 20字 → 12字 + 個別株モードで category 等を省略

[moomoo_trade_v1.py:2826-2843](moomoo_trade_v1.py:2826) の出力規則を強化:

```
旧: reason（日本語20字以内）
新: reason（日本語 12 字以内）

旧: 個別株モード: category / beneficiaries / victims は省略推奨
新: 個別株モード: category / beneficiaries / victims は出力しない（厳守）
```

出力例も全て 12 字以内に書き直し、AI が真似する内容を短く誘導。

### 想定効果（出力側）

ログ実測の output token 分布:

| サイズ帯 | 件数 | 比率 |
|---|---|---|
| <100 | 2,308 | 41.7% (主に score=0 の最小 JSON) |
| 100-199 | 23 | 0.4% |
| 200-299 | 360 | 6.5% |
| 300-499 | 2,250 | 40.7% (reason 20 字+解説段落混入の典型) |
| 500-999 | 593 | 10.7% (markdown 装飾混入) |
| ≥1000 | 1 | 0.0% |

stop_sequences と reason 12 字化で 300-499 帯と 500-999 帯を圧縮できる見込み:

- 平均 266 tok → **150 tok 程度** に圧縮（推定）
- 1 リクエストあたり 116 tok × $5/MTok = $0.00058 削減
- 3,500 req/日想定: **$2.03/日 = $61/月削減**

### 累積コスト見込み（v3.8.x シリーズ全体）

| 削減項目 | バージョン | 月額削減 |
|---|---|---|
| 入力側ノイズフィルタ (ETF モード) | v3.8.3 | $25-40 |
| 入力側ノイズフィルタ (個別株モード) | v3.8.4 | $10-15 |
| トピック重複 TTL 延長 | v3.8.4 | $15-20 |
| max_tokens 削減 (max ケース) | v3.8.4 | $1 (微小) |
| stop_sequences + reason 短縮 | v3.8.4 | $40-60 |
| **累積想定** | | **月 $90-135** |

実測は v3.8.4 運用開始 1 週間後に再評価。

### 安全性確認

- **stop_sequences での JSON 切断リスク**: `\n\n` と `\n```` の 2 つのみ採用。JSON 本文は 1 行記述厳守のため、これらが現れる = JSON 終了後。万一切断されても既存の JSON parser fallback (line 3653-3702) で raw 文字列から JSON 抽出する仕組みあり。
- **max_tokens 1000 で切断**: ログ実測で 0.02% (1/5535)。万一切断時は中立判定 (score=0) にフォールバック。
- **reason 12 字化**: Discord 通知の表示内容は短くなるが、追加情報は v3.4.0 のメタフィルタ side で reason に追記される設計のため、運用上の支障なし。

### 変更ファイル

- [moomoo_trade_v1.py:165](moomoo_trade_v1.py:165) — `BOT_VERSION = v3.8.4`
- [moomoo_trade_v1.py:2538](moomoo_trade_v1.py:2538) — `_PROMPTS_VERSION = 1.5.0`
- [moomoo_trade_v1.py:1697](moomoo_trade_v1.py:1697) — `TOPIC_COOLDOWN_SEC = 3600`
- [moomoo_trade_v1.py:2826](moomoo_trade_v1.py:2826) — 出力規則を 12 字化
- [moomoo_trade_v1.py:3623](moomoo_trade_v1.py:3623) — max_tokens / stop_sequences
- [moomoo_trade_v1.py:7651](moomoo_trade_v1.py:7651) — process_stock_news ノイズフィルタ

---

## v3.8.3 (2026-05-09) — AI 呼出前ノイズフィルタ（API コスト最適化）

### 背景

`moomoo_trade_v1.log` (4/30〜5/9, 28,068 件の AI 判定) を分析した結果、**全 AI 判定の約 48%（毎日 1,500〜4,000 件）が score=0** を返しており、しかもその大半は統合 SYSTEM_PROMPT に既にルール化されているパターンであることが判明。

| 日付 | AI 判定総数 | API 呼出 (cache miss) | score=0 件数 (率) |
|---|---|---|---|
| 4/30 | 2,100 | 1,657 | 1,008 (48%) |
| 5/01 | 2,737 | 2,130 | 1,274 (46%) |
| 5/02 | 2,778 | 2,595 | 1,343 (48%) |
| 5/06 | 3,480 | 3,206 | 1,700 (48%) |
| 5/07 | 8,430 | 7,798 | 4,126 (49%) |
| 5/08 | 5,547 | 5,309 | 2,719 (49%) |
| 5/09 | 2,996 | 2,689 | 1,480 (49%) |

5/7 単日で最頻ノイズパターンの内訳:
- アナリスト格付け維持/変更: 320 件超 (`Maintains Buy on X, Raises PT to $Y` 形式)
- 非主要 ETF 構成銘柄の決算: 220 件超
- 個人投資アドバイス・ハウツー: 100 件超
- 決算 transcript: 33 件超

### 実装: HEADLINE_NOISE_PATTERNS pre-filter

`process_headlines()` の既存フィルタチェーン (短文 → キーワード → 類似重複) に **②.5 段階目** として正規表現ベースのノイズフィルタを追加。マッチ時は AI 呼出をスキップして即 return。

`HEADLINE_NOISE_PATTERNS` の構成 (24 パターン):

1. **アナリスト目標株価変更系** (5/7 単日 320+ 件): `Maintains/Reiterates Buy/Hold/...`、`Raises/Lowers/Cuts PT to $Y`、`Upgrades/Downgrades [銘柄]`
2. **決算 transcript** (常に過去事象): `Q[1-4] YYYY Earnings Call Transcript`
3. **個人金融・ハウツー記事**: `Best money market account`、`Ask an Advisor`、`Should I Use/Pay/...`、`$X Invested In Y N Years Ago`、`If You Invested`
4. **業界比較・ランキング・プロモ**: `Comparing X With Industry`、`N Stocks To Watch`、`IBD 50 Stocks`、`Best Young Stocks`、`Stock You Can Actually Buy`
5. **連れ高・連れ安 (sympathy trade)**: `trading higher amid sympathy with`、`Shares of X-related companies are...`
6. **海外ローカル規制**: `UK's FCA/PRA/HMRC`、`UK regulator`
7. **Daily aggregator**: `Deal Dispatch:`、`Today's Movers`、`Daily Movers`、`Market Digest:`

### 統計・ログ

- **毎ヒット**: `[ノイズフィルタ] {label} → AI呼出スキップ: {headline}` を INFO ログ出力
- **日次サマリ**: 日付が変わると前日分を 1 行サマリ出力（ヒット総数・推定削減コスト・上位 5 ラベル）
- **環境変数**: `NOISE_FILTER_ENABLED=false` で無効化可能 (回帰時の保険)

### 想定削減効果

- 1 AI 呼出のコスト: 約 $0.0018 (output 200tok × $5/MTok + cache_read 7,500tok × $0.10/MTok)
- 高信頼度パターンが捕捉する見込み: **700-1,500 件/日**
- **月額コスト削減: $40-90**
- 注意: 個別株モード (process_stock_news) には未適用。必要なら次バージョンで拡張。

### 安全性

24 パターンはすべて **既存の SYSTEM_PROMPT ルール** と 1:1 対応するため、AI 出力を変更するリスクはゼロ（AI が同じ判定を返す代わりに regex で先回り判定）。万一誤検知が発覚した場合は `NOISE_FILTER_ENABLED=false` で即座に旧挙動に戻せる。

### 変更ファイル

- [moomoo_trade_v1.py:2966](moomoo_trade_v1.py:2966) — `HEADLINE_NOISE_PATTERNS` 定義 + assert + regex コンパイル
- [moomoo_trade_v1.py:3380](moomoo_trade_v1.py:3380) — env 設定、`_check_noise_pattern()`、統計関数群
- [moomoo_trade_v1.py:6890](moomoo_trade_v1.py:6890) — `process_headlines` のフィルタチェーンに ②.5 段階目を統合
- [moomoo_trade_v1.py:9744](moomoo_trade_v1.py:9744) — 起動バナーに状態表示追加

---

## v3.8.2 (2026-05-09) — 個別株対称ガード + 閾値の env 化

5/4-8 週間レポートで個別株が **AMD -$76 / MU -$41 / Finnhub-NVDA -$65 / META -$21** と構造的損失を出し、強制損切り 10 件 -$388.91 のすべてが個別株であったのを受けた対策。発見④「個別株専用 confidence 引き上げ」と同じ方向の改修。

### 構成 D' — `is_stock_uptrend(symbol)` 追加

`is_stock_downtrend()` の対称版。`STOCK_TICKERS` に含まれる個別株が単独で急騰中のとき、悪材料ニュースに飛びついた SHORT を抑制する。`place_short()` 内に「symbol が `STOCK_TICKERS` に含まれる場合」のガードを追加。

ETF 側 (構成 B' / C', v3.8.1) と完全に対称な設計:

| 構成 | LONG ブロック | SHORT ブロック |
|---|---|---|
| **B / B'** | `is_macro_downtrend()` (QQQ ベース) | `is_macro_uptrend()` |
| **C / C'** | `is_smh_downtrend()` | `is_smh_uptrend()` |
| **D / D'** | `is_stock_downtrend(sym)` | **`is_stock_uptrend(sym)` ←本パッチで追加** |

### 個別株トレンド閾値を env 化

旧 v2.99.4 はハードコード固定値だったため、ボラの異なる銘柄や受講生ごとの調整ができなかった。env で全 4 値が調整可能に:

| env 名 | デフォルト | 用途 |
|---|---|---|
| `STOCK_DOWNTREND_15M_PCT` | -0.7 | LONG ブロック (15分閾値) |
| `STOCK_DOWNTREND_60M_PCT` | -1.5 | LONG ブロック (60分閾値) |
| `STOCK_UPTREND_15M_PCT` | +0.7 | **SHORT ブロック (15分閾値) ←新規** |
| `STOCK_UPTREND_60M_PCT` | +1.5 | **SHORT ブロック (60分閾値) ←新規** |

- ETF 側 (構成 B/C/B'/C') の閾値は引き続きハードコード (受講生間で共通化したい設計のため)。
- 個別株のみ env 化したのは、銘柄ごとのボラ差が ETF より大きいため (例: NVDA は 1 日 5% 動くこともある一方、AAPL は 1% 程度)。
- 既存の `_INDEX_PRICE_HISTORY` は v2.99.4 から `STOCK_TICKERS` を追跡しており、データ取得部の追加改修は不要。

### 起動時バナーログを 2 段化

旧版は「下落トレンドフィルタ」1 行で構成 B/C/D を表示していたが、SHORT ブロックの構成 B'/C'/D' を別行で表示するように変更。env で値を変えた場合に効いた閾値が一目で確認できる。

```
下落トレンドフィルタ (LONG ブロック): 構成B(QQQ 15分≤-0.3%/60分≤-0.7%) ...
上昇トレンドフィルタ (SHORT ブロック): 構成B'(QQQ 15分≥+0.1%/60分≥+0.25%) ...
```

---

## v3.8.1 (2026-05-09)

2026-05-08 TTD ニュース事案 (QQQ SHORT 6 株 暴発、PnL=-$4215.93 強制損切り試行) の根本対策として、3 つの構造的バグを修正。

### Patch 1: ETFガード強化 — 空リスト → カテゴリ単独発動を停止

**対象**: `process_headlines()` 内、`_validate_etf_impact()` 直後。

**症状**: AI が `victims=['QQQ']` を返し、ETFガードが「QQQ の主要構成銘柄も関連セクター語も含まれない」として除外したのに、空リスト `victims=[]` のまま下流に流れた。`resolve_execution_symbols()` がセクター語マッチも空 → `EXECUTION_MAP['QQQ']=['QQQ']` のフォールバックで結局 QQQ を指名し、SHORT 注文が発動した。

**修正**: `score=±1` で `beneficiaries`/`victims` が空の場合、AI 判定を中立化して `return`。`_orig_beneficiaries_etfs` / `_orig_victims_etfs` を `_validate_etf_impact()` 前に保存しておき、ログに「元 AI 指定 ETF」として記録する。

**v3.1.3 旧ガードの削除**: `_victims_all_etfs_rejected and not exec_targets` 分岐は削除。`EXECUTION_MAP` が常に非空 (`[trigger_sym]`) を返すため `not exec_targets` 条件が事実上 False となり、本事案を捕捉できていなかった。新ガードが旧ガードのカバー範囲を完全に包含する。

### Patch 5: place_short 対称ガード — マクロ上昇トレンド検知時の SHORT 一時停止

**対象**: `place_short()` 入口、起動時ポジション不明チェックの直後。

**症状**: AI が明示的に `victims=['QQQ']` を返したケース (= ETFガード強化を通過) で、QQQ が当日アップトレンド中にもかかわらず SHORT が発動した。`place_buy()` には `is_macro_downtrend()` ガードがあるが、`place_short()` 側に対称ガードが存在せず、上昇相場での無理な逆張りで含み損が拡大した。

**修正**: `place_short(symbol)` で symbol が `QQQ` / `SPY` のとき `is_macro_uptrend()` を、`SMH` のとき `is_smh_uptrend()` を呼んで上昇トレンドを検知。検知時は SHORT をスキップ。

**v3.1.3 既存ガードとの関係**: v3.1.3 の上昇トレンドブロックは「`exec_targets` が空のフォールバック経路のみ」だった (`process_headlines` 内、line 6887-6910)。本パッチは `place_short` 自体の入口に追加したので、AI が明示的に ETF を victims 指定したケースもカバーされる。

### Dead Code 削除: `_short_cover_ordered`

v2.98 で `_emergency_cover_short` を廃止した際、本フラグをセットする箇所が消失。読出のみが残存し、常に `False` のまま動作していた死コードを完全削除。
- `_short_cover_ordered: dict[str, bool] = {}` の定義を削除
- `global _short_cover_ordered` 宣言を削除
- `sync_positions()` 内の解消検知ブロックを削除
- `_handle_pending_timeout` 内の deprecated コメントを削除

### avg_cost 未確定ガード (v3.1.3 既実装、ここでは再記録)

`risk_monitor_loop` で `position_qty` 設定済 / `avg_cost=0` の窓 (発注 0〜8 秒) に PnL 計算が走り、`PnL=(0-price)*qty` で大幅マイナスとなり強制損切りが暴発する問題は v3.1.3 で修正済み (`if qty != 0 and ts.avg_cost <= 0: continue`)。今回の事案にも該当するが、既に修正済みのためコード変更なし。

---

## v3.8.0 (2026-05-09) — OVERNIGHT 全モード停止

LIVE モードでも OVERNIGHT (20:00〜04:00 ET) はニュース取得・AI 判定・発注を停止するように変更。

**理由**: アフター終了後は新規ニュースの取引価値が低い一方、Anthropic API + ニュース取得の API コストが平日 24h 稼働で発生し続けていた。moomoo 24h 取引対応のため LIVE は継続稼働させていたが、運用上のメリットがコストを下回ると判断。

**実装**: `market_schedule_loop()` 内の `_should_stop` から `and is_demo` を除去し、`(session == SESSION_OVERNIGHT)` を全モード共通の停止条件に。Discord メッセージは `is_demo` 判定で `[実発注]` / `[デモ]` を切り替える。翌プリマーケット (04:00 ET) で自動再開。

---

## v3.7.x (2026-05-09) — プロンプト本体内蔵

- **v3.7.2**: `prompts.py` を本体に再統合し Google Drive 自動更新を撤去。100 人規模配布での Drive レート制限・HTML 警告ページ・ネットワーク不安定問題を排除。毎日 ZIP で再配布する運用に移行。
- **v3.7.1**: hotfix - Google Drive ウイルススキャン警告ページに対応。
- **v3.7.0**: プロンプト + データ分離 / コメント圧縮で大幅スリム化。

## v3.6.0 (2026-05-09) — プロンプト Drive 集中管理

プロンプト本体を `prompts.py` に分離し Google Drive で集中管理 (v3.7.x で本体内蔵に戻した経緯あり)。

---

## プロンプト本体 (UNIFIED_SYSTEM_PROMPT_TEMPLATE) 履歴

`_PROMPTS_VERSION` で管理。`moomoo_trade_v1.py` のバージョンとは独立。

### v1.4.0 (2026-05-09) — 5/4-8 週次ログ分析で 5 ルール追加

`成績収集_改善版20260424(8).xlsx` の 492 件のニュース付きトレード分析（LOSS 192 / WIN 292）から、現行 v3.8.x ルールが捕捉していなかった 104 件のロスパターンを 5 種類に分類し、プロンプトに追加。試算では 49 件 / -$199.83 が新規にブロック可能。

#### 追加した 5 ルール

| 略称 | 内容 | 検出数 | 損失額 | 代表例 |
|---|---|---|---|---|
| **A** | Sympathy trade（連れ高記事） | 5 件 | -$38 | "Shares of cybersecurity-related companies are trading higher amid sympathy with Datadog and Fortinet" |
| **B** | Daily aggregator / 集約記事 | 2 件 | -$39 | "Deal Dispatch: J.M. Smucker Considers..., Meta Buys..., Buzzfeed On Brink..." |
| **C** | 噂レベル材料 (preliminary / strategic review / in talks) | 3 件 | -$43 | "Apple, Intel Have Reached Preliminary Agreement..." (WSJ Reports 単独) |
| **D** | 海外規制・域外材料 (UK FCA / EU / 中国) | 14 件 | -$19 | "UK's FCA to review claims management market" |
| **E** | **【最大損失源】個別株単独サージ → ETF 連想禁止** | 30+ 件 | -$300+ | "Supermicro stock soars on strong margin" / "Disney Stock Surges On Earnings Beat" / "AMD stock soars on Q1 earnings beat" |

#### Rule E (個別株単独サージ → ETF 連想禁止) — 重点解説

5/4-8 週で **最大の損失源**。AI が「テック銘柄が上がっている → QQQ も上がる」と推論しがちだが、5〜60 分後にはすでに織り込み済みで逆動きする。

- 「【単一個別株名】 stock (soars/surges/rises/jumps/plunges/slides/falls) on (earnings/forecast/news/deal)」形式は ETFモードでは絶対 score = 0
- 個別株モード (target_symbol = 該当銘柄) では通常通り判定可能（個別株自身の判断材料として有効）
- 例外: AAPL/MSFT/NVDA で EPS Beat 30% 超など極端ケースのみ score=1 で confidence ≤ 0.65（Sell-the-news 抑制）

#### 既存ルールの拡張

- **観測・推測語ルール**: "preliminary"/"in talks"/"strategic review" を追加 (Rule C)
- **コラム・ハウツー・羅列ルール**: "Deal Dispatch"/"Daily Movers"/"WSJ Reports 単独" を追加 (Rule B)

#### 無関係企業の filler 判定への対策

「Citi changes referral pay for bankers and wealth advisers」が 18 件中 15 損失 (-$74) という事案を受け、

> ニュース見出しに【取引判断に直結する確定事象キーワード】が含まれない場合は、
> 企業名が S&P500 構成でも必ず score = 0 (低 confidence の filler 判定を出さない)

を一般化ルールとして追加。

#### サイズへの影響

トークン数は v1.3.0 の 5,532 → v1.4.0 で約 6,200 と推定（+12%）。Prompt Caching 閾値 4,096 を依然として +2,000 tok 余裕でクリア。

---

### v1.3.0 (2026-05-09) — ETF / 個別株プロンプトを統合

- 旧 `ETF_SYSTEM_PROMPT_TEMPLATE` (~5,366 tok) と `STOCK_SYSTEM_PROMPT_TEMPLATE` (~4,724 tok) を `UNIFIED_SYSTEM_PROMPT_TEMPLATE` 1 本に統合。
- 判定モードは user メッセージ冒頭で `判定モード: ETF` / `判定モード: 個別株 / 対象銘柄: XYZ` として注入。
- `_build_stock_prompt()` および `_STOCK_PROMPTS_CACHE` を削除。`{symbol}` がテンプレート内に埋め込まれなくなったため、全リクエストが同一の SYSTEM_PROMPT キャッシュをヒット。
- `analyze_news()` のシグネチャから `system_prompt` パラメータを削除し、`target_symbol: Optional[str]` を追加。
- 統合プロンプトの実測サイズ: 約 5,500 tok (Anthropic count_tokens 確認済み)。Prompt Caching 閾値 4,096 tok を +1,400 tok 程度上回る安全圏。
- 想定コスト: 入力側 ~$1.98/日 (旧 ETF/銘柄別キャッシュ $2.23/日 から ~10% 削減)。

### v1.2.0 (2026-05-09) — 圧縮版 (一時実装、後に v1.3.0 で再構築)

各プロンプトを ~5K tok → ~2.2K tok に圧縮。キャッシュ閾値以下となりキャッシュ無効化。
**判明した問題**: キャッシュなし入力単価 ($1/MTok) が cache_read 単価 ($0.10/MTok) の 10 倍のため、トークン数を 1/2.5 に圧縮しても 4 倍コストになる計算ミス。v1.3.0 で再統合。

### v1.1.0 以前

`prompts.py` 経由で外部管理。詳細はリポジトリ履歴参照。

---

## v2.86〜v3.5.x の主要変更 (圧縮サマリ)

各バージョンの詳細な実装意図は本体コードの `★ v2.xx:` インラインコメント側で記録されています。このセクションでは「いつ何を変えたか」の年表のみ記録。

- **v3.5.3**: hotfix - Python 3.9 で `datetime` の標準 import が module 参照になる問題への対応。
- **v3.5.2**: moomoo サポート (2026-05-08) 正式回答に基づき `TrdSide.BUY_BACK` 直接指定に統一 (旧 `hasattr` ガード廃止)。
- **v3.4.x〜v3.5.x**: SHORT 機能の安定化 / リスク監視 / 日次決済の堅牢化。
- **v3.1.3**: マクロ上昇トレンドガード (`is_macro_uptrend` / `is_smh_uptrend`) 追加 — bear ETF フォールバック専用。
- **v3.1.0**: ショート取引識別フィールド (trade_type) を GAS 連携データに追加。
- **v3.0.0**: Anthropic Prompt Caching を有効化 (system 構造化リスト + cache_control)。
- **v2.99.x**: ETF 影響ガード (`_validate_etf_impact`) 追加、JSON パース堅牢化、シンボル別バッファ。
- **v2.98**: 「架空ショート三層防御」を廃止し、SHORT を正規ポジションとして扱う方針に統一。
- **v2.97**: Reuters / AP を `_RSS_FEEDS` から削除。
- **v2.95**: GAS Webhook に AI beneficiaries / victims を送信するフィールド追加。
- **v2.94**: SEMI_STRONG / SEMI 二段階カテゴリ判定 (5/4-5/5 半導体っぽいニュース → SMH 流入損失事案への対策)。
- **v2.93**: 730 件の実トレード分析でニュースソース重みを再調整 (Yahoo を 0.85→0.95、Alpaca を 1.00→0.95、Finnhub を 0.90→0.85)。
- **v2.92**: マクロ環境 (直近 5/15/60 分の QQQ/SPY 騰落率) を AI 判定の文脈として渡す機能を追加。
- **v2.91〜v2.94**: SEMI_STRONG カテゴリ絞り込み、SMH 専用閾値、上昇トレンドフィルタ。
- **v2.90**: Alpaca News WebSocket "already authenticated" レスポンスのログレベル調整。
- **v2.89**: 週末決済発火条件に「平日 (`dow_et<5`) + RTH/AFTERHOURS」ガードを追加。
- **v2.88**: ニュースソース重み付けロジックを max ベースに統一。
- **v2.87**: NYSE 休日カレンダー対応 (完全休場 / 早期クローズ / 連休前決済)。
- **v2.86**: ctx リソースリーク対策の context manager (`_trade_ctx`, `_quote_ctx`) を導入。

それ以前の履歴 (〜v2.85) は本ファイル外で管理。
